---
title: (ACE) 작업실적
date: 2025-02-20
tags: [erp, 개발, 연동sp 참고]
---

# (ACE) 작업실적

CREATE PROC djb_SPDSFCWorkReportSaveMES

@WorkingTag NVARCHAR(2) ,

@MesKey NVARCHAR(50) , -- MES키값

@WorkDate NVARCHAR(30) , -- 작업일

@WorkOrderNo NVARCHAR(20) , -- 작업지시번호

@WorkOrderSerl INT , -- 작업지시순번

@LotSerl NVARCHAR(4) ,

@LotNo NVARCHAR(30) , -- Lot번호

@Qty DECIMAL(19,5), -- 제품생산중량

@OKQty DECIMAL(19,5), -- 양품수량

@WorkEmpId NVARCHAR(20) , -- 작업자순번

@Remark NVARCHAR(200), -- 비고

@ItemSeq NVARCHAR(MAX), -- 투입품목 ItemSeq1/ItemSeq2/ItemSeq3

@InputQty NVARCHAR(MAX), -- 투입수량 Qty1/Qty2/Qty3

@InputLotNo NVARCHAR(MAX), --

@Status NVARCHAR(20) OUTPUT, -- OK/ 그 외

@ErrMsg NVARCHAR(500) OUTPUT, -- 결과

@WorkReportSeq INT OUTPUT -- 생산실적키

AS

 

 

SET NOCOUNT ON

SET ANSI_WARNINGS OFF

 

-- 오류메시지 (@ErrMsg) 영어로 보내야함

 

SELECT @Status = 'OK', @ErrMsg = 'X'

 

DECLARE @NumStatus INT,

@MessageType INT,

@Results NVARCHAR(250),

@IsTran NVARCHAR(1),

@EmpSeq INT,

@SMAutoGoodIn INT,

@Dec INT,

@MasterSeq INT,

@BizUnit INT,

@SMLotSet INT, -- SELECT \* FROM \_TDASMinor WHERE MajorSeq = 6068

@RealLotNo NVARCHAR(20),

@QcNo NVARCHAR(20),

@QcSeq INT,

@GoodInSeq INT,

@IsAutoGoodIn NVARCHAR(1),

@IsLotMng NVARCHAR(1),

@SerialNo NVARCHAR(MAX),

@ServiceSeq INT,

@LanguageSeq INT,

@UserSeq INT,

@PgmSeq INT,

@IsTransaction INT,

@WorkOrderSeq INT,

@DeptSeq INT,

@CompanySeq INT

 

SELECT @CompanySeq = 1

 

-- 사원내부코드

SELECT @EmpSeq = 0

 

-- 생산실적 데이터

SET @ServiceSeq = 100520034

SET @WorkingTag = 'A'

 

SET @CompanySeq = @CompanySeq

SET @LanguageSeq = 2 -- 영문으로 오류메시지 출력

SELECT @UserSeq = 1 -- ISNULL((SELECT UserSeq FROM \_TCAUser WHERE CompanySeq = @CompanySeq AND EmpSeq = @EmpSeq),0)

SET @PgmSeq = 100520056

SET @IsTransaction = 1

 

SELECT @WorkOrderSeq = A.WorkOrderSeq,

@DeptSeq = A.DeptSeq

FROM \_TPDSFCWorkOrder AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.WorkOrderNo = @WorkOrderNo

AND A.WorkOrderSerl = @WorkOrderSerl

 

CREATE TABLE \#TPDSFCWorkOrder

(

IDX_NO INT,

WorkingTag NVARCHAR(1),

Status INT,

MessageType INT,

Result NVARCHAR(250),

WorkOrderSeq INT,

WorkOrderSerl INT,

WorkReportSeq INT,

WorkDate NVARCHAR(8),

DeptSeq INT,

WHSeq INT,

Qty DECIMAL(19,5),

OKQty DECIMAL(19,5),

SerialNo NVARCHAR(MAX),

RegEmpSeq INT

)

 

INSERT INTO \#TPDSFCWorkOrder

(

IDX_NO, WorkingTag, Status, MessageType, Result, WorkOrderSeq, WorkOrderSerl, WorkReportSeq, WorkDate,

DeptSeq,

WHSeq, Qty, OKQty, SerialNo,

RegEmpSeq

)

SELECT 1 AS IDX_NO, @WorkingTag, 0 Status, 0 MessageType, '' Result, @WorkOrderSeq, @WorkOrderSerl, @WorkReportSeq, @WorkDate,

@DeptSeq,

0, @Qty, @OKQty, @SerialNo,

@EmpSeq

 

SELECT @WorkingTag = ISNULL(@WorkingTag , ''),

@MesKey = ISNULL(@MesKey , ''),

@WorkDate = ISNULL(@WorkDate , ''),

@WorkOrderNo = ISNULL(@WorkOrderNo , ''),

@WorkOrderSerl = ISNULL(@WorkOrderSerl , 0),

@LotSerl = ISNULL(@LotSerl , ''),

@LotNo = ISNULL(@LotNo , ''),

@Qty = ISNULL(@Qty , 0),

@OKQty = ISNULL(@OKQty , 0),

@WorkEmpId = ISNULL(@WorkEmpId , ''),

@Remark = ISNULL(@Remark , ''),

@ItemSeq = ISNULL(@ItemSeq , ''),

@InputQty = ISNULL(@InputQty , ''),

@InputLotNo = ISNULL(@InputLotNo , ''),

--'' = ISNULL('' , '0'),

--'' = ISNULL('' , ''),

@Status = ISNULL(@Status , ''),

@ErrMsg = ISNULL(@ErrMsg , ''),

@WorkReportSeq = ISNULL(@WorkReportSeq , 0)

-- SELECT @EmpSeq = dbo.\_FCACompanyMultiUser(@CompanySeq, @UserSeq)

 

SELECT TOP 1 @SMAutoGoodIn = CONVERT(INT, EnvValue) FROM \_TCOMEnv WITH(NOLOCK) WHERE EnvSeq = 6202 AND CompanySeq = @CompanySeq

 

EXEC dbo.\_SWCOMEnv @CompanySeq, 5, 1, @@PROCID, @Dec OUTPUT

-- SELECT @SMAutoGoodIn = 6069001 -- 강제로 자동입고처리 되도록 함.

 

-- IDX_NO 자연수확인

UPDATE A

SET Result = 'IDX_NO must be a natural number.', -- 'IDX_NO는 자연수 이어야 합니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND ISNULL(A.IDX_NO, 0) \<= 0

 

-- IDX_NO 중복체크

UPDATE A

SET Result = 'Duplicate IDX_NO entered.', -- 'IDX_NO가 중복되어 입력되었습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

JOIN (SELECT IDX_NO FROM \#TPDSFCWorkOrder GROUP BY IDX_NO HAVING COUNT(\*) \> 1) AS B ON A.IDX_NO = B.IDX_NO

WHERE A.Status = 0

 

-- WorkingTag 확인

UPDATE A

SET Result = 'WorkingTag was entered incorrectly.', --'WorkingTag가 잘못 입력 되었습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag NOT IN ('A', 'U', 'D')

 

-- WorkinTag에 따른 파람확인

UPDATE A

SET Result = 'No operation instruction internal code or operation instruction order has been entered.', --'작업지시내부코드 또는 작업지시순번이 입력되지 않았습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND (ISNULL(A.WorkOrderSeq , 0) = 0 OR ISNULL(A.WorkOrderSerl, 0) = 0)

 

-- WorkinTag에 따른 파람확인

UPDATE A

SET Result = 'No operation instruction internal code or operation instruction order has been entered.', --'작업지시내부코드 또는 작업지시순번이 입력되지 않았습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND ISNULL(A.WorkReportSeq , 0) = 0

 

-- 작업일자 확인

UPDATE A

SET Result = 'Workdate does not exist.', --'작업일자가 존재 하지 않습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND LTRIM(RTRIM(ISNULL(A.WorkDate, ''))) = ''

-- 수량 확인

UPDATE A

SET Result = 'There is no production quantity.', --'생산수량이 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(A.Qty, 0) = 0

/\*

UPDATE A

SET Result = '생산수량은 300을 초과할수 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(A.Qty, 0) \> 300

\*/

-- OK수량 확인

UPDATE A

SET Result = 'There are no good quantity.', -- '양품수량이 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(A.OKQty, 0) = 0

UPDATE A

SET Result = 'Good quantity cannot be larger than production quantity.', -- '양품수량은 생산수량보다 클 수 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(A.OKQty, 0) \> ISNULL(A.Qty, 0)

 

-- 로그테이블과 실적테이블 join 실적테이블에 값이 있으면 오류

IF EXISTS ( SELECT TOP 1 1

FROM djb_TPDMesWorkReportSaveLog AS A WITH(NOLOCK)

JOIN \_TPDSFCWorkReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.MesKey = @MesKey AND ISNULL(B.WorkReportSeq,0) \<\> 0 )

BEGIN

UPDATE A

SET Result = 'There is a history generated by the corresponding MES key.', --'해당 MES키로 생성된 실적이 있습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

END

 

IF EXISTS(SELECT TOP 1 1 FROM \#TPDSFCWorkOrder WHERE Status \<\> 0)

BEGIN

/\*

UPDATE A

SET Result = '에러가 발생하였습니다.',

MessageType = 12007,

Status = 99998

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

\*/

SELECT @ErrMsg = Result ,

@WorkReportSeq = WorkReportSeq ,

@Status = 'Err'

FROM \#TPDSFCWorkOrder AS A

 

INSERT INTO djb_TPDMesWorkReportSaveLog(WorkingTag , MesKey , WorkDate, WorkOrderNo, WorkOrderSerl,

LotSerl , LotNo , Qty , OKQty , WorkEmpId ,

Remark , ItemSeq , InputQty, InputLotNo , Status ,

ErrMsg , WorkReportSeq , LastDateTIme)

SELECT @WorkingTag , @MesKey , @WorkDate , @WorkOrderNo , @WorkOrderSerl,

@LotSerl , @LotNo , @Qty , @OKQty , @WorkEmpId ,

@Remark , @ItemSeq , @InputQty , @InputLotNo , @Status ,

@ErrMsg , @WorkReportSeq , GETDATE()

 

RETURN

END

-- SELECT \* FROM \_TPDSFCWorkOrder WHERE WorkOrderSeq = 724 -- CompanySeq, WorkOrderSeq, WorkOrderSerl

-- SELECT \* FROM \_TPDSFCWorkReport WHERE WorkReportSeq = 104 -- CompanySeq, WorkReportSeq

-- SELECT \* FROM \_TPDQCTestReport WHERE SourceSeq = 104 -- CompanySeq, QCSeq

-- SELECT \* FROM \_TPDSFCGoodIn WHERE WorkReportSeq = 104 -- CompanySeq, GoodInSeq

CREATE TABLE \#TPDSFCWorkReport

(

IDX_NO INT,

WorkingTag NVARCHAR(1),

Status INT,

MessageType INT,

Result NVARCHAR(250),

 

WorkOrderSeq INT,

WorkOrderSerl INT,

MasterSeq INT,

WorkReportSeq INT,

QCNo NVARCHAR(20),

QCSeq INT,

GoodInSeq INT,

SubEtcInSeq INT,

BizUnit INT,

FactUnit INT,

WorkDate NVARCHAR(8),

DeptSeq INT,

WorkCenterSeq INT,

GoodItemSeq INT,

ProcRev NVARCHAR(2),

AssyItemSeq INT,

ProcSeq INT,

ProdUnitSeq INT,

ProdQty DECIMAL(19,5),

OKQty DECIMAL(19,5),

BadQty DECIMAL(19,5),

StdUnitProdQty DECIMAL(19,5),

StdUnitOKQty DECIMAL(19,5),

StdUnitBadQty DECIMAL(19,5),

RealLotNo NVARCHAR(100),

Remark NVARCHAR(1000),

FieldWhSeq INT,

WhSeq INT,

WorkType INT,

WorkCondition1 NVARCHAR(100),

WorkCondition2 NVARCHAR(100),

WorkCondition3 NVARCHAR(100),

WorkCondition4 DECIMAL(19,5),

WorkCondition5 DECIMAL(19,5),

WorkCondition6 DECIMAL(19,5),

WorkCond7 DECIMAL(19,5),

WorkCond8 NVARCHAR(100),

WorkCond9 NVARCHAR(100),

WorkCond10 NVARCHAR(100),

 

IsAutoGoodIn INT, -- SELECT \* FROM \_TDASMinor WHERE MajorSeq = 6069

 

SerialNo NVARCHAR(MAX)

)

 

 

-- \_TLGSerialMaster -- CompanySeq, SerialNo

-- \_TLGInOutSerialSub -- CompanySeq, InOutType, InOutSeq, InOutSerl, DataKind, InOutDataSerl, InOutSerialSerl

-- \_SWLGInOutDailyForWorkReport

-- \_SWLGInOutDailyforGoodIn

-- 신규

-- WorkCond1 제조일, WorkCond6 생산실적 Loss수량, WorkCond10 MES 생성여부

INSERT INTO \#TPDSFCWorkReport

(

IDX_NO, WorkingTag, Status, MessageType, Result,

WorkOrderSeq, WorkOrderSerl, MasterSeq, WorkReportSeq, QCNo, QCSeq, GoodInSeq, SubEtcInSeq,

BizUnit, FactUnit, WorkDate, DeptSeq, WorkCenterSeq, GoodItemSeq, ProcRev, AssyItemSeq, ProcSeq, ProdUnitSeq, ProdQty,

OKQty, BadQty, StdUnitProdQty, StdUnitOKQty, StdUnitBadQty, RealLotNo, Remark, FieldWhSeq, WhSeq, WorkType,

WorkCondition1, WorkCondition2, WorkCondition3, WorkCondition4, WorkCondition5,

WorkCondition6, WorkCond7, WorkCond8, WorkCond9, WorkCond10,

IsAutoGoodIn, SerialNo

)

SELECT A.IDX_NO, A.WorkingTag, A.Status, A.MessageType, A.Result,

A.WorkOrderSeq, A.WorkOrderSerl, 0 AS MasterSeq, 0 AS WorkReportSeq, '' AS QCNo, 0 AS QCSeq, 0 AS GoodInSeq, 0 AS SubEtcInSeq,

B.BizUnit, B.FactUnit, A.WorkDate, A.DeptSeq, B.WorkCenterSeq, B.GoodItemSeq, B.ProcRev, B.AssyItemSeq, B.ProcSeq, B.ProdUnitSeq, A.Qty AS ProdQty,

A.OKQty, 0 AS BadQty, 0 AS StdUnitProdQty, 0 AS StdUnitOKQty, 0 AS StdUnitBadQty,C.LotNo AS RealLotNo, @Remark AS Remark, D.FieldWhSeq, A.WHSeq, B.WorkType,

C.ProdDate AS WorkCondition1, B.WorkCond2 AS WorkCondition2, B.WorkCond3 AS WorkCondition3, B.WorkCond4 AS WorkCondition4, B.WorkCond5 AS WorkCondition5,

B.WorkCond6 AS WorkCondition6, B.WorkCond7, B.WorkCond8, B.WorkCond9, '1',

CASE WHEN ISNULL(@SMAutoGoodIn, 6069001) = 6069001 THEN '1' -- 전체사용

WHEN ISNULL(@SMAutoGoodIn, 6069001) = 6069003 AND ISNULL(F.SMAssetGrp, 0) = 6008004 THEN '1' -- 반제품만

ELSE '0' -- 사용안함

END, A.SerialNo

FROM \#TPDSFCWorkOrder AS A

JOIN \_TPDSFCWorkOrder AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkOrderSeq = A.WorkOrderSeq

AND B.WorkOrderSerl = A.WorkOrderSerl

JOIN djb_TPDSFCWorkOrder AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.WorkOrderSeq = C.WorkOrderSeq

AND B.WorkOrderSerl = C.WorkOrderSerl

LEFT OUTER JOIN \_TPDBaseWorkCenter AS D WITH(NOLOCK) ON D.CompanySeq = B.CompanySeq

AND D.WorkCenterSeq = B.WorkCenterSeq

LEFT OUTER JOIN \_TDAItem AS E WITH(NOLOCK) ON E.CompanySeq = B.CompanySeq

AND E.ItemSeq = B.GoodItemSeq

LEFT OUTER JOIN \_TDAItemAsset AS F WITH(NOLOCK) ON F.CompanySeq = E.CompanySeq

AND F.AssetSeq = E.AssetSeq

WHERE A.Status = 0

AND A.WorkingTag = 'A'

 

-- 수정 및 삭제

INSERT INTO \#TPDSFCWorkReport

(

IDX_NO, WorkingTag, Status, MessageType, Result,

WorkOrderSeq, WorkOrderSerl, MasterSeq, WorkReportSeq, QCNo, QCSeq, GoodInSeq, SubEtcInSeq,

BizUnit, FactUnit, WorkDate, DeptSeq, WorkCenterSeq, GoodItemSeq, ProcRev, AssyItemSeq, ProcSeq, ProdUnitSeq, ProdQty,

OKQty, BadQty, StdUnitProdQty, StdUnitOKQty, StdUnitBadQty, RealLotNo, Remark, FieldWhSeq, WhSeq, WorkType,

WorkCondition1, WorkCondition2, WorkCondition3, WorkCondition4, WorkCondition5,

WorkCondition6, WorkCond7, WorkCond8, WorkCond9, WorkCond10,

IsAutoGoodIn, SerialNo

)

SELECT A.IDX_NO, A.WorkingTag, A.Status, A.MessageType, A.Result,

A.WorkOrderSeq, A.WorkOrderSerl, 0 AS MasterSeq, C.WorkReportSeq, '' AS QCNo, 0 AS QCSeq, 0 AS GoodInSeq, C.SubEtcInSeq,

B.BizUnit, B.FactUnit, A.WorkDate, A.DeptSeq, B.WorkCenterSeq, B.GoodItemSeq, B.ProcRev, B.AssyItemSeq, B.ProcSeq, B.ProdUnitSeq, A.Qty AS ProdQty,

A.OKQty, 0 AS BadQty, 0 AS StdUnitProdQty, 0 AS StdUnitOKQty, 0 AS StdUnitBadQty, C.RealLotNo, B.Remark, D.FieldWhSeq, A.WHSeq, B.WorkType,

B.WorkCond1 AS WorkCondition1, B.WorkCond2 AS WorkCondition2, B.WorkCond3 AS WorkCondition3, B.WorkCond4 AS WorkCondition4, B.WorkCond5 AS WorkCondition5,

B.WorkCond6 AS WorkCondition6, B.WorkCond7, B.WorkCond8, B.WorkCond9, B.WorkCond10,

CASE WHEN ISNULL(@SMAutoGoodIn, 6069001) = 6069001 THEN '1' -- 전채사용

WHEN ISNULL(@SMAutoGoodIn, 6069001) = 6069003 AND ISNULL(F.SMAssetGrp, 0) = 6008004 THEN '1' -- 반제품만

ELSE '0' -- 사용안함

END, A.SerialNo

FROM \#TPDSFCWorkOrder AS A

JOIN \_TPDSFCWorkOrder AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkOrderSeq = A.WorkOrderSeq

AND B.WorkOrderSerl = A.WorkOrderSerl

LEFT OUTER JOIN \_TPDSFCWorkReport AS C WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.WorkOrderSeq = B.WorkOrderSeq

AND C.WorkOrderSerl = B.WorkOrderSerl

LEFT OUTER JOIN \_TPDBaseWorkCenter AS D WITH(NOLOCK) ON D.CompanySeq = B.CompanySeq

AND D.WorkCenterSeq = B.WorkCenterSeq

LEFT OUTER JOIN \_TDAItem AS E WITH(NOLOCK) ON E.CompanySeq = B.CompanySeq

AND E.ItemSeq = B.GoodItemSeq

LEFT OUTER JOIN \_TDAItemAsset AS F WITH(NOLOCK) ON F.CompanySeq = E.CompanySeq

AND F.AssetSeq = E.AssetSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

 

IF NOT EXISTS(SELECT TOP 1 1 FROM \#TPDSFCWorkReport)

BEGIN

SELECT @IsTran = '0'

 

UPDATE A

SET Result = 'There is nothing to deal with.', --'처리할 건이 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkOrder AS A

WHERE A.Status = 0

GOTO ERROR_PROC

RETURN

END

 

CREATE TABLE \#Item_djb -- 20230822 \_SWLGGetInOutStock(현재고) SP 에서 \#Item 사용하므로 오류 발생됨 임시테이블 명 수정

(

ItemSeq INT,

ItemClass INT,

AssetName NVARCHAR(100),

ItemClassName NVARCHAR(100),

ItemName NVARCHAR(200),

ItemNo NVARCHAR(100),

Spec NVARCHAR(200),

IsInQC NCHAR(1),

IsFinQC NCHAR(1),

IsOutQC NCHAR(1),

ItemClassMName NVARCHAR(100),

ItemClassLName NVARCHAR(100),

IsAfterQCPAM NCHAR(1),

IsPurQc NCHAR(1),

IsOspQC NCHAR(1)

)

--품목별예외등록 테이블 변환

CREATE TABLE \#ItemQCKind

(

ItemSeq INT,

IsInQC NCHAR(1),

IsFinQC NCHAR(1),

IsOutQC NCHAR(1),

IsAfterQCPAM NCHAR(1),

IsPurQc NCHAR(1),

IsOspQC NCHAR(1)

)

 

--대상품목담기

INSERT INTO \#Item_djb

SELECT A.ItemSeq, C.UMItemClass, B.AssetName, ISNULL(G.MinorName, ''), A.ItemName, A.ItemNo, A.Spec, '0', '0', '0',

H.ItemClassMName, H.ItemClassLName, '0', '0', '0'

FROM \_TDAItem AS A WITH(NOLOCK)

JOIN \_TDAItemAsset AS B WITH(NOLOCK) ON A.AssetSeq = B.AssetSeq

AND A.CompanySeq = B.CompanySeq

JOIN \_TDAItemClass AS C WITH(NOLOCK) ON A.ItemSeq = C.ItemSeq

AND C.UMajorItemClass IN (2001,2004)

AND A.CompanySeq = C.CompanySeq

LEFT OUTER JOIN \_TPDQAItemQcKind AS D WITH(NOLOCK) ON A.ItemSeq = D.ItemSeq

AND A.CompanySeq = D.CompanySeq

LEFT OUTER JOIN \_TPDQAItemClassQCKind AS E WITH(NOLOCK) ON C.UMItemClass = E.UMItemClass

AND C.CompanySeq = E.CompanySeq

LEFT OUTER JOIN \_TPDQACustNotQCItem AS F WITH(NOLOCK) ON A.ItemSeq = F.ItemSeq

AND A.CompanySeq = F.CompanySeq

LEFT OUTER JOIN \_TDAUMinor AS G WITH(NOLOCK) ON C.UMItemClass = G.MinorSeq

AND G.MajorSeq IN (2001,2004)

AND C.CompanySeq = G.CompanySeq

LEFT OUTER JOIN \_VDAItemClass AS H WITH(NOLOCK) ON H.CompanySeq = C.CompanySeq -- 17.01.11 hmlee 품목분류 추가

AND H.ItemClassSSeq = C.UMItemClass

JOIN \#TPDSFCWorkReport AS TT ON A.ItemSeq = TT.AssyItemSeq

WHERE B.SMAssetGrp \<\> 6008005 --재공품제외

AND A.CompanySeq = @CompanySeq

 

--------------------------------------

--품목소분류별 검사대상 체크

--------------------------------------

UPDATE A

SET IsInQC = B.IsInQC,

IsFinQC = B.IsFinQC,

IsOutQC = B.IsOutQC,

IsAfterQCPAM = B.IsAfterQCPAM,

IsPurQc = B.IsPurQc,

IsOspQC = B.IsOspQC

FROM \#Item_djb AS A

JOIN \_TPDQAItemClassQCKind AS B WITH(NOLOCK) ON A.ItemClass = B.UMItemClass

AND B.CompanySeq = @CompanySeq

 

UPDATE A

SET IsInQC = '0',

IsFinQC = '0',

IsOutQC = '0',

IsAfterQCPAM = '0',

IsPurQc = '0',

IsOspQC = '0'

FROM \#Item_djb AS A

JOIN \_TPDQAItemClassQCKind AS B WITH(NOLOCK) ON A.ItemClass = B.UMItemClass

AND B.IsNotQC = '1'

AND B.CompanySeq = @CompanySeq

 

 

--------------------------------------

--품목별검사여부 예외등록 체크

--------------------------------------

--테이블변환 (인수검사건부터 담고, 최종검사, 출하검사, 입고후시험 여부 확인)

--품목별예외등록에 등록된 품목은 품목별예외등록에 등록된 대로 조회되어야 하기 때문에,

--최종 조회를 편하게 하기 위해 테이블 변환

INSERT INTO \#ItemQCKind

SELECT DISTINCT A.ItemSeq, B.IsUse, '0', '0', '0', '0', '0'

FROM \#Item_djb AS A

JOIN \_TPDQAItemQCKind AS B WITH(NOLOCK) ON A.ItemSeq = B.ItemSeq

AND B.SMQcKind = 6018002

AND B.CompanySeq = @CompanySeq

UPDATE A

SET IsFinQC = B.IsUse

FROM \#ItemQCKind AS A

JOIN \_TPDQAItemQcKind AS B WITH(NOLOCK) ON A.ItemSeq = B.ItemSeq

AND B.SMQcKind = 6018003

AND B.CompanySeq = @CompanySeq

--품목별검사여부 예외등록 반영

UPDATE A

SET IsFinQC = B.IsFinQC

FROM \#Item_djb AS A

JOIN \#ItemQCKind AS B ON A.ItemSeq = B.ItemSeq

 

UPDATE A

SET IsAutoGoodIn = '0'

FROM \#TPDSFCWorkReport AS A

JOIN \#Item_djb AS B ON A.AssyItemSeq = B.ItemSeq

WHERE B.IsFinQC = '1'

 

 

-- 실제 지시건이 있는지 확인

/\*

UPDATE A

SET Result = '해당작업지시에 입력된 LotNo가 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

JOIN \_TDAItemStock AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.GoodItemSeq = B.ItemSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(B.IsLotMng,'') = '1'

AND ISNULL(A.RealLotNo, '') = ''

\*/

 

-- 실제 지시건이 있는지 확인

UPDATE A

SET Result = 'This work order does not exist.', --'해당 작업지시건이 존재 하지 않습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

LEFT OUTER JOIN \_TPDSFCWorkOrder AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkOrderSeq = A.WorkOrderSeq

AND B.WorkOrderSerl = A.WorkOrderSerl

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND B.CompanySeq IS NULL

 

-- 실제 실적건이 있는지 확인

UPDATE A

SET Result = 'This work report does not exist.', --'해당 작업실적건이 존재 하지 않습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

LEFT OUTER JOIN \_TPDSFCWorkReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND B.CompanySeq IS NULL

-- 작업지시 변경

UPDATE A

SET Result = 'Unable to change work order.',--'작업지시건을 변경할 수 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

LEFT OUTER JOIN \_TPDSFCWorkReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND (ISNULL(A.WorkOrderSeq, 0) \<\> ISNULL(B.WorkOrderSeq, 0) OR ISNULL(A.WorkOrderSerl, 0) \<\> ISNULL(B.WorkOrderSerl, 0))

 

UPDATE A

SET Result = 'Multi-inspection work report cannot be modified/deleted.' , --'다중검사처리된 작업실적건은 수정/삭제할 수 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

OUTER APPLY(SELECT COUNT(\*) AS Cnt

FROM \_TPDQCTestReport AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.SourceSeq = A.WorkReportSeq

AND Z.SourceType = 6097003 -- 최종검사처리 SELECT \* FROM \_TDASMinor WHERE MajorSeq = 6097

) AS B

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND ISNULL(B.Cnt, 0) \> 1

UPDATE A

SET Result = 'Multi-receiving work report cannot be modified/deleted.', --'다중입고처리된 작업실적건은 수정/삭제할 수 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

OUTER APPLY(SELECT COUNT(\*) AS Cnt

FROM \_TPDSFCGoodIn AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.WorkReportSeq = A.WorkReportSeq

) AS B

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND ISNULL(B.Cnt, 0) \> 1

 

 

-- 검사/생성입고 키값 찾아오기

UPDATE A

SET QCNo = ISNULL(B.QCNo , ''),

QCSeq = ISNULL(B.QCSeq , 0),

GoodInSeq = ISNULL(C.GoodInSeq, 0)

FROM \#TPDSFCWorkReport AS A

OUTER APPLY(SELECT TOP 1 Z.QCSeq, Z.QCNo

FROM \_TPDQCTestReport AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.SourceSeq = A.WorkReportSeq

AND Z.SourceType = 6097003 -- 최종검사처리 SELECT \* FROM \_TDASMinor WHERE MajorSeq = 6097

) AS B

OUTER APPLY(SELECT TOP 1 Z.GoodInSeq

FROM \_TPDSFCGoodIn AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.WorkReportSeq = A.WorkReportSeq

) AS C

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

 

-- 생산입고창고 찾아오기

UPDATE A

SET WHSeq = CASE WHEN ISNULL(A.WHSeq , 0) \<\> 0 THEN A.WHSeq

WHEN ISNULL(C.SMAssetGrp , 0) = 6008004 AND ISNULL(D.ProdInSemiWhSeq, 0) \<\> 0 THEN D.ProdInSemiWhSeq

WHEN ISNULL(D.ProdInWHSeq, 0) \<\> 0 THEN D.ProdInWHSeq

WHEN ISNULL(E.WHSeq , 0) \<\> 0 THEN E.WHSeq

ELSE 0

END

FROM \#TPDSFCWorkReport AS A

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.GoodItemSeq

LEFT OUTER JOIN \_TDAItemAsset AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.AssetSeq = C.AssetSeq

LEFT OUTER JOIN \_TPDBaseWorkCenter AS D WITH(NOLOCK) ON D.CompanySeq = @CompanySeq

AND D.WorkCenterSeq = A.WorkCenterSeq

OUTER APPLY(SELECT TOP 1 Z.WHSeq

FROM \_TDAWHItem AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.ItemSeq = A.GoodItemSeq

ORDER BY Z.WHSeq DESC ) AS E

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(A.WHSeq, 0) = 0

-- 필수체크

UPDATE A

SET Result = CASE WHEN ISNULL(A.BizUnit , 0) = 0 THEN 'Business Unit' --'사업단위'

WHEN ISNULL(A.WorkDate , '') = '' THEN 'Work Date' -- '작업일'

WHEN ISNULL(A.DeptSeq , 0) = 0 THEN 'Production department' --'생산부서'

WHEN ISNULL(A.WorkCenterSeq , 0) = 0 THEN 'Work Center' --'워크센터'

WHEN ISNULL(A.GoodItemSeq , 0) = 0 THEN 'Product item' --'제품품목'

WHEN ISNULL(A.AssyItemSeq , 0) = 0 THEN 'Process item' --'공정품목'

WHEN ISNULL(A.ProcRev , '') = '' THEN 'Process order' --'공정차수'

WHEN ISNULL(A.ProcSeq , 0) = 0 THEN 'Process' --'공정'

WHEN ISNULL(A.ProdUnitSeq , 0) = 0 THEN 'Unit' --'단위'

WHEN ISNULL(A.FieldWHSeq , 0) = 0 THEN 'On-site warehouse' --'현장창고'

WHEN ISNULL(A.WHSeq , 0) = 0 THEN 'warehouse' --'창고'

ELSE ''

END + 'is not entered .', --'이/가 입력 되지 않았습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND (ISNULL(A.BizUnit , 0) = 0 OR

ISNULL(A.WorkDate , '') = '' OR

ISNULL(A.DeptSeq , 0) = 0 OR

ISNULL(A.WorkCenterSeq , 0) = 0 OR

ISNULL(A.GoodItemSeq , 0) = 0 OR

ISNULL(A.AssyItemSeq , 0) = 0 OR

ISNULL(A.ProcRev , '') = '' OR

ISNULL(A.ProcSeq , 0) = 0 OR

ISNULL(A.ProdUnitSeq , 0) = 0 OR

ISNULL(A.FieldWHSeq , 0) = 0 OR

ISNULL(A.WHSeq , 0) = 0

)

-- MasterSeq채번

UPDATE A

SET MasterSeq = B.MasterSeq

FROM \#TPDSFCWorkReport AS A

JOIN (SELECT A.IDX_NO,

ROW_NUMBER() OVER(ORDER BY IDX_NO) AS MasterSeq

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

) AS B ON A.IDX_NO = B.IDX_NO

-- MasterSeq채번

-- 수불 마감

CREATE TABLE \#TLGClosingCheck

(

IDX_NO INT ,

InOutYM NCHAR(6) ,

ItemSeq INT ,

WHSeq INT ,

MessageType INT ,

Status INT ,

Result NVARCHAR(255)

)

 

INSERT INTO \#TLGClosingCheck

(

IDX_NO, InOutYM, ItemSeq, WHSeq, MessageType, Status, Result

)

SELECT A.IDX_NO, LEFT(A.WorkDate,6), A.AssyItemSeq, A.WHSeq, 0, 0, ''

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

 

EXEC \_SWLGClosingCheck @CompanySeq, @LanguageSeq

 

UPDATE A

SET Result = B.Result,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGClosingCheck AS B WITH(NOLOCK) ON A.IDX_NO = B.IDX_NO

WHERE A.Status = 0

AND B.Status \<\> 0

DROP TABLE \#TLGClosingCheck

 

 

-- 창고 사업단위 확인

UPDATE A

SET Result = 'The business unit of the warehouse entered is different from the business unit of the work instruction.', --'입력한 창고의 사업단위가 작업지시의 사업단위와 다릅니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

JOIN \_TDAWH AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WHSeq = A.WHSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(A.BizUnit, 0) \<\> ISNULL(B.BizUnit, 0)

 

/\*

HCWOO

 

-- 제품별 공정별소요자재(MBOM) 등록여부 체크

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

2170 , -- 제품별공정별소요자재의 공정품을 확인하세요.

@LanguageSeq ,

2031 , N'제품',

658 , N'공정차수',

510 , N'공정'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

LEFT OUTER JOIN \_TPDROUItemProc AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.BizUnit = A.BizUnit

AND B.ItemSeq = A.GoodItemSeq

AND B.AssyItemSeq = A.AssyItemSeq

AND B.ProcRev = A.ProcRev

AND B.ProcSeq = A.ProcSeq

LEFT OUTER JOIN \_TPDROUItemProcManagement AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.BizUnit = A.BizUnit

AND C.ItemSeq = A.GoodItemSeq

AND C.ProcRev = A.ProcRev

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND B.CompanySeq = @CompanySeq

AND (B.CompanySeq IS NULL OR C.CompanySeq IS NULL)

 

-- 해당 공정품과 제품별공정별소요자재에 등록된 공정품의 일치여부 체크

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1293 , -- 제품별공정별소요자재의 공정품을 확인하세요.

@LanguageSeq ,

11356 , N'제품별공정별소요자재',

3970 , N'공정품'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDROUItemProc AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.BizUnit = A.BizUnit

AND B.ItemSeq = A.GoodItemSeq

AND B.ProcRev = A.ProcRev

AND B.ProcSeq = A.ProcSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND A.AssyItemSeq \<\> B.AssyItemSeq

\*/

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1293 , -- 데이터의 최종공정여부을(를) 확인하세요.

@LanguageSeq ,

355 , N'데이터',

3240 , N'최종공정여부'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDROUItemProc AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.BizUnit = A.BizUnit

AND B.ItemSeq = A.GoodItemSeq

AND B.ProcRev = A.ProcRev

AND B.ProcSeq = A.ProcSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND B.IsLastProc \<\> '1'

 

-- 사내 워크센터가 아닌 경우 체크

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1293 , -- 워크센터의 워크센터구분을(를) 확인하세요.

@LanguageSeq ,

1059 , N'워크센터',

1060 , N'워크센터구분'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDBaseWorkCenter AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkCenterSeq = A.WorkCenterSeq

AND B.SMWorkCenterType NOT IN (6011001, 6011002)

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

-- 확정/중단확인

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1152 , -- @1된 건이 선택되었습니다.....(SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 1152)

@LanguageSeq ,

1326,'' -- SELECT \* FROM \_TCADictionary WHERE Word like '%중단%'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TCOMStop AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.TableSeq = 5 -- SELECT \* FROM \_TCOMProgTable WHERE ProgTableSeq = 5

AND B.StopSeq = A.WorkOrderSeq

AND B.StopSerl = A.WorkOrderSerl

AND B.StopSubSerl = 0

 

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(B.IsStop, '') = '1' -- 중단

 

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1152 , -- @1된 건이 선택되었습니다.....(SELECT \* FROM \_TCAMessageLanguage WHERE Message like '%선택%')

@LanguageSeq ,

11177,'' -- SELECT \* FROM \_TCADictionary WHERE Word like '%미확정%'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TCOMConfirm AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ConfirmSeq = 6320 -- SELECT \* FROM \_TCOMConfirmDef WHERE ConfirmSeq = 6320

AND B.CfmSeq = A.WorkOrderSeq

AND B.CfmSerl = A.WorkOrderSerl

AND B.CfmSubSerl = 0

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

AND ISNULL(B.CfmCode, '') = '0' -- 미확정

-- 진행확인

SELECT A.WorkOrderSeq, A.WorkOrderSerl,

CASE WHEN (SELECT TOP 1 EnvValue FROM \_TCOMEnv WITH(NOLOCK) WHERE EnvSEq = 6218 AND CompanySeq = @CompanySeq) = '1'

AND

ISNULL(A.Qty, 0) \< ISNULL(B.Qty, 0) - ISNULL(C.Qty, 0) + ISNULL(D.Qty, 0) THEN '1'

ELSE '0'

END AS IsOverFlow,

ISNULL(A.Qty, 0) AS OrderQty,

(ISNULL(B.Qty, 0) - ISNULL(C.Qty, 0) + ISNULL(D.Qty, 0)) AS ReportQty

INTO \#TPDSFCWorkOrderProg

FROM (SELECT DISTINCT

B.WorkOrderSeq, B.WorkOrderSerl, B.OrderQty AS Qty

FROM \#TPDSFCWorkReport AS Z

JOIN \_TPDSFCWorkOrder AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkOrderSeq = Z.WorkOrderSeq

AND B.WorkOrderSerl = Z.WorkOrderSerl

) AS A

OUTER APPLY (SELECT SUM(ISNULL(Z.ProdQty, 0)) AS Qty

FROM \#TPDSFCWorkReport AS Z

WHERE Z.WorkOrderSeq = A.WorkOrderSeq

AND Z.WorkOrderSerl = A.WorkOrderSerl

AND Z.Status = 0

AND Z.WorkingTag In ('A', 'U')

) AS B

OUTER APPLY (SELECT SUM(ISNULL(Y.ProdQty, 0)) AS Qty

FROM \#TPDSFCWorkReport AS Z

JOIN \_TPDSFCWorkReport AS Y WITH(NOLOCK) ON Y.CompanySeq = @CompanySeq

AND Y.WorkReportSeq = Z.WorkReportSeq

WHERE Z.WorkOrderSeq = A.WorkOrderSeq

AND Z.WorkOrderSerl = A.WorkOrderSerl

AND Z.Status = 0

AND Z.WorkingTag In ('U', 'D')

) AS C

OUTER APPLY (SELECT SUM(ISNULL(Z.ProdQty, 0)) AS Qty

FROM \_TPDSFCWorkReport AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.WorkOrderSeq = A.WorkOrderSeq

AND Z.WorkOrderSerl = A.WorkOrderSerl

) AS D

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

106 ,

@LanguageSeq , -- 실적수량은(는) 작업지시의 지시수량을(를) 초과할 수 없습니다.

34746 , N'실적수량',

16926 , N'작업지시',

9012 , N'지시수량'

 

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \#TPDSFCWorkOrderProg AS B ON B.WorkOrderSeq = A.WorkOrderSeq

AND B.WorkOrderSerl = A.WorkOrderSerl

AND B.IsOverFlow = '1'

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

 

-- 생산입고로 간 데이터 체크

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1044 , -- 다음 작업이 진행되어서 변경,삭제할 수 없습니다.

@LanguageSeq

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCGoodIn AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND A.IsAutoGoodIn \<\> '1' -- 자동입고가 아닌건..

 

-- 검사여부체크 (검사는 진행 연결이 되지 않음)

-- 최종검사

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1285, -- 최종검사으로(로) 진행된 자료는 처리할 수 없습니다.

@LanguageSeq,

9410, N'최종검사',

2524, N'진행',

326 , N'처리'

 

UPDATE A

SET Result = @Results, --REPLACE(@Results, N'으로(로)', N'로'),

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDQCTestReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.SourceSeq = A.WorkReportSeq

AND B.SourceType IN (6097003) -- 6097003: 최종검사

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND B.SMTestResult \<\> 6035001 -- 자동입고가 아닌 건 (실제 검사처리) -- SELECT \* FROM \_TDASMinor WHERE MajorSEq = 6035

 

-- 공정검사

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1285 , -- 공정검사으로(로) 진행된 자료는 처리할 수 없습니다.

@LanguageSeq ,

3965 , N'공정검사',

2524 , N'진행',

326 , N'처리'

 

UPDATE A

SET Result = @Results, --REPLACE(@Results, N'으로(로)', N'로'),

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDQCTestReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.SourceSeq = A.WorkReportSeq

AND B.SourceType IN (6097004) -- 6097004: 공정검사

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND B.SMTestResult \<\> 60350016 -- 자동입고가 아닌 건 (실제 검사처리)

 

-- 외주용역비 전표 생성 여부

IF ISNULL((SELECT TOP 1 EnvValue FROM \_TCOMEnv WITH(NOLOCK) WHERE EnvSeq = 6513 AND CompanySeq = @CompanySeq), '') NOT IN (N'1', N'True')

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1310 , -- 외주용역의 전표이(가) 생성되어 있어 처리 할 수 없습니다.

@LanguageSeq ,

23697 , N'외주용역',

9 , N'전표',

326 , N'처리'

 

UPDATE A

SET Result = @Results, --REPLACE(@Results, N'이(가)', N'가'),

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCOutsourcingCostItem AS B WITH(NOLOCK) ON A.WorkReportSeq = B.WorkReportSeq

AND B.CompanySeq = @CompanySeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND ISNULL(B.SlipSeq, 0) \> 0

END

ELSE

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1310 , -- 외주용역의 전표이(가) 생성되어 있어 처리 할 수 없습니다.

@LanguageSeq ,

23697 , N'외주용역',

9 , N'전표',

326 , N'처리'

UPDATE A

SET Result = @Results, --REPLACE(@Results, N'이(가)', N'가'),

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCGoodIn AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

JOIN \_TPDSFCOutsourcingCostItem AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.WorkReportSeq = B.GoodInSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

AND ISNULL(C.SlipSeq, 0) \> 0

END

 

-- 공용자재 투입배부처리 된 경우 수정/삭제 불가

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1109 , -- 월공용자재투입배부처리에 적용된 내역이 있으므로 수정/삭제할 수 없습니다.

@LanguageSeq ,

50367 , N'월공용자재투입배부처리'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCComMatInputDiv AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.WorkingTag IN ('U', 'D')

AND A.Status = 0

 

-- 에러 발생시...

IF EXISTS(SELECT TOP 1 1 FROM \#TPDSFCWorkReport WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '0'

GOTO ERROR_PROC

RETURN

END

-- 재계산

UPDATE A

SET BadQty = ISNULL(A.ProdQty, 0) - ISNULL(A.OKQty, 0),

StdUnitProdQty = dbo.\_FWPDGetDecPointQty((ISNULL(A.ProdQty, 0) \* ISNULL(D.ConvQty, 0)), @Dec, C.SMDecPointSeq),

StdUnitOKQty = dbo.\_FWPDGetDecPointQty((ISNULL(A.OKQty, 0) \* ISNULL(D.ConvQty, 0)), @Dec, C.SMDecPointSeq),

StdUnitBadQty = dbo.\_FWPDGetDecPointQty(((ISNULL(A.ProdQty, 0) - ISNULL(A.OKQty, 0)) \* ISNULL(D.ConvQty, 0)), @Dec, C.SMDecPointSeq)

FROM \#TPDSFCWorkReport AS A

JOIN \_TDAItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.AssyItemSeq

JOIN \_TDAUnit AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.UnitSeq = C.UnitSeq

OUTER APPLY (SELECT CASE WHEN ISNULL(Z.ConvDen,0) = 0 THEN 0

ELSE ISNULL(Z.ConvNum,0) / ISNULL(Z.ConvDen,0)

END AS ConvQty

FROM \_TDAItemUnit AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.ItemSeq = A.AssyItemSeq

AND Z.UnitSeq = A.ProdUnitSeq

AND ISNULL(Z.ConvDen,0) \<\> 0

) AS D

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

 

 

 

-- \[작업실적\] 작업실적 LotNo 설정

SELECT TOP 1 @SMLotSet = CONVERT(INT, EnvValue)

FROM \_TCOMEnv WITH(NOLOCK)

WHERE EnvSeq = 6213

AND CompanySeq = @CompanySeq

 

SELECT @MasterSeq = 0

WHILE 1=1

BEGIN

SELECT TOP 1

@MasterSeq = A.MasterSeq,

@WorkDate = A.WorkDate,

@BizUnit = A.BizUnit,

@IsAutoGoodIn = A.IsAutoGoodIn,

@IsLotMng = ISNULL(B.IsLotMng, '')

FROM \#TPDSFCWorkReport AS A

LEFT OUTER JOIN \_TDAItemStock AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.AssyItemSeq

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND A.MasterSeq \> @MasterSeq

ORDER BY A.MasterSeq

 

IF @@ROWCOUNT = 0 BREAK

-- 초기화

SELECT @RealLotNo = '', @WorkReportSeq = 0, @QcNo = '', @QcSeq = 0, @GoodInSeq = 0

/\* LotNo =\> 작업지시에서 생성함 HCWOO

IF @IsLotMng = '1' -- Lot관리하는 건이면~

BEGIN

-- LotNo자동생성

IF @SMLotSet = 6068003 -- 자동생성

BEGIN

EXEC \_SWCOMCreateNo 'PD', '\_TPDSFCWorkReport', @CompanySeq, @BizUnit, @WorkDate, @RealLotNo OUTPUT -- RealLotNo 생성

END

ELSE IF @SMLotSet = 6068002 -- 작업지시번호

BEGIN

SELECT @RealLotNo = B.WorkOrderNo

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCWorkOrder AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkOrderSeq = A.WorkOrderSeq

AND B.WorkOrderSerl = A.WorkOrderSerl

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND A.MasterSeq = @MasterSeq

END

ELSE IF @SMLotSet = 6068001 -- 생산계획번호

BEGIN

SELECT @RealLotNo = C.ProdPlanNo

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCWorkOrder AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkOrderSeq = A.WorkOrderSeq

AND B.WorkOrderSerl = A.WorkOrderSerl

JOIN \_TPDMPSDailyProdPlan AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.ProdPlanSeq = C.ProdPlanSeq

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND A.MasterSeq = @MasterSeq

END

END

\*/

-- 자동입고

IF @IsAutoGoodIn = '1'

BEGIN

EXEC \_SWCOMCreateNo 'PD', '\_TPDQCTestReport', @CompanySeq, @BizUnit, @WorkDate, @QcNo OUTPUT -- RealLotNo 생성

EXEC @QcSeq = \_SWCOMCreateSeq @CompanySeq, '\_TPDQCTestReport' , 'QCSeq' , 1 -- QcSeq 생성

EXEC @GoodInSeq = \_SWCOMCreateSeq @CompanySeq, '\_TPDSFCGoodIn' , 'GoodInSeq', 1 -- GoodInSeq 생성

 

UPDATE A

SET --RealLotNo = @RealLotNo,

QcNo = @QcNo,

QcSeq = @QcSeq + 1,

GoodInSeq = @GoodInSeq + 1

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND A.MasterSeq = @MasterSeq

END

EXEC @WorkReportSeq = \_SWCOMCreateSeq @CompanySeq, '\_TPDSFCWorkReport', 'WorkReportSeq', 1 -- WorkReportSeq 생성

 

UPDATE A

SET --RealLotNo = @RealLotNo,

WorkReportSeq = @WorkReportSeq + 1

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND A.MasterSeq = @MasterSeq

END

/\*투입\*/

CREATE TABLE \#TPDSFCMatInput

(

IDXSeq INT IDENTITY(1,1),

Seq BIGINT,

 

WorkingTag NVARCHAR(1),

Status INT,

MessageType INT,

Result NVARCHAR(250),

 

CompanySeq INT,

WorkReportSeq INT,

MESSeq INT,

MESWorkReportNo NVARCHAR(30),

ItemSerl INT,

InputDate NCHAR(8),

MatItemSeq INT,

MatUnitSeq INT,

Qty DECIMAL(19,5),

StdUnitQty DECIMAL(19,5),

RealLotNo NVARCHAR(30),

SerialNoFrom NVARCHAR(20),

ProcSeq INT,

AssyYn NCHAR(1),

IsConsign NCHAR(1),

GoodItemSeq INT,

InputType INT,

IsPaid NCHAR(1),

IsPjt NCHAR(1),

PjtSeq INT,

WBSSeq INT,

LastUserSeq INT,

LastDateTime DATETIME,

Remark NVARCHAR(200),

ProdWRSeq INT,

IsReInput NCHAR(1),

WHSeq INT,

PgmSeq INT

)

/\*

CREATE TABLE \#TPDSFCMatInput

(

WorkingTag NVARCHAR(1),

Status INT,

MessageType INT,

Result NVARCHAR(250),

 

WorkReportSeq INT,

ItemSerl INT,

MatItemSeq INT,

MatUnitSeq INT,

Qty DECIMAL(19,5),

StdUnitQty DECIMAL(19,5),

RealLotNo NVARCHAR(100),

WhSeq INT

)

\*/

-- \_SWPDSFCWorkReportMatOutAutoSave

-- 자재투입

DECLARE @Tmp INT,

@TmpItemSeq NVARCHAR(MAX),

@TmpInputQty NVARCHAR(MAX)

 

SELECT @TmpItemSeq = @ItemSeq ,

@TmpInputQty = @InputQty

 

--\_FCOMSplit

IF (ISNULL(@TmpItemSeq, '') \<\> '' AND ISNULL(@TmpInputQty, '') \<\> '')

BEGIN

 

IF SUBSTRING(@TmpItemSeq, LEN(@TmpItemSeq), 1) \<\> ','

BEGIN

SELECT @TmpItemSeq = @TmpItemSeq + ','

END

IF SUBSTRING(@TmpInputQty, LEN(@TmpInputQty), 1) \<\> ','

BEGIN

SELECT @TmpInputQty = @TmpInputQty + ','

END

SELECT @Tmp = 1

 

CREATE TABLE \#TempInputItem

(

Idx INT IDENTITY(1,1), ItemSeq INT

)

CREATE TABLE \#InputQty

(

Idx INT IDENTITY(1,1),

InputQty DECIMAL(19,5)

)

 

WHILE (1=1)

BEGIN

INSERT \#TempInputItem (ItemSeq)

SELECT SUBSTRING(@TmpItemSeq, @Tmp, CHARINDEX(',', @TmpItemSeq, @Tmp) - @Tmp)

 

SELECT @Tmp = CHARINDEX(',', @TmpItemSeq, @Tmp) + 1

 

--IF @Tmp = 1 RETURN

IF @Tmp \> LEN(@TmpItemSeq)

BREAK

END

 

SELECT @Tmp = 1

 

WHILE (1=1)

BEGIN

INSERT \#InputQty (InputQty)

SELECT SUBSTRING(@TmpInputQty, @Tmp, CHARINDEX(',', @TmpInputQty, @Tmp) - @Tmp)

 

SELECT @Tmp = CHARINDEX(',', @TmpInputQty, @Tmp) + 1

 

--IF @Tmp = 1 RETURN

IF @Tmp \> LEN(@TmpInputQty)

BREAK

END

 

SELECT A.Idx ,A.ItemSeq, B.InputQty, ISNULL(C.IsLotMng, '0') AS IsLotMng

INTO \#TempMatItem

FROM \#TempInputItem AS A

FULL OUTER JOIN \#InputQty AS B ON A.Idx = B.Idx

LEFT OUTER JOIN \_TDAItemStock AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq AND A.ItemSeq = C.ItemSeq

 

IF EXISTS(SELECT TOP 1 1 FROM \#TempMatItem WHERE ISNULL(ItemSeq,0) = 0 OR ISNULL(InputQty,0) = 0)

BEGIN

UPDATE A

SET Result = 'A required material error has occurred.', --'소요자재 에러가 발생하였습니다.',

MessageType = 12007,

Status = 99998

FROM \#TPDSFCWorkOrder AS A JOIN \#TempMatItem AS B ON (1=1)

WHERE ISNULL(B.ItemSeq,0) = 0 OR ISNULL(B.InputQty,0) = 0

GOTO ERROR_PROC

RETURN

END

-- Lot 사용시 자동출고는 못쓰지만, 가능케 하도록 수정..

-- 선입선출로 할 예정, LotMaster의 CreateDate 로 일단은

--SELECT \* FROM \_TLGLotMaster

 

DECLARE @OutWHSeq INT,

@CurrDate NCHAR(8)

 

SELECT @CurrDate = CONVERT(NCHAR(8), GETDATE(), 112)

 

SELECT @OutWHSeq = B.MatOutWhSeq

FROM \#TPDSFCWorkReport AS A JOIN \_TPDBaseWorkCenter AS B WITH(NOLOCK) ON A.WorkCenterSeq = B.WorkCenterSeq AND B.CompanySeq = @CompanySeq

 

 

CREATE TABLE \#NeedQtySum

(

Seq INT IDENTITY(1,1),

WorkOrderSeq INT,

WorkOrderSerl INT,

WorkReportSeq INT,

ProcSeq INT,

MatItemSeq INT,

UnitSeq INT,

Qty NUMERIC(19,5),

StdUnitQty NUMERIC(19,5),

InputType INT,

IsAutoOut NCHAR(1),

AssyYn NCHAR(1),

BizUnit INT,

OutWHSeq INT,

WorkCenterSeq INT,

GoodItemSeq INT, -- 제품코드

ProcRev NCHAR(2), -- 공정차수

AssyItemSeq INT, -- 공정품코드

ItemSerl INT,

WorkDate NCHAR(8),

FieldWHSeq INT, -- 워크센터 현장창고

IsLot NCHAR(1), -- Lot관리여부

MESSeq INT,

MESWorkReportNo NVARCHAR(30)

)

 

INSERT INTO \#NeedQtySum(

WorkOrderSeq ,WorkOrderSerl, WorkReportSeq, ProcSeq , MatItemSeq ,

UnitSeq ,Qty , StdUnitQty , InputType , IsAutoOut ,

AssyYn ,BizUnit , OutWHSeq , WorkCenterSeq , GoodItemSeq,

ProcRev ,AssyItemSeq , ItemSerl , WorkDate , FieldWHSeq ,

IsLot

)

SELECT A.WorkOrderSeq, A.WorkOrderSerl, A.WorkReportSeq, A.ProcSeq , B.ItemSeq ,

C.UnitSeq , B.InputQty , B.InputQty , 6042002 , '' ,

'' , A.BizUnit , @OutWHSeq , A.WorkCenterSeq, A.GoodItemSeq,

A.ProcRev , A.AssyItemSeq , B.Idx , @WorkDate , A.FieldWhSeq ,

B.IsLotMng

FROM \#TPDSFCWorkReport AS A

JOIN \#TempMatItem AS B ON 1=1

JOIN \_TDAItem AS C ON C.CompanySeq = @CompanySeq AND B.ItemSeq = C.ItemSeq

 

CREATE TABLE \#GetInOutItemLot

(

LotNo NVARCHAR(30),

ItemSeq INT,

ValiDate NCHAR(8),

CreateDate NCHAR(8)

)

CREATE TABLE \#GetInOutLotStock

(

BizUnit INT,

WHSeq INT,

MainWHSeq INT,

LotNo NVARCHAR(30),

ItemSeq INT,

UnitSeq INT,

PrevQty DECIMAL(19,5),

InQty DECIMAL(19,5),

OutQty DECIMAL(19,5),

StockQty DECIMAL(19,5),

STDPrevQty DECIMAL(19,5),

STDInQty DECIMAL(19,5),

STDOutQty DECIMAL(19,5),

STDStockQty DECIMAL(19,5)

)

CREATE TABLE \#GetInOutLotStock_tmp

(

BizUnit INT,

WHSeq INT,

MainWHSeq INT,

LotNo NVARCHAR(30),

ItemSeq INT,

UnitSeq INT,

STDStockQty DECIMAL(19,5)

)

CREATE TABLE \#GetInOutLotStock_Result

(

BizUnit INT,

WHSeq INT,

MainWHSeq INT,

LotNo NVARCHAR(30),

ItemSeq INT,

UnitSeq INT,

STDStockQty DECIMAL(19,5)

)

-- 창고별 LOT 재고

CREATE TABLE \#ItemLotStock

(

WorkDate NCHAR(8),

ItemLotSerl INT ,

WHSeq INT ,

LotNo NVARCHAR(30) ,

ItemSeq INT ,

STDStockQty DECIMAL(19,5)

)

CREATE TABLE \#ItemLotQty

(

DataSeq INT ,

ItemLotSerl INT ,

WHSeq INT ,

ItemSeq INT ,

LotNo NVARCHAR(30) ,

Qty DECIMAL(19,5) ,

STDQty DECIMAL(19,5) ,

Type NCHAR(1),

WorkDate NCHAR(8) ,

WorkReportSeq INT

)

-- 출고 대상 수주

CREATE TABLE \#ItemOrderQty

(

DataSeq INT,

IDX_NO INT ,

WHSeq INT ,

ItemSeq INT ,

STDQty DECIMAL(19,5) ,

WorkReportSeq INT

)

 

-- 일단 Lot자재 뽑기

SELECT A.WorkDate AS WorkDate ,

A.WorkReportSeq AS WorkReportSeq,

A.ItemSerl AS ItemSerl ,

A.MatItemSeq AS MatItemSeq ,

A.UnitSeq ,

A.Qty AS Qty ,

A.StdUnitQty AS StdUnitQty ,

ROW_NUMBER() OVER(ORDER BY Seq) AS IDX_NO,

A.OutWHSeq AS OutWHSeq

INTO \#LotItem

FROM \#NeedQtySum AS A

WHERE A.IsLot = N'1'

DECLARE @ItemLotSerl INT,

@DataSeq INT,

@NumItemSeq INT,

@ItemLotSerlMAX INT

-- @BizUnit INT

--SELECT TOP 1

-- @BizUnit = BizUnit

-- FROM \#T_IF_WorkReport

INSERT INTO \#GetInOutItemLot (LotNo, ItemSeq, ValiDate, CreateDate)

SELECT DISTINCT L.LotNo, L.ItemSeq, L.ValiDate, L.CreateDate

FROM \#LotItem AS T JOIN \_TLGLotMaster AS L WITH (NOLOCK) ON L.CompanySeq = @CompanySeq

AND T.MatItemSeq = L.ItemSeq

JOIN \_TDAItem AS A WITH(NOLOCK) ON L.ItemSeq = A.ItemSeq

AND L.CompanySeq = A.CompanySeq

JOIN \_TDAItemAsset AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

AND A.AssetSeq = C.AssetSeq

JOIN \_TDAItemStock AS F WITH(NOLOCK) ON F.CompanySeq = A.CompanySeq

AND F.ItemSeq = A.ItemSeq

WHERE A.CompanySeq = @CompanySeq

AND C.IsQty \<\> '1'

AND F.IsLotMng = '1'

AND T.WorkDate = @WorkDate

--select \* from \#LotItem order by MatItemSeq, outwhseq

--select \* from \#GetInOutItemLot

-- 창고재고 가져오기 (작업일당일)

EXEC \_SWLGGetInOutLotStock @CompanySeq = @CompanySeq, -- 법인코드

@BizUnit = @BizUnit, -- 사업부문

@DateFr = @WorkDate, -- 조회기간Fr

@DateTo = @WorkDate, -- 조회기간To

@WHSeq = 0, -- 창고지정

@SMWHKind = 0, -- 창고구분별 조회

@IsSubDisplay = '0', -- 기능창고포함('1' : 기능창고별 조회, '0' : 사업단위창고에 포함)

@IsOnlyStock = '1', -- 재고만 표시('1' : 재고, '0' : 이월,입고,출고,재고)

@IsStockAmt = '0', -- 재고금액 계산

@StockAmtType = 0, -- 전월재고단가, 표준재고단가

@UnitType = 1, -- 1: 기준단위, 2: 수불단위, 3: 환산단위(단위별 조회)

@ConvUnitSeq = 0, -- 환산단위코드

@QryType = 'S', -- 'S': 1007 실재고, 'B':1008 자산포함, 'A':1011 가용재고

@IsTrustCust = '' -- 수탁여부

-- 해당 작업일로 일단 담기.

INSERT INTO \#GetInOutLotStock_tmp

SELECT BizUnit ,

WHSeq ,

MainWHSeq ,

LotNo ,

ItemSeq ,

UnitSeq ,

STDStockQty

FROM \#GetInOutLotStock

TRUNCATE TABLE \#GetInOutLotStock

-- 창고재고 가져오기 (현재일기준)

EXEC \_SWLGGetInOutLotStock @CompanySeq = @CompanySeq, -- 법인코드

@BizUnit = @BizUnit, -- 사업부문

@DateFr = @CurrDate, -- 조회기간Fr

@DateTo = @CurrDate, -- 조회기간To

@WHSeq = 0, -- 창고지정

@SMWHKind = 0, -- 창고구분별 조회

@IsSubDisplay = '0', -- 기능창고포함('1' : 기능창고별 조회, '0' : 사업단위창고에 포함)

@IsOnlyStock = '1', -- 재고만 표시('1' : 재고, '0' : 이월,입고,출고,재고)

@IsStockAmt = '0', -- 재고금액 계산

@StockAmtType = 0, -- 전월재고단가, 표준재고단가

@UnitType = 1, -- 1: 기준단위, 2: 수불단위, 3: 환산단위(단위별 조회)

@ConvUnitSeq = 0, -- 환산단위코드

@QryType = 'S', -- 'S': 1007 실재고, 'B':1008 자산포함, 'A':1011 가용재고

@IsTrustCust = '' -- 수탁여부

INSERT INTO \#GetInOutLotStock_tmp

SELECT BizUnit ,

WHSeq ,

MainWHSeq ,

LotNo ,

ItemSeq ,

UnitSeq ,

STDStockQty

FROM \#GetInOutLotStock

-- 작업일과, 현재일에 대한 재고중 최소값으로 투입한다.(-재고를 만들지 않기 위함)

INSERT INTO \#GetInOutLotStock_Result

SELECT BizUnit ,

WHSeq ,

MainWHSeq ,

LotNo ,

ItemSeq ,

UnitSeq ,

MIN(STDStockQty) AS STDStockQty

FROM \#GetInOutLotStock_tmp

GROUP BY BizUnit ,

WHSeq ,

MainWHSeq ,

LotNo ,

ItemSeq ,

UnitSeq

 

IF EXISTS (SELECT A.ItemSeq, SUM(A.STDStockQty), B.Qty

FROM \#GetInOutLotStock_Result AS A JOIN \#NeedQtySum AS B ON A.ItemSeq = B.MatItemSeq

GROUP BY A.ItemSeq, B.Qty

HAVING SUM(A.STDStockQty) \< B.Qty)

BEGIN

SELECT @ErrMsg = 'Lot management item material inventory is insufficient.', --'Lot관리 품목 자재재고가 부족합니다.' ,

@WorkReportSeq = 0 ,

@Status = 'Err'

 

INSERT INTO djb_TPDMesWorkReportSaveLog(WorkingTag , MesKey , WorkDate, WorkOrderNo, WorkOrderSerl,

LotSerl , LotNo , Qty , OKQty , WorkEmpId ,

Remark , ItemSeq , InputQty, InputLotNo , Status ,

ErrMsg , WorkReportSeq , LastDateTIme)

SELECT @WorkingTag , @MesKey , @WorkDate , @WorkOrderNo , @WorkOrderSerl,

@LotSerl , @LotNo , @Qty , @OKQty , @WorkEmpId ,

@Remark , @ItemSeq , @InputQty , @InputLotNo , @Status ,

@ErrMsg , @WorkReportSeq , GETDATE()

RETURN

END

 

-- HCWOO 여기부터..

 

-- 선입선출로 빼야함..

--select \* from \#NeedQtySum WHERE IsLot = '1'

INSERT INTO \#ItemLotStock (ItemLotSerl, WHSeq , LotNo, ItemSeq, STDStockQty )

SELECT ROW_NUMBER() OVER (PARTITION BY A.ItemSeq ORDER BY B.CreateDate, A.LotNo),

A.WHSeq, A.LotNo, A.ItemSeq, A.STDStockQty

FROM (SELECT DISTINCT WorkDate, MatItemSeq, OutWHSeq

FROM \#LotItem) AS X

JOIN \#GetInOutLotStock_Result AS A ON A.ItemSeq = X.MatItemSeq AND A.WHSeq = X.OutWHSeq

JOIN \_TLGLotMaster AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.LotNo = A.LotNo

AND B.ItemSeq = A.ItemSeq

WHERE A.STDStockQty \> 0

AND X.WorkDate = @WorkDate

 

 

-- select '창고재고'

-- select \* from \#GetInOutLotStock where STDStockQty\>0 order by itemseq, whseq

--select '대상lot'

--select \* from \#ItemLotStock order by itemseq, whseq

 

--select \* from \#GetInOutLotStock

INSERT INTO \#ItemOrderQty

SELECT ROW_NUMBER() OVER(ORDER BY IDX_NO), IDX_NO, OutWHSeq, MatItemSeq, StdUnitQty, WorkReportSeq

FROM \#LotItem

WHERE WorkDate = @WorkDate

--select \* From \#ItemOrderQty order by DataSeq

 

DECLARE @Count INT,

@WHSeq INT

 

SELECT @Count = COUNT(1) FROM \#ItemOrderQty

SET @ItemLotSerl = 1

SET @DataSeq = 0

SELECT @WHSeq = 0

WHILE(@DataSeq \<= @Count)

BEGIN

SELECT TOP 1

@DataSeq = A.DataSeq,

@NumItemSeq = A.ItemSeq,

@WHSeq = A.WHSeq

FROM \#ItemOrderQty AS A

WHERE A.DataSeq \> @DataSeq

AND EXISTS (SELECT 1 FROM \#ItemLotStock WHERE ItemSeq = A.ItemSeq AND WHSeq = A.WHSeq)

ORDER BY A.DataSeq

IF @@ROWCOUNT = 0 BREAK

SELECT @ItemLotSerl = 1 -- 초기화

SELECT @ItemLotSerlMAX = MAX(ItemLotSerl)

FROM \#ItemLotStock

WHERE ItemSeq = @NumItemSeq

AND WHSeq = @WHSeq

-- Loop돌면서 LOT별로 재고수량 할당

WHILE(@ItemLotSerl \<= @ItemLotSerlMAX)

BEGIN

-- 1. 출고수량이 LOT별 재고수량보다 작은 경우(기할당수량 감안)

INSERT INTO \#ItemLotQty (DataSeq, ItemLotSerl, WHSeq, ItemSeq, LotNo, STDQty, Type, WorkDate, WorkReportSeq)

SELECT @DataSeq, @ItemLotSerl, B.WHSeq, A.ItemSeq, B.LotNo, A.STDQty, '1', A.WorkDate, A.WorkReportSeq

FROM (

SELECT X.WHSeq, X.ItemSeq, X.STDQty - ISNULL(Y.STDQty, 0) AS STDQty, @WorkDate AS WorkDate, X.WorkReportSeq

FROM \#ItemOrderQty AS X

OUTER APPLY (

SELECT X2.DataSeq, SUM(X2.STDQty) AS STDQty

FROM \#ItemLotQty AS X2

WHERE X2.DataSeq = X.DataSeq

AND X2.WorkDate = @WorkDate

AND X2.WorkReportSeq = X.WorkReportSeq

AND X2.WHSeq = X.WHSeq

GROUP BY X2.DataSeq

) AS Y

WHERE X.DataSeq = @DataSeq

) AS A

CROSS APPLY (

SELECT X.WHSeq, X.ItemSeq, X.LotNo, X.STDStockQty - ISNULL(Y.STDQty, 0) AS STDQty

FROM \#ItemLotStock AS X

OUTER APPLY (

SELECT X2.WHSeq, X2.ItemSeq, X2.LotNo, SUM(X2.STDQty) AS STDQty

FROM \#ItemLotQty AS X2

WHERE X2.WHSeq = X.WHSeq

AND X2.ItemSeq = X.ItemSeq

AND X2.LotNo = X.LotNo

AND X2.WorkDate = @WorkDate

AND X2.WHSeq = X.WHSeq

GROUP BY X2.WHSeq, X2.ItemSeq, X2.LotNo

) AS Y

WHERE X.ItemSeq = A.ItemSeq

AND X.WHSeq = A.WHSeq

AND X.ItemLotSerl = @ItemLotSerl

) AS B

WHERE A.STDQty \<= B.STDQty

AND A.STDQty \<\> 0

AND B.STDQty \<\> 0

-- 2. 출고수량이 LOT별 재고수량보다 큰 경우(기할당수량 감안)

INSERT INTO \#ItemLotQty (DataSeq, ItemLotSerl, WHSeq, ItemSeq, LotNo, STDQty, Type, WorkDate, WorkReportSeq)

SELECT @DataSeq, @ItemLotSerl, B.WHSeq, A.ItemSeq, B.LotNo, B.STDQty, '2' , A.WorkDate , A.WorkReportSeq

FROM (

SELECT X.WHSeq, X.ItemSeq, X.STDQty - ISNULL(Y.STDQty, 0) AS STDQty, @WorkDate AS WorkDate, X.WorkReportSeq

FROM \#ItemOrderQty AS X

OUTER APPLY (

SELECT X2.DataSeq, SUM(X2.STDQty) AS STDQty -- 할당수량 SUM

FROM \#ItemLotQty AS X2

WHERE X2.DataSeq = X.DataSeq

AND X2.WorkDate = @WorkDate

AND X2.WorkReportSeq = X.WorkReportSeq

AND X2.WHSeq = X.WHSeq

GROUP BY X2.DataSeq

) AS Y

WHERE X.DataSeq = @DataSeq

) AS A

CROSS APPLY (

SELECT X.WHSeq, X.ItemSeq, X.LotNo, X.STDStockQty - ISNULL(Y.STDQty, 0) AS STDQty

FROM \#ItemLotStock AS X

OUTER APPLY (

SELECT X2.WHSeq, X2.ItemSeq, X2.LotNo, SUM(X2.STDQty) AS STDQty -- 할당수량 SUM

FROM \#ItemLotQty AS X2

WHERE X2.WHSeq = X.WHSeq

AND X2.ItemSeq = X.ItemSeq

AND X2.LotNo = X.LotNo

AND X2.WorkDate = @WorkDate

AND X2.WHSeq = X.WHSeq

GROUP BY X2.WHSeq, X2.ItemSeq, X2.LotNo

) AS Y

WHERE X.ItemSeq = A.ItemSeq

AND X.WHSeq = A.WHSeq

AND X.ItemLotSerl = @ItemLotSerl

) AS B

WHERE A.STDQty \>= B.STDQty

AND A.STDQty \<\> 0

AND B.STDQty \<\> 0

-- 출고수량 할당이 완료된 경우, Loop BREAK

IF NOT EXISTS (

SELECT 1

FROM \#ItemOrderQty AS X

OUTER APPLY (

SELECT X2.DataSeq, SUM(X2.STDQty) AS STDQty -- 할당수량 SUM

FROM \#ItemLotQty AS X2

WHERE X2.DataSeq = X.DataSeq

AND X2.WorkDate = @WorkDate

AND X2.WHSeq = X.WHSeq

GROUP BY X2.DataSeq

) AS Y

WHERE X.DataSeq = @DataSeq

AND X.STDQty \<\> ISNULL(Y.STDQty, 0)

)

BEGIN

BREAK

END

SET @ItemLotSerl = @ItemLotSerl + 1

END

END

--select \* from \#ItemLotQty order by itemseq, whseq

--- LOT 관리안하는 자재 현재고만큼만 투입

CREATE TABLE \#GetInOutItem

(

ItemSeq INT

)

CREATE TABLE \#GetInOutStock

(

BizUnit INT,

WHSeq INT,

MainWHSeq INT,

ItemSeq INT,

UnitSeq INT,

PrevQty DECIMAL(19,5),

InQty DECIMAL(19,5),

OutQty DECIMAL(19,5),

StockQty DECIMAL(19,5),

STDPrevQty DECIMAL(19,5),

STDInQty DECIMAL(19,5),

STDOutQty DECIMAL(19,5),

STDStockQty DECIMAL(19,5),

StockAmt DECIMAL(19,5)

)

CREATE TABLE \#GetInOutStock_tmp

(

BizUnit INT,

WHSeq INT,

MainWHSeq INT,

ItemSeq INT,

UnitSeq INT,

STDStockQty DECIMAL(19,5)

)

CREATE TABLE \#GetInOutStock_Result

(

BizUnit INT,

WHSeq INT,

MainWHSeq INT,

ItemSeq INT,

UnitSeq INT,

STDStockQty DECIMAL(19,5)

)

CREATE TABLE \#ItemNotLotQty

(

DataSeq INT ,

ItemLotSerl INT ,

WHSeq INT ,

ItemSeq INT ,

Qty DECIMAL(19,5) ,

STDQty DECIMAL(19,5) ,

WorkDate NCHAR(8)

)

SELECT WorkDate,

WorkReportSeq,

ItemSerl,

MatItemSeq,

UnitSeq,

Qty,

StdUnitQty,

ROW_NUMBER() OVER(ORDER BY Seq) AS IDX_NO,

OutWHSeq

INTO \#NotLotItem

FROM \#NeedQtySum

WHERE ISNULL(IsLot,'') \<\> N'1'

ORDER BY Seq

 

INSERT INTO \#GetInOutItem

SELECT MatItemSeq

FROM \#NotLotItem

WHERE WorkDate = @WorkDate

-- 창고재고 가져오기 (작업일기준)

EXEC \_SWLGGetInOutStock @CompanySeq = @CompanySeq, -- 법인코드

@BizUnit = @BizUnit, -- 사업부문

@DateFr = @WorkDate, -- 조회기간Fr

@DateTo = @WorkDate, -- 조회기간To

@WHSeq = 0, -- 창고지정

@SMWHKind = 0, -- 창고구분별 조회

@IsSubDisplay = '0', -- 기능창고포함('1' : 기능창고별 조회, '0' : 사업단위창고에 포함)

@IsOnlyStock = '1', -- 재고만 표시('1' : 재고, '0' : 이월,입고,출고,재고)

@IsStockAmt = '0', -- 재고금액 계산

@UnitType = 1, -- 1: 기준단위, 2: 수불단위, 3: 환산단위(단위별 조회)

@ConvUnitSeq = 0, -- 환산단위코드

@QryType = 'S', -- 'S': 1007 실재고, 'B':1008 자산포함, 'A':1011 가용재고

@IsTrustCust = '' -- 수탁여부

INSERT INTO \#GetInOutStock_tmp

SELECT BizUnit ,

WHSeq ,

MainWHSeq ,

ItemSeq ,

UnitSeq ,

STDStockQty

FROM \#GetInOutStock

TRUNCATE TABLE \#GetInOutStock

-- 창고재고 가져오기 (작업일기준)

EXEC \_SWLGGetInOutStock @CompanySeq = @CompanySeq, -- 법인코드

@BizUnit = @BizUnit, -- 사업부문

@DateFr = @CurrDate, -- 조회기간Fr

@DateTo = @CurrDate, -- 조회기간To

@WHSeq = 0, -- 창고지정

@SMWHKind = 0, -- 창고구분별 조회

@IsSubDisplay = '0', -- 기능창고포함('1' : 기능창고별 조회, '0' : 사업단위창고에 포함)

@IsOnlyStock = '1', -- 재고만 표시('1' : 재고, '0' : 이월,입고,출고,재고)

@IsStockAmt = '0', -- 재고금액 계산

@UnitType = 1, -- 1: 기준단위, 2: 수불단위, 3: 환산단위(단위별 조회)

@ConvUnitSeq = 0, -- 환산단위코드

@QryType = 'S', -- 'S': 1007 실재고, 'B':1008 자산포함, 'A':1011 가용재고

@IsTrustCust = '' -- 수탁여부

INSERT INTO \#GetInOutStock_tmp

SELECT BizUnit ,

WHSeq ,

MainWHSeq ,

ItemSeq ,

UnitSeq ,

STDStockQty

FROM \#GetInOutStock

INSERT INTO \#GetInOutStock_Result

SELECT BizUnit ,

WHSeq ,

MainWHSeq ,

ItemSeq ,

UnitSeq ,

MIN(STDStockQty)

FROM \#GetInOutStock_tmp

GROUP BY BizUnit ,

WHSeq ,

MainWHSeq ,

ItemSeq ,

UnitSeq

/\* Lot 관리 안하는 품목 재고가 없어도 만들어지게 수정함

UPDATE \#NeedQtySum

SET StdUnitQty = CASE WHEN X.StdUnitQty \<= ISNULL(A.STDStockQty,0) THEN X.StdUnitQty ELSE ISNULL(A.STDStockQty,0) END

FROM \#NeedQtySum AS X LEFT OUTER JOIN \#GetInOutStock_Result AS A ON A.ItemSeq = X.MatItemSeq AND A.WHSeq = X.OutWHSeq

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.ItemSeq = B.ItemSeq

LEFT OUTER JOIN \_TDAItemUnit AS F WITH(NOLOCK) ON F.CompanySeq = @CompanySeq

AND B.ItemSeq = F.ItemSeq

AND X.UnitSeq = F.UnitSeq

WHERE X.IsLot \<\> '1'

AND X.WorkDate = @WorkDate

 

UPDATE \#NeedQtySum

SET Qty = CASE WHEN ISNULL(F.ConvDen,0) = 0 THEN 0 ELSE X.StdUnitQty / (CASE WHEN ISNULL(F.ConvDen,0) = 0 THEN 0 ELSE ISNULL(F.ConvNum,0) / ISNULL(F.ConvDen,0) END) END

FROM \#NeedQtySum AS X LEFT OUTER JOIN \_TDAItemUnit AS F WITH(NOLOCK) ON F.CompanySeq = @CompanySeq

AND X.MatItemSeq = F.ItemSeq

AND X.UnitSeq = F.UnitSeq

 

DELETE FROM \#NeedQtySum WHERE StdUnitQty \<= 0

\*/

CREATE TABLE \#NeedQtySum_Result

(

Seq INT,

OriSeq INT,

WorkOrderSeq INT,

WorkOrderSerl INT,

WorkReportSeq INT,

ProcSeq INT,

MatItemSeq INT,

LotNo NVARCHAR(30),

UnitSeq INT,

Qty NUMERIC(19,5),

StdUnitQty NUMERIC(19,5),

InputType INT,

IsAutoOut NCHAR(1),

AssyYn NCHAR(1),

BizUnit INT,

OutWHSeq INT,

WorkCenterSeq INT,

GoodItemSeq INT, -- 제품코드

ProcRev NCHAR(2), -- 공정차수

AssyItemSeq INT, -- 공정품코드

ItemSerl INT,

WorkDate NCHAR(8),

FieldWHSeq INT, -- 워크센터 현장창고

IsLot NCHAR(1), -- Lot관리여부

)

--select \* from \#ItemLotQty

--select \* from \#NeedQtySum

INSERT INTO \#NeedQtySum_Result

SELECT ROW_NUMBER() OVER(ORDER BY A.ItemSerl, ISNULL(B.LotNo,'')),

A.Seq,

A.WorkOrderSeq,

A.WorkOrderSerl,

A.WorkReportSeq,

A.ProcSeq,

A.MatItemSeq,

ISNULL(B.LotNo,'') AS LotNo,

A.UnitSeq,

ISNULL(B.STDQty,A.Qty),

ISNULL(B.STDQty,A.StdUnitQty),

A.InputType,

A.IsAutoOut,

A.AssyYn,

A.BizUnit,

A.OutWHSeq,

A.WorkCenterSeq,

A.GoodItemSeq,

A.ProcRev,

A.AssyItemSeq,

ROW_NUMBER() OVER(ORDER BY A.ItemSerl, ISNULL(B.LotNo,'')),

A.WorkDate,

A.FieldWHSeq,

A.IsLot

FROM \#NeedQtySum AS A LEFT OUTER JOIN \#ItemLotQty AS B ON A.WorkDate = B.WorkDate

AND A.MatItemSeq = B.ItemSeq

AND A.IsLot = '1'

AND A.WorkReportSeq = B.WorkReportSeq

/\*

-- lot재고가 없는경우 있는만큼만 투입하라는 요청에 의해 없는건은삭제..

DELETE \#NeedQtySum_Result

WHERE IsLot = '1'

AND ISNULL(LotNo,'') = ''

\*/

INSERT INTO \#TPDSFCMatInput

(

WorkingTag, Status,

CompanySeq , Seq , WorkReportSeq , ItemSerl , InputDate , MatItemSeq ,

MatUnitSeq , Qty , StdUnitQty , RealLotNo , SerialNoFrom ,

ProcSeq , AssyYn , IsConsign , GoodItemSeq , InputType ,

IsPaid , IsPjt , PjtSeq , WBSSeq , LastUserSeq ,

LastDateTime, Remark , ProdWRSeq , IsReInput , WHSeq ,

PgmSeq

)

SELECT B.WorkingTag, 0 AS Status,

@CompanySeq , 0 , A.WorkReportSeq , A.ItemSerl , A.WorkDate , A.MatItemSeq ,

A.UnitSeq , A.Qty , A.StdUnitQty , A.LotNo , '' ,

A.ProcSeq , A.AssyYn , '' , A.GoodItemSeq , A.InputType ,

'' , '' , 0 , 0 , @UserSeq ,

GETDATE() , '' , 0 , '0' , A.OutWHSeq ,

999999

FROM \#NeedQtySum_Result AS A

JOIN \#TPDSFCWorkReport AS B ON A.WorkReportSeq = B.WorkReportSeq

WHERE B.Status = 0

 

END

/\*

IF EXISTS(SELECT TOP 1 1 FROM \_TCOMEnv WITH(NOLOCK) WHERE EnvSeq = 6201 AND EnvValue = '1' AND CompanySeq = @CompanySeq)

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1130 , -- 외주용역의 전표이(가) 생성되어 있어 처리 할 수 없습니다.

@LanguageSeq ,

50367 , N'월공용자재투입배부처리'

 

UPDATE A

SET Result = REPLACE(@Results, N'이(가)', N'가'),

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCComMatInputDiv AS B WITH(NOLOCK) ON A.WorkReportSeq = B.WorkReportSeq

AND B.CompanySeq = @CompanySeq

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U', 'D')

CREATE TABLE \#MatNeed_GoodItem

(

IDX_NO INT IDENTITY(1,1) ,

WorkingTag NVARCHAR(10) ,

BizUnit INT , -- 사업단위내부코드

ItemSeq INT , -- 제품내부코드

ProcRev NCHAR(2) , -- 공정차수

ProcSeq INT , -- 공정내부코드

AssyItemSeq INT , -- 공정품내부코드

UnitSeq INT , -- 단위코드 (공정품코드가 있으면 공정품단위코드, 없으면 제품단위코드)

Qty DECIMAL(19,5) , -- 수량 (공정품코드가 있으면 공정품수량, 없으면 제품수량)

ProdPlanSeq INT , -- 생산계획내부코드

WorkOrderSeq INT , -- 작업지시내부코드

WorkOrderSerl INT , -- 작업지시내부순번

IsOut NCHAR(1) , -- 로스율 적용에 사용 '1'이면 OutLossRate 적용

WorkReportSeq INT NULL ,

WorkDate NCHAR(8) NULL ,

WorkCenterSeq INT NULL ,

OutWHSeq INT NULL ,

FieldWHSeq INT NULL ,

MatOutWHSeq INT NULL ,

GoodOutWHSeq INT NULL ,

)

 

CREATE TABLE \#MatNeed_MatItem_Result

(

IDX_NO INT, -- IDX

MatItemSeq INT, -- 자재내부코드

UnitSeq INT, -- 자재단위내부코드

NeedQty DECIMAL(19,5), -- 소요량

STDUnitSeq INT, -- 기준단위내부코드

STDNeedQty DECIMAL(19,5), -- 기준단위소요량

InputType INT, -- 자재투입구분 6042

SMDelvType INT, -- 조달구분 6032

SMAddType INT, -- 가감구분(추가)6048

)

INSERT INTO \#MatNeed_GoodItem

(

WorkingTag, ItemSeq, ProcRev, ProcSeq, AssyItemSeq, UnitSeq, Qty, ProdPlanSeq, WorkOrderSeq, WorkOrderSerl, IsOut,

WorkReportSeq, BizUnit, OutWHSeq, WorkCenterSeq, WorkDate, FieldWHSeq, MatOutWhSeq, GoodOutWHSeq

)

SELECT 'A' WorkingTag, A.GoodItemSeq, A.ProcRev, A.ProcSeq, A.AssyItemSeq, A.ProdUnitSeq, A.ProdQty, 0 AS ProdPlanSeq, A.WorkOrderSeq, A.WorkOrderSerl, '0'IsOut,

A.WorkReportSeq, A.BizUnit, 0 AS OutWHSeq, A.WorkCenterSeq , A.WorkDate, A.FieldWHSeq, A.WHSeq AS MatOutWhSeq, A.WHSeq AS GoodOutWHSeq

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

 

-------------------------

-- 소요자재 가져오기

-------------------------

EXEC dbo.\_SWPDMMGetItemNeedQty @CompanySeq = @CompanySeq

, @SMDelvType = N'0' -- 의미없음

, @IsMRP = N'0' -- MRP여부

, @IsOutsourcing = N'0' -- 외주여부

, @IsMatAdd = N'0' -- 추가자재만조회

, @SMOutKind = 0 -- 출고구분

INSERT INTO \#TPDSFCMatInput

(

WorkingTag, Status, MessageType, Result,

WorkReportSeq, ItemSerl, MatItemSeq, MatUnitSeq, Qty, StdUnitQty, RealLotNo, WhSeq

)

SELECT A.WorkingTag, 0 AS Status, 0 AS MessageType, '' AS Result,

A.WorkReportSeq, ROW_NUMBER() OVER(PARTITION BY A.WorkReportSeq ORDER BY B.IDX_NO) ItemSerl,

B.MatItemSeq, B.UnitSeq AS MatUnitSeq, B.NeedQty AS Qty, B.STDNeedQty AS StdUnitQty, '' AS RealLotNo,

CASE WHEN P.SMOutKind \<\> 6005002 THEN A.FieldWHSeq -- 자동출고아닌 경우 -- SELECT \* FROM \_TDASMinor WHERE MajorSeq = 6005

WHEN S.SMAssetGrp = 6008005 THEN A.FieldWhSeq -- 공정품

WHEN T.MinorValue = N'1' THEN A.MatOutWhSeq -- 자재

ELSE CASE WHEN ISNULL(A.GoodOutWHSeq, 0) \<\> 0 THEN A.GoodOutWHSeq

ELSE ISNULL(W.WHSeq, 0)

END

END AS WhSeq

FROM \#MatNeed_GoodItem AS A

JOIN \#MatNeed_MatItem_Result AS B ON A.IDX_NO = B.IDX_NO

JOIN \_TDAItem AS I WITH(NOLOCK) ON B.MatItemSeq = I.ItemSeq

AND I.CompanySeq = @CompanySeq

JOIN \_TDAItemAsset AS S WITH(NOLOCK) ON I.AssetSeq = S.AssetSeq

AND I.CompanySeq = S.CompanySeq

JOIN \_TDASMinor AS T WITH(NOLOCK) ON S.SMAssetGrp = T.MinorSeq

AND T.MajorSeq = 6008

LEFT OUTER JOIN \_TDAItemProduct AS P WITH(NOLOCK) ON B.MatItemSeq = P.ItemSeq

AND P.CompanySeq = @CompanySeq

 

OUTER APPLY(SELECT TOP 1

Z.WHSeq

FROM \_TDAWHItem AS Z WITH(NOLOCK)

WHERE Z.CompanySeq = @CompanySeq

AND Z.ItemSeq = A.ItemSeq

ORDER BY Z.WHSeq

) AS W

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@NumStatus OUTPUT,

@Results OUTPUT,

1369 ,

@LanguageSeq ,

20707 , N'투입자재',

626 , N'출고창고'

UPDATE A

SET Result = @Results,

MessageType = @MessageType,

Status = @NumStatus

FROM \#TPDSFCWorkReport AS A

JOIN \#TPDSFCMatInput AS B ON A.WorkReportSeq = B.WorkReportSeq

WHERE A.Status = 0

AND ISNULL(B.WhSeq, 0) = 0

 

UPDATE A

SET Result = 'Lot관리하는 투입품이 존재 합니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

JOIN \#TPDSFCMatInput AS B ON A.WorkReportSeq = B.WorkReportSeq

JOIN \_TDAItemStock AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.ItemSeq = B.MatItemSeq

WHERE A.Status = 0

AND ISNULL(C.IsLotMng, '') = '1'

END

ELSE

BEGIN

UPDATE A

SET Result = '투입된 작업실적은 수정/삭제 할 수 없습니다.',

MessageType = 12007,

Status = 99999

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCMatInput AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

END

\*/

/\*투입\*/

 

-- 에러 발생시...

IF EXISTS(SELECT TOP 1 1 FROM \#TPDSFCWorkReport WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '0'

GOTO ERROR_PROC

RETURN

END

 

-- 진행

CREATE TABLE \#SComSourceDailyBatch

(

ToTableName NVARCHAR(100),

ToSeq INT,

ToSerl INT,

ToSubSerl INT,

FromTableName NVARCHAR(100),

FromSeq INT,

FromSerl INT,

FromSubSerl INT,

ToQty DECIMAL(19,5),

ToStdQty DECIMAL(19,5),

ToAmt DECIMAL(19,5),

ToVAT DECIMAL(19,5),

ToDOMAmt DECIMAL(19,5),

ToDOMVAT DECIMAL(19,5),

FromQty DECIMAL(19,5),

FromSTDQty DECIMAL(19,5),

FromAmt DECIMAL(19,5),

FromVAT DECIMAL(19,5),

FromDOMAmt DECIMAL(19,5),

FromDOMVAT DECIMAL(19,5)

)

 

BEGIN TRAN

-- 생산입고 / 작업실적 진행 제거

TRUNCATE TABLE \#SComSourceDailyBatch

INSERT INTO \#SComSourceDailyBatch

(

ToTableName, ToSeq, ToSerl, ToSubSerl,

FromTableName, FromSeq, FromSerl, FromSubSerl,

ToQty, ToStdQty, ToAmt, ToVAT, ToDOMAmt, ToDOMVAT,

FromQty, FromSTDQty, FromAmt, FromVAT, FromDOMAmt, FromDOMVAT

)

SELECT '\_TPDSFCGoodIn' ToTableName, B.GoodInSeq ToSeq, 0 ToSerl, 0 ToSubSerl,

'\_TPDSFCWorkReport' FromTableName, C.WorkReportSeq FromSeq, 0 FromSerl, 0 FromSubSerl,

B.ProdQty ToQty, B.StdProdQty ToStdQty, 0 ToAmt, 0 ToVAT, 0 ToDOMAmt, 0 ToDOMVAT,

C.OKQty FromQty, C.StdUnitProdQty FromSTDQty, 0 FromAmt, 0 FromVAT, 0 FromDOMAmt, 0 FromDOMVAT

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCGoodIn AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.GoodInSeq = A.GoodInSeq

JOIN \_TPDSFCWorkReport AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

 

EXEC dbo.\_SWComSourceDailyBatch 'D', @CompanySeq, @UserSeq

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'DELETE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

-- 작업실적 / 작업지시 진행 제거

TRUNCATE TABLE \#SComSourceDailyBatch

INSERT INTO \#SComSourceDailyBatch

(

ToTableName, ToSeq, ToSerl, ToSubSerl,

FromTableName, FromSeq, FromSerl, FromSubSerl,

ToQty, ToStdQty, ToAmt, ToVAT, ToDOMAmt, ToDOMVAT,

FromQty, FromSTDQty, FromAmt, FromVAT, FromDOMAmt, FromDOMVAT

)

SELECT '\_TPDSFCWorkReport' ToTableName, B.WorkReportSeq ToSeq, 0 ToSerl, 0 ToSubSerl,

'\_TPDSFCWorkOrder' FromTableName, C.WorkOrderSeq FromSeq, C.WorkOrderSerl FromSerl, 0 FromSubSerl,

B.ProdQty ToQty, B.StdUnitProdQty ToStdQty, 0 ToAmt, 0 ToVAT, 0 ToDOMAmt, 0 ToDOMVAT,

C.OrderQty FromQty, C.STDUnitQty FromSTDQty, 0 FromAmt, 0 FromVAT, 0 FromDOMAmt, 0 FromDOMVAT

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCWorkReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

JOIN \_TPDSFCWorkOrder AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.WorkOrderSeq = A.WorkOrderSeq

AND C.WorkOrderSerl = A.WorkOrderSerl

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

 

EXEC dbo.\_SWComSourceDailyBatch 'D', @CompanySeq, @UserSeq

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'DELETE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

 

-- 삭제

DELETE B

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCGoodIn AS B ON B.CompanySeq = @CompanySeq

AND B.GoodInSeq = A.GoodInSeq

WHERE A.Status = 0

AND A.WorkingTag = 'D'

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'DELETE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

DELETE B

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDQCTestReport AS B ON B.CompanySeq = @CompanySeq

AND B.QCSeq = A.QCSeq

WHERE A.Status = 0

AND A.WorkingTag = 'D'

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'DELETE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

DELETE B

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCWorkReport AS B ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag = 'D'

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'DELETE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

-- 수정

UPDATE B

SET BizUnit = A.BizUnit,

InDate = A.WorkDate,

WHSeq = A.WHSeq,

GoodItemSeq = A.GoodItemSeq,

UnitSeq = A.ProdUnitSeq,

ProdQty = A.OKQty,

StdProdQty = A.StdUnitOKQty,

InDeptSeq = A.DeptSeq,

EmpSeq = @EmpSeq,

RealLotNo = A.RealLotNo,

Remark = A.Remark,

FactUnit = A.FactUnit,

WorkCond1 = A.WorkCondition1,

WorkCond10 = A.WorkCond10,

WorkCond2 = A.WorkCondition2,

WorkCond3 = A.WorkCondition3,

WorkCond4 = A.WorkCondition4,

WorkCond5 = A.WorkCondition5,

WorkCond6 = A.WorkCondition6,

WorkCond7 = A.WorkCond7,

WorkCond8 = A.WorkCond8,

WorkCond9 = A.WorkCond9,

LastUserSeq = @UserSeq,

LastDateTime = GETDATE()

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCGoodIn AS B ON B.CompanySeq = @CompanySeq

AND B.GoodInSeq = A.GoodInSeq

WHERE A.Status = 0

AND A.WorkingTag = 'U'

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'UPDATE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

UPDATE B

SET BizUnit = A.BizUnit,

ItemSeq = A.AssyItemSeq,

ReqQty = A.OKQty,

SelectDate = A.WorkDate,

TestStartDate = A.WorkDate,

TestEndDate = A.WorkDate,

PassedQty = A.OKQty,

ReqInQty = A.OKQty,

Remark = A.Remark,

EmpSeq = @EmpSeq,

DeptSeq = A.DeptSeq,

FactUnit = A.FactUnit,

StdUnitReqQty = A.StdUnitOKQty,

StdUnitPassedQty = A.StdUnitOKQty,

LastUserSeq = @UserSeq,

LastDateTime = GETDATE()

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDQCTestReport AS B ON B.CompanySeq = @CompanySeq

AND B.QCSeq = A.QCSeq

WHERE A.Status = 0

AND A.WorkingTag = 'U'

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'UPDATE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

 

UPDATE B

SET FactUnit = A.FactUnit,

WorkDate = A.WorkDate,

DeptSeq = A.DeptSeq,

WorkCenterSeq = A.WorkCenterSeq,

GoodItemSeq = A.GoodItemSeq,

ProcRev = A.ProcRev,

AssyItemSeq = A.AssyItemSeq,

ProcSeq = A.ProcSeq,

ProdUnitSeq = A.ProdUnitSeq,

ProdQty = A.ProdQty,

OKQty = A.OKQty,

BadQty = A.BadQty,

StdUnitProdQty = A.StdUnitProdQty,

StdUnitOKQty = A.StdUnitOKQty,

StdUnitBadQty = A.StdUnitBadQty,

WorkType = A.WorkType,

RealLotNo = A.RealLotNo,

WorkCondition1 = A.WorkCondition1,

WorkCondition2 = A.WorkCondition2,

WorkCondition3 = A.WorkCondition3,

WorkCondition4 = A.WorkCondition4,

WorkCondition5 = A.WorkCondition5,

WorkCondition6 = A.WorkCondition6,

Remark = A.Remark,

SubEtcInSeq = A.SubEtcInSeq,

FieldWhSeq = A.FieldWhSeq,

BizUnit = A.BizUnit,

WorkCond10 = A.WorkCond10,

WorkCond7 = A.WorkCond7,

WorkCond8 = A.WorkCond8,

WorkCond9 = A.WorkCond9,

LastUserSeq = @UserSeq,

LastDateTime = GETDATE()

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCWorkReport AS B ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag = 'U'

 

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'UPDATE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

 

-- 신규

INSERT INTO \_TPDSFCWorkReport

(

CompanySeq, WorkReportSeq, FactUnit, WorkDate, DeptSeq, WorkOrderSeq, WorkOrderSerl, WorkCenterSeq,

GoodItemSeq, ProcRev, AssyItemSeq, ProcSeq, ProdUnitSeq, ProdQty, OKQty, BadQty, StdUnitProdQty, StdUnitOKQty, StdUnitBadQty,

WorkStartTime, WorkEndTime, WorkHour, ProcHour, WorkerQty, WorkType,

ChainGoodsSeq, RealLotNo, SerialNoFrom, CCtrSeq, EmpSeq,

WorkCondition1, WorkCondition2, WorkCondition3, WorkCondition4, WorkCondition5,

WorkCondition6, Remark, IsProcQC, IsLastProc, IsPjt, PJTSeq, WBSSeq, ItemBomRev,

SubEtcInSeq, FieldWhSeq, WorkTimeGroup, LastUserSeq, LastDateTime, SerialNoTo, CustSeq,

ProgFromSeq, ProgFromSerl, ProgFromSubSerl, ProgFromTableSeq, IsLastQc,

ReOrderQty, LossCostQty, DisuseQty, StdUnitReOrderQty, StdUnitLossCostQty, StdUnitDisuseQty,

SampleQty, StdSampleQty, SampleUnitSeq, BizUnit,

ProgFromQty, ProgFromSTDQty, ProgFromAmt, ProgFromVAT, ProgFromDOMAmt, ProgFromDOMVAT, ProgPrevFromTableSeq,

IsRecycle, BatchSize, BatchQty, PgmSeq, WorkCond10, WorkCond7, WorkCond8, WorkCond9,

ToolSeq, ValiDate, CanisterSerial, CanisterSeq

)

SELECT @CompanySeq, A.WorkReportSeq, A.FactUnit, A.WorkDate, A.DeptSeq, A.WorkOrderSeq, A.WorkOrderSerl, A.WorkCenterSeq,

A.GoodItemSeq, A.ProcRev, A.AssyItemSeq, A.ProcSeq, A.ProdUnitSeq, A.ProdQty, A.OKQty, A.BadQty, A.StdUnitProdQty, A.StdUnitOKQty, A.StdUnitBadQty,

'' AS WorkStartTime, '' AS WorkEndTime, 0 AS WorkHour, 0 AS ProcHour, 0 AS WorkerQty, A.WorkType,

0 AS ChainGoodsSeq, A.RealLotNo, '' AS SerialNoFrom, 0 AS CCtrSeq, 0 AS EmpSeq,

A.WorkCondition1, A.WorkCondition2, A.WorkCondition3, A.WorkCondition4, A.WorkCondition5,

A.WorkCondition6, A.Remark, '0' AS IsProcQC, '1' AS IsLastProc, '' AS IsPjt, 0 AS PJTSeq, 0 AS WBSSeq, '' AS ItemBomRev,

A.SubEtcInSeq, A.FieldWhSeq, 0 AS WorkTimeGroup, @UserSeq AS LastUserSeq, GETDATE() AS LastDateTime, '' AS SerialNoTo, 0 AS CustSeq,

NULL AS ProgFromSeq, NULL AS ProgFromSerl, NULL AS ProgFromSubSerl, NULL AS ProgFromTableSeq, '1' AS IsLastQc,

0 AS ReOrderQty, 0 AS LossCostQty, 0 AS DisuseQty, 0 AS StdUnitReOrderQty, 0 AS StdUnitLossCostQty, 0 AS StdUnitDisuseQty,

NULL AS SampleQty, NULL AS StdSampleQty, NULL AS SampleUnitSeq, A.BizUnit,

NULL AS ProgFromQty, NULL AS ProgFromSTDQty, NULL AS ProgFromAmt, NULL AS ProgFromVAT, NULL AS ProgFromDOMAmt, NULL AS ProgFromDOMVAT, NULL AS ProgPrevFromTableSeq,

NULL AS IsRecycle, 0 AS BatchSize, 0 AS BatchQty, @PgmSeq AS PgmSeq, A.WorkCond10, A.WorkCond7, A.WorkCond8, A.WorkCond9,

0 AS ToolSeq, '' AS ValiDate, NULL AS CanisterSerial, NULL AS CanisterSeq

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND ISNULL(A.WorkReportSeq, 0) \<\> 0

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'INSERT FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

 

INSERT INTO \_TPDQCTestReport

(

CompanySeq, QCSeq, BizUnit, QCNo, SourceType, SourceSeq, SourceSerl, ItemSeq,

SMTestMethod, SMSamplingStd, SMAQLLevel, AQLPoint, SMAQLStrict, AQLAcValue, AQLReValue, AcBadRatio,

ReqQty, ReqSampleQty, SelectDate, TestStartDate, TestEndDate, TestDocNo, SampleNo,

RealSampleQty, BadSampleQty, BadSampleRate, SMTestResult, PassedQty, RejectQty, DisuseQty,

ReqInQty, TestUsedTime, Remark, SMRejectTransType, EmpSeq, DeptSeq,

LastUserSeq, LastDateTime,

Memo1, Memo2, FileSeq, TestOrderEmpSeq, TestOrderDeptSeq,

SampleCollectMthRemark, SampleCollectEmpSeq, SampleCollectPlace, TestOrderDate,

StoreAppEmpSeq, StoreAppDeptSeq, StoreAppDate, ProgFromSeq, ProgFromSerl, ProgFromSubSerl, ProgFromTableSeq,

ReOrderQty, LossCostQty, Memo3, Memo4, DeliveryAppEmpSeq, DeliveryAppDeptSeq, DeliveryAppDate, FactUnit,

ProgFromQty, ProgFromSTDQty, ProgFromAmt, ProgFromVAT, ProgFromDOMAmt, ProgFromDOMVAT, ProgPrevFromTableSeq,

StdUnitReqQty, StdUnitPassedQty, StdUnitRejectQty, PgmSeq,

Dummy1, Dummy10, Dummy2, Dummy3, Dummy4, Dummy5, Dummy6, Dummy7, Dummy8, Dummy9,

TestOrderRemark, StoreAppRemark, TestTitleRemark, TestFinalDate, TestPassWHSeq

)

SELECT @CompanySeq, A.QCSeq, A.BizUnit, A.QCNo, 6097003 AS SourceType, A.WorkReportSeq AS SourceSeq, 0 AS SourceSerl, A.AssyItemSeq AS ItemSeq,

0 AS SMTestMethod, 0 AS SMSamplingStd, 0 AS SMAQLLevel, 0 AS AQLPoint, 0 AS SMAQLStrict, 0 AS AQLAcValue, 0 AS AQLReValue, 0 AS AcBadRatio,

A.OKQty AS ReqQty, 0 AS ReqSampleQty, A.WorkDate AS SelectDate, A.WorkDate AS TestStartDate, A.WorkDate AS TestEndDate, '' AS TestDocNo, '' AS SampleNo,

0 AS RealSampleQty, 0 AS BadSampleQty, 0 AS BadSampleRate, 6035001 AS SMTestResult, A.OKQty AS PassedQty, 0 AS RejectQty, 0 AS DisuseQty,

A.OKQty AS ReqInQty, 0 AS TestUsedTime, A.Remark, 0 AS SMRejectTransType, @EmpSeq AS EmpSeq, A.DeptSeq,

@UserSeq AS LastUserSeq, GETDATE() AS LastDateTime,

NULL AS Memo1, NULL AS Memo2, NULL AS FileSeq, NULL AS TestOrderEmpSeq, NULL AS TestOrderDeptSeq,

NULL AS SampleCollectMthRemark, NULL AS SampleCollectEmpSeq, NULL AS SampleCollectPlace, NULL AS TestOrderDate,

NULL AS StoreAppEmpSeq, NULL AS StoreAppDeptSeq, NULL AS StoreAppDate, NULL AS ProgFromSeq, NULL AS ProgFromSerl, NULL AS ProgFromSubSerl, NULL AS ProgFromTableSeq,

NULL AS ReOrderQty, NULL AS LossCostQty, NULL AS Memo3, NULL AS Memo4, NULL AS DeliveryAppEmpSeq, NULL AS DeliveryAppDeptSeq, NULL AS DeliveryAppDate, NULL AS FactUnit,

NULL AS ProgFromQty, NULL AS ProgFromSTDQty, NULL AS ProgFromAmt, NULL AS ProgFromVAT, NULL AS ProgFromDOMAmt, NULL AS ProgFromDOMVAT, NULL AS ProgPrevFromTableSeq,

A.StdUnitOKQty AS StdUnitReqQty, A.StdUnitOKQty AS StdUnitPassedQty, 0 AS StdUnitRejectQty, @PgmSeq AS PgmSeq,

NULL AS Dummy1, NULL AS Dummy10, NULL AS Dummy2, NULL AS Dummy3, NULL AS Dummy4, NULL AS Dummy5, NULL AS Dummy6, NULL AS Dummy7, NULL AS Dummy8, NULL AS Dummy9,

NULL AS TestOrderRemark, NULL AS StoreAppRemark, NULL AS TestTitleRemark, NULL AS TestFinalDate, NULL AS TestPassWHSeq

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND ISNULL(A.QCSeq, 0) \<\> 0

 

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'INSERT FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

INSERT INTO \_TPDSFCGoodIn

(

CompanySeq, GoodInSeq, BizUnit, InDate, WHSeq, GoodItemSeq, UnitSeq, ProdQty, StdProdQty,

UnitPrice, Amt, FlowDate, InDeptSeq, EmpSeq, WorkOrderSeq, WorkReportSeq, WorkSerl, QCSeq,

RealLotNo, SerialNoFrom, Remark, PJTSeq, WBSSeq, LastUserSeq, LastDateTime,

ProgFromSeq, ProgFromSerl, ProgFromSubSerl, ProgFromTableSeq, IsWorkOrderEnd, FactUnit,

ProgFromQty, ProgFromSTDQty, ProgFromAmt, ProgFromVAT, ProgFromDOMAmt, ProgFromDOMVAT, ProgPrevFromTableSeq,

PgmSeq, WorkCond1, WorkCond10, WorkCond2, WorkCond3, WorkCond4,

WorkCond5, WorkCond6, WorkCond7, WorkCond8, WorkCond9

)

SELECT @CompanySeq, A.GoodInSeq, A.BizUnit, A.WorkDate AS InDate, A.WHSeq, A.GoodItemSeq, A.ProdUnitSeq AS UnitSeq, A.OKQty AS ProdQty, A.StdUnitOKQty AS StdProdQty,

0 AS UnitPrice, 0 AS Amt, '' AS FlowDate, A.DeptSeq AS InDeptSeq, @EmpSeq AS EmpSeq, A.WorkOrderSeq, A.WorkReportSeq, 0 AS WorkSerl, A.QCSeq,

A.RealLotNo, '' AS SerialNoFrom, A.Remark, 0 AS PJTSeq, 0 AS WBSSeq, @UserSeq AS LastUserSeq, GETDATE() AS LastDateTime,

NULL AS ProgFromSeq, NULL AS ProgFromSerl, NULL AS ProgFromSubSerl, NULL AS ProgFromTableSeq, '0' AS IsWorkOrderEnd, A.FactUnit,

NULL AS ProgFromQty, NULL AS ProgFromSTDQty, NULL AS ProgFromAmt, NULL AS ProgFromVAT, NULL AS ProgFromDOMAmt, NULL AS ProgFromDOMVAT, NULL AS ProgPrevFromTableSeq,

@PgmSeq AS PgmSeq, A.WorkCondition1 AS WorkCond, A.WorkCond10, A.WorkCondition2 AS WorkCond2, A.WorkCondition3 AS WorkCond3, A.WorkCondition4 AS WorkCond4,

A.WorkCondition5 AS WorkCond5, A.WorkCondition6 AS WorkCond6, A.WorkCond7, A.WorkCond8, A.WorkCond9

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.WorkingTag = 'A'

AND ISNULL(A.GoodInSeq, 0) \<\> 0

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'INSERT FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

 

-- 생산입고 / 작업실적 진행 제거

TRUNCATE TABLE \#SComSourceDailyBatch

INSERT INTO \#SComSourceDailyBatch

(

ToTableName, ToSeq, ToSerl, ToSubSerl,

FromTableName, FromSeq, FromSerl, FromSubSerl,

ToQty, ToStdQty, ToAmt, ToVAT, ToDOMAmt, ToDOMVAT,

FromQty, FromSTDQty, FromAmt, FromVAT, FromDOMAmt, FromDOMVAT

)

SELECT '\_TPDSFCGoodIn' ToTableName, B.GoodInSeq ToSeq, 0 ToSerl, 0 ToSubSerl,

'\_TPDSFCWorkReport' FromTableName, C.WorkReportSeq FromSeq, 0 FromSerl, 0 FromSubSerl,

B.ProdQty ToQty, B.StdProdQty ToStdQty, 0 ToAmt, 0 ToVAT, 0 ToDOMAmt, 0 ToDOMVAT,

C.OKQty FromQty, C.StdUnitProdQty FromSTDQty, 0 FromAmt, 0 FromVAT, 0 FromDOMAmt, 0 FromDOMVAT

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCGoodIn AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.GoodInSeq = A.GoodInSeq

JOIN \_TPDSFCWorkReport AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

 

EXEC dbo.\_SWComSourceDailyBatch 'A', @CompanySeq, @UserSeq

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'INSERT FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

-- 작업실적 / 작업지시 진행 제거

TRUNCATE TABLE \#SComSourceDailyBatch

INSERT INTO \#SComSourceDailyBatch

(

ToTableName, ToSeq, ToSerl, ToSubSerl,

FromTableName, FromSeq, FromSerl, FromSubSerl,

ToQty, ToStdQty, ToAmt, ToVAT, ToDOMAmt, ToDOMVAT,

FromQty, FromSTDQty, FromAmt, FromVAT, FromDOMAmt, FromDOMVAT

)

SELECT '\_TPDSFCWorkReport' ToTableName, B.WorkReportSeq ToSeq, 0 ToSerl, 0 ToSubSerl,

'\_TPDSFCWorkOrder' FromTableName, C.WorkOrderSeq FromSeq, C.WorkOrderSerl FromSerl, 0 FromSubSerl,

B.ProdQty ToQty, B.StdUnitProdQty ToStdQty, 0 ToAmt, 0 ToVAT, 0 ToDOMAmt, 0 ToDOMVAT,

C.OrderQty FromQty, C.STDUnitQty FromSTDQty, 0 FromAmt, 0 FromVAT, 0 FromDOMAmt, 0 FromDOMVAT

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCWorkReport AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

JOIN \_TPDSFCWorkOrder AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.WorkOrderSeq = A.WorkOrderSeq

AND C.WorkOrderSerl = A.WorkOrderSerl

WHERE A.Status = 0

AND A.WorkingTag IN ('A', 'U')

 

EXEC dbo.\_SWComSourceDailyBatch 'A', @CompanySeq, @UserSeq

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'INSERT FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

-- 투입(무조건 삭제 후 추가)

DELETE B

FROM \#TPDSFCWorkReport AS A

JOIN \_TPDSFCMatInput AS B ON B.CompanySeq = @CompanySeq

AND B.WorkReportSeq = A.WorkReportSeq

WHERE A.Status = 0

AND A.WorkingTag IN ('U', 'D')

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'DELETE FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

 

INSERT INTO \_TPDSFCMatInput

(

CompanySeq, WorkReportSeq, ItemSerl, InputDate, MatItemSeq, MatUnitSeq, Qty, StdUnitQty,

RealLotNo, SerialNoFrom, ProcSeq, AssyYn, IsConsign, GoodItemSeq, InputType,

IsPaid, IsPjt, PjtSeq, WBSSeq, LastUserSeq, LastDateTime,

Remark, ProdWRSeq, IsReInput, WHSeq, PgmSeq

)

SELECT @CompanySeq, A.WorkReportSeq, A.ItemSerl, B.WorkDate AS InputDate, A.MatItemSeq, A.MatUnitSeq, A.Qty, A.StdUnitQty,

A.RealLotNo AS RealLotNo, A.SerialNoFrom AS SerialNoFrom, B.ProcSeq, '0' AS AssyYn, '' AS IsConsign, B.GoodItemSeq, 6042002 AS InputType,

'0' AS IsPaid, '' AS IsPjt, 0 AS PjtSeq, 0 AS WBSSeq, @UserSeq AS LastUserSeq, GETDATE() AS LastDateTime,

B.Remark, 0 AS ProdWRSeq, NULL AS IsReInput, A.WHSeq, @PgmSeq AS PgmSeq

FROM \#TPDSFCMatInput AS A

JOIN \#TPDSFCWorkReport AS B ON A.WorkReportSeq = B.WorkReportSeq

WHERE B.Status = 0

AND B.WorkingTag = 'A'

AND ISNULL(A.WorkReportSeq, 0) \<\> 0

AND ISNULL(A.ItemSerl, 0) \<\> 0

IF @@ERROR \<\> 0

BEGIN

SELECT @IsTran = '1'

UPDATE \#TPDSFCWorkReport SET Result = 'INSERT FAIL', MessageType = 12007, Status = 99999 WHERE Status = 0

GOTO ERROR_PROC

RETURN

END

-- 투입(무조건 삭제 후 추가)

 

 

-- 수불 반영

CREATE TABLE \#TLGInOutDailyBatch

(

WorkingTag NVARCHAR(10),

InOutType INT,

InOutSeq INT,

InOutSerl INT,

InOutSubSerl INT,

DataKind INT,

MessageType INT,

Result NVARCHAR(250),

Status INT

)

CREATE TABLE \#TLGInOutMonth

(

InOutYM NCHAR(6),

WHSeq INT,

ItemSeq INT

)

CREATE TABLE \#TLGInOutMonthLot

(

InOutYM NCHAR(6),

WHSeq INT, LotNo NVARCHAR(30),

ItemSeq INT

)

 

-- 투입처리

TRUNCATE TABLE \#TLGInOutDailyBatch

TRUNCATE TABLE \#TLGInOutMonth

TRUNCATE TABLE \#TLGInOutMonthLot

INSERT INTO \#TLGInOutDailyBatch

(

WorkingTag, Status, MessageType, Result, InOutType,

InOutSeq, InOutSerl, InOutSubSerl, DataKind

)

SELECT DISTINCT

A.WorkingTag, A.Status, A.MessageType, A.Result, 130 AS InOutType, -- 생산실적

A.WorkReportSeq AS InOutSeq, 0 AS InOutSerl, A.ItemSerl AS InOutSubSerl, 0 AS DataKind

FROM \#TPDSFCMatInput AS A

WHERE A.Status = 0

EXEC dbo.\_SWLGInOutDailyDELETE @CompanySeq = @CompanySeq, @Level = 3, @LanguageSeq = @LanguageSeq -- 수불 삭제

EXEC dbo.djb_SWLGInOutDailyINSERT @CompanySeq = @CompanySeq, @Level = 3, @LanguageSeq = @LanguageSeq -- 수불 생성

 

IF EXISTS(SELECT 1 FROM \#TLGInOutDailyBatch WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '1'

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGInOutDailyBatch AS B ON A.WorkReportSeq = B.InOutSeq

WHERE A.Status = 0

AND B.Status \<\> 0

GOTO ERROR_PROC

RETURN

END

ELSE

BEGIN

-- 창고재고 집계

EXEC dbo.\_SWLGWHStockUPDATE @CompanySeq = @CompanySeq

-- (-)재고 체크

EXEC dbo.\_SWLGInOutMinusCheck @CompanySeq = @CompanySeq, @TableName = N'#TLGInOutDailyBatch', @LanguageSeq = @LanguageSeq

EXEC dbo.\_SWLGInOutLotMinusCheck @CompanySeq = @CompanySeq, @TableName = N'#TLGInOutDailyBatch', @LanguageSeq = @LanguageSeq

IF EXISTS(SELECT 1 FROM \#TLGInOutDailyBatch WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '1'

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGInOutDailyBatch AS B ON A.WorkReportSeq = B.InOutSeq

WHERE A.Status = 0

AND B.Status \<\> 0

GOTO ERROR_PROC

RETURN

END

END

-- 작업실적

TRUNCATE TABLE \#TLGInOutDailyBatch

TRUNCATE TABLE \#TLGInOutMonth

TRUNCATE TABLE \#TLGInOutMonthLot

 

INSERT INTO \#TLGInOutDailyBatch

(

WorkingTag, Status, MessageType, Result, InOutType,

InOutSeq, InOutSerl, InOutSubSerl, DataKind

)

SELECT DISTINCT

A.WorkingTag, A.Status, A.MessageType, A.Result, 130 AS InOutType, -- 생산실적

A.WorkReportSeq AS InOutSeq, 0 AS InOutSerl, 0 AS InOutSubSerl, 0 AS DataKind

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

EXEC dbo.\_SWLGInOutDailyDELETE @CompanySeq = @CompanySeq, @Level = 1, @LanguageSeq = @LanguageSeq -- 수불 삭제

EXEC dbo.djb_SWLGInOutDailyINSERT @CompanySeq = @CompanySeq, @Level = 1, @LanguageSeq = @LanguageSeq -- 수불 생성

 

 

IF EXISTS(SELECT 1 FROM \#TLGInOutDailyBatch WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '1'

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGInOutDailyBatch AS B ON A.WorkReportSeq = B.InOutSeq

WHERE A.Status = 0

AND B.Status \<\> 0

GOTO ERROR_PROC

RETURN

END

ELSE

BEGIN

-- 창고재고 집계

EXEC dbo.\_SWLGWHStockUPDATE @CompanySeq = @CompanySeq

-- (-)재고 체크

EXEC dbo.\_SWLGInOutMinusCheck @CompanySeq = @CompanySeq, @TableName = N'#TLGInOutDailyBatch', @LanguageSeq = @LanguageSeq

EXEC dbo.\_SWLGInOutLotMinusCheck @CompanySeq = @CompanySeq, @TableName = N'#TLGInOutDailyBatch', @LanguageSeq = @LanguageSeq

IF EXISTS(SELECT 1 FROM \#TLGInOutDailyBatch WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '1'

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGInOutDailyBatch AS B ON A.WorkReportSeq = B.InOutSeq

WHERE A.Status = 0

AND B.Status \<\> 0

GOTO ERROR_PROC

RETURN

END

END

 

 

-- 생산입고

TRUNCATE TABLE \#TLGInOutDailyBatch

TRUNCATE TABLE \#TLGInOutMonth

TRUNCATE TABLE \#TLGInOutMonthLot

 

INSERT INTO \#TLGInOutDailyBatch

(

WorkingTag, Status, MessageType, Result, InOutType,

InOutSeq, InOutSerl, InOutSubSerl, DataKind

)

SELECT A.WorkingTag, A.Status, A.MessageType, A.Result, 140 AS InOutType, -- 생산입고

A.GoodInSeq AS InOutSeq, 0 AS InOutSerl, 0 AS InOutSubSerl, 0 AS DataKind

FROM \#TPDSFCWorkReport AS A

WHERE A.Status = 0

AND A.GoodInSeq \<\> 0

EXEC dbo.\_SWLGInOutDailyDELETE @CompanySeq = @CompanySeq, @Level = 1, @LanguageSeq = @LanguageSeq -- 수불 삭제

EXEC dbo.djb_SWLGInOutDailyINSERT @CompanySeq = @CompanySeq, @Level = 1, @LanguageSeq = @LanguageSeq -- 수불 생성

IF EXISTS(SELECT 1 FROM \#TLGInOutDailyBatch WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '1'

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGInOutDailyBatch AS B ON A.GoodInSeq = B.InOutSeq

WHERE A.Status = 0

AND B.Status \<\> 0

GOTO ERROR_PROC

RETURN

END

ELSE

BEGIN

-- 창고재고 집계

EXEC dbo.\_SWLGWHStockUPDATE @CompanySeq = @CompanySeq

-- (-)재고 체크

EXEC dbo.\_SWLGInOutMinusCheck @CompanySeq = @CompanySeq, @TableName = N'#TLGInOutDailyBatch', @LanguageSeq = @LanguageSeq

EXEC dbo.\_SWLGInOutLotMinusCheck @CompanySeq = @CompanySeq, @TableName = N'#TLGInOutDailyBatch', @LanguageSeq = @LanguageSeq

IF EXISTS(SELECT 1 FROM \#TLGInOutDailyBatch WHERE Status \<\> 0)

BEGIN

SELECT @IsTran = '1'

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkReport AS A

JOIN \#TLGInOutDailyBatch AS B ON A.GoodInSeq = B.InOutSeq

WHERE A.Status = 0

AND B.Status \<\> 0

GOTO ERROR_PROC

RETURN

END

END

 

-- 수불 반영

-- 메인 키값 적용

UPDATE A

SET WorkReportSeq = B.WorkReportSeq

FROM \#TPDSFCWorkOrder AS A

JOIN \#TPDSFCWorkReport AS B ON A.IDX_NO = B.IDX_NO

WHERE A.Status = 0

AND B.Status = 0

COMMIT TRAN

-- ROLLBACK TRAN

 

SELECT @ErrMsg = Result ,

@WorkReportSeq = WorkReportSeq ,

@Status = 'OK'

FROM \#TPDSFCWorkOrder AS A

 

INSERT INTO djb_TPDMesWorkReportSaveLog(WorkingTag , MesKey , WorkDate, WorkOrderNo, WorkOrderSerl,

LotSerl , LotNo , Qty , OKQty , WorkEmpId ,

Remark , ItemSeq , InputQty, InputLotNo , Status ,

ErrMsg , WorkReportSeq , LastDateTIme)

SELECT @WorkingTag , @MesKey , @WorkDate , @WorkOrderNo , @WorkOrderSerl,

@LotSerl , @LotNo , @Qty , @OKQty , @WorkEmpId ,

@Remark , @ItemSeq , @InputQty , @InputLotNo , @Status ,

@ErrMsg , @WorkReportSeq , GETDATE()

-- 재처리 시 오류 발생한 건의 실적키 UPDATE

UPDATE A

SET WorkReportSeq = @WorkReportSeq

FROM djb_TPDMesWorkReportSaveLog AS A

WHERE A.MesKey = @MesKey

AND A.Status \<\> 'OK'

RETURN

ERROR_PROC:

 

DECLARE @ErrString NVARCHAR(MAX)

IF @IsTran = '1' -- TRAN이면....

BEGIN

SELECT @ErrString = ''

SELECT @ErrString = ISNULL(@ErrString, '')

\+ CONVERT(NVARCHAR(10), ISNULL(IDX_NO, 0)) + '~,'

\+ CONVERT(NVARCHAR(10), ISNULL(Status, 0)) + '~,'

\+ CONVERT(NVARCHAR(10), ISNULL(MessageType, 0)) + '~,'

\+ ISNULL(Result, '') + '~,' + '~/'

FROM \#TPDSFCWorkReport AS A

WHERE ISNULL(A.Status, 0) \<\> 0

 

/\*이거 때문에... 변수에 담아서 처리함.\*/

ROLLBACK TRAN

/\*이거 때문에... 변수에 담아서 처리함.\*/

 

UPDATE A

SET Result = B.Item4,

MessageType = B.Item3,

Status = B.Item2

FROM \#TPDSFCWorkReport AS A

JOIN dbo.\_FCOMSplitTwo(@ErrString, '~/', '~,') AS B ON A.IDX_NO = B.Item1

 

END

UPDATE A

SET Result = B.Result ,

MessageType = B.MessageType,

Status = B.Status

FROM \#TPDSFCWorkOrder AS A

JOIN \#TPDSFCWorkReport AS B ON B.IDX_NO = A.IDX_NO

WHERE A.Status = 0

AND B.Status \<\> 0

 

SELECT @ErrMsg = Result ,

@WorkReportSeq = WorkReportSeq ,

@Status = 'Err'

FROM \#TPDSFCWorkOrder AS A

 

INSERT INTO djb_TPDMesWorkReportSaveLog(WorkingTag , MesKey , WorkDate, WorkOrderNo, WorkOrderSerl,

LotSerl , LotNo , Qty , OKQty , WorkEmpId ,

Remark , ItemSeq , InputQty, InputLotNo , Status ,

ErrMsg , WorkReportSeq , LastDateTIme)

SELECT @WorkingTag , @MesKey , @WorkDate , @WorkOrderNo , @WorkOrderSerl,

@LotSerl , @LotNo , @Qty , @OKQty , @WorkEmpId ,

@Remark , @ItemSeq , @InputQty , @InputLotNo , @Status ,

@ErrMsg , @WorkReportSeq , GETDATE()

 

RETURN