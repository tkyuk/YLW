---
title: \<\<0303_cs.txt\>\>
date: 2022-03-03
tags: [erp, 개발, 교육 및 자료]
---

# \<\<0303_cs.txt\>\>

0303

aspx - 위에 html 부분에 작성해도 나타난다

 

aspx 컨트롤영역 추가할 때

Ylw.Common 붙은 애들 사용해야한다 (도구상자 x)

> 필요한게 있으면 화면에서 복붙 해야한다

 

속성

BizClass - 권한 느낌 (1,2 주면 별도 설정을 통해 특정 사람한테만 보임 / 거의 9 들어감)

 

DicCode (비워두어도 상관 없지만 / 채우는게 나음 ex. ID 가 cmbAccunit =\> DicCode = Accunit

KViewState

 

TabKeyIndex =\> 기타에서 하면 안되고 밑에 '동작'에서 바꿔야한다

TextMode =\> 멀티박스 / 싱글박스

 

UserClass - BizClass와 연관 거의 안건듬

 

\* aspx - html

Function ~ =\> 툴바

button - message ~ no

=\> no 클릭하면 버튼 나옴

 

ActionPostBackBefore - After =\> 동작 전 / 동작 후 (액션 =\> ex. 툴바 누르는 것)

JumpIn - cs에서 처리

JumpOut - aspx에서 처리

 

Resize 시트가 여러개 일 때 만들어줘야함 (1개일 때는 필요없음)

전체를 비율로 분배하기 때문에 화면 보면서 만들어야함

 

 

SheetComboInit() 없으면 초기화가 안된다

 

AddRowsToSheet - 기본적으로 보여지는 시트 갯수 (기본값이 10 / 안뿌리고 싶으면 0 작성)

 

 

page unload

새로 화면이 초기화 될때마다 매번 조회할 수 없으니 파일에 뿌림

r

실제로 화면에 뿌림 - DataDisplay

 

@@

저장 후 작동

1\. 가져온 순서

2,3컬럼명 =\> 보통 같게 씀

저장화면에서 중요한거 - sheetmesagecheck / 이걸 안하면 serl에 값이 안들어감

call form1 부분 무조건 있어야하고

데이터 갯수도 무조건 맞아야함

시트 여러개면 갯수 맞춰서 해야함

시트추가할 경우 이 부분 무조건 추가해야함

save sp - 1048 =\> 0,1 에러체크, 결과 / 뒤에 msorserl은 신규 저장 시 키값을 설정하면 시트에 // 저장한 결과 키값을 뿌려줌

신규 저장하면 1뿌려줌

\- =\> 한칸을 띄워도 이어주겠다

==\> 시트별로 하나씩 있어야하고, 키값은 무조건 있어야한다

최대 5개

 

 

컨트롤에 대한 코드도움 설정

> 다른부분 =\> 코드도움의 키 값

원래는 숫자인데 알아보기 편하게

(58번)d - ksystem - Common - Const - CodeHelp cs에 나와있음

custnm거래처번호를 같이가져옴

2 1

3 4 5 6 7 8 9 순서 =\> 7에 작성해야 뿌려지는 순서가 7인걸 가져옴

 

 

shwodic - 시트 코드헬프 관리하는 부분

case문으로 관리한다

2 1

3 4 5 6 7 8 9

키값은 무조건 4번째에 들어가있다

마지막 5개는 제뉴인 파라미터처럼 (vntTmp)

 

@@

ylwsh_Change = 시트 변경 있을 때 이벤트

col 이 숫자형태로 넘어옴

colx = 위치

위치랑 똑같으면 동작

 

11번째가 바뀌면 col - 11 row -1첫번째 행

colx를 통해서 col와 같은 값이 있는지 확인

 

컬럼 개별적으로 change를 못걸고 시트전체에 건다

=====================================

=====================================

cs - 화면, db 이어주는 서비스 역할

private void setviewstate

>  

 

 

 

settoolbar - 1이면 보임 (툴바)

movepageprevious - 맨 끝으로 이동 (0이라 비활성화 되어있다)

 

setinfoforpage

digformnms =\> 다이얼로그 화면 명칭들 나와있음

 

setcomboandvar

> var = vari

위에서부터 차례대로 0,1,2,3,4에 담김

통제값은 위에서부터 이어져서 숫자셈 (5에서 끝났으면 6부터 시작)

setCombo (이 컨트롤에 콤보/몇번째 테이블에/ true=addtotalrow있음(빈칸있는거)

 

\* 통제값 = 환경설정값 (ex. 원화소수점 자리수) (variable 폴더에 다 나와있음)

007009 =\> 007 + 009 =\> tdacontrol + tdacontrolsub

\* sp

tdacontrol

tdacontrolsub

 

전자결제 =\> 고정 / 그냥 가져다가 쓰면 된다

 

private void action - 안건들이는게 좋다 (aspx로 이동)

 

private bool newpage = 초기설정

> isopen =\> 신규모드 설정 =\> 없으면 신규일 때 A인걸 수정하면 U로 바뀜
>
> isopen=true =\> U

 

setdaultvalue =\> 위에서 설정했던 것들이 적용

 

default(기본값) =\> 디자인 속성 말고 html에서 설정

 

checkauthoritybefore =\> 권한 체크 (보통 부서로 한다)

> 권한체크안할거면 없어도 됨
>
> 조직도 체크해서 하위조직만 체크할 때 이거를 쓴다

 

조회 후 messagecheckquery가 넘어온 값들에 대해 체크

=false -\> 값이 이상한 경우

else

> if =\> \[0\]\[2\] = 3번째 값이 에러일 때 처리

에러가 아니면

displaydatamaster =\> 넘어온 값들 넣어줌

(cs에서는 aspx와 달리 자체에 있다)

cs에서 완료후 datadisplay를 타서 aspx에서 결과를 보여준다

 

저장시 마스터 키가 생김 =\> 키를 txtorderno.text = dsmsg.table 부분에 뿌려줌

savesheet에서 txtorderno 키값을 가져와서 저장

==\> 시트에 저장하기 전에 주문번호를 먼저가져와서 저장

savesheetvalue 할 때 orderno를 먼저가져간다

---------------------

\*

savemulti

멀티로 가져와서 한 줄씩 잘라서 사용

멀티쓰면 = addcolitem

한번에 가져오기 때문에 varchar(4000) 처럼 길다 / sp에서 while 써야함

ex. 합계 출력할 때 별도로 sp에서 컬럼 하나 더 추가

 

\*save 쓰면 for사용 (additem)

 

==\> 선택사항

---------------------

jump in

StringArray - 넘어온 값들을 잘라줌

\[1\]\[1\] 폼아이디 (넘어온 화면 명칭)

\[2\]\[1\]

\[3\]\[1\] 키값

\[4\]\[1\]

\[5\]\[1\] 플래그값 (N \| Q )

> =\> NEW 인지 QUERY인지
>
> 점프할 때 무슨동작할지
>
> 조회모드인지 신규모드인지

TRUE - 조회 =\> 화면 수정

FALSE - 신규 - 키값을 공백으로 넣어줘야함

\_\~~ =\> 사이트 이니셜인것들 제외

연결된 키값을 뿌려줌

 

SONO =\> 넘겨주는 화면에서 =\> 조회모음의 키값을 설정하는 부분을 SONO로 해야

넘어왔을 때 처리가 가능

==\> 넘겨주는 쪽에서 설정을 해야한다

 

 

==========================

==========================

JUMP OUT (aspx)

수주등록 =\> 거래명세서 점프 한다고 가정

화면아이디 담아주고

strJumpPgmIdTruc =\> \_사이트명 잘라주는 과정

 

Case문으로 점프할 화면 설정

 

SONo에 키값이 넘어감

 

AddJumpString - 담아주는 과정

strjumppgmid strjumpmsg =\> 각각 거래명세서, 키값

 

out에서 키값넘기고 in에서 받는다

============================

aspx

그룹웨어, 파일업로드는 추가 스크립트가 있다

\#include file= \~\~~ File/GroupWare.bas

 

선택한 시트의 행의 수를 전체 행의 수를 가져와서 비교 / 같으면 전체 삭제

Form1.ylwsh.DataRowCnt = 전체행수

 

시트콤보 쓸때

sheetcomboinit() 있어야함 (기본적으로 있기 때문에 굳이 지울 필요는 없긴함)

---------------------

cs

통제값

setview - 처음 열때

getview - 이전에 열렸던 부분

겟뷰에 추가하면 셋뷰에도 추가하면 된다

 

\_page.Add / \_vari.d\~~ 부분에 추가

------------------

DataDBSales (=공통DB) // 이상한 DB로 나와있으면 참조에 있는 것들로 바꿔줌

 

dsdata = libdb.query 마스터와 아이템은 따로 조회하고 마지막에 조회타임아웃시간을 설정

> libdb.querymaster =\> 옛날 방식 =\> 바꿔줘야함

query, save,savemulti 이렇게 3개만 아용

 

page.messagecheckquery =\> 첫줄확인해서 있는지 없는지 확인

=\> 없으면 =false이므로 별도로 동작 안한다

================================

================================

\* csproj

f4 =\> 속성 열림

 

 

CommonUTIL.js - 파일 첨부

===================

 

콤보는 통제값보다 앞에 입력

\_page.addcombo

\_page.addvariable

 

EXEC SDAGetSystemDate @vcSystemDate OUTPUT

=\> 시간값이 담긴다 =\> 그 값을 등록일, 수정일 등에 넣어준다

202203031207 형식으로 담김