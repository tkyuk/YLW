---
title: 3/4 GNI 등록
date: 2022-03-04
tags: [erp, 개발, 교육 및 자료]
---

# 3/4 GNI 등록

\<\<0304.sql\>\>\<\<0304.hwp\>\>

GNI 특징 - 대분류-소분류 참조

 

 

SELECT \* FROM TDAMajor -- 대분류

SELECT \* FROM TDAMinor -- 소분류

SELECT \* FROM TCTCodeHelpMajor -- 코드헬프

-- 테이블안에 추가정보가 다 있다 (단점 : 최대 12개)

-- Major = Header1~12

-- Minor = Item 1~12

 

-- GNI에서는 소스타입이 있다 =\> E, C, T

-- SourceType (거의 E사용)

 

-- E - 사용자정의코드 MinorCd를 키 값으로 조인

-- C - 시스템정의코드 ITEM1을 키 값으로 조인 (MinorCd말고 ITEM1을 키값으로 가져옴)

-- T =\> 다른 테이블에서 들어오는 것

 

 

-- SourceType = C인 경우

 

-- 1. 대분류의 소분류 중에 코드헬프를 쓰는 소분류는 대분류를 조회 했을 때 HelpKind 부분에 값이 있다 (HelpKind 3,4,5,6,7 = 453)

SELECT MajorCd, MajorNm, HelpKind3, HelpKind4, HelpKind5 FROM TDAMajor WHERE MajorCd LIKE '099%'

-- 2. 소분류 - 코드헬프를 쓰는 Item에 코드헬프의 JoinTable값이 있다

SELECT \* FROM TDAMinor WHERE MinorCd LIKE '099%' -- Item 3,4,5,6,7 =\> 1120102 / 4110302 / 4210102 / 1120302 / 4110102 / 4210302

-- 3. 코드헬프 소분류 조회 시 키값 = 대분류(HelpKind) / JoinTable = 소분류(Item) 넣어서 조회

SELECT \* FROM TDAMinor WHERE MinorCd LIKE '453%' AND JoinTable = '1120102'

-- 대분류(HelpKind) =\> 사용할 코드헬프의 키값

-- 소분류(Item) =\> 사용할 코드헬프의 JoinTable값

 

-- SourceType = T인 경우

-- =\> 다른 테이블에서 들어오는 것

-- =\> 다른 등록화면 SP에서 추가입력

-- =\> 코드헬프 못 씀 / 대분류등록에서도 볼 수 없다

 

-- T 예시 (057)

-- TDAMajor : (SourceType = T, MajorNm = 단위) ==\> 단위 테이블에서 값을 가져온다 (단위 테이블 = TDAUnit)

-- TDAMinor

-- JoiniTable =\> UnitCd ==\> 실제 테이블 (단위테이블)의 키 값(UnitCd)

-- MinorNm =\> UnitNm ==\> 실제 테이블 (단위테이블)의 이름 (UnitNm)

-- MinorNm을 선택하면 JoinTable (테이블 키값)이 들어옴

-- 단위 대분류

SELECT \* FROM TDAMajor WHERE MajorCd like '057%'

 

-- 단위 소분류

SELECT \* FROM TDAMinor WHERE MinorCd like '057%'

 

-- 단위 테이블

SELECT \* FROM TDAUnit

 

-- 밑의 과정이 단위 소분류와 같다

SELECT \*

FROM TDAMinor as a

JOIN TDAUnit as b

on a.JoinTable = b.UnitCd

WHERE MinorCd like '057%'

 

 

------------------------------------------------------------------------------------

--대분류 조회 및 추가

--(마스타 및 운영관리) 마스터등록 = 공통마스터 = 대분류등록

 

--\* 대분류 추가 (ex. Q39)

--여유칸이 없으면 DB에서 넣어줘야한다

 

--(대분류)코드 값을 지정해야함 / 숫자로만 된거 =\> 시스템에서 만들어져 있는거

--EX. 새로 만들 때 Q38 =\> Q39으로 만들면된다

 

--만들기전에 검색해야함

SELECT \* FROM TDAMajor WHERE MajorCd like 'Q%'

--추가 검색 유의 사항

SELECT \* FROM TCTCodeHelpMajor WHERE MajorCd LIKE 'Q%'

-- 조회화면 TDAMAJOR에서는 없던 QGC / QGT가 나옴

-- =\> 이 값들은 만들 때 피해줘야한다

 

-- 제일 마지막꺼 복사

-- MajorCd : Q38 -\> Q39

-- SysType : 밑에서 설명

-- MajorNm : T-교육

-- SourceType : E (만든거니까 사용자정의코드인 E사용)

-- RegDate,AddDate : 오늘 날짜로 수정

 

-- 소분류 추가

-- 소분류 title =\> 추가정보 느낌

-- 소분류 등록에서 안보임 =\> SysType를 1로 바꿔줘야 보임 (TDAMajor의 두 번째 컬럼)

-- 각 자리마다 페이지를 가지고 있다

-- (모듈을 알 수 없으므로,

-- 1. 모든 자리를 1로 바꾸거나 2. 연관된 데이터 참조)

 

-- 소분류등록에서 값을 입력하면 ITEM1~ 차례대로 써진다

SELECT \* FROM TDAMinor WHERE MinorCd LIKE 'Q39%'

 

 

 

 

 

----------------------------

--스플리트하는 SP에서 금액은 'N'을 붙여줘야 에러가 안나고 0이 나온다

-- =\> 금액은 0이 넘어와도 N'0'으로 넘어와야함

 

 

-- 저장할 때 쓰는 SP (현재시간 출력)

DECLARE @vcSystemDate NCHAR(12) --YYYYMMDDHHmm

 

EXEC SDAGetSystemDate @vcSystemDate OUTPUT

 

select @vcSystemDate

 

 

--TDAErrorMsg (제뉴인처럼 단어 조합은 안됨 / 대신 직접 작성)

DECLARE @vcStatus NVARCHAR(100),

@vcResult NVARCHAR(100)

EXEC SDAErrMsg 'AAUTH', @vcResult OUTPUT, @vcStatus OUTPUT

-- =====

-- 애초에 OK가 아니면 다 에러 취급

SELECT @vcStatus, @vcResult

 

--==========================================

--==========================================

GNI 진행

TSSInvoiceItem --거래명세서 // SONo,SOSerl join하면 진행

진행된 상태인지 안된 상태인지

제뉴인과 달리 찾아주는 sp가 없음

 

수주 - 출하 - 거래명세서 - 세금명세서 순

밑으로 내려갈수록 이전의 키값들을 다 가지고 있다 (누적)

SELECT \* FROM TSASOItem -- 수주

SELECT \* FROM TSSDelvReqItem

-- 출하의뢰 (수주번호의 SONo, SOSerl를 가지고 있음) -- DelvNo - 출하의뢰번호

SELECT \* FROM TSSInvoiceITem

-- 거래명세서 (수주번호의 SONo, SOSerl + 출하의뢰의 DelvNo, DelvSerl를 가지고 있음)

 

 

TssbillItem - 세금계산

 

이후 프로세스가 이전 프로세스의 키값을 가지고 있음

(출하에는 수주 키값 있지만 수주에는 출하 키값은 없다)

 

수주 1 - 100

(수주1의) 거래명세서 1 - 50 A/1

 

(수주1의) 거래명세서 2 - 50 마스터 키값, 시리얼 = A/2

세금계산서 1 - 25 A/2

세금계산서 2- 25 A/2

(A만 걸면 안되고 2까지 같이 걸어줘야함)

 

\* 거래명세서 입장에서 봤을 때

수주는 그냥 가져올 수 있지만

세금계산서 여러개가 발생했으므로 모든 건을 집계 해서 (sum) 가져와야함

25 + 25 이렇게 집계해야 50개를 가져올 수 있기 때문에

 

진행은 Seq, Serl(아이템 키)까지 걸어줘야함

+. 키 값들이 다 CHAR형 / 숫자는 NUMERIC

 

SCTQry_ylwsh05008

 

SSASalesOrder01Save

채번 시

날짜 기준으로 가져옴

날짜 + MAX값 가져와서 + 1

SDANextSerialno // @wSONo =\> 새로 채번한 값

MAX값 구하고 저장하기 전 그 찰나에 누가 데이터를 저장할 수 있기 때문에

if문 부분이 있다

 

여러 사람의 중복으로 병목현상 =\> 무한루프 =\> 채번같은 경우는 밖으로 빼줘서 해야한다

 

 

\* 업무별 외부키가 별도로 없다

번호자체가 테이블에서 키값으로 쓰이기 때문

 

\*\* TSASOM ProgressType

TSASOItem - ProgClss

0 - 초기값

1 - 완료

1이 아닌값 2 - 중단

 

StkQty - 전체

NextQty - 진행 수량

 

StkQty, NextQty 비교하여 진행, 중단 결정

 

SSADelvReq2Save

진행수량 관리해주는 로직

TDACONTROLSUB =\> 수량통제

영업/수출관리 - 기본정보 - 운영환경관리 (영업수출) 진행 =\> 요청수량 초과허용 체크

 

영업/수출관리 - 기본정보 - 판매단가

(컨트롤)화폐에 따라서 판매단가가 바뀜

 

 

SDAEnvModSal

 

영업/수출관리 - 판매단가

우선순위

( 거래처 - 유통 - 품목 순 ) 단가등록 // 운영환경관리에 나와 있음

 

 

 

 

\<\<0307.txt\>\>\<\<SCTQry_ylwa05006_ksystem_sjpark.sql\>\>\<\<SCTQry_ylwa06002_0307.sql\>\>

3/7 GNI 조회모음

0307

SP와 세팅화면으로만 구성되어있음

 

영업/수출관리 - 영업활동 - 수주현황

=\> 폼화면과 달리 SP만

 

폼명 : ylwa00000 \_site

ylwa + 숫자 5개 \_사이트명 (필요시 작성)

뒤에 아무것도 없으면 영림원 표준

 

구매/수입관리 - 구매계획관리 - 구매요청현황 =\> 구매합니다

==\> 구매품의를 올림

 

구매요청 =\> 구매품의

SCTQry\_ 가 앞에 붙는다

 

 

마스타 및 운영관리 - 운영관리 - 조회모음등록

폼 ID 작성

1\. NOQ, AddTotal 기능

2\. 전체선택 체크박스 추가

3\. 출력물 - 활성여부

 

4\. dynamic sheet (보통은 General Sheet)

 

before 품목명 품목코드

after 품목명 품목코드 + 재질 + 색상

after 품목명 품목코드 + 재질 + 색상 + 규격

하나 추가할 때 마다 할 일이 많음

==\> 동적으로 보여주기

품목명 품목코드 + val

===========

고정

 

5\. 구조변경 (시트 갯수가 여러개 일 때 )

6\. 시트관계

> S1 =\> (S2, S3) ==\> S1 누르면 S2,3 각각 뜸
>
> MajorCd를 담아서감

 

Step-2

일반 - 위치 지정 같은거

콤보 쪽은 좀 중요 =\> 하드 코딩이 가능

 

 

권한넣는법 - 폼등록

> viewflag =\> 버튼 보여줌

===============

마스타 = 운영관리 - 조회모음등록

 

조회모음에 등록한다고

 

운영관리 프로그램 폼등록

 

 

-----

sp

SCTQry_ylwa05006_ksystem_sjpark

거래처 =\> 대표자명이 있음 =\> 옆에 추가 (대표)

원화금액 옆에 컬럼 추가 total

 

==========================

\*조건추가

조회모음 - 조회모음등록

Code유형 - Text, CodeHelp,

CodeHelp - 있는걸 사용

Text -

 

sp

변수 3번째 추가 =\>i3stCombCond에 추가

WHERE에 조건 추가

> 필수가 아니기 때문에 = '' OR 추가

 

Text =\> 일일히 정해주는 것

> Seq - 01,02,03,04
>
> Code = 마음대로 (보통은 001 002 003 004)
>
> 새로 생길 때마다 추가해줘야해서 잘 안쓰긴함

 

SP =\> 하부조건