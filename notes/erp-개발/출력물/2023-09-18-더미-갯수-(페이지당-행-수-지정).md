---
title: 더미 갯수 (페이지당 행 수 지정)
date: 2023-09-18
tags: [erp, 개발, 출력물]
---

# 더미 갯수 (페이지당 행 수 지정)

![](assets/더미-갯수-(페이지당-행-수-지정)/image30.png)\<\<RptPDMMOutExtraReq_noe.ozr\>\>

SetGlobal("DefaultRow", 32);        // 페이지당 행 수

SetGlobal("BlankRow", GetGlobal("DefaultRow")); // 빈칸 개수

SetGlobal("RowCnt",0);        // No

 

SetGlobal("BlankRow", GetGlobal("BlankRow") - 1);

 

var nIdx = This.GetDataSetRowIndex();

var lastpage = This.GetDataSetRowCount();

 

if(GetGlobal("BlankRow") == 0)        // 한페이지당 최대행수 이고

{

> if(nIdx != lastpage)
>
> {
>
> This.SetForceNewPage(true);
>
> SetGlobal("BlankRow", GetGlobal("DefaultRow"));
>
>  
>
> }

}

![](assets/더미-갯수-(페이지당-행-수-지정)/image31.png)

 

더미풋터

This.SetRepeatNumber(GetGlobal("BlankRow"));

 

그룹풋터

> SetGlobal("BlankRow", GetGlobal("DefaultRow"));
>
> SetGlobal("RowCnt",0);        // No