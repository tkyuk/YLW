---
title: No 순번
date: 2023-03-14
tags: [erp, 개발, 출력물]
---

# No 순번

<span class="mark">// 페이지마다 채번</span>

This.SetText(This.GetDataSetRowIndex());

 

 

 

 

var index = 1;

if (GetGlobal("row_index")!= null) index = GetGlobal("row_index");

This.SetText(index);

SetGlobal("row_index", (index + 1));

 

오즈스크립트

 

int i;

i=i+1;

setattr("caption",(string)i);

 

var idx = GetGlobal("idx");

var GroupIndex = GetGlobal("GroupIndex");

idx = idx + 1

This.SetText(idx+(GroupIndex\*8));

SetGlobal("idx", idx);

<span class="mark">// 페이지 넘어가도 행번호 이어가기</span>

var index = 1;

if (GetGlobal("row_index")!= null) index = GetGlobal("row_index");

This.SetText(index);

 

if (This.GetDataSetValue("DataBlock1.ItemName") !=null){

> SetGlobal("row_index", (index + 1));

}

else{

> This.SetText(" ");

}