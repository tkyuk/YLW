---
title: 숫자 반점 지우는 방법
date: 2022-09-02
tags: [erp, 개발, 출력물]
---

# 숫자 반점 지우는 방법

var amt = This.GetComponentByName("FixedTableLabel68").GetText();

var vat = This.GetComponentByName("FixedTableLabel69").GetText();

var samt = amt.replace(/,/gi,"");

var svat = amt.replace(/,/gi,"");

var total = Number(samt) + Number(svat);

 

This.SetText(total);