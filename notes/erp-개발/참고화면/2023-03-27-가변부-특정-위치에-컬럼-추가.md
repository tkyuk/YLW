---
title: 가변부 특정 위치에 컬럼 추가
date: 2023-03-27
tags: [erp, 개발, 참고화면]
---

# 가변부 특정 위치에 컬럼 추가

품목조회_hsg

 

 

기존 가변부 임시테이블에 10 곱함

10

20

30 \<= 31 을 넣어서 특정 위치 밑에 넣게 만듬

40

50

DROP PROCEDURE hsg_SWDAItemListQuery

GO

CREATE PROCEDURE hsg_SWDAItemListQuery

@ServiceSeq INT = 0,

@WorkingTag NVARCHAR(10)= '',

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 0,

@PgmSeq INT = 0,

@IsTransaction BIT = 0

AS

 

 

> --DECLARE @CLOCK DATETIME
>
> --SELECT @CLOCK = GETDATE()

 

DECLARE @ItemSeq INT,

@UMItemClass INT,

@ItemName NVARCHAR(200),

@ItemNo NVARCHAR(100),

@Spec NVARCHAR(200),

@AssetSeq INT,

@RegUserSeq INT,

@SMStatus INT,

@UMEtcItemClass INT,

@IsSet NCHAR(1),

@RegDateFr NCHAR(8),

@RegDateTo NCHAR(8),

@UMEtcItemClassValue INT,

@IsSTDItem NCHAR(1),

@UMItemClassL INT,

@UMItemClassM INT,

@EmpSeq INT,

@DeptSeq INT,

@SerialNo                 NVARCHAR(100),

@InPutType         INT,

@TitleSerl         INT,

@AddText1                 NVARCHAR(200),

@AddText2                 NVARCHAR(200),

@AddText3                 NVARCHAR(200),

@AddText4                 NVARCHAR(200),

@AddText5                 NVARCHAR(200),

@AddText6                 NVARCHAR(200),

@AddText7                 NVARCHAR(200),

@AddCd3                 INT                         ,

@AddCd5                 INT,

@TimeDifference_Hour INT, --2022.10.28 정민찬 :: 법인별 시간차 적용

@TimeDifference_Minute INT --2022.10.28 정민찬 :: 법인별 시간차 적용

SELECT @AssetSeq = ISNULL(AssetSeq, 0),

@UMItemClass = ISNULL(UMItemClass, 0),

@UMEtcItemClass = ISNULL(UMEtcItemClass, 0),

@UMEtcItemClassValue = ISNULL(UMEtcItemClassValue, 0),

@ItemSeq = ISNULL(ItemSeq, 0),

@ItemName = ISNULL(ItemName, ''),

@ItemNo = ISNULL(ItemNo, ''),

@Spec = ISNULL(Spec, ''),

@SMStatus = ISNULL(SMStatus, 0),

@RegDateFr = ISNULL(RegDateFr, ''),

@RegDateTo = ISNULL(RegDateTo, ''),

@RegUserSeq = ISNULL(RegUserSeq, 0),

@IsSTDItem = ISNULL(IsSTDItem, '0'),

@IsSet = ISNULL(IsSet, '0') ,

> @UMItemClassL = ISNULL(UMItemClassL, 0),
>
> @UMItemClassM = ISNULL(UMItemClassM, 0),
>
> @EmpSeq                 = ISNULL(EmpSeq, 0),
>
> @DeptSeq         = ISNULL(DeptSeq, 0),
>
> @InPutType         = ISNULL(InPutType,0),                                        -- 입력형태
>
> @TitleSerl         = ISNULL(TitleSerl,0),                                        -- 추가정보명Seq
>
> @AddText1         = LTRIM(RTRIM(ISNULL(AddText1,''))), -- 추가정보값
>
> @AddText2         = LTRIM(RTRIM(ISNULL(AddText2,''))), -- 추가정보값
>
> @AddText3         = LTRIM(RTRIM(ISNULL(AddText3,''))), -- 추가정보값
>
> @AddText4         = LTRIM(RTRIM(ISNULL(AddText4,''))), -- 추가정보값
>
> @AddText5         = LTRIM(RTRIM(ISNULL(AddText5,''))), -- 추가정보값
>
> @AddText6         = LTRIM(RTRIM(ISNULL(AddText6,''))), -- 추가정보값
>
> @AddText7         = LTRIM(RTRIM(ISNULL(AddText7,''))), -- 추가정보값
>
> @AddCd3                 = ISNULL(AddCd3,0),                                                -- 추가정보값Seq
>
> @AddCd5                 = ISNULL(AddCd5,0)                                                -- 추가정보값Seq

FROM \#BIZ_IN_DataBlock1

 

IF @RegDateFr = ''

SELECT @RegDateFr = '19000101'

 

IF @RegDateTo = ''

SELECT @RegDateTo = '29991231'

 

-- hsg. TitleSerl DECIMAL 변경

CREATE TABLE \#Temp_ItemUserDefineData(TitleSerl DECIMAL(19,5), Title NVARCHAR(100), MngValName NVARCHAR(200), SMInputType INT, Seq INT,

MngValText NVARCHAR(200), CodeHelpSeq INT, CodeHelpParams NVARCHAR(100), Mask NVARCHAR(50),

IsNON NCHAR(1), ItemSeq INT)

 

CREATE TABLE \#Temp_ItemUserDefine

(

ColIDX INT IDENTITY(0,1),

TitleName NVARCHAR(100),

TitleSeq INT,

InputType NVARCHAR(50),

> DecLen                INT

)

---------------------------------------

--조회시 법인별 시간차 적용 :: 2022.10.28 정민찬

---------------------------------------

SELECT @TimeDifference_Hour = TimeDifference_Hour,

@TimeDifference_Minute = TimeDifference_Minute

FROM \_TCACompanyConfig WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq

 

-- 추가정보 Title

-- 2010.08.03 정동혁 : 품목분류도 다이나믹부에 조회되도록 수정.

-- 품목분류

INSERT \#Temp_ItemUserDefine( TitleName, TitleSeq, InputType, DecLen )

SELECT A.MajorName, A.MajorSeq, 'ItemClass', 0

FROM \_TDAUMajor AS A WITH(NOLOCK)

LEFT OUTER JOIN \_TDADefineItemClass AS B WITH(NOLOCK) ON ( A.CompanySeq = B.CompanySeq AND A.MajorSeq = B.UMajorItemClass )

WHERE A.CompanySeq = @CompanySeq

AND ((@WorkingTag \<\> 'M' AND B.IsItem = '1') OR (@WorkingTag = 'M' AND B.IsMaterial = '1')) -- 제상품 / 자재

AND EXISTS (SELECT 1 FROM \_TDAItemClass WITH(NOLOCK) WHERE CompanySeq = A.CompanySeq AND UMajorItemClass = A.MajorSeq AND (UMajorItemClass \< 2001 OR UMajorItemClass \> 2006))

-- AND A.MajorSeq \> 2000000

ORDER BY B.Priority, A.MajorSeq

--select \* from \_TDADefineItemClass where CompanySeq = 1

 

 

---- 추가정보

--INSERT \#Temp_ItemUserDefine( TitleName, TitleSeq, InputType, DecLen )

--SELECT -- A.Title,

-- CASE WHEN ISNULL(A.WordSeq,0) = 0 THEN ISNULL(A.Title, '') ELSE ISNULL(Dic.Word,'') END AS Title,

-- A.TitleSerl, ( CASE SMInputType WHEN 1027006 THEN 'enCheck'

> --                                                 WHEN 1027007 THEN 'enDate'
>
> --                                                 WHEN 1027002 THEN 'enFloat' --2022.12.23 숫자형식 지정 : 정민찬
>
> --                                                 ELSE 'enText'
>
> --                                                 END ), A.DecLen

-- FROM \_TCOMUserDefine AS A WITH(NOLOCK)

-- LEFT OUTER JOIN \_TCADictionary AS Dic WITH(NOLOCK) ON A.CompanySeq = @CompanySeq

-- AND Dic.LanguageSeq = @LanguageSeq

-- AND A.WordSeq = Dic.WordSeq

-- WHERE A.CompanySeq = @CompanySeq

-- AND A.TableName = '\_TDAItem'

-- AND ((@WorkingTag \<\> 'M' AND A.DefineUnitSeq = 8010001) OR (@WorkingTag = 'M' AND A.DefineUnitSeq = 8010002)) -- 제상품 / 자재

-- hsg. hsg_인축독성항목 - 추가정보정의 : 과세여부 가변부에 추가

-- 임시테이블에 title 데이터 담고 과세여부 insert 후 title 테이블에 insert (순번 맞추기 위해 TitleSerl에 10 곱함)

SELECT -- A.Title,

CASE WHEN ISNULL(A.WordSeq,0) = 0 THEN ISNULL(A.Title, '') ELSE ISNULL(Dic.Word,'') END AS Title,

A.TitleSerl\*10 AS TitleSerl, ( CASE SMInputType WHEN 1027006 THEN 'enCheck'

> WHEN 1027007 THEN 'enDate'
>
> WHEN 1027002 THEN 'enFloat' --2022.12.23 숫자형식 지정 : 정민찬
>
> ELSE 'enText'
>
> END ) AS InputType , A.DecLen

 

INTO \#TempTitle_ItemUserDefine

FROM \_TCOMUserDefine AS A WITH(NOLOCK)

LEFT OUTER JOIN \_TCADictionary AS Dic WITH(NOLOCK) ON A.CompanySeq = @CompanySeq

AND Dic.LanguageSeq = @LanguageSeq

AND A.WordSeq = Dic.WordSeq

WHERE A.CompanySeq = @CompanySeq

AND A.TableName = '\_TDAItem'

AND ((@WorkingTag \<\> 'M' AND A.DefineUnitSeq = 8010001) OR (@WorkingTag = 'M' AND A.DefineUnitSeq = 8010002)) -- 제상품 / 자재

 

INSERT INTO \#TempTitle_ItemUserDefine VALUES('과세여부',10000671,'enCheck',0)

 

-- 추가정보

INSERT \#Temp_ItemUserDefine( TitleName, TitleSeq, InputType, DecLen )

SELECT \*

FROM \#TempTitle_ItemUserDefine AS A

ORDER BY A.TitleSerl

 

 

-- \[품목소분류검사대상등록\] 화면 검사대상 여부 : 2022.03.22 우승현

SELECT '1' AS IsUse, \* INTO \#IsQC FROM \_TPDQAItemClassQCKind WHERE CompanySeq = @CompanySeq AND IsNotQC = 0

AND(IsFinQC = 1 OR IsInQC = 1 OR IsOutQC = 1 OR IsAfterQCPAM = 1 OR IsPurQC = 1 OR IsOspQC = 1 OR IsPMSOutQC = 1)

 

CREATE TABLE \#Temp_TDAItemInfo_New(

> IDX_NO                        INT IDENTITY(1, 1),        ItemSeq INT, ItemName NVARCHAR(200),

ItemNo NVARCHAR(100), Spec NVARCHAR(200), TrunName NVARCHAR(200),

AssetName NVARCHAR(100), AssetSeq INT, UnitName NVARCHAR(100),

UnitSeq INT, SMABC INT, SMABCName NVARCHAR(100),

SMStatus INT, SMStatusName NVARCHAR(100), SMInOutKind INT,

SMInOutKindName NVARCHAR(100), DeptName NVARCHAR(100), DeptSeq INT,

EmpName NVARCHAR(100), EmpSeq INT, STDItemName NVARCHAR(200),

STDItemSeq INT, ItemEngName NVARCHAR(200), ItemClassLName NVARCHAR(100),

ItemClassMName NVARCHAR(100), ItemClassSName NVARCHAR(100), UMItemClass INT,

RegUser NVARCHAR(100), LastUser NVARCHAR(100), RegDate NCHAR(8),

LastDate NCHAR(8), IsSTDItem NCHAR(1), IsOption NCHAR(1), IsSet NCHAR(1), IsQC NCHAR(1),

SMOutKindName NVARCHAR(100), IsBOMReg NCHAR(1), IsProcMat NCHAR(1),

SMLimitTermKindName NVARCHAR(100), SMLimitTermKind INT, IsLotMng NCHAR(1),

IsSerialMng NCHAR(1), SMAssetGrp INT, PurCustName NVARCHAR(100), TrustCustName NVARCHAR(100),

Remark NVARCHAR(4000), SMVatKindName NVARCHAR(100), PriceInVat NCHAR(1),

IsFileCheck NCHAR(1),                        IsImageCheck        NCHAR(1) , STDItemNo NVARCHAR(100), STDItemSpec NVARCHAR(200), -- 대표품번, 대표규격 : 2021.11.29 김희연 추가

MKCustName NVARCHAR(100), IsPrice NCHAR(1)

)

CREATE INDEX IDX\_#Temp_ItemUserDefine ON \#Temp_ItemUserDefine(ColIDX)

CREATE INDEX IDX\_#Temp_TDAItemInfo_New ON \#Temp_TDAItemInfo_New(ItemSeq)

CREATE INDEX IDX\_#Temp_ItemUserDefineData ON \#Temp_ItemUserDefineData(TitleSerl, ItemSeq)

-- 품목정보 (고정컬럼)

INSERT INTO \#Temp_TDAItemInfo_New(

ItemSeq , ItemName ,

ItemNo , Spec , TrunName ,

AssetName , AssetSeq , UnitName ,

UnitSeq , SMABC , SMABCName ,

SMStatus , SMStatusName , SMInOutKind ,

SMInOutKindName , DeptName , DeptSeq ,

EmpName , EmpSeq , STDItemName ,

STDItemSeq , ItemEngName , ItemClassLName ,

ItemClassMName , ItemClassSName , UMItemClass ,

RegUser , LastUser , RegDate ,

LastDate , IsSTDItem , IsOption , IsSet , IsQC ,

SMOutKindName , IsBOMReg , IsProcMat ,

SMLimitTermKindName , SMLimitTermKind , IsLotMng ,

IsSerialMng , SMAssetGrp , PurCustName , TrustCustName,

Remark , SMVatKindName , PriceInVat ,

IsFileCheck ,                IsImageCheck , STDItemNo , STDItemSpec, -- 대표품번, 대표규격 : 2021.11.29 김희연 추가

MKCustName , IsPrice

)

SELECT DISTINCT

ISNULL(A.ItemSeq,0) AS ItemSeq, -- 품목코드

ISNULL(A.ItemName,'') AS ItemName, -- 품목명

ISNULL(A.ItemNo,'') AS ItemNo, -- 품목번호

ISNULL(A.Spec,'') AS Spec, -- 규격

ISNULL(A.TrunName,'') AS TrunName, -- TrunName

ISNULL(C.AssetName,'') AS AssetName, -- 품목자산분류

ISNULL(A.AssetSeq,0) AS AssetSeq, -- 품목자산분류코드

ISNULL((SELECT UnitName FROM \_TDAUnit WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND UnitSeq = A.UnitSeq), '') AS UnitName,-- 기준단위

ISNULL(A.UnitSeq,0) AS UnitSeq, -- 기준단위코드

ISNULL(A.SMABC,0) AS SMABC, -- 중요도

ISNULL(CD.WordSite,ISNULL(CD.Word,ISNULL(SC.MinorName,''))) AS SABCName, -- 중요도명

--(SELECT ISNULL(MinorName,'') FROM \_TDASMinor WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND MajorSeq = 2002 AND MinorSeq = A.SMABC) AS SMABCName, -- 중요도

ISNULL(A.SMStatus,0) AS SMStatus, -- 품목 상태

ISNULL(MD.WordSite, ISNULL(MD.Word, AM.MinorName)) AS SMStatusName, -- 품목상태명

--(SELECT ISNULL(MinorName,'') FROM \_TDASMinor WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND MajorSeq = 2001 AND MinorSeq = A.SMStatus) AS SMStatusName, -- 품목 상태

ISNULL(A.SMInOutKind,0) AS SMInOutKind, -- 내외자구분

ISNULL(MDIO.WordSite,ISNULL(MDIO.Word,AMIO.MinorName)) AS SMInOutKindName, -- 내외자구분명,

--(SELECT ISNULL(MinorName,'') FROM \_TDASMinor WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND MajorSeq = 8007 AND MinorSeq = A.SMInOutKind) AS SMInOutKindName, -- 내외자구분

ISNULL((SELECT DeptName FROM \_TDADept WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND DeptSeq = A.DeptSeq), '') AS DeptName,-- 부서

ISNULL(A.DeptSeq,0) AS DeptSeq, -- 부서코드

ISNULL((SELECT EmpName FROM \_TDAEmp WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND EmpSeq = A.EmpSeq), '') AS EmpName, -- 관리자

ISNULL(A.EmpSeq,0) AS EmpSeq, -- 담당자코드

ISNULL(G.ItemName,'') AS STDItemName, -- 대표품목명

ISNULL(A.STDItemSeq, 0) AS STDItemSeq, -- 대표품목코드

ISNULL(A.ItemEngName,'') AS ItemEngName, -- 영문명

-- CASE WHEN ISNULL(L.ValueSeq,0) = 0 THEN ''

> --ELSE ( SELECT ISNULL(MinorName,'')
>
> --                 FROM \_TDAUMinor WITH(NOLOCK)

-- WHERE CompanySeq = @CompanySeq AND MinorSeq = L.ValueSeq ) END AS ItemClassLName, -- 품목대분류

-- CASE WHEN ISNULL(K.ValueSeq,0) = 0 THEN ''

> --ELSE ( SELECT ISNULL(MinorName,'')

-- FROM \_TDAUMinor WITH(NOLOCK)

-- WHERE CompanySeq = @CompanySeq AND MinorSeq = K.ValueSeq ) END AS ItemClassMName, -- 품목중분류

-- ISNULL(H.MinorName,'') AS ItemClassSName, -- 품목소분류

-- ISNULL(B.UMItemClass,0) AS UMItemClass, -- 품목소분류코드

H.ItemClassLName AS ItemClassLName,

H.ItemClassMName AS ItemClassMName,

H.ItemClassSName AS ItemClassSName,

H.ItemClassSSeq AS UMItemClass,

CASE WHEN ISNULL(A.RegUserSeq,0) = 0 THEN ''

ELSE (SELECT ISNULL(UserName,'') FROM \_TCAUser WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND UserSeq = A.RegUserSeq) END AS RegUser,

CASE WHEN ISNULL(A.RegDateTime, '') = '' THEN ''

ELSE (SELECT ISNULL(UserName,'') FROM \_TCAUser WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND UserSeq = A.LastUserSeq) END AS LastUser,

CONVERT(NCHAR(8), DATEADD(MI,@TimeDifference_Minute, DATEADD(HH, @TimeDifference_Hour, A.RegDateTime)), 112) AS RegDate, -- 2022.10.28 : 정민찬 법인별 시간차 적용

CONVERT(NCHAR(8), DATEADD(MI,@TimeDifference_Minute, DATEADD(HH, @TimeDifference_Hour, A.LastDateTime)), 112) AS LastDate, -- 2022.10.28 : 정민찬 법인별 시간차 적용

ISNULL(A.IsSTDItem, '0') AS IsSTDItem, -- 대표품목여부

ISNULL(D.IsOption,'0') AS IsOption, -- 옵션품목여부

ISNULL(D.IsSet, '0') AS IsSet, -- 세트품목여부

CASE WHEN P.IsUse IS NULL AND ISNULL(P2.IsUse,'0') = '0' THEN '0'

WHEN P.IsUse = 0 THEN '0'

WHEN P.IsUse = 1 THEN '1'

WHEN P.IsUse IS NULL AND ISNULL(P2.IsUse,'0') \<\> '0' THEN '1' END AS IsQC, --검사대상 2022.03.22 우승현

ISNULL(ISNULL(ED.WordSite,ED.Word),ES.MinorName) AS SMOutKindName, --불출구분

--ISNULL((SELECT MinorName FROM \_TDASMinor WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND MinorSeq = E.SMOutKind),'') AS SMOutKindName, -- 불출구분

> (CASE WHEN EXISTS ( SELECT 1 FROM \_TPDBOM WITH(NOLOCK) WHERE CompanySeq = A.CompanySeq AND ItemSeq = A.ItemSeq ) THEN '1' ELSE '0' END ) AS IsBOMReg, -- BOM등록여부
>
> (CASE WHEN EXISTS ( SELECT 1 FROM \_TPDROUItemProcMat WITH(NOLOCK) WHERE CompanySeq = A.CompanySeq AND ItemSeq = A.ItemSeq ) THEN '1' ELSE '0' END ) AS IsProcMat, -- 제품별 공정별 소요자재여부

ISNULL((SELECT MinorName FROM \_TDASMinor WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND MinorSeq = ST.SMLimitTermKind), '') AS SMLimitTermKindName, -- 유통기한

ST.LimitTerm AS SMLimitTermKind, -- 유통기한

ST.IsLotMng AS IsLotMng, -- Lot관리

ST.IsSerialMng AS IsSerialMng, -- Serial관리

C.SMAssetGrp AS SMAssetGrp, -- 2010. 12. 17 hkim 상품(6008001)은 BOM 점프를 막기 위해

ISNULL((SELECT CustName FROM \_TDACust WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND CustSeq = IP.PurCustSeq), '') AS PurCustName,

ISNULL((SELECT CustName FROM \_TDACust WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND CustSeq = IP.TrustCustSeq), '') AS TrustCustName,

LTRIM(RTRIM(ISNULL(F.Remark, ''))) AS Remark, -- 비고 2011.03.02 정혜영

ISNULL(ISNULL(SD.WordSite,SD.Word),SM.MinorName) AS SMVatKindName, -- 부가세구분 2012.05.14 김준식 2022.05.27 김희연 다국어 적용

ISNULL(D.IsVat,'') AS PriceInVat, -- 판매단가에부가세포함 2012.05.14 김준식

CASE WHEN ISNULL(F.FileSeq, 0) \<\> 0         THEN '1' ELSE '0' END AS IsFileCheck, -- 파일첨부여부, 2012.09.17 윤보라

CASE WHEN ISNULL(F.ImgFile, '') \<\> '' THEN '1' ELSE '0' END AS IsImageCheck, -- 이미지 여부 : 20160406 장경선

ISNULL(G.ItemNo,'') AS STDItemNo, -- 대표품번 2021.11.29 김희연

ISNULL(G.Spec ,'') AS STDItemSpec, -- 대표규격 2021.11.29 김희연

ISNULL((SELECT CustName From \_TDACust WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND CustSeq = IP.MKCustSeq), '') AS MKCustName, -- 제조사 2022.03.28 김희연

CASE WHEN BP.ItemSeq IS NOT NULL THEN '1' ELSE '0' END AS IsPrice -- 단가등록여부 2022.06.10 우승현

FROM \_TDAItem                                                AS A WITH(NOLOCK)

> LEFT OUTER JOIN \_TDAItemClass                AS B WITH(NOLOCK) ON ( A.CompanySeq = B.CompanySeq AND A.ItemSeq = B.ItemSeq AND B.UMajorItemClass IN (2001,2004) )

LEFT OUTER JOIN \_TDAItemAsset                AS C WITH(NOLOCK) ON ( A.CompanySeq = C.CompanySeq AND A.AssetSeq = C.AssetSeq )

LEFT OUTER JOIN \_TDAItemSales                AS D WITH(NOLOCK) ON ( A.CompanySeq = D.CompanySeq AND A.ItemSeq = D.ItemSeq )

LEFT OUTER JOIN \_TDAItemProduct         AS E WITH(NOLOCK) ON ( A.CompanySeq = E.CompanySeq AND A.ItemSeq = E.ItemSeq )

LEFT OUTER JOIN \_TDAItemImage AS F WITH(NOLOCK) ON ( A.CompanySeq = F.CompanySeq AND A.ItemSeq = F.ItemSeq )

LEFT OUTER JOIN \_TDAItem AS G WITH(NOLOCK) ON ( A.CompanySeq = G.CompanySeq AND A.STDItemSeq = G.ItemSeq )

LEFT OUTER JOIN \_VDAItemClass AS H WITH(NOLOCK) ON ( B.CompanySeq = H.CompanySeq AND B.UMItemClass = H.ItemClassSSeq )

> --LEFT OUTER JOIN \_TDAUMinor                 AS H WITH(NOLOCK) ON ( B.CompanySeq = H.CompanySeq AND H.MajorSeq = LEFT( B.UMItemClass, 4 ) AND B.UMItemClass = H.MinorSeq )

LEFT OUTER JOIN \_TDASMinor                 AS J WITH(NOLOCK) ON ( C.CompanySeq = J.CompanySeq AND J.MajorSeq = 6008 AND C.SMAssetGrp = J.MinorSeq )

-- LEFT OUTER JOIN \_TDAUMinorValue         AS K WITH(NOLOCK) ON ( H.CompanySeq = K.CompanySeq AND K.MajorSeq IN (2001,2004) AND H.MinorSeq = K.MinorSeq AND K.Serl IN (1001,2001) )

-- LEFT OUTER JOIN \_TDAUMinorValue         AS L WITH(NOLOCK) ON ( K.CompanySeq = L.CompanySeq AND L.MajorSeq IN (2002,2005) AND K.ValueSeq = L.MinorSeq AND L.Serl = 2001 )

LEFT OUTER JOIN \_TDAItemStock                AS ST WITH(NOLOCK) ON ( A.CompanySeq = ST.CompanySeq AND A.ItemSeq = ST.ItemSeq )

LEFT OUTER JOIN \_TDAItemPurchase        AS IP WITH(NOLOCK) ON ( A.CompanySeq = IP.CompanySeq AND A.ItemSeq = IP.ItemSeq )

LEFT OUTER JOIN \_TDASMinor AS SM WITH(NOLOCK) ON ( D.CompanySeq = SM.CompanySeq AND D.SMVatKind = SM.MinorSeq )

--LEFT OUTER JOIN (SELECT ItemSeq, UMajorItemClass, UMItemClass

-- FROM \_TDAItemClass

-- WHERE CompanySeq = @CompanySeq

-- AND UMajorItemClass \> 2000000

-- GROUP BY ItemSeq, UMajorItemClass, UMItemClass) AS O ON A.ItemSeq = O.ItemSeq

LEFT OUTER JOIN \_TDAItemClass AS O WITH(NOLOCK) ON ( A.CompanySeq = O.CompanySeq AND A.ItemSeq = O.ItemSeq AND O.UMajorItemClass \> 2000000 ) -- WBS에서 200만번대임

OUTER APPLY (SELECT MAX(IsUse) AS IsUse

FROM \_TPDQAItemQCKind AS X1 WITH(NOLOCK)

WHERE A.ItemSeq = X1.ItemSeq

AND A.CompanySeq = X1.CompanySeq ) AS P

LEFT OUTER JOIN \#IsQC AS P2 ON ( A.CompanySeq = P2.CompanySeq AND B.UMItemClass = P2.UMItemClass )

LEFT OUTER JOIN \_TDASMinor AS AM WITH (NOLOCK) ON AM.CompanySeq = A.CompanySeq -- 품목상태명

AND AM.MajorSeq = 2001

AND AM.MinorSeq = A.SMStatus

LEFT OUTER JOIN \_TCADictionary AS MD WITH(NOLOCK) ON MD.WordSeq = AM.WordSeq

AND MD.LanguageSeq = @LanguageSeq

LEFT OUTER JOIN \_TDASMinor AS AMIO WITH (NOLOCK) ON AMIO.CompanySeq = A.CompanySeq -- 내외자구분명

AND AMIO.MajorSeq = 8007

AND AMIO.MinorSeq = A.SMInOutKind

LEFT OUTER JOIN \_TCADictionary AS MDIO WITH(NOLOCK) ON MDIO.WordSeq = AMIO.WordSeq

AND MDIO.LanguageSeq = @LanguageSeq

LEFT OUTER JOIN \_TCADictionary AS SD WITH(NOLOCK) ON SD.WordSeq = SM.WordSeq --부가세구분

AND SD.LanguageSeq = @LanguageSeq

LEFT OUTER JOIN \_TDASMinor AS SC WITH (NOLOCK) ON SC.CompanySeq = A.CompanySeq -- 중요도

AND SC.MajorSeq = 2002

AND SC.MinorSeq = A.SMABC

LEFT OUTER JOIN \_TCADictionary AS CD WITH(NOLOCK) ON CD.WordSeq = sc.WordSeq

AND CD.LanguageSeq = @LanguageSeq

LEFT OUTER JOIN \_TDASMinor AS ES WITH (NOLOCK) ON ES.CompanySeq = E.CompanySeq -- 불출

AND ES.MajorSeq = 6005

AND ES.MinorSeq = E.SMOutKind

LEFT OUTER JOIN \_TCADictionary AS ED WITH(NOLOCK) ON ED.WordSeq = ES.WordSeq

AND ED.LanguageSeq = @LanguageSeq

LEFT OUTER JOIN (SELECT DISTINCT CompanySeq, ItemSeq --2022.06.10 우승현 추가

FROM \_TPUBASEBuyPriceItem ) AS BP ON BP.CompanySeq = A.CompanySeq

AND BP.ItemSeq = A.ItemSeq

WHERE (A.CompanySeq = @CompanySeq)

AND (@ItemSeq = 0 OR A.ItemSeq = @ItemSeq)

AND (@ItemName = '' OR A.ItemName like @ItemName + '%')

AND (@ItemNo = '' OR A.ItemNo like @ItemNo + '%')

AND (@Spec = '' OR A.Spec like @Spec + '%')

AND (@AssetSeq = 0 OR A.AssetSeq = @AssetSeq)

AND (@RegUserSeq = 0 OR A.RegUserSeq = @RegUserSeq)

AND (@SMStatus = 0 OR A.SMStatus = @SMStatus)

AND (@UMItemClass = 0 OR B.UMItemClass = @UMItemClass)

AND (@UMItemClassM = 0 OR H.ItemClassMSeq = @UMItemClassM)

AND (@UMItemClassL = 0 OR H.ItemClassLSeq = @UMItemClassL)

AND (@WorkingTag = '' OR (@WorkingTag = 'I' AND J.MinorValue = 0) OR (@WorkingTag = 'M' AND J.MinorValue = 1))

AND (@IsSet = '0' OR D.IsSet = @IsSet)

AND (@IsSTDItem = '0' OR A.IsSTDItem = @IsSTDItem )

AND (@RegDateFr = '' OR @RegDateFr \<= CONVERT(NCHAR(8), DATEADD(MI,@TimeDifference_Minute, DATEADD(HH, @TimeDifference_Hour, A.RegDateTime)), 112)) -- 2022.10.28 : 정민찬 법인별 시간차 적용

AND (@RegDateTo = '' OR @RegDateTo \>= CONVERT(NCHAR(8), DATEADD(MI,@TimeDifference_Minute, DATEADD(HH, @TimeDifference_Hour, A.RegDateTime)), 112)) -- 2022.10.28 : 정민찬 법인별 시간차 적용

AND (@UMEtcItemClassValue = 0 OR O.UMItemClass = @UMEtcItemClassValue)

AND (@DeptSeq = 0 OR A.DeptSeq = @DeptSeq)

AND (@EmpSeq = 0 OR A.EmpSeq = @EmpSeq)

--AND A.RegDateTime BETWEEN @RegDateFr AND @RegDateTo -- 2019.04.19 UPDATED BY 정영훈 :

ORDER BY ISNULL(A.ItemNo, ''), ISNULL(A.ItemName, ''), ISNULL(A.ItemSeq, 0)

 

 

-- 추가정보 (실제Data)

INSERT INTO \#Temp_ItemUserDefineData (TitleSerl , Title , MngValName , SMInputType, Seq ,

MngValText, CodeHelpSeq, CodeHelpParams, Mask , IsNON,

ItemSeq) -- 김희연 추가 2021.08.18

SELECT A.TitleSerl AS TitleSerl,

-- A.Title AS Title,

CASE WHEN ISNULL(A.WordSeq,0) = 0 THEN ISNULL(A.Title, '') ELSE ISNULL(Dic.Word,'') END AS Title,

'' AS MngValName,

A.SMInputType AS SMInputType,

ISNULL(B.MngValSeq,0) AS Seq,

B.MngValText AS MngValText,

A.CodeHelpConst AS CodeHelpSeq,

A.CodeHelpParams AS CodeHelpParams,

A.MaskAndCaption AS Mask,

A.IsEss AS IsNON,

C.ItemSeq AS ItemSeq

FROM \_TCOMUserDefine                                  AS A WITH(NOLOCK)

LEFT OUTER JOIN \_TCADictionary AS Dic WITH(NOLOCK) ON A.CompanySeq = @CompanySeq

AND Dic.LanguageSeq = @LanguageSeq

AND A.WordSeq = Dic.WordSeq

LEFT OUTER JOIN \_TDAItemUserDefine        AS B WITH(NOLOCK) ON ( B.CompanySeq = A.CompanySeq AND A.TitleSerl = B.MngSerl )

JOIN \#Temp_TDAItemInfo_New                        AS C ON ( B.ItemSeq = C.ItemSeq )

WHERE A.CompanySeq = @CompanySeq

AND A.TableName = '\_TDAItem'

AND ((@WorkingTag = 'I' AND A.DefineUnitSeq = 8010001) -- 제상품

OR (@WorkingTag = 'M' AND A.DefineUnitSeq = 8010002)) -- 자재

 

EXEC \_SWCOMGetCodeHelpDataName @CompanySeq, @LanguageSeq, '#Temp_ItemUserDefineData'

 

UPDATE \#Temp_ItemUserDefineData

SET MngValName = ( CASE WHEN SMInputType IN (1027003, 1027005) THEN ISNULL(ValueName,'') ELSE ISNULL(MngValText,'') END ) -- 코드헬프, 콤보

--===============================================================--

-- hsg 과세여부 추가

--===============================================================--

CREATE TABLE \#TMP_Item

(

ItemSeq INT,

IsTax NCHAR(1)

)

 

-- hsg_인축독성항목 - '과세여부' 체크 된건

INSERT INTO \#TMP_Item

SELECT DISTINCT

A.ItemSeq, ISNULL(C.ValueText,'0')

FROM \#Temp_ItemUserDefineData AS A

LEFT OUTER JOIN \_TDAItemUserDefine AS B WITH(NOLOCK) ON B.ItemSeq = A.ItemSeq

AND B.MngSerl = 1000066 -- hsg_인축독성항목

LEFT OUTER JOIN \_TDAUMinorValue AS C WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.MinorSeq = B.MngValSeq

AND C.Serl = 15000001 -- 과세여부

-- hsg_어독성항목 - '과세여부' 체크 된건

UPDATE \#TMP_Item

SET IsTax = D.ValueText

FROM \#TMP_Item AS A

LEFT OUTER JOIN \_TDAItemUserDefine AS B WITH(NOLOCK) ON B.ItemSeq = A.ItemSeq

AND B.MngSerl = 1000066 -- hsg_인축독성항목

LEFT OUTER JOIN \_TDAItemUserDefine AS C WITH(NOLOCK) ON C.ItemSeq = A.ItemSeq

AND C.MngSerl = 1000067 -- hsg_어독성항목

LEFT OUTER JOIN \_TDAUMinorValue AS D WITH(NOLOCK) ON D.CompanySeq = C.CompanySeq

AND D.MinorSeq = C.MngValSeq

AND D.Serl = 15000001 -- 과세여부

LEFT OUTER JOIN \_TDAUMinorValue AS E WITH(NOLOCK) ON E.CompanySeq = C.CompanySeq

AND E.MinorSeq = C.MngValSeq

AND E.Serl = 15000002 -- 인축독성항목

WHERE (ISNULL(B.MngValSeq, 0) = ISNULL(E.ValueSeq, 0) AND ISNULL(D.ValueText, '') = '1')

AND A.IsTax \<\> '1'

 

INSERT INTO \#Temp_ItemUserDefineData

(TitleSerl,        Title , MngValName,        SMInputType, Seq, MngValText,        CodeHelpSeq, CodeHelpParams, Mask, IsNON, ItemSeq)

SELECT 1000067.1, '과세여부', A.IsTax , 1027006 , 0, 0, 0, '', '', 0, A.ItemSeq

FROM \#TMP_Item AS A

--===============================================================--

-- hsg 과세여부 종료

--===============================================================--

 

 

> CREATE TABLE \#RESULT_UserDefine (ItemSeq INT)
>
> INSERT INTO \#RESULT_UserDefine (ItemSeq )
>
> SELECT DISTINCT A.ItemSeq

         FROM \#Temp_TDAItemInfo_New AS A

> LEFT OUTER JOIN \#Temp_ItemUserDefineData AS B ON A.ItemSeq = B.ItemSeq
>
> WHERE (@TitleSerl = 0 OR B.TitleSerl = @TitleSerl)
>
> AND (@InPutType IN (0,1027001) AND (@AddText1 = '' OR B.MngValName LIKE @AddText1 + N'%'))
>
>          OR (@InPutType IN (0,1027002) AND (@AddText2 = '' OR B.MngValName LIKE @AddText2 + N'%'))
>
> OR (@InPutType IN (0,1027003) AND (@AddCd3 = 0 OR B.Seq = @AddCd3))
>
>          OR (@InPutType IN (0,1027004) AND (@AddText4 = '' OR B.MngValName LIKE @AddText4 + N'%'))
>
>          OR (@InPutType IN (0,1027005) AND (@AddCd5 = 0 OR B.Seq = @AddCd5))
>
>          OR (@InPutType IN (0,1027006) AND (@AddText6 = '' OR B.MngValName LIKE @AddText6 + N'%'))
>
>          OR (@InPutType IN (0,1027007) AND (@AddText7 = '' OR B.MngValText LIKE @AddText7 + N'%'))
>
>  
>
> SELECT IDENTITY(INT, 0, 1) AS RowIDX,
>
> A.\*
>
> INTO \#RESULT_TDAItemInfo
>
> FROM \#Temp_TDAItemInfo_New AS A
>
> JOIN \#RESULT_UserDefine AS B ON A.ItemSeq = B.ItemSeq
>
> ORDER BY A.IDX_NO

 

 

-- 최종 데이터 조회

> SELECT \* FROM \#Temp_ItemUserDefine ORDER BY ColIDX

 

SELECT \* FROM \#RESULT_TDAItemInfo ORDER BY RowIDX

 

SELECT A.RowIDX AS RowIDX,

C.ColIDX AS ColIDX,

B.MngValName AS AddInfoName

FROM \#RESULT_TDAItemInfo                 AS A -- 고정Data Table

JOIN \#Temp_ItemUserDefineData                AS B ON ( A.ItemSeq = B.ItemSeq ) -- 가변Data Table

JOIN \#Temp_ItemUserDefine                        AS C ON ( B.TitleSerl\*10 = C.TitleSeq ) -- Title Table

WHERE C.InputType \<\> 'ItemClass'

 

UNION

 

SELECT A.RowIDX AS RowIDX, -- 2010.08.03 정동혁 : 품목분류도 다이나믹부에 조회되도록 수정.

C.ColIDX AS ColIDX,

D.MinorName AS AddInfoName

FROM \#RESULT_TDAItemInfo AS A

JOIN \_TDAItemClass AS B WITH(NOLOCK) ON ( A.ItemSeq = B.ItemSeq AND B.CompanySeq = @CompanySeq )

JOIN \#Temp_ItemUserDefine AS C ON ( B.UMajorItemClass = C.TitleSeq )

JOIN \_TDAUMinor AS D WITH(NOLOCK) ON ( B.UMItemClass = D.MinorSeq AND D.CompanySeq = @CompanySeq )

WHERE C.InputType = 'ItemClass'

> ORDER BY 1,2

 

> --SELECT DATEDIFF( ms, @CLOCK, GETDATE() ), 'END' -- 6063

 

 

RETURN