---
title: 기본 문법
date: 2022-04-05
tags: [erp, 개발, SQL]
---

# 기본 문법

- 행 전체 삭제

TRUNCATE TABLE table_name;

A 컬럼이 구분 (8019001) 일 때 \~~

아니면 \~~

CASE WHEN A.SMQuoteKind = '8019001'

THEN ISNULL(B.ItemName , '')

ELSE ISNULL(A.ItemName , '')

END,

CASE WHEN A.SMQuoteKind = '8019001'

THEN ISNULL(B.ItemNo , '')

ELSE ISNULL(A.ItemNo , '')

END,

CASE WHEN A.SMQuoteKind = '8019001'

THEN ISNULL(B.Spec , '')

ELSE ISNULL(A.Spec , '')

END,

- 테이블 컬럼 추가

> ALTER TABLE gcos_TPUQuoteReqResult ADD ItemSerl INT NULL
>
>  

- 테이블 속성 변경

> ALTER TABLE jis_TSLOrderDVPlan ALTER COLUMN OutTime NVARCHAR(30) NULL
>
>  
>
>  
>
>  

 

- 테이블 컬럼명 변경

> EXEC SP_RENAME '테이블명.기존컬럼명', '바꿀컬럼명', 'COLUMN'
>
>  
>
> sp_rename 'mcnv_TACAsstKOR.KORYear','MCNKYear'
>
>  
>
>  

| 테이블명칼럼 변경 | ALTER TABLE 테이블명 ALTER COLUMN 컬럼명 데이터형식 NULL허용 |
|----|----|

 

| 칼럼 삭제  | ALTER TABLE 테이블명 DROP COLUMN 컬럼명 |
|------------|-----------------------------------------|

 

| 테이블의 필드 순서 변경 | ALTER TABLE 테이블명 CHANGE COLUMN 변경전 필드명 변경후 필드명 varchar(255) NULL AFTER 기준 필드명;  |
|----|----|

 

- 테이블 행 삭제

> BEGIN TRAN
>
> DELETE FROM mcnv_TACAsstText
>
> WHERE AsstSeq=7262
>
>  
>
> SELECT \* FROM mcnv_TACAsstText ORDER BY ASSTSEQ DESC
>
>  
>
> ROLLBACK TRAN
>
>