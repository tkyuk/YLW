---
title: 코드도움 분기 처리 \_/
date: 2025-12-17
tags: [erp, 개발, SQL]
---

# 코드도움 분기 처리 \_/

-- Param1 : ItemSeq

-- Param2 : WHSeq

-- Param3 : InOutSeq\_/InOutType 자기 자신 제외

-- Param4 : IsMinusCheck 1인 경우 출고된 건 안 나오게 설정

 

 

if txtItemName2.TextCd == 0 or txtItemName2.TextCd == '' or txtItemName2.TextCd == '0' or txtItemName2.TextCd == nil then

> SS3.IsCodeHelpCancelEvent = true

end

 

if SS3.ActiveColumnName == 'SerialNo' then

> SS3.ColumnCodeHelpParams = txtItemNo2.TextCd..'\|\|'..fltInOutSeq2.Value..'\_/'..fltInOutType2.Value..'\|1'

end

 

SP : svs_SWCACodeHelpSerialNo

 

SELECT @InOutSeq = ISNULL(LTRIM(RTRIM((SELECT Value FROM \_FCOMStrSplitOne(@Param3,'\_/') WHERE idx = 1))), N''),

@InOutType = ISNULL(LTRIM(RTRIM((SELECT Value FROM \_FCOMStrSplitOne(@Param3,'\_/') WHERE idx = 2))), N'')

 

<span class="mark">숫자</span>

SELECT @InOutSeq = TRY_CAST(ISNULL(LTRIM(RTRIM((SELECT Value FROM \_FCOMStrSplitOne(@Param3,'\_/') WHERE idx = 1))), 0) AS INT),

@InOutType = TRY_CAST(ISNULL(LTRIM(RTRIM((SELECT Value FROM \_FCOMStrSplitOne(@Param3,'\_/') WHERE idx = 2))), 0) AS INT)