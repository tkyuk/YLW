---
title: 최근단가 가져오기 (ex. 구매품의)
date: 2023-08-22
tags: [erp, 개발, SQL]
---

# 최근단가 가져오기 (ex. 구매품의)

-- 두 번째 내용이 표준 \_SPUGetDelvPrice SP 참고하여 작성

-- (임시테이블 INPUT 에도 동일하게 OUTPUT 컬럼 찍어줘야함

 

DROP PROC dat_SPUBaseGetQutoPrice

GO

CREATE PROC dat_SPUBaseGetQutoPrice

@ServiceSeq INT = 0 ,

@WorkingTag NVARCHAR(10) = '',

@CompanySeq INT = 1 ,

@LanguageSeq INT = 1 ,

@UserSeq INT = 0 ,

@PgmSeq INT = 0 ,

@IsTransaction BIT = 0

 

AS

 

DECLARE @Count INT,

@Seq INT,

@MessageType INT,

@Status INT,

@GetPriceType INT,

@MaxRow                 INT,

@Results NVARCHAR(250)

 

--CREATE TABLE \#TMP_MAXPrice(

-- CompanySeq INT ,

-- ItemSeq INT ,

-- CurrSeq INT ,

-- Price DECIMAL(19,5)

--)

--INSERT INTO \#TMP_MAXPrice

-- SELECT DISTINCT A.CompanySeq, A.ItemSeq, A.CurrSeq, A.Price

-- FROM \_TPUORDApprovalReqItem AS A WITH(NOLOCK)

-- INNER JOIN (

-- SELECT A.CompanySeq, A.ItemSeq, MAX(A.ApproReqSeq) AS ApproReqSeq,MAX(A.ApproReqSerl) AS ApproReqSerl

-- FROM \_TPUORDApprovalReqItem AS A WITH(NOLOCK)

-- WHERE A.ItemSeq IN (SELECT DISTINCT ItemSeq FROM \#BIZ_IN_DataBlock2)

-- GROUP BY A.CompanySeq, A.ItemSeq

-- )

-- AS C ON A.CompanySeq = C.CompanySeq

-- AND A.ItemSeq = C.ItemSeq

-- AND A.ApproReqSeq = C.ApproReqSeq

-- AND A.ApproReqSerl = C.ApproReqSerl

 

 

 

-- SELECT A1.CurrSeq AS CurrSeq,

-- B.CurrName AS CurrName ,

-- A1.Price AS QutoPrice,

-- A1.ItemSeq AS ItemSeq

-- FROM \#BIZ_IN_DataBlock2 AS A

-- LEFT OUTER JOIN \#TMP_MAXPrice AS A1 ON A1.ItemSeq = A.ItemSeq                                -- 17.09.05 hjlim 수정 (JOIN -\> LEFT OUTER JOIN)

> --                                                                 AND A1.CompanySeq = @CompanySeq

-- LEFT OUTER JOIN \_TDACurr AS B ON B.CompanySeq = A1.CompanySeq

-- AND B.CurrSeq = A1.CurrSeq

 

> BEGIN
>
> SELECT @MaxRow = MAX(IDX_NO) FROM \#BIZ_IN_DataBlock2
>
> SELECT @Count = 1
>
> WHILE ( 1 = 1)
>
> BEGIN
>
> IF @Count \> @MaxRow BREAK
>
> UPDATE \#BIZ_IN_DataBlock2
>
> SET QutoPrice = B.Price ,

CurrSeq = B.CurrSeq,

CurrName = D.CurrName

> FROM \#BIZ_IN_DataBlock2         AS A
>
> JOIN \_TPUORDApprovalReqItem AS B WITH(NOLOCK) ON A.ItemSeq = B.ItemSeq
>
> JOIN \_TPUORDApprovalReq         AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq
>
>          AND B.ApproReqSeq = C.ApproReqSeq
>
> JOIN \_TDACurr          AS D WITH(NOLOCK) ON D.CompanySeq = B.CompanySeq
>
> AND D.CurrSeq = B.CurrSeq
>
> WHERE B.CompanySeq = @CompanySeq
>
> AND A.IDX_NO = @Count
>
> AND C.ApproReqNo IN (SELECT TOP 1 ISNULL(MAX(B.ApproReqNo), 0)

FROM \_TPUORDApprovalReqItem AS A

>                                          JOIN \_TPUORDApprovalReq         AS B ON A.CompanySeq = B.CompanySeq
>
> AND A.ApproReqSeq = B.ApproReqSeq
>
> JOIN \#BIZ_IN_DataBlock2 AS C ON A.ItemSeq        = C.ItemSeq
>
> WHERE A.CompanySeq = @CompanySeq
>
> AND C.IDX_NO = @Count
>
> AND A.Price \<\> 0)
>
> SELECT @Count = @Count + 1
>
> END
>
> END

SELECT \* FROM \#BIZ_IN_DataBlock2

 

 

RETURN

\_SPUGetDelvPrice