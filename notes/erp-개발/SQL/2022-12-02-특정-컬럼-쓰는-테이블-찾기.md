---
title: 특정 컬럼 쓰는 테이블 찾기
date: 2022-12-02
tags: [erp, 개발, SQL]
---

# 특정 컬럼 쓰는 테이블 찾기

--특정 컬럼 쓰는 테이블 찾기

SELECT

T.name AS table_name, C.name AS column_name

FROM sys.tables AS T

INNER JOIN sys.columns AS C ON T.object_id = C.object_id

WHERE C.name = 'SqlScriptSeq'

ORDER BY table_name