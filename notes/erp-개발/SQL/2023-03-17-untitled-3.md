---
title: 
date: 2023-03-17
tags: [erp, 개발, SQL]
---

# 

- 하위 BOM 계산

- 현재고 구하기 (특정창고)

- 구매요청, 구매품의, 구매발주 중단, 진행 안된 수량 구하기

- 미검사 수량 가져오기

- 구매단위 가져오기 및 수량 환산

>  

DROP PROC djb_SWLGMatShortList

GO

CREATE PROC djb_SWLGMatShortList

@ServiceSeq INT = 0,

@WorkingTag NVARCHAR(10)= '',

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 0,

@PgmSeq INT = 0,

@IsTransaction BIT = 0

AS

DECLARE @BizUnit INT , -- 사업단위

@DVDateFr NCHAR(8) , -- 납기예정일Fr

@DVDateTo NCHAR(8) , -- 납기예정일To

@eessSeq INT , -- 납기예정기준

@IsStkQty NCHAR(1) , -- 과부족건만 조회

@UmUse INT , -- 사용처

@ItemName NVARCHAR(100), -- 품명

@ItemNo NVARCHAR(100), -- 품번

@AssetSeq INT -- 자산분류

 

SELECT @BizUnit = ISNULL(BizUnit , 0),

@DVDateFr = ISNULL(DVDateFr ,''),

@DVDateTo = ISNULL(DVDateTo ,''),

@ProgressSeq = ISNULL(ProgressSeq , 0),

@IsStkQty = ISNULL(IsStkQty ,''),

@UmUse = ISNULL(UmUse , 0),

@ItemName = ISNULL(ItemName ,''),

@ItemNo = ISNULL(ItemNo ,''),

@AssetSeq = ISNULL(AssetSeq , 0)

FROM \#BIZ_IN_DataBlock1 AS A

 

IF @DVDateTo = '' SELECT @DVDateTo = '99991231'

 

-- 1. 품목 구하기 (출고가능확인요청 / 수주)

CREATE TABLE \#TmpItem

(

ItemSeq INT,

Qty DECIMAL(19,5),

AssetSeq INT

)

 

IF @ProgressSeq = '1' -- 출고가능확인요청 건 (확인된 건 / 중단건 제외 / 납기예정기준에 해당하는 건)

BEGIN

INSERT INTO \#TmpItem

SELECT A.ItemSeq, A.Qty, B.AssetSeq

FROM djb_TSLOrderCfmReq AS A

JOIN \_TDAItem AS B ON B.CompanySeq = A.CompanySeq

AND B.ItemSeq = A.ItemSeq

WHERE A.IsStop \<\> '1'

AND A.IsCfm = '1'

AND A.DVDate BETWEEN @DVDateFr AND @DVDateTo

AND A.BizUnit = @BizUnit

--AND (@ItemName = '' OR B.ItemName LIKE @ItemName + '%')

--AND (@ItemNo = '' OR B.ItemNo LIKE @ItemNo + '%')

--AND (@AssetSeq = 0 OR @AssetSeq = B.AssetSeq)

END

 

 

ELSE -- 수주 건 (중단건 제외 / 납기예정기준에 해당하는 건)

BEGIN

INSERT INTO \#TmpItem

SELECT A.ItemSeq, A.Qty, B.AssetSeq

FROM \_TSLOrderItem AS A

JOIN \_TDAItem AS B ON B.CompanySeq = A.CompanySeq

AND B.ItemSeq = A.ItemSeq

LEFT OUTER JOIN \_TCOMStop AS C ON C.TableSeq = 19

AND C.StopSeq = A.OrderSeq

AND C.StopSerl = A.OrderSerl

WHERE ISNULL(C.IsStop,0) = '0'

AND A.DVDate BETWEEN @DVDateFr AND @DVDateTo

--AND (@ItemName = '' OR B.ItemName LIKE @ItemName + '%')

--AND (@ItemNo = '' OR B.ItemNo LIKE @ItemNo + '%')

--AND (@AssetSeq = 0 OR @AssetSeq = B.AssetSeq)

END

 

--=================================================================

-- 품목 구하기 종료

--=================================================================

-- 2. 자산분류에 따라 임시테이블 2개 생성

-- 제품/반제품 : 해당 품목의 최종차수 BOM을 풀어서, 하위 모든 레벨의 소요 자재(원자재/부자재/소모자재등… )를 계산하여 가져옴. (출고가능확인요청의 수량으로 소요량 계산)

-- 상품, 원자재, 부자재 : 그대로 가져옴

 

-- 상품, 원자재, 부자재 임시테이블 생성

SELECT A.ItemSeq, SUM(A.Qty) AS Qty

INTO \#TDAItem1

FROM \#TmpItem AS A

WHERE A.AssetSeq NOT IN (SELECT A.AssetSeq

FROM \_TDAItemAsset AS A WITH(NOLOCK)

WHERE A.SMAssetGrp IN (6008002,6008004))

GROUP BY A.ItemSeq

 

-- 제품/반제품 임시테이블 생성

SELECT A.ItemSeq, SUM(A.Qty) AS Qty

INTO \#TMP_TDAItem2

FROM \#TmpItem AS A

WHERE A.AssetSeq IN (SELECT A.AssetSeq

FROM \_TDAItemAsset AS A WITH(NOLOCK)

WHERE A.SMAssetGrp IN (6008002,6008004))

GROUP BY A.ItemSeq

 

CREATE TABLE \#TDAGood

(

GoodItemSeq INT,

ProcRev NVARCHAR(2),

Qty DECIMAL(19,5)

)

 

INSERT INTO \#TDAGood

SELECT ItemSeq AS ItemSeq,

'00' ProcRev,

Qty

FROM \#TMP_TDAItem2

 

UPDATE A

SET ProcRev = B.ProcRev

FROM \#TDAGood AS A JOIN (SELECT A.GoodItemSeq, MAX(B.ProcRev) AS ProcRev

FROM \#TDAGood AS A JOIN \_TPDROUItemProcMat AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND A.GoodItemSeq = B.ItemSeq

AND B.BizUnit = @BizUnit

GROUP BY A.GoodItemSeq) AS B ON A.GoodItemSeq = B.GoodItemSeq

;WITH CTS_TPDBOM(GoodItemSeq, GoodVer, ItemSeq, ItemVer, SubItemSeq, SubItemVer, NeedQty, Lvl) AS -- 재귀함수....

(

SELECT A.GoodItemSeq, A.ProcRev, B.ItemSeq, CONVERT(NVARCHAR(2), B.ItemBomRev), B.SubItemSeq, CONVERT(NVARCHAR(2), B.SubItemBomRev),

CONVERT(DECIMAL(19, 5), CASE WHEN NeedQtyDenominator = 0 THEN 0 ELSE NeedQtyNumerator / NeedQtyDenominator END \* A.Qty),

CONVERT(INT, 1)

FROM \#TDAGood A

JOIN \_TPDBOM AS B ON B.ItemSeq = A.GoodItemSeq AND B.ItemBomRev = A.ProcRev

UNION ALL

SELECT A.GoodItemSeq, GoodVer, B.ItemSeq, CONVERT(NVARCHAR(2), B.ItemBomRev), B.SubItemSeq, CONVERT(NVARCHAR(2), B.SubItemBomRev),

CONVERT(DECIMAL(19, 5), CASE WHEN NeedQtyDenominator = 0 THEN 0 ELSE NeedQtyNumerator / NeedQtyDenominator END \* A.NeedQty),

CONVERT(INT, Lvl + 1)

FROM CTS_TPDBOM AS A

JOIN \_TPDBOM AS B ON B.ItemSeq = A.SubItemSeq AND B.ItemBomRev = A.SubItemVer

)

SELECT A.SubItemSeq, A.NeedQty

--B.ItemName AS GoodName, B.AssetSeq AS GoodAsset, C.ItemName AS ItemName, C.AssetSeq AS ItemAsset, D.ItemName AS SubItemName, D.AssetSeq AS SubItemAsset

INTO \#TDAItem2

FROM CTS_TPDBOM AS A

JOIN \_TDAItem AS B ON B.ItemSeq = A.GoodItemSeq

JOIN \_TDAItem AS C ON C.ItemSeq = A.ItemSeq

JOIN \_TDAItem AS D ON D.ItemSeq = A.SubItemSeq

WHERE D.AssetSeq NOT IN (SELECT A.AssetSeq

FROM \_TDAItemAsset AS A WITH(NOLOCK)

WHERE A.SMAssetGrp IN (6008002,6008004))

ORDER BY Lvl

 

 

 

CREATE TABLE \#TMP_ItemSeq

(

ItemSeq INT

)

CREATE TABLE \#TMP_ResultItem

(

ItemSeq INT ,

Qty DECIMAL(19,5)

)

CREATE TABLE \#ResultItem

(

ItemSeq INT ,

StockQty DECIMAL(19,5), -- 현재고

SafeQty DECIMAL(19,5), -- 안전재고

NeedQty DECIMAL(19,5), -- 필요량

POReqQty DECIMAL(19,5), -- 구매요청

AppReqQty DECIMAL(19,5), -- 구매품의

POQty DECIMAL(19,5), -- 구매발주

QCWaitQty DECIMAL(19,5), -- 검사대기

ExpectQty DECIMAL(19,5), -- 예상재고

StkQty DECIMAL(19,5), -- 과부족

POUnitSeq INT , -- 구매단위코드

PoBaseQty DECIMAL(19,5) -- 발주산정량

)

 

 

INSERT INTO \#TMP_ResultItem

SELECT \* FROM \#TDAItem1

UNION

SELECT \* FROM \#TDAItem2

 

 

-- 각 수량에 대해 Insert 시 사용할 품목테이블 생성

INSERT INTO \#TMP_ItemSeq

SELECT A.ItemSeq

FROM \#TMP_ResultItem AS A WITH(NOLOCK)

LEFT OUTER JOIN \_TDAItemClass AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

AND B.UMajorItemClass = 2140257

LEFT OUTER JOIN \_TDAItem AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.ItemSeq = A.ItemSeq

WHERE (@UmUse = 0 OR @UmUse = B.UMItemClass) -- 사용처 조회조건 추가 (자재기타분류의 djb_사용처분류 값)

AND (@ItemName = '' OR C.ItemName LIKE @ItemName + '%')

AND (@ItemNo = '' OR C.ItemNo LIKE @ItemNo + '%')

AND (@AssetSeq = 0 OR @AssetSeq = C.AssetSeq)

GROUP BY A.ItemSeq

 

 

-- 최종 테이블

INSERT INTO \#ResultItem(ItemSeq, NeedQty)

SELECT A.ItemSeq, SUM(A.Qty) AS Qty

FROM \#TMP_ResultItem AS A

LEFT OUTER JOIN \_TDAItemClass AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

AND B.UMajorItemClass = 2140257

LEFT OUTER JOIN \_TDAItem AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.ItemSeq = A.ItemSeq

WHERE (@UmUse = 0 OR @UmUse = B.UMItemClass) -- 사용처 조회조건 추가 (자재기타분류의 djb_사용처분류 값)

AND (@ItemName = '' OR C.ItemName LIKE @ItemName + '%')

AND (@ItemNo = '' OR C.ItemNo LIKE @ItemNo + '%')

AND (@AssetSeq = 0 OR @AssetSeq = C.AssetSeq)

GROUP BY A.ItemSeq

--=================================================================

-- 필요량 구하기 종료

--=================================================================

 

 

--=================================================================

-- 현재고(StockQty) 구하기 시작 (특정창고들의 현재고를 합산 : 사용자정의코드)

--=================================================================

-- 임시 테이블 생성

CREATE TABLE \#GetInOutItem (ItemSeq INT)

CREATE TABLE \#GetInOutStock

(

BizUnit INT ,

WHSeq INT ,

MainWHSeq INT ,

ItemSeq INT ,

UnitSeq INT ,

PrevQty DECIMAL(19, 5),

InQty DECIMAL(19, 5),

OutQty DECIMAL(19, 5),

StockQty DECIMAL(19, 5),

STDPrevQty DECIMAL(19, 5),

STDInQty DECIMAL(19, 5),

STDOutQty DECIMAL(19, 5),

STDStockQty DECIMAL(19, 5),

StockAmt DECIMAL(19, 5)

)

 

-- 대상 품목 수집

INSERT INTO \#GetInOutItem (ItemSeq)

SELECT A.ItemSeq

FROM \#TMP_ItemSeq AS A

JOIN \_TDAItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

JOIN \_TDAItemAsset AS C WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.AssetSeq = B.AssetSeq

WHERE C.IsQty \<\> '1'

-- 재고조회

EXEC \_SWLGGetInOutStock @CompanySeq = @CompanySeq,

@BizUnit = @BizUnit ,

@DateFr = @DVDateFr,

@DateTo = @DVDateTo,

@WHSeq = 0 ,

@SMWHKind = 0 ,

@IsSubDisplay = '1' ,

@IsOnlyStock = '1' ,

@IsStockAmt = '0' ,

@StockAmtType = 0 ,

@UnitType = 1 ,

@ConvUnitSeq = 0 ,

@QryType = 'S' ,

@IsTrustCust = ''

 

-- 사용자정의코드(djb_현재고산출창고)에 등록된 창고만 품목별로 집계

SELECT ItemSeq, SUM(StockQty) AS StockQty

INTO \#StockQty

FROM \#GetInOutStock AS A

JOIN \_TDAUMinorValue AS B ON B.CompanySeq = @CompanySeq

AND B.ValueSeq = A.WHSeq

AND B.MajorSeq = 1000600

GROUP BY ItemSeq

 

 

-- 현재고

INSERT INTO \#ResultItem (ItemSeq, StockQty)

SELECT A.ItemSeq, ISNULL(B.StockQty, 0)

FROM \#TMP_ItemSeq AS A

LEFT OUTER JOIN \#StockQty AS B ON A.ItemSeq = B.ItemSeq

--=================================================================

-- 현재고 구하기 종료

--=================================================================

 

 

--=================================================================

-- 안전재고 구하기 시작 (자재추가정보정의-안전재고_djb에서 가져오기)

--=================================================================

INSERT INTO \#ResultItem (ItemSeq, SafeQty)

SELECT A.ItemSeq, ISNULL(B.MngValText, 0)

FROM \#TMP_ItemSeq AS A

LEFT OUTER JOIN \_TDAItemUserDefine AS B ON A.ItemSeq = B.ItemSeq

AND B.MngSerl = 1000147

--=================================================================

-- 안전재고 구하기 종료

--=================================================================

 

--=================================================================

-- 구매요청수량 구하기 (중단건 제외 / 진행 안된 건 조회)

--=================================================================

INSERT INTO \#ResultItem (ItemSeq,POReqQty)

SELECT A.ItemSeq,SUM(ISNULL(B.Qty, 0))

FROM \#TMP_ItemSeq AS A

LEFT OUTER JOIN \_TPUORDPOReqItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

LEFT OUTER JOIN sys_TPUORDPOReq AS E WITH(NOLOCK) ON E.CompanySeq = B.CompanySeq

AND E.POReqSeq = B.POReqSeq

LEFT OUTER JOIN \_TCOMStop AS C WITH(NOLOCK) ON C.TableSeq = 12

AND C.StopSeq = B.POReqSeq

AND C.StopSerl = B.POReqSerl

LEFT OUTER JOIN \_TCOMSourceDaily AS D WITH(NOLOCK) ON D.FromTableSeq = 12

AND D.FromSeq = B.POReqSeq

AND D.FromSerl = B.POReqSerl

 

WHERE ISNULL(C.IsStop,0) = '0'

AND D.CompanySeq IS NULL

AND E.BizUnit = @BizUnit

AND B.DelvDate BETWEEN @DVDateFr AND @DVDateTo

GROUP BY A.ItemSeq

HAVING SUM(ISNULL(B.Qty, 0)) \<\> 0

--=================================================================

-- 구매요청수량 구하기 종료

--=================================================================

 

--=================================================================

-- 구매품의수량 구하기 (중단건 제외 / 진행 안된 건 조회)

--=================================================================

INSERT INTO \#ResultItem (ItemSeq,AppReqQty)

SELECT A.ItemSeq,SUM(ISNULL(B.Qty, 0))

FROM \#TMP_ItemSeq AS A

LEFT OUTER JOIN \_TPUORDApprovalReqItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

LEFT OUTER JOIN sys_TPUORDApprovalReq AS E WITH(NOLOCK) ON E.CompanySeq = B.CompanySeq

AND E.ApproReqSeq = B.ApproReqSeq

LEFT OUTER JOIN \_TCOMStop AS C WITH(NOLOCK) ON C.TableSeq = 12

AND C.StopSeq = B.ApproReqSeq

AND C.StopSerl = B.ApproReqSerl

LEFT OUTER JOIN \_TCOMSourceDaily AS D WITH(NOLOCK) ON D.FromTableSeq = 12

AND D.FromSeq = B.ApproReqSeq

AND D.FromSerl = B.ApproReqSerl

 

WHERE ISNULL(C.IsStop,0) = '0'

AND D.CompanySeq IS NULL

AND E.BizUnit = @BizUnit

AND B.DelvDate BETWEEN @DVDateFr AND @DVDateTo

GROUP BY A.ItemSeq

HAVING SUM(ISNULL(B.Qty, 0)) \<\> 0

--=================================================================

-- 구매품의수량 구하기 종료

--=================================================================

 

--=================================================================

-- 구매발주수량 구하기 (중단건 제외 / 진행 안된 건 조회)

--=================================================================

INSERT INTO \#ResultItem (ItemSeq,POQty)

SELECT A.ItemSeq,SUM(ISNULL(B.Qty, 0))

FROM \#TMP_ItemSeq AS A

LEFT OUTER JOIN \_TPUORDPOItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

LEFT OUTER JOIN sys_TPUORDPO AS E WITH(NOLOCK) ON E.CompanySeq = B.CompanySeq

AND E.POSeq = B.POSeq

LEFT OUTER JOIN \_TCOMStop AS C WITH(NOLOCK) ON C.TableSeq = 13

AND C.StopSeq = B.POSeq

AND C.StopSerl = B.POSerl

LEFT OUTER JOIN \_TCOMSourceDaily AS D WITH(NOLOCK) ON D.FromTableSeq = 13

AND D.FromSeq = B.POSeq

AND D.FromSerl = B.POSerl

 

WHERE ISNULL(C.IsStop,0) = '0'

AND D.CompanySeq IS NULL

AND E.BizUnit = @BizUnit

AND B.DelvDate BETWEEN @DVDateFr AND @DVDateTo

GROUP BY A.ItemSeq

HAVING SUM(ISNULL(B.Qty, 0)) \<\> 0

--=================================================================

-- 구매발주수량 구하기 종료

--=================================================================

 

 

--=================================================================

-- 검사대기 수량 구하기 (미검사 수량)

--=================================================================

 

-- 구매납품

INSERT INTO \#ResultItem (ItemSeq,QCWaitQty)

SELECT B.ItemSeq, SUM(B.Qty) AS Qty

FROM \_TPUDelv AS A WITH(NOLOCK)

JOIN \_TPUDelvItem AS B WITH(NOLOCK) ON B.CompanySeq = A.CompanySeq

AND B.DelvSeq = A.DelvSeq

JOIN \#TMP_ItemSeq AS C ON C.ItemSeq = B.ItemSeq

LEFT OUTER JOIN \_TPDQCTestReport AS D WITH(NOLOCK) ON D.CompanySeq = B.CompanySeq

AND D.SourceSeq = B.DelvSeq

AND D.SourceSerl = B.DelvSerl

AND D.SourceType = 6097001 -- 구매검사

WHERE A.CompanySeq = @CompanySeq

AND A.BizUnit = @BizUnit

AND A.DelvDate BETWEEN @DVDateFr AND @DVDateTo

AND A.SMImpType IN (8008001, 8008002, 8008003)

AND ISNULL(D.SMTestResult, B.SMQcType) = 6035002 -- 미검사

GROUP BY B.ItemSeq

 

 

--=================================================================

-- 검사대기 수량 구하기 종료

--=================================================================

-- 예상재고, 과부족 수량 구하기 전에 ItemSeq 집계

SELECT A.ItemSeq ,

SUM(ISNULL(A.StockQty ,0)) AS StockQty , -- 현재고

SUM(ISNULL(A.SafeQty ,0)) AS SafeQty , -- 안전재고

SUM(ISNULL(A.NeedQty ,0)) AS NeedQty , -- 필요량

SUM(ISNULL(A.POReqQty ,0)) AS POReqQty , -- 구매요청

SUM(ISNULL(A.AppReqQty,0)) AS AppReqQty, -- 구매품의

SUM(ISNULL(A.POQty ,0)) AS POQty , -- 구매발주

SUM(ISNULL(A.QCWaitQty,0)) AS QCWaitQty, -- 검사대기

0 AS ExpectQty, -- 예상재고

0 AS StkQty , -- 과부족

0 AS POUnitSeq, -- 구매단위코드

0 AS PoBaseQty -- 발주산정량

INTO \#Result

FROM \#ResultItem AS A

GROUP BY A.ItemSeq

 

--=================================================================

-- 예상재고 구하기 (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)

-- 과부족 구하기

/\*

IF (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty) \< 0                                                                                                                                                        

> --\> SafeQty + \[(StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)\*-1\]                                                                                                                                                

ELSE                                                                                                                                                        

> IF SafeQty \<= (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)                                                                                                                                                
>
>         --\> 0                                                                                                                                        
>
> IF SafeQty \> (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)                                                                                                                                                
>
>         --\> SafeQty - (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)                                                                                                                                        

\*/

--=================================================================

UPDATE \#Result

SET ExpectQty = StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty,

StkQty = CASE WHEN (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty) \< 0

THEN SafeQty + ((StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)\*-1)

ELSE CASE WHEN SafeQty \<= (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty)

THEN 0

ELSE SafeQty - (StockQty - NeedQty + POReqQty + AppReqQty + POQty + QCWaitQty) END END

FROM \#Result

 

-- 발주산정량 구하기 : 과부족 수량을 구매단위로 환산한 수량

DECLARE @GoodQtyDecLength INT

EXEC @GoodQtyDecLength = dbo.\_SWCOMEnvR @CompanySeq, 5, @UserSeq, @@PROCID

 

UPDATE \#Result

SET POUnitSeq = ISNULL(B.STDUnitSeq, 0),

PoBaseQty = ROUND((CASE WHEN ISNULL(C.ConvDen,0) = 0 THEN 0 ELSE ISNULL(Z.StkQty,0) \* ISNULL(C.ConvNum,0) / ISNULL(C.ConvDen,0) END), @GoodQtyDecLength)

FROM \#Result AS Z

JOIN \_TDAItem AS A WITH(NOLOCK) ON A.CompanySeq = @CompanySeq

AND A.ItemSeq = Z.ItemSeq

JOIN \_TDAItemDefUnit AS B WITH(NOLOCK) ON B.CompanySeq = A.CompanySeq

AND B.ItemSeq = A.ItemSeq

JOIN \_TDAItemUnit AS C WITH(NOLOCK) ON C.CompanySeq = A.CompanySeq

AND C.ItemSeq = A.ItemSeq

AND C.UnitSeq = B.STDUnitSeq

WHERE UMModuleSeq = 1116001 -- 구매

AND A.AssetSeq NOT IN (2,4)

> --AND ISNULL(Z.StkQty,0) \<\> 0

 

 

SELECT ISNULL(F.ItemClassLName,'') AS UMItemClassLName, -- 대분류

ISNULL(F.ItemClassLSeq , 0) AS UMItemClassL , -- 대분류코드

ISNULL(F.ItemClassMName,'') AS UMItemClassMName, -- 중분류

ISNULL(F.ItemClassMSeq , 0) AS UMItemClassM , -- 중분류코드

ISNULL(F.ItemClassSName,'') AS UMItemClassName , -- 소분류

ISNULL(F.ItemClassSSeq , 0) AS UMItemClass , -- 소분류코드

ISNULL(B.ItemName ,'') AS ItemName , -- 품명

ISNULL(B.ItemNo ,'') AS ItemNo , -- 품번

ISNULL(B.Spec ,'') AS Spec , -- 규격

ISNULL(A.ItemSeq , 0) AS ItemSeq , -- 품목코드

ISNULL(C.UnitName , 0) AS UnitName , -- 기준단위

ISNULL(B.UnitSeq , 0) AS UnitSeq , -- 단위코드

ISNULL(D.DelvDay , 0) AS DelvDay , -- 평균조달기간

ISNULL(A.StockQty , 0) AS StockQty , -- 현재고

ISNULL(A.SafeQty , 0) AS SafeQty , -- 안전재고

ISNULL(A.NeedQty , 0) AS NeedQty , -- 필요량

ISNULL(A.POReqQty , 0) AS POReqQty , -- 구매요청

ISNULL(A.AppReqQty , 0) AS AppReqQty , -- 구매품의

ISNULL(A.POQty , 0) AS POQty , -- 구매발주

ISNULL(A.QCWaitQty , 0) AS QCWaitQty , -- 검사대기

ISNULL(A.ExpectQty , 0) AS ExpectQty , -- 예상재고

ISNULL(A.StkQty , 0) AS StkQty , -- 과부족

ISNULL(G.UnitName ,'') AS POUnit , -- 구매단위

ISNULL(A.POUnitSeq , 0) AS POUnitSeq , -- 구매단위코드

ISNULL(A.PoBaseQty , 0) AS PoBaseQty -- 발주산정량

FROM \#Result AS A

JOIN \_TDAItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.ItemSeq = A.ItemSeq

JOIN \_TDAUnit AS C WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.UnitSeq = B.UnitSeq

JOIN \_TDAItemPurchase AS D WITH(NOLOCK) ON D.CompanySeq = B.CompanySeq

AND D.ItemSeq = B.ItemSeq

LEFT OUTER JOIN \_TDAItemClass AS E WITH(NOLOCK) ON E.CompanySeq = @CompanySeq

AND E.ItemSeq = A.ItemSeq

AND E.UMajorItemClass IN (2001, 2004)

LEFT OUTER JOIN \_VDAItemClass AS F WITH(NOLOCK) ON F.CompanySeq = E.CompanySeq

AND F.ItemClassSSeq = E.UMItemClass

LEFT OUTER JOIN \_TDAUnit AS G WITH(NOLOCK) ON G.CompanySeq = @CompanySeq

AND G.UnitSeq = A.POUnitSeq

WHERE @IsStkQty = '0' OR (@IsStkQty = '1' AND A.StkQty \> 0) -- 과부족건만 조회 체크 시 과부족이 0보다 큰 건만 조회

 

RETURN