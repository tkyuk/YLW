---
title: api
date: 2024-11-19
tags: [erp, 개발, SQL]
---

# api

IF OBJECT_ID ('tempdb..#Tmp_Response_Tmp') IS NOT NULL DROP TABLE \#Tmp_Response_Tmp

 

 

DECLARE @APIRet INT, -- OLE 자동화 프로시저 호출 결과 리턴 값을 담을 변수(0:성공, 0 아닌 숫자는 실패)

@APIToken INT, -- OLE 자동화 프로시저 개채 생성 토큰 값을 담을 변수(여기서는 1회용 개체 사용권)

@URL NVARCHAR(256), -- HTTP 서버 URL 변수

@RequestText NVARCHAR(MAX),

@ResponseText NVARCHAR(MAX),

@APIIP NVARCHAR(100),

@APIDNS NVARCHAR(100),

@APICertID NVARCHAR(100),

@APICertKey NVARCHAR(100),

@MessageType INT,

@DocHandle INT,

@SQLScriptID NVARCHAR(100),

@SQL NVARCHAR(MAX)

 

DECLARE @ServiceID NVARCHAR(100) = '',

@MethodID NVARCHAR(100) = 'HistoryQuery',

@SendDataXml NVARCHAR(MAX) = '',

@Status INT = 0 ,

@Result NVARCHAR(250) = '' ,

@RecvDataXml NVARCHAR(MAX) = ''

 

SELECT @Status = 0,

@Result = '',

@RecvDataXml = '',-- 호출한 SP에서 에러가 발생하지 않도록 결과값에 추가

@ServiceID = LTRIM(RTRIM(ISNULL(@ServiceID , ''))),

@MethodID = LTRIM(RTRIM(ISNULL(@MethodID , '')))

--,@SendDataXml = LTRIM(RTRIM(ISNULL(@SendDataXml , '')))

SELECT @APIIP = LTRIM(RTRIM(ISNULL('KEY_1' , ''))),

@APIDNS = LTRIM(RTRIM(ISNULL('proas' , ''))),

@APICertID = LTRIM(RTRIM(ISNULL('KEY_1' , ''))),

@APICertKey = LTRIM(RTRIM(ISNULL('aaa' , '')))

 

SELECT @URL = '<https://www.proas.co.kr:8088/Angkor.Ylw.Common.HttpExecute/RestOutsideService.svc/OpenApi/>' + 'WBS.Ylw.WPKG.BisWorkItemPatch/HistoryQuery'

 

CREATE TABLE \#Tmp_Response_Tmp(ResponseText NVARCHAR(MAX))

 

DECLARE @json NVARCHAR(MAX);

 

--=============================

 

SET @json =

'{

"ROOT" : {

"certId" : "KEY_1",

"certKey" : "aaa",

"tokenKey" : "tokenkey1",

"dsn" : "proas_oper",

"dsnBis" : "proas_bis",

"dsnOper" : "proas_oper",

"companySeq" : "1",

 

"data" : {

"ROOT" : \[

{

"WorkingTag": "",

"IDX_NO": "1",

"DataSeq": "1",

"Status": "0",

"Selected": "1",

"Sel": "1",

"WorkItemName": "24.11.05 옵션 탭 - 판매가 변경 이벤트 추가",

"WorkItemSeq": "18835758",

"WorkItemVer": "5",

"Remark": "옵션 탭 - 옵션유형 ''할인'' 건의 판매가 변경 시품목정보 탭 - 판매단가 변경 및 시트 재계산 되도록 로직 반영",

"PkgType": "수정",

"PkgTypeSeq": "213003",

"PriorityType": "보통",

"PriorityTypeSeq": "81001",

"PkgUserName": "박성재",

"PkgUserSeq": "51399",

"PkgDateTime": "2024-11-19 10:32:06",

"BusinessName": "영업관리",

"BusinessSeq": "8",

"IsMonthly": "0",

"MonthlyDate": "",

"TABLE_NAME": "DataBlock3",

"PackageTypeSeq": "297003",

"IsHistory" :"1"

}

\]

}

}

}'

 

BEGIN TRY --예외 실행문 시작

--커넥션 생성

--MSXML2.ServerXMLHTTP 웹페이지를 읽는 xml파서 개체 형식의 OLE 인스턴스 개체 생성(출력된 토큰값으로 사용)

EXEC @APIRet = SP_OACreate 'MSXML2.ServerXMLHTTP', @APIToken OUT

 

--OLE 인스턴스 개체 생성 결과가 정상일 경우 시작

IF @APIRet = 0

BEGIN

--API접속

EXEC sp_OAMethod @APIToken, 'open', NULL, 'POST', @URL, 'false'

EXEC sp_OAMethod @APIToken, 'setRequestHeader', NULL, 'Content-type', 'application/json' -- 공백안됨

EXEC sp_OAMethod @APIToken, 'setTimeouts', NULL, 200000, 200000, 200000, 200000 --연결 유지 시간 정의 (URL접속시간/Socket접속시간/전송시간/결과시간)

EXEC sp_OAMethod @APIToken, 'Send', NULL, @json

-- 결과INSERT

INSERT INTO \#Tmp_Response_Tmp(ResponseText)

EXEC sp_OAGetProperty @APIToken, 'ResponseText'

 

 

SELECT @ResponseText = REPLACE(A.ResponseText, '\<?xml version="1.0" encoding="UTF-8"?\>', '') --응답 결과 출력

FROM \#Tmp_Response_Tmp AS A

 

SELECT @ResponseText = REPLACE(REPLACE(@ResponseText, '\[', ''), '\]', '')

 

 

SELECT \* FROM OPENJSON(@ResponseText)

 

SELECT SiteName

,SiteInit

FROM OPENJSON(@ResponseText)

WITH(SiteName NVARCHAR(100) '\$.DataBlock4.SiteName',

CustSeq NVARCHAR(100) '\$.DataBlock4.CustSeq',

SiteInit NVARCHAR(100) '\$.DataBlock4.SiteInit'

);

 

 

END

EXEC sp_OADestroy @APIToken

END TRY

BEGIN CATCH

PRINT ERROR_MESSAGE()

END CATCH