---
title: 전자결재 체크로직
date: 2025-05-08
tags: [erp, 개발, SQL]
---

# 전자결재 체크로직

emk_SWDACustBankAccCheck

--=================================================

-- 전자결재 체크

--=================================================

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1243 , -- 결재진행 중입니다. 수정이나 삭제할 수 없습니다.

@LanguageSeq ,

0, N''

 

UPDATE \#BIZ_OUT_DataBlock2

SET Result = REPLACE(@Results,'@2', ''),

MessageType = @MessageType,

Status = @Status

FROM \#BIZ_OUT_DataBlock2 AS A JOIN \_TCOMGroupWare AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkKind = 'OTHoli_das'

AND B.TblKey = A.AppSeq

AND B.IsProg = '1' AND B.IsEnd \<\> '1'

WHERE A.Status = 0

AND A.WorkingTag IN ('U','D')

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

18 , -- 결재진행 중입니다. 수정이나 삭제할 수 없습니다.

@LanguageSeq ,

0, N'결재처리된 건 '

 

UPDATE \#BIZ_OUT_DataBlock2

SET Result = REPLACE(@Results,'@2', ''),

MessageType = @MessageType,

Status = @Status

FROM \#BIZ_OUT_DataBlock2 AS A JOIN \_TCOMGroupWare AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND B.WorkKind = 'OTHoli_das'

AND B.TblKey = A.AppSeq

AND B.IsProg = '1' AND B.IsEnd = '1'

WHERE A.Status = 0

AND A.WorkingTag IN ('U','D')

 

--=================================================

-- 전자결재 체크 종료

--=================================================