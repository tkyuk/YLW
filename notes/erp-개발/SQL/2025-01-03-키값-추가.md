---
title: 키값 추가
date: 2025-01-03
tags: [erp, 개발, SQL]
---

# 키값 추가

-- 1. NULL허용인 경우 NOT NULL 으로 변경

ALTER TABLE uck_TACLabCostReplace ALTER COLUMN CCtrSeq INT NOT NULL

 

-- 2. 기존 키 제약 조건 삭제

ALTER TABLE uck_TACLabCostReplace DROP CONSTRAINT PKuck_TACLabCostReplace;

 

-- 3. 새로운 키 추가

ALTER TABLE uck_TACLabCostReplace ADD CONSTRAINT PKuck_TACLabCostReplace PRIMARY KEY (CompanySeq, AccUnit, AccMonth, AccSeq,CCtrSeq)