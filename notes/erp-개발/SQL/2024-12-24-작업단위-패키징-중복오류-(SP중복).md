---
title: 작업단위 패키징 중복오류 (SP중복)
date: 2024-12-24
tags: [erp, 개발, SQL]
---

# 작업단위 패키징 중복오류 (SP중복)

SP명칭 변경되어 \_TWPKGSourceFile 테이블에 같은 코드로 다른 SP명 두개 존재

=\> INSERT 시 1건에 대해 2건이 INSERT 되면서 중복오류

=\> \_TWPKGSourceFile 테이블 중복 건 삭제 (운영DB)

BEGIN TRAN

DELETE FROM \_TWPKGSourceFile WHERE SqlScriptSeq = 39821051 AND SourceFileSeq = 2773

ROLLBACK TRAN

 

SELECT \*

FROM \_TCASQLScripts                        C WITH(NOLOCK)

--)         ON A.ElementSeq        = C.SqlScriptSeq AND C.CompanySeq = 0

--                                                                                                                                         --AND ((ISNULL(A.DBType, 0) \<\> ISNULL(C.DBType, 0)) OR ((A.CheckValue IS NULL AND C.CheckInDateTime IS NOT NULL) OR A.CheckValue \<\> C.CheckInDateTime)))

INNER JOIN \_TWPKGSourceFile G WITH(NOLOCK) ON C.SqlScriptSeq = G.SqlScriptSeq

--INNER JOIN \_TWPKGSourceElement F WITH(NOLOCK) ON (A.ElementSeq = F.ElementSeq

-- AND ((A.CheckValue IS NULL AND G.CheckInTime IS NOT NULL) OR A.CheckValue \<\> G.CheckInTime))

--                                 INNER JOIN \_TCACompany D WITH(NOLOCK) ON D.CompanySeq = 1

--                                INNER JOIN \_TDEVCust E WITH(NOLOCK) ON E.ClientSeq = C.ClientSeq

--                                                                                                 AND E.DevSystemSeq = C.DevSystemSeq

WHERE C.SqlScriptSeq = 39821051

--AND C.ElementVer = 18

AND G.IsDeleted \<\> 1

--AND F.ElementTypeSeq = 290005

AND G.FileType = 322001

 

SELECT \* FROM \_TWPKGElement

WHERE ElementTypeSeq = 290005

AND ElementSeq = 39821051

AND ElementVer = 18

-- PRIMARY KEY 제약 조건 'PK_TWPKGElement'을(를) 위반했습니다. 개체 'dbo.\_TWPKGElement'에 중복 키를 삽입할 수 없습니다. 중복 키 값은 (290005, 39821051, 18)입니다.

 

 

SELECT \*

FROM \_TCASQLScripts                        C WITH(NOLOCK)

 

 

--)         ON A.ElementSeq        = C.SqlScriptSeq AND C.CompanySeq = 0

--                                                                                                                                         --AND ((ISNULL(A.DBType, 0) \<\> ISNULL(C.DBType, 0)) OR ((A.CheckValue IS NULL AND C.CheckInDateTime IS NOT NULL) OR A.CheckValue \<\> C.CheckInDateTime)))

INNER JOIN \_TWPKGSourceFile G WITH(NOLOCK) ON C.SqlScriptSeq = G.SqlScriptSeq

WHERE C.SqlScriptSeq = 39821051

 

BEGIN TRAN

DELETE FROM \_TWPKGSourceFile WHERE SqlScriptSeq = 39821051 AND SourceFileSeq = 2773

ROLLBACK TRAN

 

SELECT \* fROM \_TCASQLScripts

WHERE SqlScriptID = 'ubase_SXHRBasEmpCardObjectPrt'

OR SqlScriptID = 'ubase_SXHRBasEmpCardObjectPrt2'