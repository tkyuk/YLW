---
title: 구매발주메일발송(다중)처리 HTML 형식
date: 2024-04-19
tags: [erp, 개발, SQL]
---

# 구매발주메일발송(다중)처리 HTML 형식

DROP PROCEDURE ets_SPUORDPOListSendMail

GO

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*

설 명 - 데이터-구매발주메일발송다중처리 : 메일생성

작성일 - 20130701

작성자 - 허승남

수정자 - 임희진 :: 2018.04.05 - 발신자, 수신자 이메일 주소 존재여부 체크로직 추가

\- 임희진 :: 2018.08.14 - SendMessage의 Html 데이터 상에 사업자상호명이 정상 매핑되도록 수정 (요청번호 201808140043)

\- 임희진 :: 2018.08.16 - 납품장소 -\> 발주부서로 변경 (요청번호 201808160092)

\- 임희진 :: 2018.08.17 - 사이트 SP 호출 로직 추가

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

CREATE PROCEDURE ets_SPUORDPOListSendMail

@xmlDocument NVARCHAR(MAX) , -- 화면의 정보를 XML문서로 전달

@xmlFlags INT = 0 , -- 해당 XML문서의 TYPE

@ServiceSeq INT = 0 , -- 서비스 번호

@WorkingTag NVARCHAR(10)= '', -- 워킹 태그

@CompanySeq INT = 1 , -- 회사 번호

@LanguageSeq INT = 1 , -- 언어 번호

@UserSeq INT = 0 , -- 사용자 번호

@PgmSeq INT = 0 -- 프로그램 번호

AS

--================================================================

-- 사이트 SP 호출 로직 추가 :: 18.08.17 \#임희진

--================================================================

--DECLARE @SiteInitialName NVARCHAR(100), @SPName NVARCHAR(100)

--SELECT @SiteInitialName = ISNULL(EnvValue,'') FROM \_TCOMEnv WITH(NOLOCK) WHERE EnvSeq = 2 AND CompanySeq = @CompanySeq

 

--SELECT @SPName = @SiteInitialName +'\_SPUORDPOListSendMail'

--IF EXISTS (SELECT \* FROM sysobjects WITH(NOLOCK) WHERE id = object_id(@SPName) and xtype = 'P')

--BEGIN

-- EXEC @SPName @xmlDocument,@xmlFlags,@ServiceSeq,@WorkingTag,@CompanySeq,@LanguageSeq,@UserSeq,@PgmSeq

-- RETURN

--END

----------------------------------------------------------------------------------------------------------------------------

-- 서비스 마스타 등록 생성

CREATE TABLE \#TPUORDPOItem (WorkingTag NCHAR(1) NULL)

EXEC \_SCAOpenXmlToTemp @xmlDocument, @xmlFlags, @CompanySeq, @ServiceSeq, 'DataBlock1', '#TPUORDPOItem'

DECLARE @MessageType INT,

@Status INT,

@Results NVARCHAR(250),

> @Word1                        NVARCHAR(20),
>
> @Word2                        NVARCHAR(20),
>
> @Word3                        NVARCHAR(20),

@PricePoint INT,

@CurAmtPoint INT,

@QtyPoint INT

-- 거래통화 단가 소수점 자리수

EXEC dbo.\_SCOMEnv @CompanySeq,7,@UserSeq,@@PROCID,@PricePoint OUTPUT

-- 외화부가세, 외화금액 소수점 자리수

EXEC dbo.\_SCOMEnv @CompanySeq,14,@UserSeq,@@PROCID,@CurAmtPoint OUTPUT

-- 구매/자재 수량 소수점 자리수

EXEC dbo.\_SCOMEnv @CompanySeq,5,@UserSeq,@@PROCID,@QtyPoint OUTPUT

EXEC dbo.\_SCOMGetDictionary @LanguageSeq, 6,        @Word1 OUTPUT -- 거래처

EXEC dbo.\_SCOMGetDictionary @LanguageSeq, 7817, @Word2 OUTPUT -- 이메일 주소

> EXEC dbo.\_SCOMGetDictionary @LanguageSeq, 994, @Word3 OUTPUT -- 발주담당자
>
> --=============================================

--거래처 이메일 주소가 누락된 건은 오류 메시지 처리

> --=============================================

EXEC dbo.\_SCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1001 , -- @1가 없습니다.(SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 1001)

@LanguageSeq ,

5498,'' -- SELECT \* FROM \_TCADictionary WHERE Word like '%받는사람 email%'

UPDATE A

SET Result = @Results + ' \[' + @Word1 + ' ' + @Word2 + '\]',

MessageType = @MessageType,

Status = @Status

FROM \#TPUORDPOItem AS A                

WHERE ISNULL(Email,'') = ''

> -- 오류발생 시 Return 되도록 추가                By 18.04.05 hjlim
>
> IF @@ROWCOUNT \> 0

BEGIN

SELECT \* FROM \#TPUORDPOItem

RETURN

END        

--=======================================================================================================================

-- 1. 메일 보낼 데이터 수집

DECLARE @SumAmt DECIMAL(19,5),

@SumVAT DECIMAL(19,5),

@POSeq INT,

@DeptSeq INT,

@BizUnit INT,

@FactUnit INT

-- 납품으로 점프시 창고 가져가기(1.창고별품목등록의 창고 2.구매단가등록의 창고 3.이전 품목별 납품창고)

CREATE TABLE \#DelvWH

(

POSeq INT,

POSerl INT,

WHSeq INT

)

INSERT INTO \#DelvWH

SELECT A.POSeq, A.POSerl , 0

FROM \_TPUORDPOItem AS A WITH(NOLOCK)

JOIN \#TPUORDPOItem AS B ON B.POSeq = A.POSeq

WHERE A.POSeq = @POSeq

AND B.Status = 0

SELECT @DeptSeq = ISNULL(A.DeptSeq, 0)

FROM \_TPUORDPO AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.POSeq = @POSeq

SELECT @BizUnit = ISNULL(BizUnit, 0),

@FactUnit = ISNULL(FactUnit, 0)

FROM \_TDADept WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq

AND DeptSeq = @DeptSeq

--1. 품목별 기본창고

IF EXISTS (SELECT 1 FROM \_TPUORDPOItem AS A WITH(NOLOCK)

JOIN \_TDAWHItem AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

AND A.ItemSeq = C.ItemSeq

JOIN \#TPUORDPOItem AS B ON B.POSeq = A.POSeq

WHERE A.CompanySeq = @CompanySeq

AND B.Status = 0)

BEGIN

UPDATE \#DelvWH

SET WHSeq = (SELECT TOP 1 WHSeq FROM \_TDAWHItem WITH(NOLOCK) WHERe CompanySeq = D.CompanySeq AND ItemSeq = D.ItemSeq)

FROM \#DelvWH AS A

JOIN \_TPUORDPOItem AS D WITH(NOLOCK) ON A.POSeq = D.POSeq

AND A.POSerl = D.POSerl

WHERE D.CompanySeq = @CompanySeq

AND A.WHSeq = 0

END

-- 2. 구매단가등록의 창고

ELSE IF EXISTS (SELECT 1 FROM \_TPUBaseBuyPriceItem AS A WITH(NOLOCK)

JOIN \_TPUORDPOItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.ItemSeq = B.ItemSeq

JOIN \#DelvWH AS C ON B.POSeq = C.POSeq

AND B.POSerl = C.POSerl

JOIN \_TPUORDPO AS D WITH(NOLOCK) ON D.CompanySeq = @CompanySeq

AND B.POSeq = D.POSeq

AND A.CustSeq = D.CustSeq

WHERE A.CompanySeq = @CompanySeq

AND C.WHSeq = 0)

BEGIN

UPDATE \#DelvWH

SET WHSeq = D.WHSeq

FROM \#DelvWH AS A

JOIN \_TPUORDPOItem AS B WITH(NOLOCK) ON A.POSeq = B.POSeq

AND A.POSerl = B.POSerl

JOIN \_TPUORDPO AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.POSeq = C.POSeq

JOIN \_TPUBaseBuyPriceItem AS D WITH(NOLOCK) ON B.CompanySeq = D.CompanySeq

AND B.ItemSeq = D.ItemSeq

AND C.CustSeq = D.CustSeq

WHERE C.PODate BETWEEN D.StartDate AND D.EndDate

AND B.CompanySeq = @CompanySeq

AND A.WHSeq = 0

END

-- 3. 이전 품목별 납품창고

ELSE

BEGIN

UPDATE \#DelvWH

SET WHSeq = C.WHSeq

FROM \#DelvWH AS A

JOIN \_TPUORDPOItem AS B WITH(NOLOCK) ON A.POSeq = B.POSeq

AND A.POSerl = B.POSerl

JOIN \_TPUDelvItem AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.ItemSeq = C.ItemSeq

WHERE B.CompanySeq = @CompanySeq

AND DelvSeq IN (SELECT DelvSeq

FROM \_TPUDelv WITH(NOLOCK)

WHERE DelvNo IN (SELECT MAX(A.DelvNo)

FROM \_TPUDelv AS A

JOIN \_TPUDelvItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.DelvSeq = B.DelvSeq

JOIN \_TPUORDPOItem AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.ItemSeq = C.ItemSeq

JOIN \#DelvWH AS D ON C.POSeq = D.POSeq

AND C.POSerl = D.POSerl

WHERE A.CompanySeq = @CompanySeq

AND D.WHSeq = 0 ) )

END

--DECLARE @STaxName NVARCHAR(100), @STaxNo NVARCHAR(100), @SVATRptAddr NVARCHAR(100), @SOwner NVARCHAR(100)

-- 하드코딩된 사업자번호 정보 주석처리 :: 18.08.16 \#임희진

--SELECT @STaxName = TaxName,

-- @STaxNo = TaxNo,

-- @SVATRptAddr = VATRptAddr,

-- @SOwner = Owner

-- FROM \_TDATaxUnit

--WHERE CompanySeq = @CompanySeq

-- AND TaxUnit = 4

SELECT DISTINCT --Master

-- IDENTITY(INT,0,1) AS IDX_NO ,

ISNULL(Z.PONo,'') AS PONo , --발주번호

ISNULL(Z.PODate,'') AS PODate , --발주일

--발주처 정보

ISNULL(N.TaxName,'') AS TaxName , --사업자 상호

ISNULL(N.TaxNo,'') AS TaxNo , --사업자번호

-- ISNULL(N.TaxNoAlias,'') AS TaxNoAlias , --사업자번호명

ISNULL(N.VATRptAddr,'') AS VATRptAddr , --사업자 주소

-- ISNULL(N.Zip,'') AS VATRptZip , --사업자 우편번호

ISNULL(N.Owner,'') AS Owner , --대표자

-- ISNULL(N.TelNo,'') AS TaxTelNo , --사업자 전화번호

ISNULL(N.FAXNo,'') AS TaxFAXNo , --사업자 FAX ,구매담당자 FAX

ISNULL(J.DeptName,'') AS DeptName , --구매담당부서

-- ISNULL(R.DeptPhone,'') AS DeptTelNo , --구매담당 부서 전화번호

ISNULL(K1.Extension,'') AS DeptTelNo , --구매담당 담당자 사내전화번호

ISNULL(R.DeptFax,'') AS DeptFAXNo , --구매담당 부서 FAX

--ISNULL(O.Fax,'') AS DeptFAXNo , --구매담당 부서 FAX

ISNULL(K.EmpName,'') AS EmpName , --구매담당자

Z.EmpSeq,

ISNULL(P.Phone,'') AS EmpPhone , --구매담당자 전화번호

ISNULL(P.CellPhone,'') AS EmpCellPhone , --구매담당자 핸드폰번호 20100523 이성덕 추가

ISNULL(P.Email,'') AS EmpEmail , --구매담당자 Email 20100426 박소연 추가

ISNULL(L.BizAddr,'') AS BizAddr , --거래처주소 20110108 이상화 추가

-- ISNULL(F1.BizUnitName,'')AS BizUnitName , --매입 사업장 20110108 이상화 추가

CASE WHEN ISNULL(A.PJTSeq,0) \<\> 0 THEN ISNULL(F1.BizUnitName,'') ELSE ISNULL(E1.BizUnitName,'') END AS BizUnitName , --매입 사업장 20110121 이성덕 수정 : 프로젝트일경우 프로젝트의 사업부문, 양산일 경우 창고의 사업부문

ISNULL(F2.Addr,'') AS FactAddr , --납품처 20110108 이상화 추가

--수주처 정보

Z.CustSeq , --거래처 내부번호

ISNULL(L.CustName,'') AS CustName , --거래처명

ISNULL(L.Owner,'') AS CustEmpName , --거래처 대표담당자

ISNULL(Q.JpName,'') AS CustJpName , --거래처 대표담당자 직위

ISNULL(Q.DeptName,'') AS CustDeptName , --거래처 대표담당자 부서

-- ISNULL(L.TelNo,'') AS TelNo , --거래처 대표담당자, 부서 전화번호

ISNULL(L.TELNo,'') AS TelNo , --거래처 대표담당자, 부서 전화번호

ISNULL(Q.MobileNo,'') AS MobileNo , --거래처 대표담당자, 핸드폰번호

ISNULL(Q.FAX,'') AS FAX , --거래처 대표담당자, 부서 FAX

ISNULL(Q.TELNo,'') AS CustEmpTELNo, --거래처 대표담당자 전화번호 :: 18.08.16 \#임희진 (항목은 존재하나 공백으로 출력되고 있어 추가)

ISNULL(Y.Email,'') AS CustEmail , --거래처 담당자 EMail

--Detail

ISNULL(F.PJTName,'') AS PJTName , --프로젝트명

ISNULL(F.PJTNo,'') AS PJTNo , --프로젝트 번호

ISNULL(B.ItemNo,'') AS ItemNo , --품번

ISNULL(A.DelvDate,'') AS DelvDate , --납품기일

ISNULL(B.ItemName,'') AS ItemName , --품명

ISNULL(B.Spec,'') AS Spec , --규격

ISNULL(C.UnitName,'') AS UnitName , --단위

ISNULL(A.Qty,0) AS Qty , --발주수량

ISNULL(A.Price,0) AS Price , --단가

ISNULL(A.CurAmt,0) AS CurAmt , --금액

ISNULL(A.CurVAT,0) AS CurVAT , --부가세

ISNULL(A.Remark1,'') AS Remark1 , --내부비고

ISNULL(B.ItemSName,'') AS ItemSName , --품목약명 20100428 김가원 추가

ISNULL(B.ItemEngName,'') AS ItemEngName , --품목영문명 20100428 김가원 추가

ISNULL(B.ItemEngSName,'')AS ItemEngSName , --품목영문약명 20100428 김가원 추가

--Trailer

ISNULL(A.Remark2,'') AS Remark2 , --외부비고

ISNULL(M.CurrName,'') AS CurrName , --통화

ISNULL(M.CurrUnit,'') AS CurrUnit , --통화표기

ISNULL(D.CustName,'') AS MakerName , --MakerName

CONVERT(DECIMAL(19,5),0) AS SumAmt , --금액합계 20100203 박소연 수정

CONVERT(DECIMAL(19,5),0) AS SumVAT , --부가세합계 20100203 박소연 수정

ISNULL(Z.Payment,'') AS Payment , --지불조건

ISNULL(Z.Remark ,'') AS Remark , --NOTE

ISNULL(O.FAX,'') AS CustFax , --Fax

CASE ISNULL(A.WHSeq, 0) WHEN 0 THEN S.WHSeq ELSE A.WHSeq END AS WHSeq,

CASE ISNULL(A.WHSeq, 0) WHEN 0 THEN EE.WHName ELSE E.WHName END AS WHName,

ISNULL(M4.ItemName,'') AS TopUnitName, ISNULL(M4.ItemNo,'') AS TopUnitNo,

A.POSeq AS POSeq,

A.POSerl AS POSerl

INTO \#TEMP_POPrint

FROM \_TPUORDPO AS Z WITH(NOLOCK)

JOIN \_TPUORDPOItem AS A WITH(NOLOCK) ON Z.POSeq = A.POSeq

AND Z.CompanySeq = A.CompanySeq

JOIN \#TPUORDPOItem AS Y ON Y.POSeq = A.POSeq

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.ItemSeq = B.ItemSeq

LEFT OUTER JOIN \_TDAUnit AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

AND A.UnitSeq = C.UnitSeq

LEFT OUTER JOIN \_TDACust AS D WITH(NOLOCK) ON A.CompanySeq = D.CompanySeq

AND A.MakerSeq = D.CustSeq

LEFT OUTER JOIN \_TDAWH AS E WITH(NOLOCK) ON A.CompanySeq = E.CompanySeq

AND A.WHSeq = E.WHSeq

LEFT OUTER JOIN \_TPJTProject AS F WITH(NOLOCK) ON A.CompanySeq = F.CompanySeq

AND A.PJTSeq = F.PJTSeq

LEFT OUTER JOIN \_TDABizUnit AS F1 WITH(NOLOCK) ON F.CompanySeq = F1.CompanySeq

AND F.BizUnit = F1.BizUnit

LEFT OUTER JOIN \_TDAFactUnit AS F2 WITH(NOLOCK) ON F1.CompanySeq = F2.CompanySeq

AND F1.BizUnit = F2.BizUnit

LEFT OUTER JOIN \_TDABizUnit AS E1 WITH(NOLOCK) ON E1.CompanySeq = E.CompanySeq

AND E1.BizUnit = E.BizUnit

LEFT OUTER JOIN \_TDAItemClass AS H WITH(NOLOCK) ON A.CompanySeq = H.CompanySeq

AND A.ItemSeq = H.ItemSeq

AND H.UMItemclass LIKE '2001%'

LEFT OUTER JOIN \_TPDQAItemClassQC AS I WITH(NOLOCK) ON H.CompanySeq = I.CompanySeq

AND H.UMItemClass= I.UMItemClass

AND I.IsPurQC = '1'

LEFT OUTER JOIN \_TDADept AS J WITH(NOLOCK) ON Z.CompanySeq = J.CompanySeq

AND Z.DeptSeq = J.DeptSeq

LEFT OUTER JOIN \_TDAEmp AS K WITH(NOLOCK) ON Z.CompanySeq = K.CompanySeq

AND Z.EmpSeq = K.EmpSeq

LEFT OUTER JOIN \_TDAEmpIN AS K1 WITH(NOLOCK) ON Z.CompanySeq = K1.CompanySeq

AND Z.EmpSeq = K1.EmpSeq

LEFT OUTER JOIN \_TDACust AS L WITH(NOLOCK) ON Z.CompanySeq = L.CompanySeq

AND Z.CustSeq = L.CustSeq

LEFT OUTER JOIN \_TDACurr AS M WITH(NOLOCK) ON Z.CompanySeq = M.CompanySeq

AND Z.CurrSeq = M.CurrSeq

LEFT OUTER JOIN \_TDATaxUnit AS N WITH(NOLOCK) ON Z.CompanySeq = N.CompanySeq

AND N.TaxUnit = J.TaxUnit

LEFT OUTER JOIN \_TDACustAdd AS O WITH(NOLOCK) ON Z.CompanySeq = O.CompanySeq

AND Z.CustSeq = O.CustSeq

LEFT OUTER JOIN \_TDAEmpIn AS P WITH(NOLOCK) ON Z.CompanySeq = P.CompanySeq

AND Z.EmpSeq = P.EmpSeq

LEFT OUTER JOIN \_TDACustEmpInfo AS Q WITH(NOLOCK) ON Z.CompanySeq = Q.CompanySeq

AND Z.CustSeq = Q.CustSeq

AND Q.IsSTD = '1'

AND Q.EmpSerl = (SELECT TOP 1 EmpSerl --거래처대표담당자정보 가져올때 TOP 1 Serl로 가져오도록 수정

FROM \_TDACustEmpInfo WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq AND CustSeq = Z.CustSeq AND IsSTD = '1')

LEFT OUTER JOIN \_TDADept AS R WITH(NOLOCK) ON Z.CompanySeq = R.CompanySeq

AND Z.DeptSeq = R.DeptSeq

LEFT OUTER JOIN \#DelvWH AS S ON A.POSeq = S.POSeq

AND A.POSerl = S.POSerl

LEFT OUTER JOIN \_TDAWH AS EE WITH(NOLOCK) ON EE.CompanySeq = @CompanySeq

AND S.WHSeq = EE.WHSeq

LEFT OUTER JOIN \_TPJTBOM AS M5 WITH(NOLOCK) ON A.CompanySeq = M5.CompanySEq

AND A.PJTSeq = M5.PJTSeq

AND A.WBSSeq = M5.BOMSerl

LEFT OUTER JOIN \_TPJTBOM AS M1 WITH(NOLOCK) ON A.CompanySeq = M1.CompanySeq

AND A.PJTSeq = M1.PJTSeq AND M1.BOMSerl \<\> -1 AND M5.UpperBOMSerl = M1.BOMSerl AND M1.BeforeBOMSerl = 0 -- 상위 BOM

LEFT OUTER JOIN \_TDAItem AS M2 WITH(NOLOCK) ON A.CompanySEq = M2.CompanySeq

AND M1.ItemSeq = M2.ItemSeq

LEFT OUTER JOIN \_TPJTBOM AS M3 WITH(NOLOCK) ON A.CompanySeq = M3.CompanySeq

AND A.PJTSeq = M3.PJTSeq

AND M3.BOMSerl \<\> -1

AND M3.BeforeBOMSerl = 0

AND SUBSTRING(M1.TreeCode,1,6) = M3.TreeCode -- 최상위

LEFT OUTER JOIN \_TDAItem AS M4 WITH(NOLOCK) ON A.CompanySeq = M4.CompanySeq

AND M3.ItemSeq = M4.ItemSeq

WHERE A.CompanySeq = @CompanySeq

AND Y.Status = 0

>  
>
> --=========================================================================
>
> --발주담당자(발신자) 이메일 주소가 누락된 건은 오류 메시지 처리 By 18.04.05 hjlim
>
> --=========================================================================

EXEC dbo.\_SCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1001 , -- @1가 없습니다.(SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 1001)

@LanguageSeq ,

5803,'' -- SELECT \* FROM \_TCADictionary WHERE Word like '%보내는 사람 email%'

UPDATE A

SET Result = @Results + ' \[' + @Word3 + ' ' + @Word2 + '\]',

MessageType = @MessageType,

Status = @Status

FROM \#TPUORDPOItem AS A        

> JOIN \#TEMP_POPrint AS B ON A.POSeq = B.POSeq

WHERE ISNULL(B.EmpEmail,'') = ''

> IF @@ROWCOUNT \> 0

BEGIN

SELECT \* FROM \#TPUORDPOItem

RETURN

END        

--======================================================

> -- 체크 끝
>
> --======================================================

SELECT IDENTITY(INT,0,1) AS IDX_NO,

A.\*

INTO \#TPUORDPOPrint

FROM \#TEMP_POPrint AS A

ORDER BY A.PONo, A.Spec

-------------------

--품의번호 추적 --- 20100716 박소연 추가

-------------------

CREATE TABLE \#TMP_SOURCETABLE

(

IDOrder INT,

TABLENAME NVARCHAR(100)

)

CREATE TABLE \#TCOMSourceTracking

(

IDX_NO INT,

IDOrder INT,

Seq INT,

Serl INT,

SubSerl INT,

Qty DECIMAL(19, 5),

STDQty DECIMAL(19, 5),

Amt DECIMAL(19, 5),

VAT DECIMAL(19, 5)

)

INSERT \#TMP_SOURCETABLE

SELECT '1','\_TPUORDApprovalReqItem'

EXEC \_SCOMSourceTracking @CompanySeq, '\_TPUORDPOItem', '#TPUORDPOPrint', 'POSeq', 'POSerl', ''

-- 품의번호 업데이트

ALTER TABLE \#TPUORDPOPrint ADD ApproReqNo NCHAR(24)

UPDATE \#TPUORDPOPrint

SET ApproReqNo = ISNULL(D.ApproReqNo, '')

FROM \#TPUORDPOPrint AS A

JOIN \#TCOMSourceTracking AS C ON A.IDX_NO = C.IDX_NO

AND C.IDOrder = '1'

LEFT OUTER JOIN \_TPUORDApprovalReq AS D WITH(NOLOCK) ON D.CompanySeq = @CompanySeq

AND C.Seq = D.ApproReqSeq

--===============================================================================================================

--금액 합계 구하기

-- SELECT @SumAmt = SUM(ISNULL(A.CurAmt,0)),

-- @SumVAT = SUM(ISNULL(A.CurVAT,0))

-- FROM \_TPUORDPOItem AS A

-- JOIN (SELECT DISTINCT POSeq FROM \#TPUORDPOPrint) AS B ON A.POSeq = B.POSeq

-- WHERE CompanySeq = @CompanySeq

--AND POSeq = @POSeq 2010. 10. 18 hkim 총 금액이 출력 안되서 변경

SELECT A.POSeq,

SUM(ISNULL(A.CurAmt,0)) AS SumAmt,

SUM(ISNULL(A.CurVAT,0)) AS SumVAT

INTO \#TEMP_SUMAmt

FROM \_TPUORDPOItem AS A

JOIN (SELECT DISTINCT POSeq FROM \#TPUORDPOPrint) AS B ON A.POSeq = B.POSeq

WHERE CompanySeq = @CompanySeq

GROUP BY A.POSeq

UPDATE A

SET SumAmt = B.SumAmt,

SumVAT = B.SumVAT

FROM \#TPUORDPOPrint AS A

LEFT OUTER JOIN \#TEMP_SUMAmt AS B ON B.POSeq = A.POSeq

--===============================================================================================================

--=======================================================================================================================

-- 2. 메일 내용 생성

DECLARE @IDX_NO INT,

@Count INT,

-- @POSeq INT,

@PrevPOSeq INT,

@html NVARCHAR(MAX),

@html_head NVARCHAR(MAX),

@html_tail NVARCHAR(MAX),

@html_tail_inData NVARCHAR(MAX),

@html_msg NVARCHAR(MAX),

@MailMsg NVARCHAR(MAX),

@SendUserSeq INT,

@RecvUserSeq INT,

@RecvEmpMail NVARCHAR(MAX), -- 테이블 내 RecvMail 길이만큼 수정 100 -\> MAX :: 20.12.03 \#임희진

@SendEmpMail NVARCHAR(200),

@TaxName NVARCHAR(100),

@TaxNo NVARCHAR(100),

@Owner NVARCHAR(100),

@VATRptAddr NVARCHAR(100),

@BizUnitName NVARCHAR(100),

@CurrName NVARCHAR(100),

@CustName NVARCHAR(100),

@CustEmpName NVARCHAR(100),

@EmpName NVARCHAR(100),

@BizAddr NVARCHAR(100),

@TaxFAXNo NVARCHAR(100),

@TelNo NVARCHAR(100),

@CustFax NVARCHAR(100),

@EmpEmail NVARCHAR(100),

@PJTNo NVARCHAR(100),

@CurAmt NVARCHAR(100),

@StrSumAmt NVARCHAR(100),

@StrSumVAT NVARCHAR(100),

@TotAmt NVARCHAR(100),

@FactAddr NVARCHAR(100),

@ItemNo NVARCHAR(100),

@ItemName NVARCHAR(100),

@Spec NVARCHAR(100),

@Qty NVARCHAR(100),

@UnitName NVARCHAR(100),

@Price NVARCHAR(100),

@DelvDate NVARCHAR(100),

@MakerName NVARCHAR(100),

@Remark1 NVARCHAR(1000),

@WHName NVARCHAR(100),

@PONo NVARCHAR(100),

@PODate NVARCHAR(100),

@Remark NVARCHAR(1000),

@Payment NVARCHAR(500),

@Subjects NVARCHAR(100),

@AttachFileName NVARCHAR(100),

@DetailCnt INT,

@DeptName NVARCHAR(100), -- 발주부서 추가 :: 18.08.16 \#임희진

@CustEmpTELNo NVARCHAR(20) , -- 거래처대표담당자 전화번호 추가 :: 18.08.16 \#임희진

@CustEmpFAX NVARCHAR(20) -- 거래처대표담당자 FAX번호 추가 :: 18.08.16 \#임희진

SELECT @Payment = Payment FROM \#TPUORDPOPrint

--메일 전송을 위한 임시 테이블

CREATE TABLE \#TEMP_TCASendMessage (

IDX_NO INT IDENTITY(1,1),

POSeq INT,

CompanySeq INT,

SendMessageID INT,

SendUserSeq INT,

SendMessageType INT,

Subject NVARCHAR(1024),

SendMessage NVARCHAR(MAX),

RecvUserSeq INT,

IsMemo NCHAR(1),

IsMail NCHAR(1),

IsSMS NCHAR(1),

SendMemo NCHAR(1),

SendMail NCHAR(1),

MemoDateTime DATETIME,

MailDateTime DATETIME,

SMSDateTime DATETIME,

SendSMS NCHAR(1),

TblKey NVARCHAR(400),

CrDateTime DATETIME,

RecvEmail NVARCHAR(MAX), -- 테이블 내 RecvMail 길이만큼 수정 100 -\> MAX :: 20.12.03 \#임희진

RecvTelNo NVARCHAR(60),

LoginPwd NVARCHAR(60),

States NVARCHAR(200),

Result NVARCHAR(MAX),

EmailPass NVARCHAR(100),

AttachMessage NVARCHAR(MAX),

AttachFileName NVARCHAR(200),

SendEmail NVARCHAR(200),

CCEmail NVARCHAR(MAX) -- 테이블 내 RecvMail 길이만큼 수정 100 -\> MAX :: 20.12.03 \#임희진

)

--수집된 대상이 있는지 체크

SELECT @Count = COUNT(\*) FROM \#TPUORDPOPrint

IF @Count \> 0

BEGIN

-- 1) HTML 메시지 생성

---메시지 생성시, 오류가 발생하면 Agent Log를 생성

---임시 테이블에서 한 건씩 가져와서,

---구매발주코드가 이전구매발주코드와 다르면, 신규로 HTML소스를 작성한다.

---같으면, 품목에 대한 HTML-TABLE 태그만 작성하여, 이전구매발주코드에 대한 HTML소스를 추가한다.

---신규로 작성된 건은 메일을 보낼 임시 테이블에 저장한다.

---이전구매발주와 다르고 이전에 작성한 메일 데이터가 존재하면, HTML태그를 닫아준다.

ALTER TABLE \#TPUORDPOPrint ADD HTMLSource NVARCHAR(MAX)

SELECT @IDX_NO = 0

SELECT @PrevPOSeq = 0

--HTML 헤더

SELECT @html_head =

'

\<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "<http://www.w3.org/TR/html4/loose.dtd>"\>

\<html\>

\<head\>

\<meta http-equiv="Content-Type" content="text/html; charset=utf-8" /\>

\<title\>구매발주서\</title\>

\<style type="text/css"\>

td{font-size:12px;}

th{font-size:12px;}

/\* ----- Tables ----- \*/

table.tablesign .top {

padding:3px 5px 3px 5px;

border-left:0; border-top:.5pt solid \#000000;;

border-right:.5pt solid \#000000; border-bottom:.5pt solid \#000000;

}

table.tablesign .left {

padding:3px 5px 3px 5px;

border-left:.5pt solid \#000000;; border-top:0;

border-right:.5pt solid \#000000; border-bottom:.5pt solid \#000000;

}

table.tablesign th {

padding:3px 0px 3px 0px;

text-align:center;

background-color:#cccccc;

border-left:0; border-top:0;

border-right:.5pt solid \#000000; border-bottom:.5pt solid \#000000;

}

table.tablesign td {

padding:3px 5px 3px 5px;

border-left:0; border-top:0;

border-right:.5pt solid \#000000; border-bottom:.5pt solid \#000000;

}

table.tablesign .dotline{

padding:3px 5px 3px 5px;

border-left:0; border-top:0;

border-right:.5pt solid \#000000; border-bottom:.5pt dashed \#000000;}

table.tablesign .noneline{

border-left:0; border-top:0;

border-right:0; border-bottom:.5pt solid \#000000;}

\</style\>

\</head\>

\<body\>

\<table border="0" cellpadding="0" cellspacing="0" class="tablesign" \>

\<tr align="center"\>

\<td colspan="7" rowspan="2" style="border-left:0px; border-top:0px; border-bottom:0px; font-size:24px;"\>\<b\>발 \&nbsp; 주 \&nbsp; 서\</b\>\</td\>

\<td class="top"\>담 당 \</td\>

\<td class="top"\>과 장\</td\>

\<td colspan="2" class="top"\>부 장\</td\>

\<td class="top"\>이 사\</td\>

\<td class="top"\>전 무\</td\>

\</tr\>

\<tr\>

\<td height="50"\>\&nbsp;\</td\>

\<td\>\&nbsp;\</td\>

\<td colspan="2"\>\&nbsp;\</td\>

\<td\>\&nbsp;\</td\>

\<td\>\&nbsp;\</td\>

\</tr\>

\<tr\>

\<td height="20" width="30" class="noneline"\>\&nbsp;\</td\>

\<td width="40" class="noneline"\>\&nbsp;\</td\>

\<td width="70" class="noneline"\>\&nbsp;\</td\>

\<td width="110" class="noneline"\>\&nbsp;\</td\>

\<td width="40" class="noneline"\>\&nbsp;\</td\>

\<td width="30" class="noneline"\>\&nbsp;\</td\>

\<td width="50" class="noneline"\>\&nbsp;\</td\>

\<td width="60" class="noneline"\>\&nbsp;\</td\>

\<td width="60" class="noneline"\>\&nbsp;\</td\>

\<td width="20" class="noneline"\>\&nbsp;\</td\>

\<td width="30" class="noneline"\>\&nbsp;\</td\>

\<td width="50" class="noneline"\>\&nbsp;\</td\>

\<td width="50" class="noneline"\>\&nbsp;\</td\>

\</tr\>

\<tr\>

\<th colspan="2" style="border-left:.5pt solid \#000000;"\>거 래 처\</th\>

\<td colspan=2\>##CustName##

\</td\>

\<th colspan="2"\>FAX 번호\</th\>

\<td colspan=2\>##TaxFAXNo##

\</td\>

\<th\>수 신 자\</th\>

\<td colspan="4"\>##EmpName## \</td\>

\</tr\>

\<tr\>

\<th colspan="2" style="border-left:.5pt solid \#000000;"\>발주번호

\</th\>

\<td colspan=2\>##PONo##

\</td\>

\<th colspan="2"\>발주일자

\</th\>

\<td colspan="2"\>##PODate##

\</td\>

\<th\>지불조건\</th\>

\<td colspan="4"\>##Payment##

\</td\>

\</tr\>

\<tr\>

\<th style="border-left:.5pt solid \#000000;"\>화폐\</th\>

\<td \>##CurrName##

\</td\>

\<th\>금액계\</th\>

\<td align="right"\>##TotAmt##

\</td\>

\<th colspan="2"\>발주부서

\</th\>

\<td colspan=2 \>##DeptName##

\</td\>

\<th\>발신자

\</th\>

\<td colspan="4"\>##EmpName##

\</td\>

\</tr\>

\<tr\>\<td height="20" colspan="13" class="noneline"\>\&nbsp;\</td\>\</tr\>

\<tr\>

\<tr\>

\<th rowspan="2" style="border-left:.5pt solid \#000000;"\>순번\</th\>

\<th colspan="2" style="border-bottom:dashed .5pt \#000000;"\>품 번\</th\>

\<th colspan="2" style="border-bottom:dashed .5pt \#000000;"\>품 명\</th\>

\<th rowspan="2"\>단위\</th\>

\<th colspan="2" style="border-bottom:dashed .5pt \#000000;"\>수 량\</th\>

\<th colspan="2" style="border-bottom:dashed .5pt \#000000;"\>단 가\</th\>

\<th colspan="3" style="border-bottom:dashed .5pt \#000000;"\>MAKER\</th\>

\</tr\>

\<tr\>

\<th colspan="2"\>품의번호\</th\>

\<th colspan="2"\>규 격\</th\>

\<th colspan="2"\>납 기\</th\>

\<th colspan="2"\>금 액\</th\>

\<th colspan="3"\>비 고\</th\>

\</tr\>

'

--HTML 꼬리

SELECT @html_tail =

'

\<tr\>\<td height="20" colspan="13" class="noneline"\>\&nbsp;\</td\>\</tr\>

\<tr\>

\<td height="70" colspan="5" valign="top" class="left" style="line-height:18px;"\>NOTE : \##Remark##\</td\>

\<td colspan="4"\>\&nbsp;\</td\>

\<td colspan="4" valign="top" style="line-height:18px;"\>전화 : \##CustEmpTELNo##\<br\>

FAX : \##CustEmpFAX##\</td\>

\</tr\>

\</table\>

\</body\>

\</html\>

'

SELECT @html_msg =

'

\<html\>

\<head\>

\<meta http-equiv="Content-Type" content=text/html; charset=EUC-KR\>

\</head\>

\<body\>

\<br /\>

\<center\>

\<table border=0 cellpadding=10 cellspacing=0\>

\<tr bgcolor=lightgray\>

\<td\>\<b\>\<font size=2 color=white\>첨부파일 내용을 확인할 수 없을 경우 : 오른쪽 마우스 클릭 -\> 인코딩 -\> 유니코드(utf-8)\</font\>\</b\>

\</td\>

\</tr\>

\</table\>

\</center\>

\<br /\>

\<br /\>

 

\<font color=blue\>\<b\>\* 당사 ERP에서 발송되는 발주서이며, 시스템 특성상 양식이 다른점 양해바랍니다.

 

\<font color=red\>\<b\>-\&nbsp;바이러스 의심시 파일을 열람하지 마시고, 우선 발주 담당자에게 연락 주시기 바랍니다.

\<br /\>\<br /\>\<br /\>

\<font color=black\>\<b\>1. Mail Information : \##TaxName## 발 주 서

\<br /\>\<br /\>\<br /\>

\<font color=black\>\<b\>2. 합계금액 : \##TotAmt##

\<br /\>\<br /\>\<br /\>

\<font color=black\>\<b\>3. 발주내역 및 납기 : 첨부 발 주 서 참조 (견적시 리스트와 순서가 다름)

\<br /\>\<br /\>\<br /\>

\<font color=black\>\<b\>3. 발주접수 후 SCM에서 발주 확인 및 납기 정보 입력 바랍니다.

\<br /\>\<br /\>\<br /\>

\##TaxName##

\<br /\>\<br /\>

\</body\>

\</html\>

'

SELECT @Payment = CASE WHEN ISNULL(@Payment,'') \<\> '' THEN @Payment ELSE '\&nbsp;' END

SELECT @html_head = REPLACE(@html_head,'##Payment##',ISNULL(@Payment,'\&nbsp;'))

--WHILE문을 통한 커서 수행, 수집된 대상의 갯수만큼

WHILE (@IDX_NO \< @Count)

BEGIN

--변수 초기화

SELECT @html = ''

SELECT @MailMsg = ''

SELECT @TaxName = '', @TaxNo = '', @Owner = '', @VATRptAddr = '', @CurrName = '',

@BizUnitName = '', @CustName = '', @CustEmpName = '', @EmpName = '',

@BizAddr = '', @TaxFAXNo = '', @TelNo = '', @CustFax = '',

@EmpEmail = '', @StrSumAmt = '', @StrSumVAT = '', @TotAmt = '',

@FactAddr = '', @DeptName = '', @Remark = '', @CustEmpTELNo = '', @CustEmpFAX = ''

SELECT @PJTNo = '', @ItemNo = '', @ItemName = '', @Spec = '', @Qty = '',

@UnitName = '', @Price = '', @CurAmt = '', @DelvDate = '', @WHName = '' ,

@MakerName = '', @Remark1 = ''

SELECT @SendUserSeq = 0, @RecvUserSeq = 0, @RecvEmpMail = '', @SendEmpMail = ''

SELECT @Subjects = '', @AttachFileName = ''

--POSeq수집

SELECT @POSeq = POSeq FROM \#TPUORDPOPrint WHERE IDX_NO = @IDX_NO

--이전구매발주코드와 현구매발주코드를 비교,

--같으면, HTML-TABLE 태그 작성 수행

--다르면, HTML 신규 태그 작성 수행

IF @PrevPOSeq \<\> @POSeq

BEGIN

--디테일 순번

SELECT @DetailCnt = 1

--이전구매발주코드가 존재하면, 곧, 신규로 생성된 알림메일 데이터가 존재한다는 의미

--이전알림메일데이터에 HTML소스 태그를 닫아준다

IF @PrevPOSeq \<\> 0

BEGIN

SELECT @html_tail_inData = ''

SELECT @html_tail_inData = @html_tail_inData + @html_tail

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##PONo##',ISNULL(@PONo,'\&nbsp;')) --구매발주번호

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##Remark##',ISNULL(@Remark,'\&nbsp;')) --마스터 - 비고

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##TaxName##',ISNULL(@TaxName,'\&nbsp;')) --사업자번호상호

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##CustEmpTELNo##',ISNULL(@CustEmpTELNo,'\&nbsp;')) --거래처 대표담당자 전화번호

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##CustEmpFAX##', ISNULL(@CustEmpFAX,'\&nbsp;')) --거래처 대표담당자 FAX번호

 

UPDATE A

-- SET SendMessage = A.SendMessage + @html_tail,

SET AttachMessage = A.AttachMessage + @html_tail_inData

FROM \#TEMP_TCASendMessage AS A

WHERE A.POSeq = @PrevPOSeq

SELECT @PONo = '', @Remark = '', @html_tail_inData = ''

END

--HTML 헤더 데이터 수집

--##TaxName## --상호

--##TaxNo## --사업자등록번호

--##Owner## --대표자명

--##VATRptAddr## --주소 - 상위

--##BizUnitName## --매입사업장

--##CurrName## --통화단위

--##CustName## --거래처명

--##CustEmpName## --대표자명

--##EmpName## --담당자

--##BizAddr## --주소

--##TaxFAXNo## --FAX - 구매정보

--##TelNo## --전화번호

--##CustFax## --FAX

--##EmpEmail## --E-Mail

--##SumAmt## --공급가액

--##SumVAT## --부가세

--##TotAmt## --합계금액 (공급가액 + 부가세)

--##FactAddr## --납품처

SELECT @PONo = CASE WHEN ISNULL(PONo,'') \<\> '' THEN ISNULL(PONo,'') ELSE '\&nbsp;' END, --구매발주번호

@PODate = CASE WHEN ISNULL(PODate,'') \<\> '' THEN LEFT(PODate,4) + '-' + RIGHT(LEFT(PODate,6),2) + '-' + RIGHT(PODate,2) ELSE '\&nbsp;' END, --구매발주일자

@Remark = CASE WHEN ISNULL(Remark,'') \<\> '' THEN ISNULL(Remark,'') ELSE '\&nbsp;' END, --마스터 - 비고

@TaxName = CASE WHEN ISNULL(TaxName,'') \<\> '' THEN ISNULL(TaxName,'') ELSE '\&nbsp;' END, --상호

@TaxNo = CASE WHEN ISNULL(TaxNo,'') \<\> '' THEN LEFT(LTRIM(RTRIM(TaxNo)),3) + '-' + RIGHT(LEFT(LTRIM(RTRIM(TaxNo)),5),2) + '-' + RIGHT(LTRIM(RTRIM(TaxNo)),5) ELSE '\&nbsp;' END, --사업자등록번호

@Owner = CASE WHEN ISNULL(Owner,'') \<\> '' THEN ISNULL(Owner,'') ELSE '\&nbsp;' END, --대표자명

@VATRptAddr = CASE WHEN ISNULL(VATRptAddr,'') \<\> '' THEN ISNULL(VATRptAddr,'') ELSE '\&nbsp;' END, --주소 - 상위

@BizUnitName = CASE WHEN ISNULL(BizUnitName,'') \<\> ''THEN ISNULL(BizUnitName,'') ELSE '\&nbsp;' END, --매입사업장

@CurrName = CASE WHEN ISNULL(CurrName,'') \<\> '' THEN ISNULL(CurrName,'') ELSE '\&nbsp;' END, --통화단위

@CustName = CASE WHEN ISNULL(CustName,'') \<\> '' THEN ISNULL(CustName,'') ELSE '\&nbsp;' END, --거래처명

@CustEmpName = CASE WHEN ISNULL(CustEmpName,'') \<\> ''THEN ISNULL(CustEmpName,'') ELSE '\&nbsp;' END, --대표자명

@EmpName = CASE WHEN ISNULL(EmpName,'') \<\> '' THEN ISNULL(EmpName,'') ELSE '\&nbsp;' END, --담당자

@BizAddr = CASE WHEN ISNULL(BizAddr,'') \<\> '' THEN ISNULL(BizAddr,'') ELSE '\&nbsp;' END, --주소

@TaxFAXNo = CASE WHEN ISNULL(TaxFAXNo,'') \<\> '' THEN ISNULL(TaxFAXNo,'') ELSE '\&nbsp;' END, --FAX - 구매정보

@TelNo = CASE WHEN ISNULL(TelNo,'') \<\> '' THEN ISNULL(TelNo,'') ELSE '\&nbsp;' END, --전화번호

@CustFax = CASE WHEN ISNULL(CustFax,'') \<\> '' THEN ISNULL(CustFax,'') ELSE '\&nbsp;' END, --FAX

@EmpEmail = CASE WHEN ISNULL(EmpEmail,'') \<\> '' THEN ISNULL(EmpEmail,'') ELSE '\&nbsp;' END, --E-Mail

@StrSumAmt = CONVERT(NVARCHAR, ISNULL(SumAmt,0)), --공급가액

@StrSumVAT = CONVERT(NVARCHAR, ISNULL(SumVAT,0)), --부가세

@TotAmt = CONVERT(NVARCHAR, ISNULL(SumAmt,0) + ISNULL(SumVAT,0)), --합계금액 (공급가액 + 부가세)

@FactAddr = CASE WHEN ISNULL(VATRptAddr,'') \<\> '' THEN ISNULL(VATRptAddr,'') ELSE '\&nbsp;' END, --납품처

@DeptName = CASE WHEN ISNULL(DeptName,'') \<\> '' THEN ISNULL(DeptName,'') ELSE '\&nbsp;' END, --발주부서

@CustEmpTELNo= CASE WHEN ISNULL(CustEmpTELNo,'') \<\> '' THEN ISNULL(CustEmpTELNo,'') ELSE '\&nbsp;' END, --거래처 대표담당자 전화번호

@CustEmpFAX = CASE WHEN ISNULL(FAX,'') \<\> '' THEN ISNULL(FAX,'') ELSE '\&nbsp;' END --거래처 대표담당자 FAX번호

FROM \#TPUORDPOPrint

WHERE IDX_NO = @IDX_NO

-- 주석처리(기존에는 마스터 납품장소 항목에 생산사업장의 주소를 출력했으나, 행별로 창고가 다를 수 있기에 발주부서로 변경) :: 18.08.16 \#임희진

-- 납품처는 생산사업장의 주소를 보여준다 이재혁

--SELECT @FactAddr = Addr

-- FROM \_TDAFactUnit AS A LEFT OUTER JOIN \_TDAWH AS B ON A.FactUnit = B.FactUnit

-- AND A.CompanySeq = B.CompanySeq

-- JOIN \#TPUORDPOPrint AS C ON C.WHSeq = B.WHSeq

-- AND C.IDX_NO = @IDX_NO

-- WHERE A.CompanySeq = @CompanySeq

--반복 데이터 수집

SELECT @PJTNo = CASE WHEN ISNULL(PJTNo,'') \<\> '' THEN ISNULL(PJTNo,'') ELSE '\&nbsp;' END, --프로젝트번호

@ItemNo = CASE WHEN ISNULL(ItemNo,'') \<\> '' THEN ISNULL(ItemNo,'') ELSE '\&nbsp;' END, --품번

@ItemName = CASE WHEN ISNULL(ItemName,'') \<\> '' THEN ISNULL(ItemName,'') ELSE '\&nbsp;' END, --품명

@Spec = CASE WHEN ISNULL(Spec,'') \<\> '' THEN ISNULL(Spec,'') ELSE '\&nbsp;' END, --규격

@Qty = CONVERT(NVARCHAR, ISNULL(Qty,0)), --발주수량

@UnitName = CASE WHEN ISNULL(UnitName,'') \<\> '' THEN ISNULL(UnitName,'') ELSE '\&nbsp;' END, --단위

@Price = CONVERT(NVARCHAR, ISNULL(Price,0)), --단가

@CurAmt = CONVERT(NVARCHAR, ISNULL(CurAmt,0)), --금액

@DelvDate = CASE WHEN ISNULL(DelvDate,'') \<\> '' THEN SUBSTRING(DelvDate, 1, 4) + '-' + SUBSTRING(DelvDate, 5, 2) + '-' + SUBSTRING(DelvDate, 7, 2) ELSE '\&nbsp;' END, --납기예정일

@MakerName = CASE WHEN ISNULL(MakerName,'') \<\> '' THEN ISNULL(MakerName,'') ELSE '\&nbsp;' END,

@Remark1 = CASE WHEN ISNULL(Remark1,'') \<\> '' THEN ISNULL(Remark1,'') ELSE '\&nbsp;' END,

@WHName = CASE WHEN ISNULL(WHName,'') \<\> '' THEN ISNULL(WHName,'') ELSE '\&nbsp;' END --납품처

FROM \#TPUORDPOPrint

WHERE IDX_NO = @IDX_NO

--메일 전송 정보

SELECT @SendUserSeq = @UserSeq, --발신자

@RecvUserSeq = 0, --수신자

-- @RecvEmpMail = '', --수신자메일

-- @SendEmpMail = '' --발신자메일

-- --테스트메일주소 입력

@RecvEmpMail = EmpEmail + '; ' + CustEmail, --수신자메일

-- @RecvEmpMail = 'sdlee@ksystem.co.kr', --수신자메일

@SendEmpMail = EmpEmail --발신자메일

-- @SendEmpMail = 'sdlee@ksystem.co.kr' --발신자메일

FROM \#TPUORDPOPrint

WHERE IDX_NO = @IDX_NO

--SELECT @Subjects = '발주번호 \[' + ISNULL(@PONo,'9999') + '\]의 구매발주서 입니다.'

SELECT @Subjects = ' 이티에스 - 발주 등록이 완료 되었습니다. 발주번호 \[' + ISNULL(@PONo,'9999') + '\]'

 

SELECT @AttachFileName = ISNULL(@PONo,'9999') + '.html'

-- SELECT @html = ''

SELECT @html = @html + @html_head --HTML 헤더 초기화

SELECT @MailMsg = @MailMsg + @html_msg --HTML 헤더 초기화

SELECT @MailMsg = REPLACE(@MailMsg,'##PONo##',ISNULL(@PONo,'\&nbsp;')) --발주번호

SELECT @MailMsg = REPLACE(@MailMsg,'##TaxName##',ISNULL(@TaxName,'\&nbsp;')) --상호

SELECT @MailMsg = REPLACE(@MailMsg,'##TotAmt##',ISNULL(@TotAmt,'\&nbsp;')) -- 24.04.19 SJPARK

--HTML 헤더 데이터 삽입

SELECT @html = REPLACE(@html,'##PONo##', ISNULL(@PONo,'\&nbsp;')) --구매발주번호

SELECT @html = REPLACE(@html,'##PODate##', ISNULL(@PODate,'\&nbsp;')) --발주일자

SELECT @html = REPLACE(@html,'##TaxName##', ISNULL(@TaxName,'\&nbsp;')) --상호

SELECT @html = REPLACE(@html,'##TaxNo##', ISNULL(@TaxNo,'\&nbsp;')) --사업자등록번호

SELECT @html = REPLACE(@html,'##Owner##', ISNULL(@Owner,'\&nbsp;')) --대표자명

SELECT @html = REPLACE(@html,'##VATRptAddr##', ISNULL(@VATRptAddr,'\&nbsp;')) --주소 - 상위

SELECT @html = REPLACE(@html,'##BizUnitName##', ISNULL(@BizUnitName,'\&nbsp;')) --매입사업장

SELECT @html = REPLACE(@html,'##CurrName##', ISNULL(@CurrName,'\&nbsp;')) --통화단위

SELECT @html = REPLACE(@html,'##CustName##', ISNULL(@CustName,'\&nbsp;')) --거래처명

SELECT @html = REPLACE(@html,'##CustEmpName##', ISNULL(@CustEmpName,'\&nbsp;')) --대표자명

SELECT @html = REPLACE(@html,'##EmpName##', ISNULL(@EmpName,'\&nbsp;')) --담당자

SELECT @html = REPLACE(@html,'##BizAddr##', ISNULL(@BizAddr,'\&nbsp;')) --주소

SELECT @html = REPLACE(@html,'##TaxFAXNo##', ISNULL(@TaxFAXNo,'\&nbsp;')) --FAX - 구매정보

SELECT @html = REPLACE(@html,'##TelNo##', ISNULL(@TelNo,'\&nbsp;')) --전화번호

SELECT @html = REPLACE(@html,'##CustFax##', ISNULL(@CustFax,'\&nbsp;')) --FAX

SELECT @html = REPLACE(@html,'##EmpEmail##', ISNULL(@EmpEmail,'\&nbsp;')) --E-Mail

SELECT @html = REPLACE(@html,'##SumAmt##', CASE WHEN ISNULL(@StrSumAmt,'') \<\> '' THEN dbo.\_FnCOMCurrency(@StrSumAmt, @CurAmtPoint) ELSE '\&nbsp;' END) --공급가액

SELECT @html = REPLACE(@html,'##SumVAT##', CASE WHEN ISNULL(@StrSumVAT,'') \<\> '' THEN dbo.\_FnCOMCurrency(@StrSumVAT, @CurAmtPoint) ELSE '\&nbsp;' END) --부가세

SELECT @html = REPLACE(@html,'##TotAmt##', CASE WHEN ISNULL(@TotAmt,'') \<\> '' THEN dbo.\_FnCOMCurrency(@TotAmt, @CurAmtPoint) ELSE '\&nbsp;' END) --합계금액 (공급가액 + 부가세)

--SELECT @html = REPLACE(@html,'##FactAddr##', ISNULL(@FactAddr,'\&nbsp;')) --납품처

SELECT @html = REPLACE(@html,'##DeptName##', ISNULL(@DeptName,'\&nbsp;')) --발주부서

--반복데이터 삽입

SELECT @html = @html + '\<tr\>'

SELECT @html = @html + '\<td class="left"\>' + CONVERT(NVARCHAR, @DetailCnt) + '\</td\>'

--SELECT @html = @html + '\<td colspan="2"\>' + ISNULL(@PJTNo,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" class="dotline"\>' + ISNULL(@ItemNo,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" class="dotline"\>' + ISNULL(@ItemName,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td class="dotline"\>' + ISNULL(@UnitName,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" align="right" class="dotline"\>' + CASE WHEN ISNULL(@Qty,'') \<\> '' THEN dbo.\_FnCOMCurrency(@Qty, @QtyPoint) ELSE '\&nbsp;' END + '\</td\>'

SELECT @html = @html + '\<td colspan="2" align="right" class="dotline"\>' + CASE WHEN ISNULL(@Price,'') \<\> '' THEN dbo.\_FnCOMCurrency(@Price, @PricePoint) ELSE '\&nbsp;' END + '\</td\>'

SELECT @html = @html + '\<td colspan="3" class="dotline"\>' + ISNULL(@MakerName,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\</tr\>

\<tr\>

\<td class="left"\>\&nbsp;\</td\>

\<td colspan="2"\>\&nbsp;\</td\>

\<td colspan="2"\>' + ISNULL(@Spec,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td\>\&nbsp;\</td\> \<td colspan="2" align="center"\>' + ISNULL(@DelvDate,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" align="right"\>' + CASE WHEN ISNULL(@CurAmt,'') \<\> '' THEN dbo.\_FnCOMCurrency(@CurAmt, @CurAmtPoint) ELSE '\&nbsp;' END + '\</td\>'

-- SELECT @html = @html + '\<td align=left\>\<font size=2\>' + ISNULL(@WHName,'\&nbsp;') + '\</font\>\</td\>'

SELECT @html = @html + '\<td colspan="3"\>' + ISNULL(@Remark1,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\</tr\>'

--신규로 생성된 값을 메일 전송 임시 테이블에 저장

INSERT INTO \#TEMP_TCASendMessage

SELECT @POSeq AS POSeq,

0 AS CompanySeq,

0 AS SendMessageID,

@SendUserSeq AS SendUserSeq,

3 AS SendMessageType,

@Subjects AS Subject,

@MailMsg AS SendMessage,

@RecvUserSeq AS RecvUserSeq,

'0' AS IsMemo,

'1' AS IsMail,

'0' AS IsSMS,

'' AS SendMemo,

0 AS SendMail,

'' AS MemoDateTime,

GETDATE() AS MailDateTime,

'' AS SMSDateTime,

'' AS SendSMS,

'PU\_'+ CONVERT(NVARCHAR(10),@POSeq) AS TblKey,

GETDATE() AS CrDateTime,

@RecvEmpMail AS RecvEmail,

'' AS RecvTelNo,

'' AS LoginPwd,

'' AS States,

0 AS Result,

'' AS EmailPass,

@html AS AttachMessage,

@AttachFileName AS AttachFileName,

@SendEmpMail AS SendEmail,

'' AS CCEmail

IF @@ERROR \<\> 0 BEGIN RETURN END -- 에러가 발생하면 리턴

--이전구매발주코드를 현구매발주코드로 갱신

SELECT @PrevPOSeq = @POSeq

END

--PrevPOSeq와 POSeq 비교하여, 같으면 테이블에 행만 추가

ELSE

BEGIN

--디테일 순번

SELECT @DetailCnt = @DetailCnt + 1

--반복 데이터 수집

SELECT @PONo = CASE WHEN ISNULL(PONo,'') \<\> '' THEN ISNULL(PONo,'') ELSE '\&nbsp;' END, --구매발주번호

@Remark = CASE WHEN ISNULL(Remark,'') \<\> '' THEN ISNULL(Remark,'') ELSE '\&nbsp;' END, --마스터 - 비고

@PJTNo = CASE WHEN ISNULL(PJTNo,'') \<\> '' THEN ISNULL(PJTNo,'') ELSE '\&nbsp;' END, --프로젝트번호

@ItemNo = CASE WHEN ISNULL(ItemNo,'') \<\> '' THEN ISNULL(ItemNo,'') ELSE '\&nbsp;' END, --품번

@ItemName = CASE WHEN ISNULL(ItemName,'') \<\> '' THEN ISNULL(ItemName,'') ELSE '\&nbsp;' END, --품명

@Spec = CASE WHEN ISNULL(Spec,'') \<\> '' THEN ISNULL(Spec,'') ELSE '\&nbsp;' END, --규격

@Qty = CONVERT(NVARCHAR, ISNULL(Qty,0)), --발주수량

@UnitName = CASE WHEN ISNULL(UnitName,'') \<\> '' THEN ISNULL(UnitName,'') ELSE '\&nbsp;' END, --단위

@Price = CONVERT(NVARCHAR, ISNULL(Price,0)), --단가

@CurAmt = CONVERT(NVARCHAR, ISNULL(CurAmt,0)), --금액

@DelvDate = CASE WHEN ISNULL(DelvDate,'') \<\> '' THEN LEFT(DelvDate,4) + '-' + RIGHT(LEFT(DelvDate,6),2) + '-' + RIGHT(DelvDate,2) ELSE '\&nbsp;' END, --납기예정일

@MakerName = CASE WHEN ISNULL(MakerName,'') \<\> '' THEN ISNULL(MakerName,'') ELSE '\&nbsp;' END,

@Remark1 = CASE WHEN ISNULL(Remark1,'') \<\> '' THEN ISNULL(Remark1,'') ELSE '\&nbsp;' END,

@WHName = CASE WHEN ISNULL(WHName,'') \<\> '' THEN ISNULL(WHName,'') ELSE '\&nbsp;' END, --납품처

@TaxName = CASE WHEN ISNULL(TaxName,'') \<\> '' THEN ISNULL(TaxName,'') ELSE '\&nbsp;' END, -- 사업자번호상호

@CustEmpTELNo= CASE WHEN ISNULL(CustEmpTELNo,'') \<\> '' THEN ISNULL(CustEmpTELNo,'') ELSE '\&nbsp;' END, --거래처 대표담당자 전화번호

@CustEmpFAX = CASE WHEN ISNULL(FAX,'') \<\> '' THEN ISNULL(FAX,'') ELSE '\&nbsp;' END --거래처 대표담당자 FAX번호

FROM \#TPUORDPOPrint

WHERE IDX_NO = @IDX_NO

SELECT @html = ''

SELECT @html = @html + '\<tr\>'

SELECT @html = @html + '\<td class="left"\>' + CONVERT(NVARCHAR, @DetailCnt) + '\</td\>'

--SELECT @html = @html + '\<td colspan="2"\>' + ISNULL(@PJTNo,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" class="dotline"\>' + ISNULL(@ItemNo,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" class="dotline"\>' + ISNULL(@ItemName,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td class="dotline"\>' + ISNULL(@UnitName,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" align="right" class="dotline"\>' + CASE WHEN ISNULL(@Qty,'') \<\> '' THEN dbo.\_FnCOMCurrency(@Qty, @QtyPoint) ELSE '\&nbsp;' END + '\</td\>'

SELECT @html = @html + '\<td colspan="2" align="right" class="dotline"\>' + CASE WHEN ISNULL(@Price,'') \<\> '' THEN dbo.\_FnCOMCurrency(@Price, @PricePoint) ELSE '\&nbsp;' END + '\</td\>'

SELECT @html = @html + '\<td colspan="3" class="dotline"\>' + ISNULL(@MakerName,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\</tr\>

\<tr\>

\<td class="left"\>\&nbsp;\</td\>

\<td colspan="2"\>\&nbsp;\</td\>

\<td colspan="2"\>' + ISNULL(@Spec,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td\>\&nbsp;\</td\> \<td colspan="2" align="center"\>' + ISNULL(@DelvDate,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\<td colspan="2" align="right"\>' + CASE WHEN ISNULL(@CurAmt,'') \<\> '' THEN dbo.\_FnCOMCurrency(@CurAmt, @CurAmtPoint) ELSE '\&nbsp;' END + '\</td\>'

-- SELECT @html = @html + '\<td align=left\>\<font size=2\>' + ISNULL(@WHName,'\&nbsp;') + '\</font\>\</td\>'

SELECT @html = @html + '\<td colspan="3"\>' + ISNULL(@Remark1,'\&nbsp;') + '\</td\>'

SELECT @html = @html + '\</tr\>'

--추가될 HTML-TABLE 태그를 갱신한다.

UPDATE A

-- SET SendMessage = A.SendMessage + @html,

SET AttachMessage = A.AttachMessage + @html

FROM \#TEMP_TCASendMessage AS A

WHERE A.POSeq = @POSeq

IF @@ERROR \<\> 0 BEGIN RETURN END -- 에러가 발생하면 리턴

END --::IF @PrevPOSeq \<\> @POSeq

--다음 커서 이동

SELECT @IDX_NO = @IDX_NO + 1

END --::WHILE (@IDX_NO \< @Count)

--추가될 HTML-TABLE 태그를 갱신한다.

SELECT @html_tail_inData = ''

SELECT @html_tail_inData = @html_tail_inData + @html_tail

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##PONo##', ISNULL(@PONo,'\&nbsp;')) --구매발주번호

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##Remark##', ISNULL(@Remark,'\&nbsp;')) --마스터 - 비고

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##TaxName##', ISNULL(@TaxName,'\&nbsp;')) --사업자번호상호

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##CustEmpTELNo##',ISNULL(@CustEmpTELNo,'\&nbsp;')) --거래처 대표담당자 전화번호

SELECT @html_tail_inData = REPLACE(@html_tail_inData,'##CustEmpFAX##', ISNULL(@CustEmpFAX,'\&nbsp;')) --거래처 대표담당자 FAX번호

UPDATE A

-- SET SendMessage = A.SendMessage + @html,

SET AttachMessage = A.AttachMessage + @html_tail_inData

FROM \#TEMP_TCASendMessage AS A

WHERE A.POSeq = @PrevPOSeq

END --::IF @Count \> 0

SELECT \* FROM \#TEMP_TCASendMessage

RETURN

go

begin tran

 

exec ets_SPUORDPOListSendMail @xmlDocument=N'\<ROOT\>

\<DataBlock1\>

\<WorkingTag /\>

\<IDX_NO\>1\</IDX_NO\>

\<DataSeq\>1\</DataSeq\>

\<Status\>0\</Status\>

\<Selected\>0\</Selected\>

\<Email\>sjpark@sysware.co.kr\</Email\>

\<POSeq\>1308\</POSeq\>

\<TABLE_NAME\>DataBlock1\</TABLE_NAME\>

\</DataBlock1\>

\</ROOT\>',@xmlFlags=2,@ServiceSeq=9328,@WorkingTag=N'',@CompanySeq=1,@LanguageSeq=1,@UserSeq=1000230,@PgmSeq=11090

 

rollback tran

색 지정 : \<font color=red\>

 

굵게 : \<b\>

 

공백 : &nbsp