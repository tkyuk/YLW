---
title: 보정 처리
date: 2024-04-24
tags: [erp, 개발, SQL]
---

# 보정 처리

> --2022.08.29 비율 합계가 100으로 반영될 수 있도록 보정 by.sykim2018
>
> INSERT INTO \#TempDiffRate(ItemSeq,DiffRate)
>
> SELECT ItemSeq, 100.00-SUM(ProdRate)
>
> FROM \#Result
>
> GROUP BY ItemSeq

 

 

 

UPDATE A

> SET ProdRate = ProdRate + DiffRate
>
> FROM \#Result AS A
>
> JOIN \#TempDiffRate AS B ON A.ItemSeq = B.ItemSeq
>
> WHERE A.IDX = 1

DROP PROC dbo.\_SWESMZItemListQuery_TTEST

GO

CREATE PROC dbo.\_SWESMZItemListQuery_TTEST

@ServiceSeq INT = 0,

@WorkingTag NVARCHAR(10)= '',

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 0,

@PgmSeq INT = 0,

@IsTransaction BIT = 0

AS

 

DECLARE @CostUnit INT,

@SMCostDiv INT,

@RptUnit INT,

@SMCostMng INT,

@CostMngAmdSeq INT,

@PlanYear NCHAR(4),

@ItemName NVARCHAR(200),

@ItemNo NVARCHAR(100),

@ItemSeq INT ,

@CostKeySeq INT,

@CostYMFr NCHAR(6),

@CostYMTo NCHAR(6),

@AssetSeq INT,

@CostAccSeq INT,

@TempCount INT,

@WorkOrderNo NVARCHAR(30),

@IsLotCost NCHAR(1),

@IsUseProc NCHAR(1),

@ItemClassSeq INT ,

@ItemClassSeqKind INT ,

@UseCost INT ,

@FSDomainSeq INT ,

@DicMat NVARCHAR(200),

@DicLabor NVARCHAR(200),

@DicExp NVARCHAR(200),

@DicOut NVARCHAR(200),

@StartCostKey INT ,

@EndCostKey INT

SELECT @CostUnit = ISNULL(A.CostUnit , 0 ),

@RptUnit = ISNULL(A.RptUnit , 0 ),

@SMCostMng = ISNULL(A.SMCostMng , 0 ),

@CostMngAmdSeq = ISNULL(A.CostMngAmdSeq, 0 ),

@PlanYear = ISNULL(A.PlanYear , N''),

@ItemName = ISNULL(A.ItemName , N''),

@ItemNo = ISNULL(A.ItemNo , N''),

@ItemSeq = ISNULL(A.ItemSeq , 0 ), --제품별제조원가상세조회 하단시트에서 품목별조회 2020.02.27 hylim

@AssetSeq = ISNULL(A.AssetSeq , 0 ),

@CostYMFr = ISNULL(A.CostYMFr , N''),

@CostYMTo = ISNULL(A.CostYMTo , N''),

@CostAccSeq = ISNULL(A.CostAccSeq , 0 ),

@WorkOrderNo = ISNULL(A.WorkOrderNo , N''),

@ItemClassSeq = ISNULL(A.ItemClassSeq , 0 ),

@ItemClassSeqKind = ISNULL(A.ItemClassKind, 0 )

FROM \#BIZ_IN_DataBlock1 AS A

 

SELECT @ItemName = CASE @ItemName WHEN N'' THEN @ItemName ELSE @ItemName + N'%' END,

@ItemNo = CASE @ItemNo WHEN N'' THEN @ItemNo ELSE @ItemNo + N'%' END,

@WorkOrderNo = CASE @WorkOrderNo WHEN N'' THEN @WorkOrderNo ELSE @WorkOrderNo + N'%' END,

@IsLotCost = N'0',

@IsUseProc = N'0',

@FSDomainSeq = 11,

@DicMat = ISNULL((SELECT ISNULL(WordSite, Word) FROM \_TCADictionary WITH(NOLOCK) WHERE WordSeq = 8342 AND LanguageSeq = @LanguageSeq), N'재료비'),

@DicLabor = ISNULL((SELECT ISNULL(WordSite, Word) FROM \_TCADictionary WITH(NOLOCK) WHERE WordSeq = 4716 AND LanguageSeq = @LanguageSeq), N'노무비'),

@DicExp = ISNULL((SELECT ISNULL(WordSite, Word) FROM \_TCADictionary WITH(NOLOCK) WHERE WordSeq = 3750 AND LanguageSeq = @LanguageSeq), N'경비'),

@DicOut = ISNULL((SELECT ISNULL(WordSite, Word) FROM \_TCADictionary WITH(NOLOCK) WHERE WordSeq = 7463 AND LanguageSeq = @LanguageSeq), N'외주비')

 

--개별원가사용여부

EXEC dbo.\_SWCOMEnv @CompanySeq, 5508, 0/\*@UserSeq\*/, @@PROCID, @IsLotCost OUTPUT

IF @IsLotCost IS NULL OR @IsLotCost = N''

SELECT @IsLotCost = N'0'

 

--사용원가선택

EXEC dbo.\_SWCOMEnv @CompanySeq, 5531, 0/\*@UserSeq\*/, @@PROCID, @UseCost OUTPUT

 

IF @UseCost = 5518002 -- 2017.06.23 sjjin 활동기준원가를 사용할 경우 공정별원가를 사용하는 것임

BEGIN

SELECT @IsUseProc = '1'

END

ELSE

BEGIN

SELECT @IsUseProc = '0'

END

-- 임시테이블 선언

CREATE TABLE \#TESMDCostKey

(

CostKeySeq INT,

CostYM NCHAR(6)

)

CREATE TABLE \#WorkOrderNo

(

WorkOrderSeq INT

)

CREATE TABLE \#InQty

(

WorkOrderSeq INT ,

ItemSeq INT ,

ProdQty DECIMAL(19, 5) ,

InQty DECIMAL(19, 5) ,

> InAmt                        DECIMAL(19, 5) ,

IsSum NCHAR(1)

)

CREATE TABLE \#TempDiffRate

(

ItemSeq INT,

DiffRate         DECIMAL(19,5)

)

CREATE TABLE \#TempItem

(

ItemSeq INT,

UMItemClassSSeq INT

)

CREATE TABLE \#ItemClass

(

ItemClassSSeq INT,

ItemClassMSeq INT,

ItemClassLSeq INT,

ItemClasSName NVARCHAR(200), --2018.11.22 hylim NVARCHAR(100)-\>NVARCHAR(200) 변경

ItemClasMName NVARCHAR(200), --2018.11.22 hylim NVARCHAR(100)-\>NVARCHAR(200) 변경

ItemClasLName NVARCHAR(200) --2018.11.22 hylim NVARCHAR(100)-\>NVARCHAR(200) 변경

)

 

INSERT INTO \#ItemClass(

ItemClassSSeq , ItemClassMSeq , ItemClassLSeq ,

ItemClasSName , ItemClasMName , ItemClasLName )

SELECT SM.MinorSeq , ISNULL(MM.MinorSeq , N''), ISNULL(LM.MinorSeq , N''),

SM.MinorName , ISNULL(MM.MinorName, N''), ISNULL(LM.MinorName, N'')

FROM \_TDAUMinor AS SM WITH(NOLOCK)

JOIN \_TDAUMinorValue AS MS WITH(NOLOCK) ON SM.MinorSeq = MS.MinorSeq

AND MS.Serl = 1001

AND SM.CompanySeq = MS.CompanySeq

LEFT OUTER JOIN \_TDAUMinor AS MM WITH(NOLOCK) ON MS.ValueSeq = MM.MinorSeq

AND MM.MajorSeq = 2002

AND SM.CompanySeq = MM.CompanySeq

LEFT OUTER JOIN \_TDAUMinorValue AS LS WITH(NOLOCK) ON MS.ValueSeq = LS.MinorSeq

AND LS.Serl = 2001

AND SM.CompanySeq = LS.CompanySeq

LEFT OUTER JOIN \_TDAUMinor AS LM WITH(NOLOCK) ON LS.ValueSeq = LM.MinorSeq

AND LM.MajorSeq = 2003

AND SM.CompanySeq = LM.CompanySeq

WHERE SM.MajorSeq = 2001

AND SM.CompanySeq = @CompanySeq

 

INSERT INTO \#TempItem

SELECT C.ItemSeq ,

ISNULL(I.ItemClassSSeq,0) AS UMItemClassSSeq

FROM \_TDAItem AS C WITH(NOLOCK)

LEFT OUTER JOIN \_TDAItemAsset AS D WITH(NOLOCK) ON C.AssetSeq = D.AssetSeq

AND C.CompanySeq = D.CompanySeq

LEFT OUTER JOIN \_TDAITemClass AS IC WITH(NOLOCK) ON C.ItemSeq = IC.ItemSeq --2021.10.29 hylim 품목소분류가 아닌 자재소분류로 잘못연결되더라도 조회는 가능하도록수정

AND C.CompanySeq = IC.CompanySeq

AND IC.UMajorItemClass = 2001

LEFT OUTER JOIN \#ItemClass AS I WITH(NOLOCK) ON IC.UMItemClass = I.ItemClassSSeq

WHERE (@ItemNo = N'' OR C.ItemNo LIKE @ItemNo )

AND (@ItemName = N'' OR C.ItemName LIKE @ItemName )

AND (@AssetSeq = 0 OR D.SMAssetGrp = @AssetSeq ) --2019.04.03 hylim 재고자산종류는 제품/반제품 대분류로 수정 --C.AssetSeq = @AssetSeq

AND ( @ItemClassSeqKind = 0 OR @ItemClassSeq = 0 OR

( @ItemClassSeqKind = 2001 AND I.ItemClassSSeq = @ItemClassSeq)

OR (@ItemClassSeqKind = 2002 AND I.ItemClassMSeq = @ItemClassSeq)

OR (@ItemClassSeqKind = 2003 AND I.ItemClassLSeq = @ItemClassSeq)

)

AND D.SMAssetGrp IN (6008002, 6008004)

AND (@ItemSeq = 0 OR C.ItemSeq = @ItemSeq) --2020.02.27 hylim \[제품별제조원가상세조회\] 하단시트조회추가

AND C.Companyseq = @CompanySeq

 

CREATE INDEX IdX1_Itemseq on \#TempItem(ItemSeq)

 

INSERT INTO \#TESMDCostKey(CostKeySeq, CostYM)

SELECT CostKeySeq, CostYM

FROM \_TESMDCostKey WITH(NOLOCK)

WHERE CostYM BETWEEN @CostYMFr AND @CostYMTo

AND SMCostMng = @SMCostMng

AND RptUnit = @RptUnit

AND CostMngAmdSeq = @CostMngAmdSeq

AND PlanYear = @PlanYear

AND CompanySeq = @CompanySeq

 

--SELECT @TempCount = @@ROWCOUNT

--===================================================================

-- 관리회계계정담기

--===================================================================

CREATE TABLE \#ESMAccount

(

SMCostKind INT ,

SMCostDiv INT ,

CostAccSeq INT ,

AccSeq INT ,

BgtSeq INT ,

UMCostType INT

)

 

EXEC \_SWESMBAccountScopeQuery 1 , 11 , 5507001 , 0

 

 

 

DECLARE @ESMBAcc TABLE

(

SMCostKind INT ,

SMCostDiv INT ,

CostAccSeq INT

)

INSERT INTO @ESMBAcc(SMCostKind, SMCostDiv, CostAccSeq)

SELECT SMCostKind, SMCostDiv, CostAccSeq

FROM \#ESMAccount

IF @ItemNo = 'TOTAL'

SELECT @ItemNo = N''

 

IF @IsLotCost = N'1' AND @WorkOrderNo \<\> N''

BEGIN

INSERT INTO \#WorkOrderNo(WorkOrderSeq)

SELECT A.WorkOrderSeq

FROM \_TESMGWorkOrderNo AS A WITH(NOLOCK)

WHERE WorkOrderNo LIKE @WorkOrderNo

AND (@CostUnit = 0 OR CostUnit = @CostUnit)

AND CompanySeq = @CompanySeq

END

--입고수량 담기

INSERT INTO \#InQty (WorkOrderSeq, ItemSeq , ProdQty , InQty ,InAmt)

SELECT A.WorkOrderSeq, A.ItemSeq , 0, SUM(A.Qty), SUM(A.Amt)

FROM (

SELECT A.CostKeySeq, A.WorkOrderSeq, A.ItemSeq, A.Qty,

ISNULL(SUM(A.Amt + A.RevAmt),0) AS Amt --사내+외주입고인 경우 입고수량 잘못조회되고있어수정 :: 2023.03.23 hylim

--2022.08.29 원가구성비율계산을 위하여 입고금액 추가 by.sykim2018 --ISNULL(A.Amt + A.RevAmt,0)

FROM \_TESMCProdFGoodInAmt AS A WITH(NOLOCK) --개별원가를 사용할 경우에만 WorkOrderSeq들어감 안쓰면 0

JOIN \#TESMDCostKey AS B ON A.CostKeySeq = B.CostKeySeq

JOIN \#TempItem AS C ON A.ItemSeq = C.ItemSeq

WHERE (@CostUnit = 0 OR CostUnit = @CostUnit)

AND A.CompanySeq = @CompanySeq

AND (@IsLotCost \<\> N'1' -- 개별원가를 사용하지 않거나

OR @IsLotCost = N'1' AND (@WorkOrderNo = N'' -- 개별원가를 사용할 경우 작업지시번호 조회조건이 입력되지 않은 경우

OR @WorkOrderNo \<\> N'' AND EXISTS (SELECT TOP 1 1 FROM \#WorkOrderNo WHERE WorkOrderSeq = A.WorkOrderSeq)) --작업지시번호 조회조건을 입력한 경우

)

GROUP BY A.CostKeySeq, A.WorkOrderSeq, A.ItemSeq, A.Qty /\*,ISNULL(A.Amt + A.RevAmt,0)\*/ --2020.12.14 hylim CostKeySeq 추가

) AS A

GROUP BY A.WorkOrderSeq, A.ItemSeq

--생산수량 담기

INSERT INTO \#InQty (WorkOrderSeq, ItemSeq , ProdQty , InQty )

SELECT A.WorkOrderSeq, A.ItemSeq , SUM(A.ProdQty), 0 --2020.12.14 hylim 생산수량가져오기 수정

FROM (

SELECT s.CostKeySeq, s.WorkOrderSeq, s.ItemSeq , s.ProdQty

FROM \_TESMCProdFGoodCostResult AS s WITH(NOLOCK) --개별원가를 사용할 경우에만 WorkOrderSeq들어감 안쓰면 0

--JOIN (

-- SELECT A.WorkOrderSeq, A.ItemSeq , A.ProcSeq

-- FROM \_TESMGWorkReport AS A WITH(NOLOCK)

-- JOIN \#TESMDCostKey AS B ON A.CostKeySeq = B.CostKeySeq

-- WHERE A.CostUnit = @CostUnit

-- AND A.IsLastProc = N'1'

-- AND A.CompanySeq = @CompanySeq

-- )AS B ON s.ItemSeq = B.ItemSeq

-- AND s.ProcSeq = B.ProcSeq

JOIN \#TESMDCostKey AS D ON s.CostKeySeq = D.CostKeySeq

JOIN \#TempItem AS I ON s.ItemSeq = I.ItemSeq

WHERE (@IsUseProc \<\> N'1'

OR @IsUseProc = N'1' AND (s.ItemSeq = s.AssyItemSeq OR s.AssyItemSeq = 0)) --최종공정 2020.08.28 hylim 활동기준원가도 최종제조원가데이터에 AssyItemSeq안들어가고있으므로수정

AND (@CostUnit = 0 OR s.CostUnit = @CostUnit)

AND s.CompanySeq = @CompanySeq

AND (@IsLotCost \<\> N'1' -- 개별원가를 사용하지 않거나

OR @IsLotCost = N'1' AND (@WorkOrderNo = N'' -- 개별원가를 사용할 경우 작업지시번호 조회조건이 입력되지 않은 경우

OR @WorkOrderNo \<\> N'' AND EXISTS (SELECT TOP 1 1 FROM \#WorkOrderNo WHERE WorkOrderSeq = s.WorkOrderSeq)) --작업지시번호 조회조건을 입력한 경우

)

GROUP BY s.CostKeySeq, s.WorkOrderSeq, s.ItemSeq , s.ProdQty

) AS A

GROUP BY A.WorkOrderSeq, A.ItemSeq

 

INSERT INTO \#InQty

SELECT WorkOrderSeq, ItemSeq , SUM(ProdQty ) , SUM(InQty), Sum(InAmt),N'1'

FROM \#InQty

GROUP BY WorkOrderSeq, ItemSeq

 

DELETE \#InQty WHERE ISNULL(IsSum, '0') = '0'

> --2021.11.01 전월재공금액(원가연월Fr), 기말재공금액(원가연월To)이 전체합이 아닌 전월/기말월에 맞춰 조회될 수 있도록 수정 by.sykim2018

--costKey는 순서대로 만들어졌다는 가정으로 시작하자.

 

SELECT @EndCostKey = MAX(CostKeySeq) ,

@StartCostKey = MIN(CostKeySeq)

FROM \#TESMDCostKey

 

 

--최종조회

SELECT IDENTITY(int,1,1)                                                         AS IDX,

> Z.AssetName AS AssetName , --품목자산분류

K.ItemClasLName AS UMItemClassLName, --품목대분류

K.ItemClasMName AS UMItemClassMName, --품목중분류

K.ItemClasSName AS UMItemClassSName, --품목소분류

B.ItemName AS ItemName , --제품번호

B.ItemNo AS ItemNo , --제품명

ISNULL(G.SMCostKindName, '') AS CostTypeName , --원가구분명(재노경) --2020.03.02 hylim 추가

C.CostAccName AS CostAccName , --관리회계계정과목

SUM(CASE WHEN @StartCostKey = A.CostKeySeq THEN ISNULL(A.PreCost,0)+ISNULL(A.RevPreCost,0) ELSE 0 END) AS PreProcAmt , --전월재공금액 --2019.01.16 hylim 수정 SUM(A.PreCost) / @TempCount

> --2021.05.12 sykim2018 전월재공금액(순투입+반제품투입)으로 조회될 수 있도록 수정

SUM(ISNULL(A.InPutCost,0)) AS Amt , --당월발생액 --2019.01.16 hylim 수정 SUM(A.InPutCost) / @TempCount

SUM(CASE WHEN @EndCostKey = A.CostKeySeq THEN ISNULL(A.ProcCost,0) + ISNULL(RevProcCost,0) ELSE 0 END) AS ProcAmt , --기말재공금액 2019.01.16 hylim 수정 SUM(A.ProcCost + RevProcCost) / @TempCount

SUM(ISNULL(A.ProdCost,0) + ISNULL(A.RevProdCost,0)) AS ProdCost , --당월제조원가 2020.11.13 hylim 당월반제품금액 합산되도록 수정 --SUM(ISNULL(A.ProdCost,0) + ISNULL(A.RevProdCost,0) - ISNULL(RevProcCost,0))

SUM(ISNULL(A.RevCost,0)) AS SubFCost , --당월반제품투입금액 --2019.01.16 hylim 수정 SUM(A.RevCost) / @TempCount

ISNULL(D.ProdQty , 0) AS ProdQty , --생산량

ISNULL(D.InQty , 0) AS InQty , --입고량

ISNULL(J.WorkOrderNo,'') AS WorkOrderNo , --작업지시번호

A.WorkOrderSeq ,

B.AssetSeq ,

K.ItemClassLSeq ,

K.ItemClassMSeq ,

K.ItemClassSSeq ,

A.ItemSeq ,

E.SMCostKind ,

A.CostAccSeq ,

> --2022.08.29 원가구성비율 추가 by.sykim2018
>
> CASE WHEN ISNULL(D.InAmt,0) = 0 THEN 0 ELSE Round(SUM(ISNULL(A.ProdCost,0) + ISNULL(A.RevProdCost,0))/ISNULL(D.InAmt,0)\*100,2) END AS ProdRate --2022.10.21 0나누기 오류 발생으로 인하여 CASE처리 by.sykim2018
>
> INTO \#Result

FROM \_TESMCProdFGoodCostResult AS A WITH(NOLOCK)

JOIN \#TempItem AS I ON A.ItemSeq = I.ItemSeq

JOIN \#TESMDCostKey AS F ON A.CostKeySeq = F.CostKeySeq

LEFT OUTER JOIN \#ItemClass AS K ON I.UMItemClassSSeq = K.ItemClassSSeq

LEFT OUTER JOIN \#InQty AS D ON A.ItemSeq = D.ItemSeq

> AND A.WorkOrderSeq = D.WorkOrderSeq --2022.11.09 개별원가 사용시에 품목으로만 연결되어 N배로 집계되고 있어 추가 by.sykim2018

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON A.ItemSeq = B.ItemSeq

AND A.CompanySeq = B.CompanySeq

LEFT OUTER JOIN \_TESMBAccount AS C WITH(NOLOCK) ON A.CostAccSeq = C.CostAccSeq

AND A.CompanySeq = C.CompanySeq

LEFT OUTER JOIN @ESMBAcc AS E ON A.CostAccSeq = E.CostAccSeq

LEFT OUTER JOIN (SELECT 5519001 AS SMCostKind,

@DicMat AS SMCostKindName ,

1 AS Sort UNION

SELECT 5519002, @DicLabor , 2 UNION

SELECT 5519003, @DicExp , 3 ) AS G ON E.SMCostKind = G.SMCostKind

LEFT OUTER JOIN \_TESMGWorkOrderNo AS J WITH(NOLOCK) ON @IsLotCost = N'1' --개별원가를 사용할 경우에만 join되도록

AND J.WorkOrderSeq = A.WorkOrderSeq

AND J.CompanySeq = @CompanySeq

LEFT OUTER JOIN \_TDAItemAsset AS Z WITH(NOLOCK) ON B.AssetSeq = Z.AssetSeq

AND B.CompanySeq = Z.CompanySeq

WHERE A.CompanySeq = 1

GROUP BY B.ItemName ,

B.ItemNo ,

A.ItemSeq ,

K.ItemClasLName,

K.ItemClasMName,

K.ItemClasSName,

K.ItemClassLSeq,

K.ItemClassMSeq,

K.ItemClassSSeq,

A.CostAccSeq ,

C.CostAccName ,

D.ProdQty ,

D.InQty ,

> D.InAmt                ,

B.AssetSeq ,

Z.AssetName ,

A.WorkOrderSeq ,

J.WorkOrderNo ,

E.SMCostKind ,

G.SMCostKindName

HAVING SUM(A.PreCost) \<\> 0 OR SUM(A.InPutCost) \<\> 0

OR SUM(ISNULL(A.ProcCost,0) + ISNULL(RevProcCost,0)) \<\> 0

OR SUM(ISNULL(A.ProdCost,0) + ISNULL(A.RevProdCost,0) - ISNULL(RevProcCost,0)) \<\> 0

OR SUM(A.RevCost) \<\> 0

ORDER BY ProdRate DESC

> --2022.08.29 비율 합계가 100으로 반영될 수 있도록 보정 by.sykim2018
>
> INSERT INTO \#TempDiffRate(ItemSeq,DiffRate)
>
> SELECT ItemSeq, 100.00-SUM(ProdRate)
>
> FROM \#Result
>
> GROUP BY ItemSeq

 

 

 

UPDATE A

> SET ProdRate = ProdRate + DiffRate
>
> FROM \#Result AS A
>
> JOIN \#TempDiffRate AS B ON A.ItemSeq = B.ItemSeq
>
> WHERE A.IDX = 1

 

> SELECT \* FROM \#Result

ORDER BY SMCostKind, CostAccName --재노경 순으로 조회 2020.03.02 hylim

 

RETURN

GO

BEGIN TRAN

DECLARE @CONST\_#BIZ_IN_DataBlock1 INT

, @CONST\_#BIZ_OUT_DataBlock1 INT

SELECT @CONST\_#BIZ_IN_DataBlock1 = 0

, @CONST\_#BIZ_OUT_DataBlock1 = 0

 

 

 

IF @CONST\_#BIZ_IN_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, CostUnit INT, CostYMFr NCHAR(6), CostYMTo NCHAR(6), RptUnit INT, SMCostMng INT, CostMngAmdSeq INT, PlanYear NCHAR(4), ItemName NVARCHAR(200), ItemNo NVARCHAR(100), ItemSeq INT, CostAccSeq INT, AssetSeq INT, AssetName NVARCHAR(100), WorkOrderNo NVARCHAR(30), ItemClassSeq INT, ItemClassKind INT

)

SET @CONST\_#BIZ_IN_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, CostUnit INT, CostYMFr NCHAR(6), CostYMTo NCHAR(6), RptUnit INT, SMCostMng INT, CostMngAmdSeq INT, PlanYear NCHAR(4), ItemName NVARCHAR(200), ItemNo NVARCHAR(100), ItemSeq INT, ProdQty DECIMAL(19, 5), InQty DECIMAL(19, 5), CostAccName NVARCHAR(100), CostAccSeq INT, PreProcAmt DECIMAL(19, 5), Amt DECIMAL(19, 5), ProcAmt DECIMAL(19, 5), ProdCost DECIMAL(19, 5), AssetSeq INT, AssetName NVARCHAR(100), SubFCost DECIMAL(19, 5), WorkOrderNo NVARCHAR(30), WorkOrderSeq INT, UMItemClassLName NVARCHAR(100), UMItemClassLSeq INT, UMItemClassMName NVARCHAR(100), UMItemClassMSeq INT, UMItemClassSName NVARCHAR(100), UMItemClassSSeq INT, ItemClassSeq INT, ItemClassKind INT, CostTypeName NVARCHAR(100), CostTypee INT, ProdRate DECIMAL(19, 5)

)

SET @CONST\_#BIZ_OUT_DataBlock1 = 1

 

END

 

DECLARE @INPUT_ERROR_MESSAGE NVARCHAR(4000)

, @INPUT_ERROR_SEVERITY INT

, @INPUT_ERROR_STATE INT

, @INPUT_ERROR_PROCEDURE NVARCHAR(128)

INSERT INTO \#BIZ_IN_DataBlock1 (WorkingTag, IDX_NO, DataSeq, Selected, Status, Result, ROW_IDX, IsChangedMst, TABLE_NAME, CostUnit, CostYMFr, CostYMTo, RptUnit, SMCostMng, CostMngAmdSeq, PlanYear, ItemName, ItemNo, ItemSeq, CostAccSeq, AssetSeq, AssetName, WorkOrderNo, ItemClassSeq, ItemClassKind)

SELECT N'A', 1, 1, 1, 0, NULL, NULL, N'0', N'', N'', N'202403', N'202403', N'', N'5512001', N'', N'', N'', N'', NULL, N'', N'', N'', N'', N'', N''

IF @@ERROR \<\> 0 RETURN

 

 

DECLARE @HasError NCHAR(1)

, @UseTransaction NCHAR(1)

-- 내부 SP용 파라메터

, @ServiceSeq INT

, @MethodSeq INT

, @WorkingTag NVARCHAR(10)

, @CompanySeq INT

, @LanguageSeq INT

, @UserSeq INT

, @PgmSeq INT

, @IsTransaction BIT

 

SET @HasError = N'0'

SET @UseTransaction = N'0'

 

BEGIN TRY

 

SET @ServiceSeq = 501355

--SET @MethodSeq = 1

SET @WorkingTag = N''

SET @CompanySeq = 1

SET @LanguageSeq = 1

SET @UserSeq = 27292

SET @PgmSeq = 500781

SET @IsTransaction = 1

 

-- ExecuteOrder : 1 : Start

EXEC \_SWESMZItemListQuery_TTEST

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

 

IF @@ERROR \<\> 0

BEGIN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 1 : End

 

GOTO_END:

 

END TRY

BEGIN CATCH

-- SQL 오류인 경우는 여기서 처리가 된다

IF @UseTransaction = N'1'

ROLLBACK TRAN

DECLARE @ERROR_MESSAGE NVARCHAR(4000)

, @ERROR_SEVERITY INT

, @ERROR_STATE INT

, @ERROR_PROCEDURE NVARCHAR(128)

 

SELECT @ERROR_MESSAGE = ERROR_MESSAGE()

, @ERROR_SEVERITY = ERROR_SEVERITY()

, @ERROR_STATE = ERROR_STATE()

, @ERROR_PROCEDURE = ERROR_PROCEDURE()

RAISERROR (@ERROR_MESSAGE, @ERROR_SEVERITY, @ERROR_STATE, @ERROR_PROCEDURE)

 

RETURN

END CATCH

 

-- SQL 오류를 제외한 체크로직으로 발생된 오류는 여기서 처리

IF @HasError = N'1' AND @UseTransaction = N'1'

ROLLBACK TRAN

 

 

DROP TABLE \#BIZ_IN_DataBlock1

DROP TABLE \#BIZ_OUT_DataBlock1

 

ROLLBACK TRAN