---
title: 거래처 담당자 / 거래처 대표 담당자
date: 2023-04-04
tags: [erp, 개발, SQL]
---

# 거래처 담당자 / 거래처 대표 담당자

DROP PROCEDURE dbo.djb_SWPUORDPOPrintQuery

GO

CREATE PROCEDURE dbo.djb_SWPUORDPOPrintQuery

@ServiceSeq INT = 0,

@WorkingTag NVARCHAR(10)= '',

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 0,

@PgmSeq INT = 0,

@IsTransaction BIT = 0

 

AS

EXEC \_SYMMETRICKeyOpen

 

DECLARE @POSeq INT

 

 

-- SELECT @POSeq = POSeq

-- FROM OPENXML(@docHandle, N'/ROOT/DataBlock1', @xmlFlags)

-- WITH ( POSeq INT )

 

---- 서비스 마스타 등록 생성

--CREATE TABLE \#TPUORDPOItem (WorkingTag NCHAR(1) NULL)

--EXEC \_SCAOpenXmlToTemp @xmlDocument, @xmlFlags, @CompanySeq, @ServiceSeq, 'DataBlock1', '#TPUORDPOItem'

 

 

-- 납품으로 점프시 창고 가져가기(1.창고별품목등록의 창고 2.구매단가등록의 창고 3.이전 품목별 납품창고)

--CREATE TABLE \#DelvWH

--(

-- POSeq INT,

-- POSerl INT,

-- WHSeq INT

--)

 

-- WHERE POSeq = @POSeq

-- SELECT @DeptSeq = ISNULL(A.DeptSeq, 0)

-- FROM \_TPUORDPO AS A

-- WHERE A.CompanySeq = @CompanySeq

-- AND A.POSeq = @POSeq

--

-- SELECT @BizUnit = ISNULL(BizUnit, 0),

-- @FactUnit = ISNULL(FactUnit, 0)

-- FROM \_TDADept

-- WHERE CompanySeq = @CompanySeq

-- AND DeptSeq = @DeptSeq

--1. 품목별 기본창고

-- IF EXISTS (SELECT 1 FROM \_TPUORDPOItem AS A

-- JOIN \_TDAWHItem AS C ON A.CompanySeq = C.CompanySeq

-- AND A.ItemSeq = C.ItemSeq

-- JOIN \#TPUORDPOItem AS B ON B.POSeq = A.POSeq

-- WHERE A.CompanySeq = @CompanySeq)

-- BEGIN

 

-- UPDATE \#DelvWH

-- SET WHSeq = (SELECT TOP 1 WHSeq FROM \_TDAWHItem WHERe CompanySeq = D.CompanySeq AND ItemSeq = D.ItemSeq)

-- FROM \#DelvWH AS A

-- JOIN \_TPUORDPOItem AS D ON A.POSeq = D.POSeq

-- AND A.POSerl = D.POSerl

---- JOIN \_TDAWHItem AS C ON D.CompanySeq = C.CompanySeq

---- AND D.ItemSeq = C.ItemSeq

-- WHERE D.CompanySeq = @CompanySeq

-- AND A.WHSeq = 0

-- END

---- 2. 구매단가등록의 창고

--ELSE IF EXISTS (SELECT 1 FROM \_TPUBaseBuyPriceItem AS A -- JOIN \_TPUORDPOItem AS B ON A.CompanySeq = B.CompanySeq

-- AND A.ItemSeq = B.ItemSeq

-- JOIN \#DelvWH AS C ON B.POSeq = C.POSeq

-- AND B.POSerl = C.POSerl

-- JOIN \_TPUORDPO AS D ON D.CompanySeq = @CompanySeq

-- AND B.POSeq = D.POSeq

-- AND A.CustSeq = D.CustSeq

-- WHERE A.CompanySeq = @CompanySeq

-- AND C.WHSeq = 0)

--BEGIN

 

-- UPDATE \#DelvWH

-- SET WHSeq = D.WHSeq

-- FROM \#DelvWH AS A

-- JOIN \_TPUORDPOItem AS B ON A.POSeq = B.POSeq

-- AND A.POSerl = B.POSerl

-- JOIN \_TPUORDPO AS C ON B.CompanySeq = C.CompanySeq

-- AND B.POSeq = C.POSeq

-- JOIN \_TPUBaseBuyPriceItem AS D ON B.CompanySeq = D.CompanySeq

-- AND B.ItemSeq = D.ItemSeq

-- AND C.CustSeq = D.CustSeq

-- WHERE C.PODate BETWEEN D.StartDate AND D.EndDate

-- AND B.CompanySeq = @CompanySeq

-- AND A.WHSeq = 0

--END

---- 3. 이전 품목별 납품창고

--ELSE

--BEGIN --탑엔지니어링 발주서출력 속도 개선으로 구문 변경

-- --UPDATE \#DelvWH

-- -- SET WHSeq = C.WHSeq

-- -- FROM \#DelvWH AS A

-- -- JOIN \_TPUORDPOItem AS B ON A.POSeq = B.POSeq

-- -- AND A.POSerl = B.POSerl

-- -- JOIN \_TPUDelvItem AS C ON B.CompanySeq = C.CompanySeq

-- -- AND B.ItemSeq = C.ItemSeq

-- -- WHERE B.CompanySeq = @CompanySeq

-- -- AND DelvSeq IN (SELECT DelvSeq

-- -- FROM \_TPUDelv

-- -- WHERE DelvNo IN (SELECT MAX(A.DelvNo)

-- -- FROM \_TPUDelv AS A

-- -- JOIN \_TPUDelvItem AS B ON A.CompanySeq = B.CompanySeq

-- -- AND A.DelvSeq = B.DelvSeq

-- -- JOIN \_TPUORDPOItem AS C ON B.CompanySeq = C.CompanySeq

-- -- AND B.ItemSeq = C.ItemSeq

-- -- JOIN \#DelvWH AS D ON C.POSeq = D.POSeq

-- -- AND C.POSerl = D.POSerl

-- -- WHERE A.CompanySeq = @CompanySeq

-- -- AND D.WHSeq = 0 ) )

-- UPDATE \#DelvWH

-- SET WHSeq = C.WHSeq

-- FROM \#DelvWH AS A

-- JOIN \_TPUORDPOItem AS B ON A.POSeq = B.POSeq

-- AND A.POSerl = B.POSerl

-- JOIN \_TPUDelvItem AS C ON B.CompanySeq = C.CompanySeq

-- AND B.ItemSeq = C.ItemSeq

-- JOIN (SELECT DelvSeq AS DelvSeq

-- FROM \_TPUDelv

-- WHERE DelvNo IN (SELECT MAX(A.DelvNo)

-- FROM \_TPUDelv AS A

-- JOIN \_TPUDelvItem AS B ON A.CompanySeq = B.CompanySeq

-- AND A.DelvSeq = B.DelvSeq

-- JOIN \_TPUORDPOItem AS C ON B.CompanySeq = C.CompanySeq

-- AND B.ItemSeq = C.ItemSeq

-- JOIN \#DelvWH AS D ON C.POSeq = D.POSeq

-- AND C.POSerl = D.POSerl

 

-- WHERE A.CompanySeq = @CompanySeq

-- AND D.WHSeq = 0 )

-- ) AS D ON C.DelvSeq = D.DelvSeq

-- WHERE B.CompanySeq = @CompanySeq

--END

 

--===============================================================================================================

--금액 합계 구하기

--===============================================================================================================

CREATE TABLE \#POSumAmt

(

POSeq INT,

> TotQty                        DECIMAL(19,5),

TotCurAmt DECIMAL(19,5),

TotCurVAT DECIMAL(19,5)

)

 

INSERT INTO \#POSumAmt (POSeq, TotQty, TotCurAmt, TotCurVAT) -- 2021.10.24 : 오주헌 단위테스트 Insert~Select문 수정

SELECT A.POSeq,

> SUM(ISNULL(A.Qty,0)),

SUM(ISNULL(A.CurAmt,0)),

SUM(ISNULL(A.CurVAT,0))

FROM \_TPUORDPOItem         AS A WITH(NOLOCK)

> JOIN \#BIZ_IN_DataBlock1 AS B ON A.POSeq = B.POSeq

WHERE A.CompanySeq = @CompanySeq

GROUP BY A.POSeq

--===============================================================================================================

 

> -------------------------------------------------------------------

-- 사업자이력 START 16.07.13 임희진 추가

-------------------------------------------------------------------

CREATE TABLE \#TTaxUnit (        TaxUnit                INT,

>         TaxNo                NVARCHAR(50),
>
>         TaxSerial         NCHAR(4),
>
>         TaxNoSerl         NVARCHAR(50),
>
>         FrDate                 NCHAR(8),
>
>         ToDate                 NCHAR(8),

        TaxName                NVARCHAR(100),

        Owner                NVARCHAR(100),

        BizType                NVARCHAR(50),

        BizItem                NVARCHAR(50),

        Zip                        NVARCHAR(10),

        TaxUnitAddr NVARCHAR(200),

        VATRptAddr NVARCHAR(200),

        TelNo         NVARCHAR(400),

        FaxNo         NVARCHAR(30) )

        

INSERT INTO \#TTaxUnit -- 2021.10.24 : 오주헌 단위테스트 Insert~Select문 수정

> (
>
> TaxUnit                
>
> ,TaxNo                
>
> ,TaxSerial         
>
> ,TaxNoSerl         
>
> ,FrDate                 
>
> ,ToDate                 
>
> ,TaxName                
>
> ,Owner                
>
> ,BizType                
>
> ,BizItem                
>
> ,Zip                        
>
> ,TaxUnitAddr
>
> ,VATRptAddr
>
> ,TelNo         
>
> ,FaxNo         
>
> )

SELECT A.TaxUnit,

> A.TaxNo,
>
> A.TaxSerial,'' AS TaxNoSerl,

A.FrDate,

A.ToDate,

A.TaxName,

A.Owner,

A.BizType,

A.BizItem,

A.Zip,

ISNULL(B.Addr,'') AS TaxUnitAddr,

A.VATRptAddr,

dbo.\_FWCOMDecrypt(A.TelNo,N'\_TDATaxUnitHist',N'TelNo',@CompanySeq),                         

A.FaxNo

FROM \_TDATaxUnitHist AS A WITH(NOLOCK)

> LEFT OUTER JOIN \_VWCOMAddress AS B ON A.CompanySeq = B.CompanySeq
>
> AND A.ContactAddressSeq = B.AddressSeq
>
> AND IsMin = '1'        

WHERE A.CompanySeq = @CompanySeq

UNION ALL

> SELECT A.TaxUnit,
>
> A.TaxNo,
>
> A.TaxSerial,
>
> A.TaxNoSerl,

ISNULL((SELECT CONVERT(NCHAR(8),DATEADD(DD,1,MAX(ToDate)),112) FROM \_TDATaxUnitHist WHERE CompanySeq = A.CompanySeq AND TaxUnit = A.TaxUnit),'19000101'),

'99991231',

A.TaxName,

A.Owner,

A.BizType,

A.BizItem,

A.Zip,

ISNULL(B.Addr,'') AS TaxUnitAddr,

A.VATRptAddr,

> dbo.\_FWCOMDecrypt(A.TelNo,N'\_TDATaxUnit',N'TelNo',@CompanySeq),
>
> A.FaxNo

FROM \_TDATaxUnit AS A WITH(NOLOCK)

> LEFT OUTER JOIN \_VWCOMAddress AS B ON A.CompanySeq = B.CompanySeq
>
> AND A.ContactAddressSeq = B.AddressSeq
>
> AND IsMin = '1'        

WHERE A.CompanySeq = @CompanySeq

ORDER BY A.TaxUnit, A.ToDate

DECLARE @pIsAccTax INT,                        -- 사업자단위과세제도여부

@pIsAccTaxDate NCHAR(8),        -- 사업자단위과세제도 적용시점

@pIsAccTaxUnit NCHAR(15)        -- 사업자단위과세제도 주사업자 번호

EXEC dbo.\_SWCOMEnv @CompanySeq, 4016, @UserSeq, @@PROCID, @pIsAccTax OUTPUT

EXEC dbo.\_SWCOMEnv @CompanySeq, 4017, @UserSeq, @@PROCID, @pIsAccTaxDate OUTPUT

> SELECT @pIsAccTax = ISNULL(@pIsAccTax,'0')

SELECT @pIsAccTaxDate = ISNULL(@pIsAccTaxDate,'19000101')

SELECT @pIsAccTaxUnit = ISNULL((SELECT TOP 1 TaxUnit FROM \_TDATaxUnit WITH (NOLOCK) WHERE CompanySeq = @CompanySeq AND SMTaxationType = '4128002'),0)

> -------------------------------------------------------------------

-- 사업자이력 END

-------------------------------------------------------------------

-- 사업자 번호 형식 2019-10-23 성지원 추가

DECLARE @TaxNo NVARCHAR(20)

EXEC dbo.\_SWCOMEnv @CompanySeq, 17 , @UserSeq, @@PROCID, @TaxNo OUTPUT -- 사업자등록번호 입력형태

SELECT @TaxNo = ISNULL(@TaxNo, '')

 

-- 납기일 -\> MAX 23.02.17 SJPARK

SELECT A.POSeq, MAX(A.DelvDate) AS DelvDate

INTO \#TMP_Item

FROM \_TPUORDPOItem AS A

JOIN \#BIZ_IN_DataBlock1 AS B ON A.POSeq = B.POSeq

GROUP BY A.POSeq

 

CREATE TABLE \#POData

(

IDX_NO INT IDENTITY(0, 1),

CustName NVARCHAR(100),

CustEmpName NVARCHAR(100),

PONo NVARCHAR(50),

DeptName NVARCHAR(100),

PODate NCHAR(8),

CurrName NVARCHAR(50),

> CurrUnit NVARCHAR(5),

ExRate DECIMAL(19,5),

> TotQty                        DECIMAL(19,5),                --2016.12.22 임희진 추가

TotCurAmt DECIMAL(19,5),

TotCurVAT DECIMAL(19,5),

EmpName NVARCHAR(100),

MasterRemark NVARCHAR(2000),

MasterRemarkOri NVARCHAR(2000), -- 2018.09.19 이형민 추가

CustSeq INT,

ItemNo NVARCHAR(100),

ApproReqNo NVARCHAR(50),

ItemName NVARCHAR(200),

Spec NVARCHAR(200), --2016.07.07 임희진 (100-\>200으로 수정)

UnitName NVARCHAR(50),

Qty DECIMAL(19,5),

DelvDate NCHAR(8),

Price DECIMAL(19,5),

CurAmt DECIMAL(19,5),

CurVAT DECIMAL(19,5),

WHName NVARCHAR(100),

MakerName NVARCHAR(100),

Remark1 NVARCHAR(1000), --2016.07.06 임희진 추가

TelNo NVARCHAR(400),

Email NVARCHAR(100),

POSeq INT,

POSerl INT,

CustFaxNo NVARCHAR(30), --2016.07.05 박수영 추가

Addr                        NVARCHAR(200),                --2016.07.13 임희진 추가                

MyTelNo                        NVARCHAR(400),                --2016.07.13 임희진 추가                

MyFaxNo                        NVARCHAR(30),                --2016.07.13 임희진 추가

> MyCompanyName NVARCHAR(100),                --2016.07.13 임희진 추가
>
> MyOwner                        NVARCHAR(100),                --2016.12.22 임희진 추가
>
> MyTaxUnitAddr        NVARCHAR(200),                --2016.12.22 임희진 추가

Extension NVARCHAR(100), --2017.04.28 이혜민 추가

Dummy1 NVARCHAR(100), --2017.05.10 이혜민 추가

Dummy2 NVARCHAR(100), --2017.05.10 이혜민 추가

Dummy3 NVARCHAR(100), --2017.05.10 이혜민 추가

Dummy4 NVARCHAR(100), --2017.05.10 이혜민 추가

Dummy5 NVARCHAR(100), --2017.05.10 이혜민 추가

Dummy6 NVARCHAR(100), --2017.05.10 이혜민 추가

Dummy7 DECIMAL(19,5), --2017.05.10 이혜민 추가

Dummy8 DECIMAL(19,5), --2017.05.10 이혜민 추가

Dummy9 DECIMAL(19,5), --2017.05.10 이혜민 추가

Dummy10 DECIMAL(19,5), --2017.05.10 이혜민 추가

DeptFax NVARCHAR(20), --2017.05.22 이혜민 추가

> EmpPhone NVARCHAR(400), --2018.04.28 이혜민 추가
>
> EmpEmail                 NVARCHAR(100), --2018.04.28 이혜민 추가
>
> StdUnitName                NVARCHAR(30),                --2019.01.28 성지원 추가
>
> StdUnitQty                DECIMAL(19,5),         --2019.01.28 성지원 추가

IsVAT NCHAR(1), --2019.02.19 박수영 추가

> WHAddr                        NVARCHAR(600),                --2019.06.12 박수영 추가
>
> MyBizNo                        NVARCHAR(50),                --2019.10.23 성지원 추가
>
> CustBizNo                NVARCHAR(50),                --2019.10.23 성지원 추가
>
> CustOwner                NVARCHAR(100),                --2019.10.23 성지원 추가

ItemCurAmt DECIMAL(19,5), --2020.07.21 이혜민 추가

MyTaxNoSerl         NVARCHAR(100), --2020.08.12 이혜민 추가

DeptCompanyName NVARCHAR(100), --2020.08.12 이혜민 추가

DeptTaxNoSerl NVARCHAR(100), --2020.08.12 이혜민 추가

Photo NVARCHAR(MAX), -- 로고 추가 23.02.16 SJPARK

UMJpName NVARCHAR(100), -- 직위 추가 23.02.16 SJPARK

ItemEngName NVARCHAR(100) -- 영문명 추가 23.02.16 SJPARK

)

 

 

 

 

 

 

 

 

 

 

 

 

--select IsSTD,\* From \_TDACustEmpInfo WHERE CustSeq = 42 ORDER BY LastDateTime DESC

 

--select Z.CustSeq,Q.CustSeq,Q.EmpName,Q.JpName,L.Owner,

-- CASE WHEN ISNULL(Q.CustSeq,0) \<\> 0 THEN RTRIM(ISNULL(Q.EmpName,''))+' '+RTRIM(ISNULL(Q.JpName,'')) ELSE RTRIM(ISNULL(L.Owner,'')) END AS CustEmpName -- 성+이름 + 직위 23.02.16 SJPARK

 

-- FROM \_TPUORDPO AS Z WITH(NOLOCK)

-- JOIN \_TPUORDPOItem AS A WITH(NOLOCK) ON Z.POSeq = A.POSeq

-- AND Z.CompanySeq = A.CompanySeq

-- JOIN \#BIZ_IN_DataBlock1 AS Y ON Y.POSeq = A.POSeq

-- LEFT OUTER JOIN \_TDACust AS L WITH(NOLOCK) ON Z.CompanySeq = L.CompanySeq

-- AND Z.CustSeq = L.CustSeq

-- LEFT OUTER JOIN \_TDACustEmpInfo AS Q WITH(NOLOCK) ON Z.CompanySeq = Q.CompanySeq

-- AND Z.CustSeq = Q.CustSeq

-- AND Q.IsSTD = '1'

-- AND Q.EmpSerl = (SELECT TOP 1 EmpSerl --거래처대표담당자정보 가져올때 TOP 1 Serl로 가져오도록 수정

-- FROM \_TDACustEmpInfo WITH(NOLOCK)

-- WHERE CompanySeq = @CompanySeq AND CustSeq = Z.CustSeq AND IsSTD = '1')

-- LEFT OUTER JOIN \_TDAContact AS CT WITH(NOLOCK) ON Q.ContactSeq = CT.ContactSeq

-- AND Q.CompanySeq = CT.CompanySeq

 

 

> -- 2021.10.24 : 오주헌 단위테스트 Insert~Select문 수정

INSERT INTO \#POData(

> CustName
>
> ,CustEmpName
>
> ,PONo
>
> ,DeptName
>
> ,PODate
>
> ,CurrName
>
> ,CurrUnit
>
> ,ExRate
>
> ,TotQty                        
>
> ,TotCurAmt
>
> ,TotCurVAT
>
> ,EmpName
>
> ,MasterRemark
>
> ,MasterRemarkOri
>
> ,CustSeq

,ApproReqNo

> ,ItemNo
>
> ,ItemName
>
> ,Spec
>
> ,UnitName
>
> ,Qty
>
> ,DelvDate
>
> ,Price
>
> ,CurAmt
>
> ,CurVAT
>
> ,WHName
>
> ,MakerName
>
> ,Remark1
>
> ,TelNo
>
> ,Email
>
> ,POSeq
>
> ,POSerl
>
> ,CustFaxNo
>
> ,Addr                        
>
> ,MyTelNo                        
>
> ,MyFaxNo                        
>
> ,MyCompanyName
>
> ,MyOwner                        
>
> ,MyTaxUnitAddr        
>
> ,Extension
>
> ,Dummy1
>
> ,Dummy2
>
> ,Dummy3
>
> ,Dummy4
>
> ,Dummy5
>
> ,Dummy6
>
> ,Dummy7
>
> ,Dummy8
>
> ,Dummy9
>
> ,Dummy10
>
> ,DeptFax
>
> ,EmpPhone
>
> ,EmpEmail                 
>
> ,StdUnitName                
>
> ,StdUnitQty                
>
> ,IsVAT
>
> ,WHAddr                        
>
> ,MyBizNo                        
>
> ,CustBizNo                
>
> ,CustOwner                
>
> ,ItemCurAmt
>
> ,MyTaxNoSerl        
>
> ,DeptCompanyName
>
> ,DeptTaxNoSerl

,Photo

,UMJpName

,ItemEngName

> )

SELECT DISTINCT

--Master

--IDENTITY(INT,0,1) AS IDX_NO ,

ISNULL(L.CustName,'') AS CustName , --거래처명

CASE WHEN ISNULL(Q.CustSeq,0) \<\> 0 THEN RTRIM(ISNULL(Q.EmpName,'')) ELSE RTRIM(ISNULL(L.Owner,'')) END AS CustEmpName , --거래처 대표담당자 (대표담당으로 등록된 담당자가 없을 경우 거래처 대표자로 출력되도록 수정)

ISNULL(Z.PONo,'') AS PONo , --발주번호

ISNULL(J.DeptName, '') AS DeptName , --발주부서

RTRIM(ISNULL(Z.PODate,'')) AS PODate , --발주일

ISNULL(M.CurrName,'') AS CurrName , --통화

> ISNULL(M.CurrUnit,'') AS CurrUnit , --통화표시 18.05.04 이혜민 추가

Z.ExRate AS ExRate , --환율

> S.TotQty                                 AS TotQty                 ,         --수량합계 16.12.22 임희진 추가

S.TotCurAmt AS TotCurAmt , --금액합계

S.TotCurVAT AS TotCurVAT , --부가세합계

'구매팀' +' '+ISNULL(K.EmpName,'') AS EmpName , --구매담당자

REPLACE(REPLACE(ISNULL(Z.Remark, ''),CHAR(10),''),CHAR(13),' / ') AS MasterRemark , --특이사항

ISNULL(Z.Remark, '') AS MasterRemarkOri, -- 2018.09.19 이형민 :: 특이사항(개행유지) 추가

Z.CustSeq , --거래처 내부번호

 

--Detail

'' AS ApproReqNo , --품의번호

ISNULL(TC.CustItemNo, B.ItemNo) AS ItemNo , --품번

ISNULL(TC.CustItemName, B.ItemName) AS ItemName , --품명

ISNULL(TC.CustItemSpec, B.Spec) AS Spec , --규격

ISNULL(C.UnitName,'') AS UnitName , --단위

ISNULL(A.Qty,0) AS Qty , --발주수량

--RTRIM(ISNULL(A.DelvDate,'')) AS DelvDate , --납품기일

TRIM(ISNULL(Z1.DelvDate,'')) AS DelvDate, -- 납기일 : 품목 MAX 값 23.02.16 SJPARK

ISNULL(A.Price,0) AS Price , --단가

ISNULL(A.CurAmt,0) AS CurAmt , --금액

ISNULL(A.CurVAT,0) AS CurVAT , --부가세

ISNULL(E.WHName, '') AS WHName , --창고

ISNULL(D.CustName,'') AS MakerName , --MakerName

REPLACE(REPLACE(LTRIM(RTRIM(ISNULL(A.Remark1,''))),CHAR(10),''),CHAR(13),' / ') AS Remark1, -- 비고(품목) 16.07.06 임희진 추가

--Tail

> CASE WHEN ISNULL(CT.ContactNumber,'') \<\> '' THEN dbo.\_FWCOMDecrypt(CT.ContactNumber,N'\_TDAContact',N'ContactNumber',@CompanySeq)
>
> ELSE ISNULL(dbo.\_FWCOMDecrypt(L.TelNo,N'\_TDACust',N'TelNo',@CompanySeq),'') END AS TelNo,                -- 17.01.11 임희진 (거래처 대표담당자 전화번호 없을 시 거래처 세무정보의 전화번호를 출력하도록 수정)

--ISNULL(CT.ContactNumber,'') AS TelNo , --거래처 대표담당자 전화번호

ISNULL(CT.Email,'') AS Email , --거래처 대표담당자, E-mail

A.POSeq AS POSeq,

A.POSerl AS POSerl,

L1.FAX AS CustFaxNo, -- 거래처 Fax번호 2016.07.05 박수영 추가

ISNULL(L2.Addr, '') AS Addr,                                 -- 거래처주소         16.07.13 임희진 추가

T.TelNo AS MyTelNo,                          -- 자사 전화번호 16.07.13 임희진 추가

> --T.FaxNo AS MyFaxNo,                          -- 자사 팩스번호 16.07.13 임희진 추가

CASE WHEN ISNULL(T.FaxNo, '') \<\> '' THEN ISNULL(T.FaxNo, '') --자사 팩스번호

WHEN ISNULL(X.DeptFax, '') \<\> '' THEN ISNULL(X.DeptFax, '') --근무부서 팩스번호

ELSE ISNULL(dbo.\_FWCOMDecrypt(P.Fax, N'\_TDAEmpIn', 'Fax', P.CompanySeq), '') END AS MyFaxNo , --개인 팩스번호 :: 22.12.13 우승현 수정

> T.TaxName AS MyCompanyName,                          -- 자사 상호 16.07.13 임희진 추가
>
> T.Owner                 AS MyOwner,                                         -- 자사 대표자명 16.12.22 임희진 추가
>
> T.TaxUnitAddr AS MyTaxUnitAddr,                                 -- 자사 주소 16.12.22 임희진 추가

V.Extension AS Extension, -- 사내번호 17.04.28 이혜민 추가

A.Dummy1 AS Dummy1, -- Dummy1~10 17.05.10 이혜민추가

A.Dummy2 AS Dummy2,

A.Dummy3 AS Dummy3,

A.Dummy4 AS Dummy4,

A.Dummy5 AS Dummy5,

A.Dummy6 AS Dummy6,

A.Dummy7 AS Dummy7,

A.Dummy8 AS Dummy8,

A.Dummy9 AS Dummy9,

A.Dummy10 AS Dummy10,

J.DeptFax AS DeptFax, -- 부서팩스번호 17.05.22 이혜민 추가

> -- 발신자 전화번호 18.04.28 이혜민 추가        -- \[개인인사정보등록\] 화면을 사용하지 않는 고객사가 있어 \[사원등록\]의 사내번호를 가져올 수 있도록 수정        2018.09.07 BY 이혜민 (요청번호:201808300165)         

 

-- 부서의 Tel, mail 가져오도록 수정

> --CASE WHEN ISNULL(P1.Phone,'') \<\> '' THEN dbo.\_FWCOMDecrypt(P1.Phone,N'\_TDAEmpIn',N'Phone',@CompanySeq) ELSE dbo.\_FWCOMDecrypt(P1.Extension,N'\_TDAEmpIn',N'Extension',@CompanySeq) END AS EmpPhone,
>
> --P1.Email AS EmpEmail, -- 발신자 이메일 18.04.28 이혜민 추가

dbo.\_FWCOMDecrypt(J.DeptPhone, N'\_TDADept', N'DeptPhone', @CompanySeq) AS EmpPhone,

dbo.\_FWCOMDecrypt(J.EMail , N'\_TDADept', N'EMail' , @CompanySeq) AS EmpEmail,

> U.UnitName AS StdUnitName,
>
> A.StdUnitQty AS StdUnitQty,

A.IsVAT AS IsVAT,

> ISNULL(F.Addr, '') AS WHAddr, --창고주소
>
> dbo.\_FCOMMaskConv(@TaxNo, T.TaxNo) AS MyBizNo,         --자사사업자번호
>
> dbo.\_FCOMMaskConv(@TaxNo, L.BizNo) AS CustBizNo, --거래처사업자번호
>
> L.Owner                                                         AS CustOwner, --거래처대표자

ISNULL(A.CurAmt,0) + ISNULL(A.CurVAT,0) AS ItemCurAmt, -- 품목별 공급가액+부가세

> T.TaxNoSerl                         AS MyTaxNoSerl, -- 자사 일련번호
>
> ISNULL(T1.TaxName, T2.TaxName) AS DeptCompanyName, -- 부서에 연결된 사업자 상호
>
> ISNULL(T1.TaxNoSerl,T2.TaxNoSerl) AS DeptTaxNoSerl , -- 부서에 연결된 사업자 일련번호

ISNULL(Z3.Photo ,'') AS Photo ,-- 로고 23.02.16 SJPARK

--CASE WHEN ISNULL(Q.CustSeq,0) \<\> 0 THEN RTRIM(ISNULL(Q.EmpName,''))+' '+RTRIM(ISNULL(Q.JpName,'')) ELSE RTRIM(ISNULL(L.Owner,'')) END AS CustEmpName , -- 성+이름 + 직위 23.02.16 SJPARK

-- 대표담당자 없을 경우 일반 담당자 (여러명일 경우 Min으로)

CASE WHEN ISNULL(Q.CustSeq,0) \<\> 0

THEN RTRIM(ISNULL(Q.EmpName,''))+' '+RTRIM(ISNULL(CT.PositionName, ''))

ELSE RTRIM(ISNULL(Q2.EmpName,''))+' '+RTRIM(ISNULL(CT2.PositionName, '')) END AS CustEmpName , -- 성+이름 + 직위 23.02.16 SJPARK

ISNULL(B.ItemEngName,'') -- 영문명 추가 23.02.16 SJPARK

FROM \_TPUORDPO AS Z WITH(NOLOCK)

JOIN \_TPUORDPOItem AS A WITH(NOLOCK) ON Z.POSeq = A.POSeq

AND Z.CompanySeq = A.CompanySeq

JOIN sys_TPUORDPO AS Z2 WITH(NOLOCK) ON Z2.POSeq = A.POSeq

AND Z2.CompanySeq = A.CompanySeq

LEFT OUTER JOIN djb_TDABizImage AS Z3 WITH(NOLOCK) ON Z3.CompanySeq = Z2.CompanySeq

AND Z3.BizUnit = Z2.BizUnit

AND Z3.UMImageKind = 1000573001

JOIN \#BIZ_IN_DataBlock1 AS Y ON Y.POSeq = A.POSeq

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.ItemSeq = B.ItemSeq

LEFT OUTER JOIN \_TDAUnit AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

AND A.UnitSeq = C.UnitSeq

LEFT OUTER JOIN \_TDACust AS D WITH(NOLOCK) ON A.CompanySeq = D.CompanySeq

AND A.MakerSeq = D.CustSeq

LEFT OUTER JOIN \_TDAWH AS E WITH(NOLOCK) ON A.CompanySeq = E.CompanySeq

> AND A.WHSeq = E.WHSeq

LEFT OUTER JOIN \_TDADept AS J WITH(NOLOCK) ON Z.CompanySeq = J.CompanySeq

AND Z.DeptSeq = J.DeptSeq

LEFT OUTER JOIN \_TDAEmp AS K WITH(NOLOCK) ON Z.CompanySeq = K.CompanySeq

AND Z.EmpSeq = K.EmpSeq

LEFT OUTER JOIN \_TDACust AS L WITH(NOLOCK) ON Z.CompanySeq = L.CompanySeq

AND Z.CustSeq = L.CustSeq

LEFT OUTER JOIN \_TDACurr AS M WITH(NOLOCK) ON Z.CompanySeq = M.CompanySeq

AND Z.CurrSeq = M.CurrSeq

LEFT OUTER JOIN \_TDAEmpIn AS P WITH(NOLOCK) ON Z.CompanySeq = P.CompanySeq

AND Z.EmpSeq = P.EmpSeq

LEFT OUTER JOIN \_TDACustEmpInfo AS Q WITH(NOLOCK) ON Z.CompanySeq = Q.CompanySeq

AND Z.CustSeq = Q.CustSeq

AND Q.IsSTD = '1'

AND Q.EmpSerl = (SELECT TOP 1 EmpSerl --거래처대표담당자정보 가져올때 TOP 1 Serl로 가져오도록 수정

FROM \_TDACustEmpInfo WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq AND CustSeq = Z.CustSeq AND IsSTD = '1')

LEFT OUTER JOIN \_TDACustEmpInfo AS Q2 WITH(NOLOCK) ON Z.CompanySeq = Q2.CompanySeq

AND Z.CustSeq = Q2.CustSeq

AND Q2.IsSTD = '0'

AND Q2.EmpSerl = (SELECT TOP 1 EmpSerl --거래처대표담당자정보 가져올때 TOP 1 Serl로 가져오도록 수정

FROM \_TDACustEmpInfo WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq AND CustSeq = Z.CustSeq AND IsSTD = '0')

 

 

LEFT OUTER JOIN \_TDAContact AS CT WITH(NOLOCK) ON Q.ContactSeq = CT.ContactSeq

AND Q.CompanySeq = CT.CompanySeq

LEFT OUTER JOIN \_TDAContact AS CT2 WITH(NOLOCK) ON Q2.ContactSeq = CT2.ContactSeq

AND Q2.CompanySeq = CT2.CompanySeq

LEFT OUTER JOIN \_TDADept AS R WITH(NOLOCK) ON Z.CompanySeq = R.CompanySeq

AND Z.DeptSeq = R.DeptSeq

LEFT OUTER JOIN \#POSumAmt AS S ON Z.POSeq = S.POSeq

LEFT OUTER JOIN \_TDACustAdd AS L1 WITH(NOLOCK) ON L.CustSeq = L1.CustSeq

AND L1.CompanySeq = @CompanySeq

> LEFT OUTER JOIN \#TTaxUnit AS T WITH(NOLOCK) ON T.TaxUnit = ( CASE WHEN ( @pIsAccTax = 4125002 ) AND ( Z.PODate \>= @pIsAccTaxDate ) THEN @pIsAccTaxUnit ELSE J.TaxUnit END ) --2022.10.12 우승현 수정 :: JOIN -\> LEFT OUTER JOIN
>
> AND Z.PODate BETWEEN T.FrDate AND T.ToDate
>
> LEFT OUTER JOIN \_VWCOMAddress                 AS L2                         ON L.CompanySeq = L2.CompanySeq
>
> AND L.BizAddrSeq = L2.AddressSeq
>
> AND L2.IsMin = '1'

LEFT OUTER JOIN \_VWHREmpInfoLast AS V ON V.CompanySeq = Z.CompanySeq

AND V.EmpSeq = Z.EmpSeq

> LEFT OUTER JOIN \_TDAEmpIn AS P1 WITH(NOLOCK) ON K.CompanySeq = P1.CompanySeq --발신자의 전화, 이메일 가져오기 추가 18.04.28 HMLEE
>
> AND K.EmpSeq = P1.EmpSeq
>
> LEFT OUTER JOIN \_TDAUnit                         AS U WITH(NOLOCK) ON A.CompanySeq = U.CompanySeq
>
> AND A.StdUnitSeq = U.UnitSeq                
>
> LEFT OUTER JOIN \_VWCOMAddress AS F WITH(NOLOCK) ON E.CompanySeq = F.CompanySeq

AND E.WHAddress = F.Addressseq

AND F.IsMin = '1'

> LEFT OUTER JOIN \_TDATaxUnitHist AS T1 WITH(NOLOCK) ON T1.CompanySeq = J.CompanySeq

AND T1.TaxUnit = J.TaxUnit

AND (Z.PODate BETWEEN T1.FrDate AND T1.ToDate)

LEFT OUTER JOIN \_TDATaxUnit AS T2 WITH(NOLOCK) ON T2.CompanySeq = J.CompanySeq

AND T2.TaxUnit = J.TaxUnit

LEFT OUTER JOIN \_TSLCustItem AS TC WITH(NOLOCK) ON TC.CompanySeq = Z.CompanySeq

AND TC.CustSeq = Z.CustSeq

AND TC.ItemSeq = A.ItemSeq

AND TC.UnitSeq = A.UnitSeq

LEFT OUTER JOIN \_VWHREmpInfoLast AS W WITH(NOLOCK) ON W.CompanySeq = K.CompanySeq

AND W.EmpSeq = K.EmpSeq

LEFT OUTER JOIN \_TDADept AS X WITH(NOLOCK) ON X.CompanySeq = W.CompanySeq

AND X.DeptSeq = W.WkDeptSeq

-- 납기일 임시테이블 23.02.17 SJPARK

LEFT OUTER JOIN \#TMP_Item AS Z1 ON Z1.POSeq = A.POSeq

WHERE A.CompanySeq = @CompanySeq

 

 

---------------------

----품의번호 추적 ---

---------------------

 

CREATE TABLE \#TMP_SOURCETABLE

(

IDOrder INT,

TABLENAME NVARCHAR(100)

)

CREATE TABLE \#TCOMSourceTracking

(

IDX_NO INT,

IDOrder INT,

Seq INT,

Serl INT,

SubSerl INT,

Qty DECIMAL(19, 5),

STDQty DECIMAL(19, 5),

Amt DECIMAL(19, 5),

VAT DECIMAL(19, 5)

)

 

INSERT \#TMP_SOURCETABLE -- 2021.10.24 : 오주헌 단위테스트 Insert~Select문 수정

> (
>
> IDOrder
>
> ,TABLENAME
>
> )

SELECT '1','\_TPUORDApprovalReqItem'

 

EXEC \_SWCOMSourceTracking @CompanySeq, '\_TPUORDPOItem', '#POData', 'POSeq', 'POSerl', ''

-- 품의번호 업데이트

 

UPDATE \#POData

SET ApproReqNo = ISNULL(D.ApproReqNo, '')

FROM \#POData AS A

JOIN \#TCOMSourceTracking AS C ON A.IDX_NO = C.IDX_NO

AND C.IDOrder = '1'

LEFT OUTER JOIN \_TPUORDApprovalReq AS D WITH(NOLOCK) ON D.CompanySeq = @CompanySeq

AND C.Seq = D.ApproReqSeq

 

 

 

SELECT CustName,

CustEmpName,

PONo,

PODate,

CurrName,

> CurrUnit,

DeptName,

ExRate,

> TotQty,

TotCurAmt,

EmpName,

MasterRemark,

MasterRemarkOri, -- 2018.09.19 이형민 추가

ItemNo,

ApproReqNo,

ItemName,

Spec,

UnitName,

Qty,

DelvDate,

Price,

CurAmt,

CurVAT,

WHName,

MakerName,

Remark1,

TelNo,

Email,

POSeq,

POSerl,

CustFaxNo,

Addr,

> MyTelNo,                        
>
> MyFaxNo,                        
>
> MyCompanyName,
>
> MyOwner,
>
> MyTaxUnitAddr,

Extension,

Dummy1,

Dummy2,

Dummy3,

Dummy4,

Dummy5,

Dummy6,

Dummy7,

Dummy8,

Dummy9,

Dummy10,

DeptFax,

> EmpPhone,
>
> EmpEmail,
>
> StdUnitName,
>
> StdUnitQty,

IsVAT,

> WHAddr,
>
> MyBizNo,
>
> CustBizNo,
>
> CustOwner,

ItemCurAmt,

MyTaxNoSerl,

DeptCompanyName,

DeptTaxNoSerl,

Photo,

UMJpName,

ItemEngName

FROM \#POData

ORDER BY PONo, POSerl

 

 

RETURN

거래처

 

 

 

 

CASE WHEN ISNULL(Q.CustSeq,0) \<\> 0 THEN RTRIM(ISNULL(Q.EmpName,''))+' '+RTRIM(ISNULL(Q.JpName,'')) ELSE RTRIM(ISNULL(L.Owner,'')) END AS CustEmpName , -- 성+이름 + 직위 23.02.16 SJPARK

 

 

 

 

-- 거래처 담당자 가져오는 테이블 (IsSTD = 1 =\> 대표 담당자)

LEFT OUTER JOIN \_TDACustEmpInfo AS Q WITH(NOLOCK) ON Z.CompanySeq = Q.CompanySeq

AND Z.CustSeq = Q.CustSeq

AND Q.IsSTD = '1'

AND Q.EmpSerl = (SELECT TOP 1 EmpSerl --거래처대표담당자정보 가져올때 TOP 1 Serl로 가져오도록 수정

FROM \_TDACustEmpInfo WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq AND CustSeq = Z.CustSeq AND IsSTD = '1')

 

-- 직위 가져오는 테이블

LEFT OUTER JOIN \_TDAContact AS CT WITH(NOLOCK) ON Q.ContactSeq = CT.ContactSeq

AND Q.CompanySeq = CT.CompanySeq