---
title: 비동기방식 메시지 호출
date: 2026-01-29
tags: [erp, 개발, Ace이벤트]
---

# 비동기방식 메시지 호출

-- 1. 데이터 검증 (동기 방식)

if (SSSerial.DataRowCnt == 0) then

MessageBoxShow('조회된 Serial이 없습니다.', '')

GoTo('END')

return

end

 

-- 2. 비동기 메시지 호출

WAsyncMessageBox('변경된 내용을 저장하시겠습니까?', '저장 확인', 'MsgBoxTypeYesNo')

--\<async\>function(result)

-- 사용자가 '예(1)'를 눌렀을 때만 안쪽 로직이 실행됨

if (result == 1) then

-- \[중요\] 다음 단계로 넘어가는 모든 로직을 여기에 작성하세요.

-- 예: SP 호출, 저장 메소드 실행 등

RunPgmMethod('SerialSave')

-- 성공 메시지 등도 이 안에서 처리하는 것이 안전합니다.

else

-- '아니오'를 누른 경우 필요한 처리가 있다면 여기에 작성 (없으면 비워둠)

return

end

--\</async\>

 

-- \[주의\] 이 위치(async 블록 밖)에는 아무것도 작성하지 마세요.

-- 작성하더라도 메시지 응답과 상관없이 즉시 실행됩니다.

- 아래 주석 구문 필요

--\<async\>function(result)

 

내용

 

 

--\</async\>

>