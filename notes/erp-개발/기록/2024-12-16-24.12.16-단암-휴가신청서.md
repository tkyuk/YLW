---
title: 24.12.16 단암 휴가신청서
date: 2024-12-16
tags: [erp, 개발, 기록]
---

# 24.12.16 단암 휴가신청서

DROP PROC dbo.danam_SPRWkEmpVacGetUseDays_TTEST

GO

CREATE PROC dbo.danam_SPRWkEmpVacGetUseDays_TTEST

@xmlDocument NVARCHAR(MAX) , -- 화면의 정보를 XML로 전달

@xmlFlags INT = 0 , -- 해당 XML의 TYPE

@ServiceSeq INT = 0 , -- 서비스 번호

@WorkingTag NVARCHAR(10)= '', -- 워킹 태그

@CompanySeq INT = 1 , -- 회사 번호

@LanguageSeq INT = 1 , -- 언어 번호

@UserSeq INT = 0 , -- 사용자 번호

@PgmSeq INT = 0 -- 프로그램 번호

AS

-- 사용할 변수를 선언한다.

DECLARE @docHandle INT -- XML문서를 핸들할 변수

,@WkItemSeq INT

,@EmpSeq INT

,@AppDate NCHAR(8)

,@OccurDays DECIMAL(19,5)

,@UseDays DECIMAL(19,5)

,@WkFrDate NCHAR(8)

,@WkTODate NCHAR(8)

,@WkTeamSeq INT

,@ShiftTeamSeq INT

,@WkUnitSeq INT

,@DayTypeSeq INT

,@IsHoli NCHAR(1)

,@BasicBegTm NCHAR(14)

,@BasicEndTm NCHAR(14)

,@PrevBegTm NCHAR(14)

,@PrevEndTm NCHAR(14)

,@AfterBegTm NCHAR(14)

,@AfterEndTm NCHAR(14)

,@PrevWkItemSeq INT

,@AfterWkItemSeq INT

,@TimeTerm NVARCHAR(30)

,@IsCC NCHAR(1)

,@CCSeq INT

,@YY NCHAR(4)

,@EmpYyAmtPbSeq INT

-- XML문서

EXEC sp_xml_preparedocument @docHandle OUTPUT, @xmlDocument -- 생성된 XML문서를 @docHandle로 핸들한다.

-- XML의 DataBlock1으로부터 값을 가져와 변수에 저장한다.

SELECT @WkItemSeq = ISNULL(WkItemSeq , 0),

@EmpSeq = ISNULL(EmpSeq , 0),

@AppDate = ISNULL(AppDate ,''),

@CCSeq = ISNULL(CCSeq , 0),

@WkFrDate = ISNULL(WkFrDate , ''),

@WkToDate = ISNULL(WkToDate , '')

FROM OPENXML(@docHandle, N'/ROOT/DataBlock1', @xmlFlags) -- XML의 DataBlock1으로부터

WITH (

WkItemSeq INT,

EmpSeq INT,

AppDate NCHAR(8),

CCSeq INT,

WkFrDate NCHAR(8),

WkToDate NCHAR(8)

)

IF EXISTS (SELECT \* FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq AND SMAbsWkSort = 3069002 )

BEGIN

--===========================

-- 년차발생및 사용일수

--===========================

CREATE TABLE \#YYMM

( YY NCHAR(4) NOT NULL, -- 년도

EmpSeq INT NOT NULL, -- 사원

OccurFrDate NCHAR(8) NULL, -- 발생시작일

OccurToDate NCHAR(8) NULL, -- 발생종료일

OccurDays NUMERIC(19, 5) NULL, -- 발생일수

UseDays DECIMAL(19, 5) NULL, -- 사용일수

UseFrDate NCHAR(8) NULL, -- 사용시작일

UseToDate NCHAR(8) NULL, -- 사용종료일

PayYM NCHAR(6) NULL, -- 지급월

PbSeq INT NULL, -- 급상여종류

PileDays NUMERIC(19, 5) NULL, -- 이월적치일수

SumPileDays NUMERIC(19, 5) NULL, -- 적치일수

GnerAmtYyMm NCHAR(6) NULL, -- 통상임금기준월

AddDays INT NULL, -- 추가발생일

OccurTime NUMERIC(19, 5) NULL, -- 발생시간

UseTime NUMERIC(19, 5) NULL, -- 사용시간

PileTime NUMERIC(19, 5) NULL, -- 이월적치시간

SumPileTime NUMERIC(19, 5) NULL, -- 적치시간

RevisionSeq INT NULL -- 개정여부 \#bhlee_20180815

)

CREATE CLUSTERED INDEX YYMM_index ON \#YYMM(EmpSeq) -- \#bhlee_20180815 (JOIN 조건에 필수로 사용되는 EmpSeq 는 CLUSTERED INDEX 생성)

CREATE TABLE \#Use

( YY NCHAR(4) NOT NULL, -- 년도

EmpSeq INT NOT NULL, -- 사원

UseDays NUMERIC(19, 5) NULL, -- 사용일수

UseTime NUMERIC(19, 5) NULL -- 사용시간

)

/\*===========================================================================================================================

\- \[2018.08.15 이범확\] \#bhlee_20180815

RevisionSeq, IsNotContinueSum 컬럼 추가

RevisionSeq -\> 개정적용연차 : 1, ELSE : 0

IsNotContinueSum -\> WHILE문에서 사용일 수가 발생일 수까지 카운트되면 중단 : 1, ELSE : 0

===========================================================================================================================\*/

CREATE TABLE \#temp

(

YY NCHAR(4)

,OccurDays DECIMAL(19, 5)

,EmpSeq INT

,Seq INT IDENTITY(1, 1)

,RevisionSeq INT

,IsNotContinueSum NCHAR(1) DEFAULT 1

)

CREATE CLUSTERED INDEX temp_index ON \#temp(EmpSeq) -- \#bhlee_20180815 (JOIN 조건에 필수로 사용되는 EmpSeq 는 CLUSTERED INDEX 생성)

/\*===========================================================================================================================

\- \[2018.08.15 이범확\] \#bhlee_20180815

2017년 5월 30일 이후 입사자들의 모든 결근내역을 담기위한 템프테이블

===========================================================================================================================\*/

CREATE TABLE \#EmpUseDays

(

YY NCHAR(4)

,EmpSeq INT

,UseDays DECIMAL(19, 5)

,AbsDate NCHAR(8)

,Seq INT

)

/\*===========================================================================================================================

\- \[2018.08.15 이범확\] \#bhlee_20180815

\#EmpUseDays 테이블의 데이터를 GROUP BY, ORDER BY 하여 \#EmpUseDays2 테이블에 INSERT

===========================================================================================================================\*/

CREATE TABLE \#EmpUseDays2

(

YY NCHAR(4)

,EmpSeq INT

,UseDays DECIMAL(19, 5)

,AbsDate NCHAR(8)

,Seq INT

)

CREATE CLUSTERED INDEX EmpUseDays2_index ON \#EmpUseDays2(EmpSeq) -- \#bhlee_20180815 (JOIN 조건에 필수로 사용되는 EmpSeq 는 CLUSTERED INDEX 생성)

/\*====================================================================================

\- \[2018.08.15 이범확\] \#bhlee_20180815

매월발생연차에 사용된 일자를 담는 템프테이블

=====================================================================================\*/

CREATE TABLE \#InEmpUseDays

(

YY NCHAR(4)

,EmpSeq INT

,UseDate NCHAR(8)

,UseDays DECIMAL(19, 5)

,Seq INT

)

CREATE CLUSTERED INDEX InEmpUseDays_index ON \#InEmpUseDays(EmpSeq) -- \#bhlee_20180815 (JOIN 조건에 필수로 사용되는 EmpSeq 는 CLUSTERED INDEX 생성)

IF @PgmSeq = 8287 -- 근태휴가신청(개인별) 화면인 경우2018.07.04 UPDATED BY 정영훈

BEGIN

DECLARE @UserEmpSeq INT

EXEC \_SCOMGetEssUserEmp @CompanySeq,@userSeq,@UserEmpSeq OUTPUT

SELECT @EmpSeq = ISNULL(@UserEmpSeq, 0)

END

IF @AppDate = ''

SELECT @AppDate = CONVERT(NCHAR(8),GETDATE(),112)

/\*================================================================================================================================

\- 년차발생내역

\- \[2018.08.15 이범확\] \#bhlee_20180815

2017년 5월 30일 이후 입사자들의 연차기산연도 데이터는 제외하여 담도록 수정

================================================================================================================================\*/

INSERT INTO \#YYMM(YY, EmpSeq, OccurFrDate, OccurToDate, OccurDays, UseDays, UseFrDate, UseToDate, PayYM, PbSeq, PileDays, SumPileDays, GnerAmtYyMm, AddDays, OccurTime, UseTime, PileTime, SumPileTime, RevisionSeq)

SELECT A.YY,

A.EmpSeq,

A.OccurFrDate,

A.OccurToDate,

A.OccurDays + ISNULL(A.PileDays, 0) + ISNULL(A.AddDays, 0) + ISNULL(A.ExProbOccurDays, 0) - (A.AddPileDays + A.BasePileDays),

0,

A.UseFrDate,

A.UseToDate,

A.PayYM,

A.PbSeq,

A.PileDays,

ISNULL(A.AddPileDays, 0) + ISNULL(A.BasePileDays, 0), -- 기본적치일수 + 추가적치일수

A.GnerAmtYyMm,

A.AddDays AS AddDays,

ISNULL(A.OccurTime, 0) + ISNULL(A.AddDays \* 8, 0), -- OccurTime

0, -- UseTime

ISNULL(A.PileTime, 0), -- PileTime

ISNULL(A.BasePileDays \* 8, 0) + ISNULL(A.AddPileDays \* 8, 0) -- SumPileTime

,0

FROM \_TPRWkYyEmpDays AS A WITH(NOLOCK)

LEFT OUTER JOIN \_fnAdmEmpOrd(@CompanySeq, '') AS B ON A.EmpSeq = B.EmpSeq

LEFT OUTER JOIN \_TDAEmpDate AS C ON C.CompanySeq = A.CompanySeq

AND C.EmpSeq = A.EmpSeq

AND C.SMDateType = 3054007

WHERE A.CompanySeq = @CompanySeq

AND (A.EmpSeq = @EmpSeq)

AND A.YY NOT IN(SELECT YY

FROM \_TDAEmpDate

WHERE SMDateType = 3054007

AND CompanySeq = A.CompanySeq

AND EmpSeq = A.EmpSeq

AND EmpDate \>= '20170530'

AND LEFT(EmpDate, 4) = A.YY) -- 2017년 5월 30일 입사자 중 기산연도로 생성된 데이터는 제외 (아래에서 담음) \#bhlee_20180815

 

/\*================================================================================================================================

\- 년차발생내역

\- \[2018.08.15 이범확\] \#bhlee_20180815

2017년 5월 30일 이후 입사자들의 연차기산연도 데이터만 담도록 함.

================================================================================================================================\*/

-- 2017년 5월 30일(포함) 이후 입사자들의 매월발생연차를 담는다. \#bhlee_20180815

INSERT INTO \#YYMM

(YY, EmpSeq, OccurFrDate, OccurToDate, OccurDays, UseDays, UseFrDate, UseToDate, PayYM

,PbSeq, PileDays, SumPileDays, GnerAmtYyMm, AddDays, OccurTime, UseTime, PileTime, SumPileTime, RevisionSeq)

SELECT LEFT(A.YM, 4), A.EmpSeq, MIN(A.OccurFrDate), MAX(A.OccurToDate), SUM(A.OccurDays), 0, MIN(A.UseFrDate), MAX(A.UseToDate), MAX(A.PayYm)

,0, 0, 0, MAX(A.PayYm), 0, SUM(A.OccurDays)\*8, SUM(A.OccurDays)\*8, 0, 0, 1

FROM \_TPRWkYyEmpDaysExProb AS A

JOIN \_TDAEmpDate AS B ON B.CompanySeq = A.CompanySeq

AND B.EmpSeq = A.EmpSeq

AND B.SMDateType = 3054007

AND B.EmpDate \>= '20170530'

WHERE A.CompanySeq = @CompanySeq

AND (A.EmpSeq = @EmpSeq OR @EmpSeq = 0)

GROUP BY LEFT(A.YM, 4), A.EmpSeq

/\*================================================================================================================================

\- \[2018.08.23 이범확\] \#bhlee_20180823

\[입사자연차발생처리\] 화면을 사용하는 경우에도 대상자를 담을 수 있도록 수정

================================================================================================================================\*/

INSERT INTO \#YYMM

SELECT Z.YY,

Z.EmpSeq,

Z.OccurFrDate,

Z.OccurToDate,

ISNULL(Z.OccurDays, 0)+ISNULL(Z.AddDays, 0),

0,

Z.UseFrDate,

Z.UseToDate,

Z.PayYM,

Z.PbSeq,

Z.PileDays,

ISNULL(Z.BasePileDays, 0) + ISNULL(Z.AddPileDays, 0), -- 기본적치일수 + 추가적치일수

Z.GnerAmtYyMm,

Z.AddDays AS AddDays,

ISNULL(Z.OccurTime, 0) + ISNULL(Z.AddDays \* 8, 0), -- OccurTime

0, -- UseTime

ISNULL(Z.PileTime, 0), -- PileTime

ISNULL(z.BasePileDays \* 8, 0) + ISNULL(Z.AddPileDays \* 8, 0) -- SumPileTime

,1

FROM \_TPRWkYyEmpDays AS Z

WHERE EmpSeq IN (SELECT A.EmpSeq

FROM \_TPRWkYyEmpDays AS A

JOIN \_TDAEmpDate AS B ON B.CompanySeq = A.CompanySeq

AND B.EmpSeq = A.EmpSeq

AND B.SMDateType = 3054007

AND B.EmpDate \>= '20170530'

WHERE A.YY + A.ProcMM = LEFT(B.EmpDate, 6)

AND NOT EXISTS(SELECT \* FROM \_TPRWkYyEmpDaysExProb WHERE CompanySeq = A.CompanySeq AND EmpSeq = A.EmpSeq)

)

AND NOT EXISTS(SELECT \* FROM \#YYMM WHERE Z.YY = YY AND Z.EmpSeq = EmpSeq)

AND (@EmpSeq = 0 OR @EmpSeq = Z.EmpSeq)

AND Z.CompanySeq = @CompanySeq

/\*====================================================================================

\- \[2018.08.15 이범확\] \#bhlee_2018015

2017년 5월 30일 이후 입사자이나 기산연도와 일치하는 발생연도 연차가 존재하지 않는 경우 대상자에서 제외

=====================================================================================\*/

INSERT INTO \#temp(YY, OccurDays, EmpSeq, RevisionSeq)

SELECT A.YY, A.OccurDays, A.EmpSeq, A.RevisionSeq

FROM \#YYMM AS A

JOIN \_TDAEmpDate AS B ON B.CompanySeq = @CompanySeq

AND B.EmpSeq = A.EmpSeq

AND B.SMDateType = 3054007

AND B.EmpDate \>= '20170530'

ORDER BY A.EmpSeq, A.YY, A.RevisionSeq DESC

-- \#bhlee_20180815

DECLARE @Count1 INT -- 변동 카운트 값

,@Max1 INT -- 최대 카운트 값

,@PresentYear NCHAR(4) -- 현재 카운트 연도

,@NextYear NCHAR(4) -- 다음 카운트 연도

,@PresentEmpSeq INT -- 현재 카운트 사원코드

,@NextEmpSeq INT -- 다음 카운트 사원코드

SELECT @Count1 = 1

,@Max1 = (SELECT COUNT(\*) FROM \#temp)

WHILE(@Count1 \<= @Max1)

BEGIN

SELECT @PresentYear = (SELECT TOP 1 YY FROM \#temp WHERE Seq = @Count1)

,@NextYear = (SELECT TOP 1 YY FROM \#temp WHERE Seq = @Count1 + 1)

,@PresentEmpSeq = (SELECT TOP 1 EmpSeq FROM \#temp WHERE Seq = @Count1)

,@NextEmpSeq = (SELECT TOP 1 EmpSeq FROM \#temp WHERE Seq = @Count1 + 1)

IF (SELECT RevisionSeq FROM \#temp WHERE Seq = @Count1) = 1 AND -- 현재 카운트가 개정적용연차이고

(SELECT RevisionSeq FROM \#temp WHERE Seq = @Count1 + 1) = 0 AND -- 다음 카운트는 개정적용연차가 아니면서

(SELECT YY FROM \#temp WHERE Seq = @Count1) \<\> @NextYear AND -- 현재 카운트 연도가 다음 카운트 연도와 다를 때

@PresentEmpSeq = @NextEmpSeq -- 현재 카운트 사원코드와 다음 카운트 사원코드가 같으면 \#bhlee_20180910

BEGIN

UPDATE \#temp

SET IsNotContinueSum = '0'

FROM \#temp

WHERE Seq = @Count1

END

IF @PresentYear \<\> ISNULL(@NextYear, '') AND @PresentEmpSeq \<\> ISNULL(@NextEmpSeq, 0)

BEGIN

UPDATE \#temp

SET IsNotContinueSum = '0'

FROM \#temp

WHERE Seq = @Count1

END

SELECT @Count1 = @Count1 + 1

END

-- \#bhlee_20180815

INSERT INTO \#EmpUseDays(YY, EmpSeq, UseDays, AbsDate)

SELECT A.YY

,A.EmpSeq

,CASE WHEN ISNULL(B.IsHalf, '0') = '1' THEN 0.5

WHEN ISNULL(B.IsHalf, '0') = '2' THEN 0.25 ELSE 1.0 END -- 반반차 추가 24.05.23 SJPARK

,B.AbsDate

FROM \#YYMM AS A

JOIN \#temp AS A1 ON A1.EmpSeq = A.EmpSeq

JOIN \_TPRWkAbsEmp AS B WITH(NOLOCK) ON A.EmpSeq = B.EmpSeq

AND B.AbsDate \>= A.UseFrDate

AND B.AbsDate \<= A.UseToDate

AND B.CompanySeq = @CompanySeq

JOIN \_TPRWkItem AS C WITH(NOLOCK) ON B.WkItemSeq = C.WkItemSeq

AND B.CompanySeq = C.CompanySeq

AND C.SMAbsWkSort = 3069002 -- 년차

INSERT INTO \#EmpUseDays2(EmpSeq, UseDays, AbsDate, Seq)

SELECT EmpSeq, UseDays, AbsDate, ROW_NUMBER()OVER(ORDER BY EmpSeq, AbsDate)

FROM \#EmpUseDays

GROUP BY EmpSeq, AbsDate, UseDays

ORDER BY EmpSeq, ROW_NUMBER()OVER(ORDER BY EmpSeq, AbsDate)

 

-- \#bhlee_20180815

DECLARE @Count2 INT -- 변동 카운트 값

,@Max2 INT -- 최대 카운트 값

,@VarYear NCHAR(4) -- 변동 연도 값

,@MaxYear NCHAR(4) -- 최대 연도 값

SELECT @Count2 = 1

,@Max2 = (SELECT COUNT(\*) FROM \#EmpUseDays2)

,@VarYear = (SELECT MIN(YY) FROM \#YYMM WHERE RevisionSeq = 1)

,@MaxYear = (SELECT MAX(YY) FROM \#YYMM WHERE RevisionSeq = 1)

SELECT @UseDays = 0

WHILE(@VarYear \<= @MaxYear)

BEGIN

WHILE(@Count2 \<= @Max2)

BEGIN

SELECT @PresentEmpSeq = (SELECT EmpSeq FROM \#EmpUseDays2 WHERE Seq = @Count2)

,@NextEmpSeq = (SELECT EmpSeq FROM \#EmpUseDays2 WHERE Seq = @Count2 + 1)

SELECT @UseDays = CASE WHEN (SELECT OccurDays FROM \#temp WHERE EmpSeq = A.EmpSeq AND YY = @VarYear AND RevisionSeq = 1) = @UseDays AND

(SELECT IsNotContinueSum FROM \#temp WHERE EmpSeq = A.EmpSeq AND YY = @VarYear AND RevisionSeq = 1) = '1' THEN -1

WHEN @UseDays = -1 THEN -1

WHEN @UseDays \<\> - 1 THEN (CASE WHEN @UseDays + 0.5 = (SELECT OccurDays FROM \#temp WHERE EmpSeq = A.EmpSeq AND YY = @VarYear AND RevisionSeq = 1) AND

@UseDays + A.UseDays \<\> (SELECT OccurDays FROM \#temp WHERE EmpSeq = A.EmpSeq AND YY = @VarYear AND RevisionSeq = 1) THEN (CASE WHEN EXISTS(SELECT \* FROM \#YYMM WHERE EmpSeq = A.EmpSeq AND CONVERT(NCHAR(4), DATEADD(YEAR, 1, @VarYear), 112) = YY AND A.AbsDate BETWEEN UseFrDate AND UseToDate AND RevisionSeq = 0) THEN -1 -- \#bhlee_20180908

WHEN EXISTS(SELECT \* FROM \#YYMM WHERE EmpSeq = A.EmpSeq AND @VarYear = YY AND A.AbsDate BETWEEN UseFrDate AND UseToDate AND RevisionSeq = 0) THEN -1 -- \#bhlee_20180908

ELSE @UseDays + A.UseDays

END)

WHEN @UseDays + A.UseDays \<\> (SELECT OccurDays FROM \#temp WHERE EmpSeq = A.EmpSeq AND YY = @VarYear AND RevisionSeq = 1) AND

(SELECT IsNotContinueSum FROM \#temp WHERE EmpSeq = A.EmpSeq AND YY = @VarYear AND RevisionSeq = 1) = '0' THEN @UseDays + A.UseDays

ELSE @UseDays + A.UseDays

END)

ELSE @UseDays + A.UseDays

END

FROM \#EmpUseDays2 AS A

WHERE Seq = @Count2

IF @UseDays \<\> -1 -- 매월발생연차에 사용된 일자를 담는다.

BEGIN

INSERT INTO \#InEmpUseDays

SELECT @VarYear, A.EmpSeq, A.AbsDate, A.UseDays, A.Seq

FROM \#EmpUseDays2 AS A

WHERE A.Seq = @Count2

AND NOT EXISTS(SELECT \*

FROM \#InEmpUseDays

WHERE UseDate = A.AbsDate

AND EmpSeq = A.EmpSeq)

AND EXISTS (SELECT \*

FROM \#YYMM

WHERE @VarYear = YY

AND EmpSeq = A.EmpSeq -- \#bhlee_20180908

AND RevisionSeq = 1 -- \#bhlee_20180908

AND A.AbsDate BETWEEN UseFrDate AND UseToDate)

IF @@ROWCOUNT = 0

BEGIN

SELECT @UseDays = 0

END

END

-- 현재 카운트 사원코드와 다음 카운트 사원코드가 일치하지 않는 경우 사용일 수를 0으로 초기화

IF @PresentEmpSeq \<\> @NextEmpSeq

BEGIN

SELECT @UseDays = 0

END

SELECT @Count2 = @Count2 + 1

END

SELECT @UseDays = 0

,@Count2 = 0

SELECT @VarYear = CONVERT(NCHAR(4), DATEADD(YEAR, 1, @VarYear), 112)

END

--================================================================================================================================

-- 년차사용

--================================================================================================================================

INSERT INTO \#Use

(YY, EmpSeq, UseDays, UseTime)

-- 2017년 05월 30일 이후 입사자

SELECT A.YY,

A.EmpSeq,

SUM(CASE WHEN ISNULL(B.IsHalf, '0') = '1' THEN 0.5

WHEN ISNULL(B.IsHalf, '0') = '2' THEN 0.25 ELSE 1.0 END) ,

SUM(CASE WHEN ISNULL(B.IsHalf, '0') = '1' THEN 4.0

WHEN ISNULL(B.IsHalf, '0') = '2' THEN 2.0 ELSE 8.0 END)

FROM \#YYMM AS A

INNER JOIN \_TPRWkAbsEmp AS B WITH(NOLOCK) ON A.EmpSeq = B.EmpSeq

AND B.AbsDate \>= A.UseFrDate

AND B.AbsDate \<= A.UseToDate

AND B.CompanySeq = @CompanySeq

INNER JOIN \_TPRWkItem AS C WITH(NOLOCK) ON B.WkItemSeq = C.WkItemSeq

AND B.CompanySeq = C.CompanySeq

AND C.SMAbsWkSort = 3069002 -- 년차

INNER JOIN \_TDAEmpDate AS D WITH(NOLOCK)ON D.CompanySeq = @CompanySeq

AND D.EmpSeq = A.EmpSeq

AND D.SMDateType = 3054007

WHERE B.AbsDate NOT IN(SELECT AbsDate FROM \#InEmpUseDays WHERE EmpSeq = A.EmpSeq AND UseDate = B.AbsDate) -- 매월발생연차에 사용된 일자는 제외 \#bhlee_20180815

AND D.EmpDate \>= '20170530'

AND A.RevisionSeq = 0

GROUP BY A.YY, A.EmpSeq

UNION ALL

-- 2017년 05월 30일 이전 입사자

SELECT A.YY,

A.EmpSeq,

SUM(CASE WHEN ISNULL(B.IsHalf, '0') = '1' THEN 0.5

WHEN ISNULL(B.IsHalf, '0') = '2' THEN 0.25 ELSE 1.0 END) ,

SUM(CASE WHEN ISNULL(B.IsHalf, '0') = '1' THEN 4.0

WHEN ISNULL(B.IsHalf, '0') = '2' THEN 2.0 ELSE 8.0 END)

FROM \#YYMM AS A

INNER JOIN \_TPRWkAbsEmp AS B WITH(NOLOCK) ON A.EmpSeq = B.EmpSeq

AND B.AbsDate \>= A.UseFrDate

AND B.AbsDate \<= A.UseToDate

AND B.CompanySeq = @CompanySeq

INNER JOIN \_TPRWkItem AS C WITH(NOLOCK) ON B.WkItemSeq = C.WkItemSeq

AND B.CompanySeq = C.CompanySeq

AND C.SMAbsWkSort = 3069002 -- 년차

INNER JOIN \_TDAEmpDate AS D WITH(NOLOCK)ON D.CompanySeq = @CompanySeq

AND D.EmpSeq = A.EmpSeq

AND D.SMDateType = 3054007

WHERE D.EmpDate \< '20170530'

GROUP BY A.YY, A.EmpSeq

--================================================================================================================================

-- 사용일수 update

--================================================================================================================================

-- 회계기준 연차 사용일 수 업데이트

UPDATE \#YYMM

SET UseDays = ISNULL(B.UseDays, 0),

UseTime = ISNULL(B.UseTime, 0)

FROM \#YYMM AS A

INNER JOIN \#Use AS B ON A.YY = B.YY

AND A.EmpSeq = B.EmpSeq

WHERE A.RevisionSeq = 0

-- 매월발생연차 사용일 수 업데이트 \#bhlee_20180815

UPDATE \#YYMM

SET UseDays = ISNULL(B.UseDays, 0)

FROM \#YYMM AS A

JOIN (SELECT SUM(S2.UseDays) AS UseDays

,S1.EmpSeq

,S1.YY

FROM \#YYMM AS S1

JOIN \#InEmpUseDays AS S2 ON S2.YY = S1.YY

AND S2.EmpSeq = S1.EmpSeq

WHERE S1.RevisionSeq = 1

GROUP BY S1.YY, S1.EmpSeq) AS B ON B.YY = A.YY

AND B.EmpSeq = A.EmpSeq

WHERE A.RevisionSeq = 1

/\*================================================================================

\- \[2018.07.11 이범확\] \#bhlee_20180711

휴가신청기간 안에 사용가능한 연차를 모두 집계할 수 있도록 수정

================================================================================\*/

-- 발생일 수

SELECT @OccurDays = A.OccurDays + A.PileDays + A.AddDays + A.ExProbOccurDays - (A.AddPileDays + A.BasePileDays) -- \#bhlee_20180711

FROM (SELECT ISNULL(SUM(OccurDays), 0) AS OccurDays

,ISNULL(SUM(PileDays), 0) AS PileDays

,ISNULL(SUM(AddDays), 0) AS AddDays

,ISNULL(SUM(ExProbOccurDays), 0) AS ExProbOccurDays

,ISNULL(SUM(AddPileDays), 0) AS AddPileDays

,ISNULL(SUM(BasePileDays), 0) AS BasePileDays

FROM \_TPRWkYyEmpDays

WHERE CompanySeq = @CompanySeq

AND UseFrDate \<= @WkFrDate

AND UseToDate \>= @WkToDate

AND EmpSeq = @EmpSeq

AND EmpSeq IN (SELECT EmpSeq

FROM \_TDAEmpDate AS B

WHERE B.EmpSeq = EmpSeq

AND B.SMDateType = 3054007

AND B.CompanySeq = CompanySeq

AND B.EmpDate \< '20170530')) AS A

/\*============================================

\- \[2018.07.11 이범확\] \#bhlee_20180711

2017년 5월 30일 이전 입사자들의 연차 사용일 수

==============================================\*/

IF EXISTS(SELECT \* FROM \_TDAEmpDate WHERE CompanySeq = @CompanySeq AND EmpSeq = @EmpSeq AND SMDateType = 3054007 AND EmpDate \< '20170530')

BEGIN

SELECT @YY = ISNULL(YY, '')

FROM \_TPRWkYyEmpDays

WHERE CompanySeq = @CompanySeq

AND UseFrDate \<= @WkFrDate

AND UseToDate \>= @WkToDate

AND EmpSeq = @EmpSeq

-- 사용일수

SELECT @UseDays = ISNULL(SUM(CASE WHEN A.IsHalf = '1' THEN 0.5

WHEN A.IsHalf = '2' THEN 0.25 ELSE 1.0 END), 0) -- 반반차 추가 24.05.23 SJPARK

 

FROM \_TPRWkAbsEmp AS A

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND EXISTS (SELECT \*

FROM \_TPRWkItem

WHERE CompanySeq = @CompanySeq

AND WkItemSeq = A.WkItemSeq

AND SMAbsWkSort = 3069002

) -- 년차항목사용

AND EXISTS (SELECT \*

FROM \_TPRWkYyEmpDays

WHERE CompanySeq = @CompanySeq

AND EmpSeq = A.EmpSeq

AND YY = @YY

AND A.AbsDate \>= UseFrDate

AND A.AbsDate \<= UseToDate

)

AND A.EmpSeq IN (SELECT EmpSeq

FROM \_TDAEmpDate

WHERE EmpSeq = A.EmpSeq

AND SMDateType = 3054007

AND EmpDate \< '20170530')

END

/\*============================================

\- \[2018.07.11 이범확\] \#bhlee_20180711

2017년 5월 30일 이후 입사자들의 연차 사용일 수

\- \[2018.08.15 이범확\] \#bhlee_20180815

==============================================\*/

IF EXISTS(SELECT \* FROM \_TDAEmpDate WHERE CompanySeq = @CompanySeq AND EmpSeq = @EmpSeq AND SMDateType = 3054007 AND EmpDate \>= '20170530')

BEGIN

 

SELECT @OccurDays = SUM(OccurDays)

,@UseDays = SUM(UseDays)

FROM \#YYMM

WHERE UseFrDate \<= @WkFrDate

AND UseToDate \>= @WkTODate

 

END

END

ELSE IF EXISTS (SELECT \* FROM \_TPRWkAppItem WHERE CompanySeq = @CompanySeq AND SMWkAppSeq = 3122005 AND WkItemSeq = @WkItemSeq) --기타휴가

BEGIN

-- 영림원 최신패치 버전 로직 추가 24.10.28 SJPARK

CREATE TABLE \#Temp_EtcWkItem(WkItemSeq INT)

-- 대표근태항목이 등록된 경우 대표근태항목으로 발생일수를 가져온다

-- 설정 안되어 있으면 화면에서 입력된 근태항목 기준

INSERT INTO \#Temp_EtcWkItem SELECT @WkItemSeq

INSERT INTO \#Temp_EtcWkItem

SELECT WkItemSeq

FROM \_TPRWkEtcUseItem WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq AND DtlWkItemSeq = @WkItemSeq

AND WkItemSeq \<\> @WkItemSeq

 

-- 사용가능일수(발생-적치일수+이월일수)

--SELECT @OccurDays = SUM(A.OccurDays)-- - SUM(A.AddPileDays) - SUM(A.BasePileDays) + SUM(A.PileDays)

-- FROM \_TPRWkEtcEmpDays A

-- WHERE (1=1)

-- AND CompanySeq = @CompanySeq

-- AND WkItemSeq = @WkItemSeq

-- AND EmpSeq = @EmpSeq

-- AND UseFrDate \<= @WkFrDate

-- AND UseToDate \>= @WkToDate

 

-- T2의 기타휴가발생등록로 수정

SELECT @OccurDays = SUM(A.OccurDays)-- - SUM(A.AddPileDays) - SUM(A.BasePileDays) + SUM(A.PileDays)

FROM \_TPRWkEtcMng A

WHERE (1=1)

AND CompanySeq = @CompanySeq

--AND WkItemSeq = @WkItemSeq

AND WkItemSeq IN (SELECT WkItemSeq FROM \#Temp_EtcWkItem) -- 영림원 최신패치 버전 로직 추가 24.10.28 SJPARK

AND EmpSeq = @EmpSeq

AND UseFrDate \<= @WkFrDate

AND UseToDate \>= @WkToDate

/\*

-- 사용일수

SELECT @UseDays = isnull(sum(case when A.IsHalf = '1' then 0.5

when A.IsHalf = '2' then 0.25 else 1.0 end),0)

FROM \_TPRWkAbsEmp A

WHERE (1=1)

AND A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.WkItemSeq = @WkItemSeq

-- AND EXISTS (SELECT \* FROM \_TPRWkEtcEmpDays

-- WHERE (1=1)

-- AND CompanySeq = @CompanySeq

-- AND EmpSeq = A.EmpSeq

-- AND UseFrDate \<= @AppDate -- 신청기간을 포함하는

-- AND UseToDate \>= @AppDate

---- AND LEFT(Ym,4) = LEFT(@AppDate,4)

-- AND WkItemSeq = @WkItemSeq

-- AND A.AbsDate \>= UseFrDate

-- AND A.AbsDate \<= UseToDate )

 

AND EXISTS (SELECT \* FROM \_TPRWkEtcMng

WHERE (1=1)

AND CompanySeq = @CompanySeq

AND EmpSeq = A.EmpSeq

AND UseFrDate \<= @AppDate -- 신청기간을 포함하는

AND UseToDate \>= @AppDate

-- AND LEFT(Ym,4) = LEFT(@AppDate,4)

AND WkItemSeq = @WkItemSeq

AND A.AbsDate \>= UseFrDate

AND A.AbsDate \<= UseToDate )

\*/

SELECT @UseDays = SUM(A.IsHalf)

> FROM (SELECT CASE WHEN ISNULL(E.IsHalf, '0') = '1' THEN 0.5

WHEN ISNULL(E.IsHalf, '0') = '2' THEN 0.25

ELSE 1.0 END AS IsHalf

> FROM \_TPRWkAbsEmp AS E JOIN (SELECT DISTINCT A.EmpSeq, A.WkItemSeq, B.DtlWkItemSeq, A.UseFrDate, A.UseToDate
>
> FROM \_TPRWkEtcMng                        AS A
>
> JOIN \_TPRWkEtcUseItem AS B ON A.CompanySeq = B.CompanySeq

AND A.WkItemSeq = B.WkItemSeq

> WHERE A.CompanySeq = @CompanySeq
>
> AND A.EmpSeq = @EmpSeq
>
> AND A.WkItemSeq IN (SELECT WkItemSeq FROM \#Temp_EtcWkItem)
>
> AND A.UseFrDate \<= @WkFrDate
>
> AND A.UseToDate \>= @WkToDate) AS S ON E.CompanySeq = @CompanySeq
>
> AND E.WkItemSeq = S.DtlWkItemSeq

WHERE (1=1)

AND E.CompanySeq = @CompanySeq

AND E.EmpSeq = @EmpSeq

AND E.EmpSeq = S.EmpSeq

AND E.AbsDate \>= S.UseFrDate

AND E.AbsDate \<= S.UseToDate

> AND E.WkItemSeq NOT IN (SELECT WkItemSeq
>
> FROM \_THRWelCon
>
> WHERE CompanySeq = E.CompanySeq
>
> AND WkItemSeq = E.WkItemSeq
>
> AND IsMatPerCtr = '1')
>
> GROUP BY E.EmpSeq, E.WkItemSeq, E.AbsDate, E.IsHalf) AS A

END

SELECT @IsCC = '0'

IF EXISTS(SELECT \* FROM \_TPRWkAppItem WHERE CompanySeq = @CompanySeq AND SMWkAppSeq = 3122003 AND WkItemSeq = @WkItemSeq )

SELECT @IsCC = '1'

IF @CCSeq \<\> 0 AND @IsCC = '1'

BEGIN

SELECT @OccurDays = HoliDays

FROM \_THRWelCon

WHERE CompanySeq = @CompanySeq

AND ConSeq = @CCSeq

END

EXEC \_SPRWkGetEmpWkInfo

@CompanySeq , -- 법인코드

@LanguageSeq , -- 사용자

@EmpSeq , -- 사원

@AppDate , -- 신청일자

@WkUnitSeq OUTPUT,

@ShiftTeamSeq OUTPUT,

@WkTeamSeq OUTPUT,

@DayTypeSeq OUTPUT,

@IsHoli OUTPUT,

@BasicBegTm OUTPUT,

@BasicEndTm OUTPUT,

@PrevBegTm OUTPUT,

@PrevEndTm OUTPUT,

@AfterBegTm OUTPUT,

@AfterEndTm OUTPUT,

@PrevWkItemSeq OUTPUT, -- 오전반차근태

@AfterWkItemSeq OUTPUT -- 오후반차근태

IF @WkItemSeq = @PrevWkItemSeq -- 오전반차이면

SELECT @TimeTerm = RIGHT(@PrevBegTm,5) +' - ' + RIGHT(@PrevEndTm,5)

ELSE IF @WkItemSeq = @AfterWkItemSeq -- 오후반차이면

SELECT @TimeTerm = RIGHT(@AfterBegTm,5) +' - ' + RIGHT(@AfterEndTm,5)

SELECT @AppDate AS AppDate,

ISNULL(@OccurDays,0) AS OccurDays,

ISNULL(@UseDays,0) AS UseDays,

(ISNULL(@OccurDays,0) - ISNULL(@UseDays,0)) AS PossibleDays,

ISNULL(@TimeTerm,'') AS TimeTerm,

ISNULL(@IsCC,0) AS IsCC

RETURN

GO

exec danam_SPRWkEmpVacGetUseDays_TTEST @xmlDocument=N'\<ROOT\>

\<DataBlock1\>

\<WorkingTag\>A\</WorkingTag\>

\<IDX_NO\>1\</IDX_NO\>

\<Status\>0\</Status\>

\<DataSeq\>1\</DataSeq\>

\<Selected\>1\</Selected\>

\<TABLE_NAME\>DataBlock1\</TABLE_NAME\>

\<IsChangedMst\>1\</IsChangedMst\>

\<EmpSeq\>533\</EmpSeq\>

\<WkItemSeq\>64\</WkItemSeq\>

\<CCSeq /\>

\<AppDate\>20241216\</AppDate\>

\<WkFrDate\>20241216\</WkFrDate\>

\<WkToDate\>20241216\</WkToDate\>

\</DataBlock1\>

\</ROOT\>',@xmlFlags=2,@ServiceSeq=1521489,@WorkingTag=N'',@CompanySeq=1,@LanguageSeq=1,@UserSeq=1,@PgmSeq=1041590

\_SPRWkEmpLeaveGetUseDays sp 참고