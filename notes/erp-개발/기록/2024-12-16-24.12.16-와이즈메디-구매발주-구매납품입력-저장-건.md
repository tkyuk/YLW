---
title: 24.12.16 와이즈메디 구매발주 -\> 구매납품입력 저장 건
date: 2024-12-16
tags: [erp, 개발, 기록]
---

# 24.12.16 와이즈메디 구매발주 -\> 구매납품입력 저장 건

DROP PROC STSLotMngInitQtySave_MG_TTEST

GO

CREATE PROC STSLotMngInitQtySave_MG_TTEST

@pWorkingTag CHAR(1) = null,

@pAccUnit CHAR(2) = null,

@pOwnerKind CHAR(1) = null,

@pPlaceKind CHAR(1) = null,

@pPlaceCd VARCHAR(6) = null,

@pCustCd CHAR(6) = null,

@pItemCd CHAR(6) = null,

@pUnitCd CHAR(3) = null,

@pLotNo NVARCHAR(20) = null,

@pQty NUMERIC(19, 5) = null,

@pLotNoOld NVARCHAR(20) = null,

> @pDelvDate                CHAR(8)                = '',
>
> @pCustLotNo                NVARCHAR(20) = '',
>
> @pRemark                VARCHAR(1000) = '',
>
>  
>
> @pLoginId                CHAR(8) = '',
>
> @pIsSub                        CHAR(1) = '',                        -- @pIsSub \<\> '1' : Lot기초재고수량등록에서 들어옴 , @pIsSub = '1' 서브Sp로 들어옴

@pStatus NVARCHAR(10) = null OUTPUT,

@pResults NVARCHAR(3000) = null OUTPUT

AS

DECLARE @wStkYM CHAR(6), @wPlaceKind CHAR(1), @wnStkQty NUMERIC(19, 5),

@vcStatus NVARCHAR(10),

@vcResults NVARCHAR(250),

> @vcCurrDate                        CHAR(12)

 

> SELECT                @pWorkingTag        = LTRIM(RTRIM(ISNULL(@pWorkingTag,''))),
>
> @pAccUnit = LTRIM(RTRIM(ISNULL(@pAccUnit ,''))),
>
> @pOwnerKind = LTRIM(RTRIM(ISNULL(@pOwnerKind ,''))),
>
> @pPlaceKind = LTRIM(RTRIM(ISNULL(@pPlaceKind ,''))),
>
> @pPlaceCd = LTRIM(RTRIM(ISNULL(@pPlaceCd ,''))),
>
> @pCustCd = LTRIM(RTRIM(ISNULL(@pCustCd ,''))),
>
> @pItemCd = LTRIM(RTRIM(ISNULL(@pItemCd ,''))),
>
> @pUnitCd = LTRIM(RTRIM(ISNULL(@pUnitCd ,''))),
>
> @pLotNo = LTRIM(RTRIM(ISNULL(@pLotNo ,''))),
>
> @pQty = LTRIM(RTRIM(ISNULL(@pQty ,0))),
>
> @pLotNoOld = LTRIM(RTRIM(ISNULL(@pLotNoOld ,''))),
>
> @pDelvDate                = LTRIM(RTRIM(ISNULL(@pDelvDate        ,''))),
>
> @pCustLotNo                = LTRIM(RTRIM(ISNULL(@pCustLotNo,''))),
>
> @pRemark                = LTRIM(RTRIM(ISNULL(@pRemark        ,''))),
>
> @pLoginId                = LTRIM(RTRIM(ISNULL(@pLoginId        ,'')))

 

SELECT @vcStatus = 'OK', @vcResults = 'X'

 

EXEC SDAGetSystemDate @vcCurrDate OUTPUT

 

SELECT @wStkYM = MIN(StartYYMM)

FROM TSBAccStart

WHERE AccUnit = @pAccUnit

 

IF @pUnitCd = ''

SELECT @pUnitCd = StkUnitCd FROM TDAItemMaster WHERE ItemCd = @pitemCd

SELECT @wPlaceKind = '', @wPlaceKind = ''

IF LEN(LTRIM(RTRIM(@pPlaceCd))) = '2'

SELECT @wPlaceKind = 'W'

ELSE IF LEN(LTRIM(RTRIM(@pPlaceCd))) = '5'

SELECT @wPlaceKind = 'D'

ELSE IF LEN(LTRIM(RTRIM(@pPlaceCd))) = '6'

SELECT @wPlaceKind = 'C'

 

 

 

 

IF @pWorkingTag = 'A'

GOTO ADD_PROC

ELSE IF @pWorkingTag = 'U'

GOTO UPDATE_PROC

ELSE IF @pWorkingTag = 'D'

GOTO DELETE_PROC

RETURN

 

ADD_PROC:

 

IF EXISTS(SELECT TOP 1 1 FROM TTSLotMngUnit

WHERE InOutYM = @wStkYM AND LotNo = @pLotNo

AND AccUnit = @pAccUnit AND OwnerKind = @pOwnerKind

AND PlaceKind = @wPlaceKind

AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd

AND ItemCd = @pItemCd AND UnitCd = @pUnitCd)

BEGIN

 

> IF EXISTS(SELECT TOP 1 1 FROM TTSLotMngUnit
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNo
>
> AND AccUnit = @pAccUnit AND OwnerKind = @pOwnerKind
>
> AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd AND UnitBaseQty = 0)
>
> BEGIN
>
> IF EXISTS(SELECT TOP 1 1 FROM TTSLotMng_MG WHERE AccUnit = @pAccUnit AND ItemCd = @pItemCd AND LotNo = @pLotNo)
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'DUP', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'DUP', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> END

 

> INSERT TTSLotMng_MG
>
> (AccUnit,ItemCd, LotNo, DelvDate, CustLotNo, Remark, RegDate, RegEmpId, UptDate, UptEmpId)
>
> SELECT
>
> @pAccUnit, @pItemCd, @pLotNo, @pDelvDate , @pCustLotNo, @pRemark, @vcCurrDate, @pLoginId, '', ''

 

 

> IF @@ROWCOUNT = 0 OR @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> END

 

 

 

 

SELECT @pLotNoOld = @pLotNo

GOTO UPDATE_PROC

RETURN

END

ELSE

BEGIN

 

> --SELECT \* FROM TTSLotMngUnit

-- WHERE InOutYM = @wStkYM AND LotNo = @pLotNo

-- AND AccUnit = @pAccUnit AND OwnerKind = @pOwnerKind

-- AND PlaceKind = @wPlaceKind

-- AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd

-- AND ItemCd = @pItemCd AND UnitCd = @pUnitCd --AND UnitBaseQty = 0

 

> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'DUP', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'DUP', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END

END

>  

END

SELECT @wnStkQty = CASE ConvNum WHEN 0 THEN @pQty ELSE @pQty \* ConvDen / ConvNum END

FROM TDAItemUnit WHERE ItemCd = @pItemCd AND UnitCd = @pUnitCd

IF @@ROWCOUNT = 0

SELECT @wnStkQty = 0

>  
>
> IF @pIsSub \<\> '1'

BEGIN TRAN

 

> IF @pIsSub \<\> '1'
>
> BEGIN
>
> INSERT TTSLotMngUnit
>
> (InOutYM, LotNo, AccUnit, OwnerKind, PlaceKind,
>
> PlaceCd, CustCd, ItemCd, UnitCd,
>
> UnitBaseQty, UnitInQty, UnitOutQty, UnitStkQty,
>
> BaseQty, InQty, OutQty, StkQty)
>
> SELECT @wStkYM, @pLotNo, @pAccUnit, @pOwnerKind, @wPlaceKind,
>
> @pPlaceCd, @pCustCd, @pItemCd, @pUnitCd,
>
> @pQty, 0, 0, @pQty,
>
> @wnStkQty, 0, 0, @wnStkQty

 

> IF @@ROWCOUNT = 0 OR @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus, @pResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN

 

> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + + '(TTSLotMngUnit)'
>
> RETURN
>
> END

 

> END

 

> ----- TTSInOutMng 에 넣기
>
> UPDATE TTSLotMng
>
> SET BaseQty = @wnStkQty,
>
> StkQty = @wnStkQty + InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNo AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd
>
> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @vcResults = @pResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> END
>
> IF @@ROWCOUNT = 0
>
> BEGIN
>
> INSERT TTSLotMng
>
> (InOutYM, LotNo, AccUnit, OwnerKind, PlaceKind,
>
> PlaceCd, CustCd, ItemCd,
>
> BaseQty, InQty, OutQty, StkQty,
>
> BaseAmt, InAmt, OutAmt, StkAmt)
>
> SELECT
>
> @wStkYM, @pLotNo, @pAccUnit, @pOwnerKind, @wPlaceKind,
>
> @pPlaceCd, @pCustCd, @pItemCd,
>
> @wnStkQty, 0, 0, @wnStkQty,
>
> 0, 0, 0, 0

 

> IF @@ERROR \<\> 0
>
> BEGIN

 

> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + + '(TTSLotMng)'
>
> RETURN                         
>
> END
>
> END
>
> END

 

> ----- TTSLot 에 넣기
>
> UPDATE TTSLot
>
> SET CrtQty = CrtQty + @wnStkQty
>
> WHERE LotNo = @pLotNo
>
> AND ItemCd = @pItemCd
>
> IF @@ROWCOUNT = 0
>
> BEGIN
>
> INSERT TTSLot
>
> (ItemCd,LotNo,CrtDate,ValiDate,CrtQty,CrtAmt,CustCd)
>
> SELECT
>
> @pItemCd, @pLotNo, @wStkYM + '01', '', @wnStkQty, 0, @pCustCd

 

> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLot)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus , @pResults = @vcResults + '(TTSLot)'
>
> RETURN                                
>
> END
>
> END
>
> END
>
> END

 

-- TTSLotMng_MG 존재하는 경우 에러 -\> 존재하는 경우 UPDATE 24.12.16 SJPARK

> IF EXISTS(SELECT TOP 1 1 FROM TTSLotMng_MG WHERE AccUnit = @pAccUnit AND ItemCd = @pItemCd AND LotNo = @pLotNo)
>
> BEGIN

UPDATE TTSLotMng_MG

SET LotNo                = @pLotNo                ,

                 DelvDate                = @pDelvDate        ,

                 CustLotNo        = @pCustLotNo        ,

                 Remark                = @pRemark                ,

                 UptDate                = @vcCurrDate        ,

                 UptEmpId                = @pLoginId

WHERE 1=1

         AND AccUnit        = @pAccUnit

         AND ItemCd        = @pItemCd

         AND LotNo                = @pLotNo

IF @@ERROR \<\> 0

> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'DUP', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'DUP', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> END
>
> END

ELSE

BEGIN

INSERT TTSLotMng_MG

(AccUnit,ItemCd, LotNo, DelvDate, CustLotNo, Remark, RegDate, RegEmpId, UptDate, UptEmpId)

SELECT

                @pAccUnit, @pItemCd, @pLotNo, @pDelvDate , @pCustLotNo, @pRemark, @vcCurrDate, @pLoginId, '', ''

IF @@ERROR \<\> 0

BEGIN

>         IF @pIsSub \<\> '1'
>
>         BEGIN
>
>                 ROLLBACK TRAN
>
>                 EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
>                 SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
>                 RETURN
>
>         END
>
>         ELSE
>
>         BEGIN
>
>                 EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
>                 SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
>                 RETURN
>
>         END

END

END

 

 

> IF @pIsSub \<\> '1' COMMIT TRAN
>
>  
>
> IF @pIsSub \<\> '1' SELECT @vcStatus, @vcResults, @pLotNo

RETURN

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

UPDATE_PROC:

 

> IF @pIsSub \<\> '1'
>
> BEGIN

 

> IF EXISTS(SELECT 1 FROM TTSLotMngUnit
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd)

 

> SELECT @pLotNo = @pLotNo
>
> ELSE
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMngUnit)'
>
> RETURN                                                
>
> END
>
> END
>
> -- IF (@pItemCd \<\> @pItemCd) OR (@pUnitCd \<\> @pUnitCd) OR (@pLotNo \<\> @pLotNo)
>
> -- BEGIN
>
> -- IF EXISTS(SELECT 1 FROM TTSLotMngUnit
>
> -- WHERE InOutYM = @wStkYM AND LotNo = @pLotNo AND AccUnit = @pAccUnit
>
> -- AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> -- AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> -- AND ItemCd = @pItemCd AND UnitCd = @pUnitCd)
>
> -- BEGIN
>
> -- EXEC SDAErrMsg 'DUP', @pResults OUTPUT, @pStatus OUTPUT
>
> -- SELECT @pStatus, @pResults
>
> -- RETURN
>
> -- END
>
> -- END
>
> SELECT @wnStkQty = CASE ConvNum WHEN 0 THEN @pQty ELSE @pQty \* ConvDen / ConvNum END
>
> FROM TDAItemUnit WHERE ItemCd = @pItemCd AND UnitCd = @pUnitCd
>
> IF @@ROWCOUNT = 0
>
> SELECT @wnStkQty = 0

 

> IF @pIsSub \<\> '1'
>
> BEGIN TRAN

 

> UPDATE TTSLotMngUnit
>
> SET UnitBaseQty = 0, UnitStkQty = UnitInQty - UnitOutQty,
>
> BaseQty = 0, StkQty = InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd

 

 

> UPDATE TTSLotMngUnit
>
> SET UnitBaseQty = @pQty, UnitStkQty = @pQty + UnitInQty - UnitOutQty,
>
> BaseQty = @wnStkQty, StkQty = @wnStkQty + InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNo AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd

 

> IF @@ROWCOUNT = 0
>
> BEGIN
>
> INSERT TTSLotMngUnit
>
> (InOutYM, LotNo, AccUnit, OwnerKind, PlaceKind,
>
> PlaceCd, CustCd, ItemCd, UnitCd,
>
> UnitBaseQty, UnitInQty, UnitOutQty, UnitStkQty,
>
> BaseQty, InQty, OutQty, StkQty)
>
> SELECT @wStkYM, @pLotNo, @pAccUnit, @pOwnerKind, @wPlaceKind,
>
> @pPlaceCd, @pCustCd, @pItemCd, @pUnitCd,
>
> @pQty, 0, 0, @pQty,
>
> @wnStkQty, 0, 0, @wnStkQty
>
> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'UPDATEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'UPDATEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMngUnit)'
>
> RETURN                         
>
> END
>
> END
>
> END

 

> UPDATE TTSLotMng
>
> SET BaseQty = 0,
>
> StkQty = InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd

 

 

> UPDATE TTSLotMng
>
> SET BaseQty = @wnStkQty,
>
> StkQty = @wnStkQty + InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNo AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd

 

> IF @@ROWCOUNT = 0
>
> BEGIN
>
> INSERT TTSLotMng
>
> (InOutYM, LotNo, AccUnit, OwnerKind, PlaceKind,
>
> PlaceCd, CustCd, ItemCd,
>
> BaseQty, InQty, OutQty, StkQty,
>
> BaseAmt, InAmt, OutAmt, StkAmt)
>
> SELECT
>
> @wStkYM, @pLotNo, @pAccUnit, @pOwnerKind, @wPlaceKind,
>
> @pPlaceCd, @pCustCd, @pItemCd,
>
> @wnStkQty, 0, 0, @wnStkQty,
>
> 0, 0, 0, 0
>
> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'UPDATEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'UPDATEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> END
>
> END

 

> ----- TTSLot 에 넣기
>
> SELECT @wnStkQty = SUM(ISNULL(StkQty,0))
>
> FROM TTSLotMng
>
> WHERE ItemCd = @pItemCd
>
> AND LotNo = @pLotNo
>
> AND OwnerKind = '0'

 

> UPDATE TTSLot
>
> SET CrtQty = @wnStkQty
>
> WHERE LotNo = @pLotNo
>
> AND ItemCd = @pItemCd
>
> IF @@ROWCOUNT = 0
>
> BEGIN
>
> INSERT TTSLot
>
> (ItemCd,LotNo,CrtDate,ValiDate,CrtQty,CrtAmt,CustCd)
>
> SELECT
>
> @pItemCd, @pLotNo, @wStkYM + '01', '', @wnStkQty, 0, @pCustCd

 

> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLot)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLot)'
>
> RETURN
>
> END
>
> END
>
> END
>
> END

 

> IF NOT EXISTS (SELECT TOP 1 1 FROM TTSLotMng_MG WHERE AccUnit = @pAccUnit AND ItemCd = @pItemCd AND LotNo = @pLotNoOld)
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
> RETURN                                                
>
> END        
>
> END
>
>  

UPDATE TTSLotMng_MG

SET LotNo                = @pLotNo                ,

> DelvDate                = @pDelvDate        ,
>
> CustLotNo        = @pCustLotNo        ,
>
> Remark                = @pRemark                ,
>
> UptDate                = @vcCurrDate        ,
>
> UptEmpId                = @pLoginId

 

WHERE 1=1

> AND AccUnit        = @pAccUnit
>
> AND ItemCd        = @pItemCd
>
> AND LotNo                = @pLotNoOld

IF @@ERROR \<\> 0

BEGIN

> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'UPDATEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'UPDATEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END

END

 

IF @pIsSub \<\> '1' COMMIT TRAN

IF @pIsSub \<\> '1' SELECT @vcStatus, @vcResults, @pLotNo

 

RETURN

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

DELETE_PROC:

 

> IF @pIsSub \<\> '1'
>
> BEGIN

 

> IF EXISTS(SELECT 1 FROM TTSLotMngUnit
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd) AND @pIsSub \<\> '1'
>
> SELECT @pLotNo = @pLotNo
>
> ELSE
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMngUnit)'
>
> RETURN
>
> END
>
> END

 

> SELECT @wnStkQty = a.UnitBaseQty
>
> FROM TTSLotMngUnit a
>
> WHERE a.InOutYM = @wStkYM AND a.LotNo = @pLotNoOld AND a.AccUnit = @pAccUnit
>
> AND a.OwnerKind = @pOwnerKind AND a.PlaceKind = @wPlaceKind
>
> AND a.PlaceCd = @pPlaceCd AND a.CustCd = @pCustCd
>
> AND a.ItemCd = @pItemCd AND a.UnitCd = @pUnitCd

 

> IF @pIsSub \<\> '1'
>
> BEGIN TRAN

 

> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ----- Old에 대한 TTSInOutMng 에 반영

 

> UPDATE TTSLotMng
>
> SET BaseQty = 0,
>
> StkQty = InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit AND OwnerKind = @pOwnerKind
>
> AND PlaceKind = @wPlaceKind AND PlaceCd = @pPlaceCd
>
> AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd

 

> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'DELETEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'DELETEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng)'
>
> RETURN
>
> END
>
> END

 

> IF (SELECT Count(\*)
>
> FROM TTSLotMng
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit AND OwnerKind = @pOwnerKind
>
> AND PlaceKind = @wPlaceKind AND PlaceCd = @pPlaceCd
>
> AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd
>
> AND InQty = 0 AND OutQty = 0 AND StkQty = 0) \> 0
>
> BEGIN
>
> DELETE TTSLotMng
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit AND OwnerKind = @pOwnerKind
>
> AND PlaceKind = @wPlaceKind AND PlaceCd = @pPlaceCd
>
> AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd
>
> END

 

> UPDATE TTSLotMngUnit
>
> SET UnitBaseQty = 0, UnitStkQty = UnitInQty - UnitOutQty ,
>
> BaseQty = 0, StkQty = InQty - OutQty
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd
>
> IF @@ROWCOUNT = 0 OR @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'DELETEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng) OR (TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'DELETEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng) OR (TTSLotMngUnit)'
>
> RETURN
>
> END
>
> END

 

> IF (SELECT Count(\*)
>
> FROM TTSLotMngUnit
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd
>
> AND InQty = 0 AND OutQty = 0 AND StkQty = 0) \> 0
>
> BEGIN
>
> DELETE TTSLotMngUnit
>
> WHERE InOutYM = @wStkYM AND LotNo = @pLotNoOld AND AccUnit = @pAccUnit
>
> AND OwnerKind = @pOwnerKind AND PlaceKind = @wPlaceKind
>
> AND PlaceCd = @pPlaceCd AND CustCd = @pCustCd
>
> AND ItemCd = @pItemCd AND UnitCd = @pUnitCd
>
> END

 

 

> ----- TTSLot 에 넣기
>
> UPDATE TTSLot
>
> SET CrtQty = CrtQty - @wnStkQty
>
> WHERE LotNo = @pLotNo
>
> AND ItemCd = @pItemCd

 

> IF @@ERROR \<\> 0
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLot) OR (TTSLotMngUnit)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'INSERTFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLot) OR (TTSLotMngUnit)'
>
> RETURN                        
>
> END
>
> END
>
> IF (SELECT Count(\*) FROM TTSLot WHERE LotNo = @pLotNo AND ItemCd = @pItemCd AND ISNULL(CrtQty,0) = 0) \> 0
>
> BEGIN
>
> DELETE TTSLot
>
> WHERE LotNo = @pLotNo AND ItemCd = @pItemCd
>
> END
>
> END
>
> END

 

> IF NOT EXISTS (SELECT TOP 1 1 FROM TTSLotMng_MG WHERE AccUnit = @pAccUnit AND ItemCd = @pItemCd AND LotNo = @pLotNoOld)
>
> BEGIN
>
> IF @pIsSub \<\> '1'
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'NOT', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLotMng_MG)'
>
> RETURN                                                
>
> END        
>
> END

 

DELETE TTSLotMng_MG

WHERE 1 = 1

> AND AccUnit        = @pAccUnit
>
> AND ItemCd        = @pItemCd
>
> AND LotNo                = @pLotNoOld

 

IF @@ROWCOUNT = 0 OR @@ERROR \<\> 0

BEGIN

> IF @pIsSub \<\> '1'
>
> BEGIN
>
> ROLLBACK TRAN
>
> EXEC SDAErrMsg 'DELETEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @vcStatus, @vcResults + '(TTSLot) OR (TTSLotMng_MG)'
>
> RETURN
>
> END
>
> ELSE
>
> BEGIN
>
> EXEC SDAErrMsg 'DELETEFAIL', @vcResults OUTPUT, @vcStatus OUTPUT
>
> SELECT @pStatus = @vcStatus, @pResults = @vcResults + '(TTSLot) OR (TTSLotMng_MG)'
>
> RETURN                
>
> END

END

 

IF @pIsSub \<\> '1' COMMIT TRAN

IF @pIsSub \<\> '1' SELECT @vcStatus, @vcResults, ''

 

RETURN

--GO

--BEGIN TRAN

--exec SUGDeliveryItemSave_MG_TTEST N'A\_/A', N'01', N'202412160007', N'\_/', N'002424\_/002424', N'008\_/008', N'3350\_/3350', N'5280\_/5420', N'17688000\_/18157000', N'3350\_/3350', N'17688000\_/18157000', N'06\_/06', N'1120102\_/1120102', N'0\_/0', N'5280\_/5420', N'17688000\_/18157000', N'008\_/008', N'5280\_/5420', N'\_/', N'\_/', N'\_/', N'202412000061\_/202412000061', N'0001\_/0002', N'\_/', N'\_/', N'\_/', N'Y24025\_/Y24026', N'\_/', N'\_/', N'006002', N'1', N'1', N'KRW', N'1\_/1', N'\_/', N'\_/', N'0\_/0', N'0\_/0', N'0\_/0', N'\_/', N'20241216', N'\_/', N'\_/', N'1\_/1', N'\_/', N'\_/', N'\_/'

--ROLLBACK TRAN

TTSLotMng_MG =\> ItemCd, LotNo에 대해 중복 불가능

 

수정 전 : 존재하는 경우 에러

 

수정 후 : 존재하는 경우 UPDATE

 

백업 : STSLotMngInitQtySave_MG_BAK241216