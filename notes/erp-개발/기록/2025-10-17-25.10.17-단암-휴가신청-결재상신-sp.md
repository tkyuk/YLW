---
title: 25.10.17 단암 휴가신청 결재상신 sp
date: 2025-10-17
tags: [erp, 개발, 기록]
---

# 25.10.17 단암 휴가신청 결재상신 sp

DECLARE @CompanySeq INT = 1

,@EmpSeq INT = 964

> ,@WkItemSeq INT = 1025
>
> ,@WkFrDate NCHAR(8) = '20250903'
>
> ,@WkToDate NCHAR(8) = '20250903'

 

 

 

-- 변수 설정

SELECT ISNULL(B.EmpSeq , 0) AS EmpSeq , -- 사원코드(2020.09.15) --@EmpSeq

ISNULL(B.WkItemSeq, 0) AS WkItemSeq,                                                        --@WkItemSeq

ISNULL(B.ConSeq , 0) AS ConSeq , --##05                                --@ConSeq

ISNULL(B.AppDate , '') AS AppDate ,                                                        --@AppDate

ISNULL(B.VacFrDate, '') AS VacFrDate,                                                        --@WkFrDate

ISNULL(B.VacToDate, '') AS VacToDate                                                         --@WkToDate

> ,\*

FROM \_TXPRWkVactionApp AS B

WHERE B.CompanySeq = @CompanySeq

> AND B.VacAppSeq = 635

 

 

SELECT A.CompanySeq, A.Serl, A.EmpSeq, A.WkItemSeq, A.UseFrDate, A.UseToDate, A.OccurDays, A.AddPileDays, A.BasePileDays, A.PileDays, A.OccurDTCnt, A.AddPileDTCnt, A.BasePileDTCnt, A.PileDTCnt

FROM \_TXPRWkEtcMng AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.WkItemSeq = @WkItemSeq

AND A.EmpSeq = @EmpSeq

--AND A.UseFrDate \<= @WkFrDate

--AND A.UseToDate \>= @WkToDate

 

-- 전체 사용일수 가져오기

SELECT A.DTCnt,\*

FROM \_TXPRWkAbsence AS A LEFT OUTER JOIN \_TXPRWkTypeTimeSet AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.WkTypeSeq = B.WkTypeSeq

AND A.WkFormSeq = B.WkFormSeq

AND A.DayTypeSeq = B.DayTypeSeq

AND B.SMWkKindSeq = 5037002

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

> AND A.WkItemSeq = @WkItemSeq

 

 

> SELECT \* FROM \_TDAEMP WHERE EMPSEQ = 964
>
> SELECT \* FROM \_TPRWKITEM WHERE WKITEMSEQ = 1025

 

 

SELECT '기타휴가 발생',\* FROM \_TXPRWkEtcMng AS A WHERE EMPSEQ = 964                 

SELECT '기타휴가 발생',\* FROM \_TXPRWkEtcMng AS A WHERE EMPSEQ = 964 AND A.UseFrDate \<= '20250903'

SELECT \* FROM \_TXPRWkAbsence WHERE EMPSEQ = 964 AND WkItemSeq = 1025

BEGIN TRAN

GO

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*

설 명 - 휴가신청

화면명 : FrmXPRWkVacationApp / FrmXPRWkVacationAppList

작 성 일 - 2019.08.21

수정내역 - 결근내역 삭제 시 Status체크 부분 추가(2019.10.30)

\- 근태환경설정 추가(2020.06.24)

\- 삭제 후 삽입에서 입력내역이 없으면 삽입으로 변경(2020.07.06)

\- 확정기준을 master에서 detail로 -\> Dtl테이블 JOIN / EmpSeq조건 제외(2020.09.15)

\- 확정 시 사용가능일수 체크 / 연차초과신청 추가(2020.09.21) -\> 우선 제외하여 패치

-\> 다시 활성화

\- 환경설정1 체크시 결근내역집계 수정(2021.01.06)

\- 2021.03.11 정영훈 : 휴가취소신청 OR 휴가취소 Data가 있으면 확정 취소 되지 않도록 한다.

\- 2021.06.03 김미송 : 1일한도 초과 체크 시 조건 오류가 있어서 수정함. 근무신청확정과 동일하게 수정

\- 초과신청활성화 하는 과정에서 연도설정이 빠져서 다시 생성(2021.08.27)

\- 2022.10.17 최자명 : 일괄확정기능 추가로 인해 체크로직 및 결근내역 추가하는 구문에 반영될 수 있도록 변수-\>템프테이블로 변경

\- 2022.10.27 남수진 : 일괄확정 기능을 사전체크 sp로 이동, 사용가능일수 체크 로직 추가

\- 2022.12.07 남수진 : 경조휴가에 대한 사용가능일수 체크 로직 추가 (위치검색: \##01)

\- 2023.01.16 남수진 : 결근내역 등록 일괄 처리 (위치검색: \##02)

\- 2023.01.30 남수진 : 근태항목의 일,시간항목이 바뀌었을때 따로 출력되어 템프에 담아 합쳐서 반영(##03)

\- 2023.12.05 송유진 : 결근테이블을 바로 조인하면 중복집계 되는 오류가 있어서 따로 집계하도록 수정 --##04

\- 2024.08.27 정수연 : 경조사코드추가하여 경조사인 경우 휴가일수 계산하는 로직 수정 --##05

\- 2024.09.04 정수연 : 사용기간관리체크 조건 걸리도록 롤백 후 경조사코드만 다시 추가 및 발생일수 0으로 계산하는 부분 주석

\- 2024.11.19 정수연 : 경조사코드를 못 받아오는 경우에 사용일수와 발생일수 계산되도록 수정

\- 2024.12.12 정수연 : 사용기간관리체크 안할 경우에도 사용가능일수 계산되도록 \##05부분 수정

\- 2025.01.10 송유진 : 확정 해제하는 휴가의 초과 일수 찾아서 삭제 202501030273 --##06

\- 2025.03.31 정수연 : 경조사 프로세스 사용하지 않을 경우도 고려하여 사용가능일수 계산되도록 수정 --##07

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

ALTER PROCEDURE dbo.\_SXPRWkVacationAppCfm

@CompanySeq INT,

@LanguageSeq INT,

@UserSeq INT,

@PgmSeq INT,

@ConfirmSeq INT,

@MessageType INT OUTPUT,

@Status INT OUTPUT,

@Results NVARCHAR(250) OUTPUT

AS

 

 

-- 변수 선언

DECLARE @EmpSeq INT,

@VacAppSeq INT,

@WkItemSeq INT,

@ConSeq INT, --##05

@AppDate NCHAR(8),

@WkFrDate NCHAR(8),

@WkToDate NCHAR(8),

@WkEnvVal NCHAR(1), -- 근태환경설정-휴가확정시급여용근태생성(2020.06.24)

@WkEnvVal2 NCHAR(1), -- 근태환경설정-연차초과신청설정(2020.09.21)

@YY NCHAR(4),

@UnitTm DECIMAL(19, 5),

@OverDTCnt DECIMAL(19, 5),

@BasePileDays DECIMAL(19, 5),

@BasePileDTCnt DECIMAL(19, 5),

@IsCancel NVARCHAR(1),

@OccurDays DECIMAL(19, 5),

@OccurDTCnt DECIMAL(19, 5),

@UseDays DECIMAL(19, 5),

@UseHours DECIMAL(19, 5),

@UseMin INT

 

-- 변수 설정

SELECT @EmpSeq = ISNULL(B.EmpSeq , 0), -- 사원코드(2020.09.15)

@WkItemSeq = ISNULL(B.WkItemSeq, 0),

@ConSeq = ISNULL(B.ConSeq , 0), --##05

@AppDate = ISNULL(B.AppDate , ''),

@WkFrDate = ISNULL(B.VacFrDate, ''),

@WkToDate = ISNULL(B.VacToDate, ''),

@VacAppSeq = ISNULL(B.VacAppSeq, 0) -- 신청코드

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionApp AS B ON A.CfmSeq = B.VacAppSeq

WHERE B.CompanySeq = @CompanySeq

AND A.Status = 0

AND @ConfirmSeq = 22000007 -- 휴가신청내역

 

-- 대상추가

CREATE TABLE \#VacEmp(EmpSeq INT)

 

-- 대상추출

INSERT INTO \#VacEmp(EmpSeq)

SELECT DISTINCT EmpSeq

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionAppDtl AS B ON A.CfmSeq = B.VacAppSeq

WHERE B.CompanySeq = @CompanySeq

AND A.Status = 0

AND @ConfirmSeq = 22000007 -- 휴가신청내역

 

 

-- 사용가능일수를 담을 임시 테이블

CREATE TABLE \#VacGetDays

(

EmpSeq INT,

VacAppSeq INT, PossibleDays DECIMAL(19, 5),

PossibleDTCnt DECIMAL(19, 5),

PossibleMinCnt DECIMAL(19, 5),

OccurDays DECIMAL(19, 5),

IsCC NCHAR(1),

WkSort INT,

YY NCHAR(4)

)

 

-- 근태환경설정(2020.06.24)

-- 휴가확정 시 최종 결근내역 생성

SELECT @WkEnvVal = ISNULL(EnvValue, '0') FROM \_TXPRWkEnv WHERE CompanySeq = @CompanySeq AND EnvSeq = 1 AND IsUse = '1'

 

-- 연차초과신청(2020.09.21)

SELECT @WkEnvVal2 = ISNULL(EnvValue, '0') FROM \_TXPRWkEnv WHERE CompanySeq = @CompanySeq AND EnvSeq = 2 AND IsUse = '1'

 

-- 연도설정(2021.08.27)

SELECT @YY = YY FROM \_TXPRWkVactionAppOver WHERE CompanySeq = @CompanySeq AND VacAppSeq = @VacAppSeq

 

--------------------------------------------------

-- 근태일 마감

--------------------------------------------------

IF EXISTS (SELECT 1

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionApp AS B ON A.CfmSeq = B.VacAppSeq

JOIN \_TXPRWkVactionAppDtl AS C ON B.CompanySeq = C.CompanySeq

--AND B.EmpSeq = C.EmpSeq -- (2020.09.15)

AND B.VacAppSeq = C.VacAppSeq

JOIN \_TXPRWkDayClose AS D ON C.CompanySeq = D.CompanySeq

AND C.VacDate = D.WkDate

WHERE B.CompanySeq = @CompanySeq

AND A.Status = 0

AND D.IsClose = '1'

AND @ConfirmSeq = 22000007 -- 휴가신청내역

)

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

2224 , -- 해당 @1이(가) 마감되었습니다. (SELECT \* FROM \_TCAMessageLanguage WHERE Message LIKE '%되었습니다.%')

@LanguageSeq ,

134, N'근태일' -- SELECT \* FROM \_TCADictionary WHERE Word like '%근태일%'

 

RETURN

 

END

 

--------------------------------------------------

-- 사용가능일수 체크

--------------------------------------------------

 

-- 연차 임시 테이블

CREATE TABLE \#Tmp_YyUseDaysEmp

(

Yy NCHAR(4), -- 사용연도

EmpSeq INT, -- 사원코드

SMYyType INT, -- 연차구분

WkTypeDate NCHAR(8), -- 근무유형기준일자

OccurDays DECIMAL(19,5), -- 총발생일수(이월적치일수+발생일수+추가발생일수)

OccurDTCnt DECIMAL(19,5), -- 총발생시간(이월적치시간+발생시간+추가발생시간)

UseFrDate NCHAR(8),

UseToDate NCHAR(8),

YyRuleSeq INT, -- 연차기준코드

YyTypeCnt INT, -- 연차구분갯수 : 1이면 자체 사용시간계산, 2이면 우선순위적용

UseDispSeq INT, -- 연차사용기준에 따른 사용일수 적용 순서 (5044001 :개정연차먼저사용, 5044002: 기본연차먼저사용)

SMYyUseBase INT, -- 연차사용기준

WkTypeSeq INT, -- 근무유형코드

UnitTm DECIMAL(19,5), -- 단위시간

UseDTCnt DECIMAL(19,5), -- 사용시간

PayDTCnt DECIMAL(19,5), -- 잔여시간

IsOccurOver NCHAR(1) -- 발생시간을 넘어선경우

)

 

CREATE TABLE \#TempUse

(

EmpSeq INT,

UseDTCnt DECIMAL(19, 5),

UseDays DECIMAL(19, 5),

OccurDays DECIMAL(19, 5), --UnitTm 나누기 위해 추가

OccurDTCnt DECIMAL(19, 5), --UnitTm 나누기 위해 추가

UnitTm DECIMAL(19, 5)

)

CREATE TABLE \#TempSum

(

EmpSeq INT,

WkSort INT,

OccurDTCnt DECIMAL(19, 5),

UseHours DECIMAL(19, 5),

UseMin DECIMAL(19, 5),

UseDays DECIMAL(19, 5),

OccurDays DECIMAL(19, 5)

 

)

 

-- 해당항목코드가 연차가 존재하는 경우

IF EXISTS (SELECT \* FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq AND SMAbsWkSort = 3069002 AND SMWkAppSort = 5011003)

BEGIN

-- 연차 발생 내역 조회

INSERT INTO \#Tmp_YyUseDaysEmp(Yy, EmpSeq, SMYyType, WkTypeDate, OccurDays, OccurDTCnt, UseFrDate, UseToDate,

YyRuleSeq, YyTypeCnt, UseDispSeq, SMYyUseBase, WkTypeSeq, UnitTm, UseDTCnt, PayDTCnt, IsOccurOver)

SELECT A.Yy, A.EmpSeq, SMYyType, @WkToDate, -- @WkToDate -\> OccurToDate로(2020.05.12) -\> @WkToDate 사용시점으로 무조건 유지하기로(2020.12.14)

ISNULL(PileDays, 0) + ISNULL(OccurDays, 0) + ISNULL(AddOccurDays, 0) - ISNULL(BasePileDays, 0) - ISNULL(AddPileDays, 0),

ISNULL(PileDTCnt, 0) + ISNULL(OccurDTCnt, 0) + ISNULL(AddOccurDTCnt, 0) - ISNULL(BasePileDTCnt, 0) - ISNULL(AddPileDTCnt, 0),

UseFrDate, UseToDate, 0,0,0,0,0,0, 0,0,'0'

FROM \_TXPRWkYyOccurDays AS A WITH(NOLOCK) -- 연차발생 테이블

WHERE A.CompanySeq = @CompanySeq

AND EmpSeq = @EmpSeq

AND UseFrDate \<= @WkFrDate

AND UseToDate \>= @WkToDate

-- 연차 사용 내역 조회

EXEC \_SXPRWkYyUseDaysGet @CompanySeq

 

SELECT @OccurDTCnt = SUM(A.OccurDTCnt), -- 0나누기 방지(2019.10.26) -\> 임시테이블로 구한다.(2020.02.18)

@UseHours = SUM(A.UseDTCnt),

@UseMin = SUM(A.UseDTCnt\*60)

FROM \#Tmp_YyUseDaysEmp AS A

INSERT INTO \#TempUse(UseDTCnt,UseDays , OccurDTCnt, OccurDays, UnitTm)

SELECT UseDTCnt, 0.0 ,OccurDTCnt, 0.0 , UnitTm

FROM \#Tmp_YyUseDaysEmp

 

-- EmpSeq, SMAbsWkSort에 따른 사용일수, 발생일수 삽입

UPDATE \#TempUse

SET UseDays = ISNULL(UseDTCnt, 0) / CASE WHEN ISNULL(UnitTm, 0) = 0 THEN 1.0 ELSE ISNULL(UnitTm, 0) END,

OccurDays = ISNULL(OccurDTCnt, 0) / CASE WHEN ISNULL(UnitTm, 0) = 0 THEN 1.0 ELSE ISNULL(UnitTm, 0) END

 

-- 각 라인의 값을 합한다.(2020.02.18)

SELECT @UseDays = SUM(UseDays),

@OccurDays = SUM(OccurDays)

FROM \#TempUse

 

END

-- 해당항목코드 중 기타 휴가가 존재하는 경우 @@@@

ELSE IF EXISTS (SELECT \* FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq AND SMAbsWkSort = 3069006 AND SMWkAppSort = 5011003) -- 기타휴가

BEGIN                        

-- 기타휴가 발생 조회

SELECT A.CompanySeq, A.Serl, A.EmpSeq, A.WkItemSeq, A.UseFrDate, A.UseToDate, A.OccurDays, A.AddPileDays, A.BasePileDays, A.PileDays, A.OccurDTCnt, A.AddPileDTCnt, A.BasePileDTCnt, A.PileDTCnt

INTO \#Temp_EtcOccur

FROM \_TXPRWkEtcMng AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.WkItemSeq = @WkItemSeq

AND A.EmpSeq = @EmpSeq

AND A.UseFrDate \<= @WkFrDate

AND A.UseToDate \>= @WkToDate

 

-- 전체 발생일수 가져오기(발생일수 - 적치일수 + 이월일수)

SELECT @OccurDays = SUM(ISNULL(A.OccurDays , 0)) - SUM(ISNULL(A.AddPileDays , 0)) - SUM(ISNULL(A.BasePileDays , 0)) + SUM(ISNULL(A.PileDays , 0)),

@OccurDTCnt = SUM(ISNULL(A.OccurDTCnt, 0)) - SUM(ISNULL(A.AddPileDTCnt, 0)) - SUM(ISNULL(A.BasePileDTCnt, 0)) + SUM(ISNULL(A.PileDTCnt, 0))

 

FROM \#Temp_EtcOccur AS A

 

> SELECT '기타휴가 발생', \* FROM \#Temp_EtcOccur

-- 전체 사용일수 가져오기

SELECT A.\*

FROM \_TXPRWkAbsence AS A LEFT OUTER JOIN \_TXPRWkTypeTimeSet AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.WkTypeSeq = B.WkTypeSeq

AND A.WkFormSeq = B.WkFormSeq

AND A.DayTypeSeq = B.DayTypeSeq

AND B.SMWkKindSeq = 5037002

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.WkItemSeq = @WkItemSeq

AND EXISTS (SELECT 1 FROM \#Temp_EtcOccur WHERE CompanySeq = A.CompanySeq AND EmpSeq = A.EmpSeq AND WkItemSeq = A.WkItemSeq AND A.AbsDate BETWEEN UseFrDate AND UseToDate)

 

-- 전체 사용일수 가져오기

SELECT CASE WHEN A.SMDTCType = 3068001 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0)), 0)

WHEN A.SMDTCType = 3068002 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0) / CASE WHEN B.FullTime = 0 THEN 1 ELSE B.FullTime END), 0)

ELSE 0

END AS UseDays ,

CASE WHEN A.SMDTCType = 3068001 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0) \* CASE WHEN B.FullTime = 0 THEN 1 ELSE B.FullTime END), 0)

WHEN A.SMDTCType = 3068002 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0)), 0)

ELSE 0

END AS UseHours,

ISNULL(SUM(ISNULL(A.MinCnt, 0)), 0) AS UseMin

 

INTO \#UseDays

 

FROM \_TXPRWkAbsence AS A LEFT OUTER JOIN \_TXPRWkTypeTimeSet AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.WkTypeSeq = B.WkTypeSeq

AND A.WkFormSeq = B.WkFormSeq

AND A.DayTypeSeq = B.DayTypeSeq

AND B.SMWkKindSeq = 5037002

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.WkItemSeq = @WkItemSeq

AND EXISTS (SELECT 1 FROM \#Temp_EtcOccur WHERE CompanySeq = A.CompanySeq AND EmpSeq = A.EmpSeq AND WkItemSeq = A.WkItemSeq AND A.AbsDate BETWEEN UseFrDate AND UseToDate)

GROUP BY A.SMDTCType

 

--근태항목의 일,시간항목이 바뀌었을때 따로 출력되어 템프에 담아 합쳐서 반영 (2022.08.10)

SELECT

@UseDays = SUM(UseDays) ,

@UseHours = SUM(UseHours),

@UseMin = SUM(UseMin)

FROM \#UseDays

 

> SELECT '기타휴가 ',
>
> @OccurDays AS OccurDays
>
> ,@OccurDTCnt AS OccurDTCnt
>
> ,@UseDays AS UseDays
>
> ,@UseHours AS UseHours
>
> ,@UseMin AS UseMin

 

>  
>
>  

END

-- 해당항목코드가 월차인 경우

ELSE IF EXISTS (SELECT \* FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq AND SMAbsWkSort = 3069003 AND SMWkAppSort = 5011003) -- 월차(2021.08.05)

BEGIN

 

SELECT A.CompanySeq, A.Ym, A.EmpSeq, A.UseFrDate, A.UseToDate, A.OccurDays, A.OccurDTCnt

 

INTO \#Temp_MmOccur

 

FROM \_TXPRWkMmEmpDays AS A WITH(NOLOCK)

 

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.UseFrDate \<= @WkFrDate

AND A.UseToDate \>= @WkToDate

 

 

 

-- 전체 발생일수 가져오기

SELECT @OccurDays = SUM(ISNULL(A.OccurDays , 0)),

@OccurDTCnt = SUM(ISNULL(A.OccurDTCnt, 0))

 

FROM \#Temp_MmOccur AS A

 

 

 

-- 전체 사용일수 가져오기

SELECT CASE WHEN A.SMDTCType = 3068001 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0)), 0)

WHEN A.SMDTCType = 3068002 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0) / CASE WHEN B.FullTime = 0 THEN 1 ELSE B.FullTime END), 0)

ELSE 0

END AS UseDays,

CASE WHEN A.SMDTCType = 3068001 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0) \* CASE WHEN B.FullTime = 0 THEN 1 ELSE B.FullTime END), 0)

WHEN A.SMDTCType = 3068002 THEN ISNULL(SUM(ISNULL(A.DTCnt, 0)), 0)

ELSE 0

END AS UseHours,

ISNULL(SUM(ISNULL(A.MinCnt, 0)), 0) AS UseMin

INTO \#UseDays2

FROM \_TXPRWkAbsence AS A LEFT OUTER JOIN \_TXPRWkTypeTimeSet AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.WkTypeSeq = B.WkTypeSeq

AND A.WkFormSeq = B.WkFormSeq

AND A.DayTypeSeq = B.DayTypeSeq

AND B.SMWkKindSeq = 5037002

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.WkItemSeq IN (SELECT WkItemSeq FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND SMAbsWkSort = 3069003)

AND EXISTS (SELECT 1 FROM \#Temp_MmOccur WHERE CompanySeq = A.CompanySeq AND EmpSeq = A.EmpSeq AND A.AbsDate BETWEEN UseFrDate AND UseToDate)

GROUP BY A.SMDTCType

 

 

--근태항목의 일,시간항목이 바뀌었을때 따로 출력되어 템프에 담아 합쳐서 반영 (##03)

SELECT

@UseDays = SUM(UseDays) ,

@UseHours = SUM(UseHours),

@UseMin = SUM(UseMin)

FROM \#UseDays2

 

END

 

-- 경조휴가인 경우 \##01

ELSE IF EXISTS (SELECT \* FROM \_TXHRWelCon WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq AND ConSeq = @ConSeq AND WkItemSeq \<\> 0 AND @ConSeq \<\> 0) --##05

BEGIN

>  

---- 경조사가 존재하는 경우만

--IF EXISTS(SELECT \* FROM \_TXHRWelCon WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq AND ConSeq = @ConSeq AND @ConSeq \<\> 0 ) --##05

--BEGIN

-- 사용일수 계산

SELECT @UseDays = COUNT(G.AbsDays) --##05

FROM (SELECT A.AbsDate AS AbsDays --##05

FROM \_TXPRWkAbsence AS A JOIN \_TPRWkItem AS B WITH(NOLOCK) ON B.CompanySeq = A.CompanySeq

AND B.WkItemSeq = A.WkItemSeq

AND B.SMAbsWkSort = 3069011 -- 경조사

 

JOIN \_TXHRWelCon AS C WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.WkItemSeq = B.WkItemSeq

--AND C.IsMatPerCtr = '1'

AND C.ConSeq = @ConSeq --##05

 

JOIN \_TXHRWelConEmp AS D WITH(NOLOCK) ON D.CompanySeq = C.CompanySeq

AND D.ConSeq = C.ConSeq

AND D.EmpSeq = A.EmpSeq

JOIN \_TXPRWkVactionApp AS E WITH(NOLOCK) ON A.CompanySeq = E.CompanySeq

AND A.AppSeq = E.VacAppSeq

AND C.ConSeq = E.ConSeq

 

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.WkItemSeq = @WkItemSeq

AND C.ConSeq = @ConSeq

AND ((C.IsMatPerCtr = '1' AND A.AbsDate BETWEEN D.MatUseFrDate AND D.MatUseToDate) OR (C.IsMatPerCtr = '0' AND A.AbsDate BETWEEN '19000101' AND '99991231')) --##05

AND ((C.IsMatPerCtr = '1' AND @WkToDate BETWEEN D.MatUseFrDate AND D.MatUseToDate) OR (C.IsMatPerCtr = '0' AND @WkToDate BETWEEN '19000101' AND '99991231')) --##05

GROUP BY A.AbsDate --##05

) AS G

 

-- 발생일수 계산

SELECT @OccurDays = ISNULL(SUM(B.HoliDays), 0)

 

FROM \_TXHRWelConEmp AS A WITH(NOLOCK) JOIN \_TXHRWelCon AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.ConSeq = B.ConSeq

 

WHERE A.CompanySeq = @CompanySeq

AND B.WkItemSeq = @WkItemSeq

AND B.ConSeq = @ConSeq --##05

AND A.EmpSeq = @EmpSeq

AND ((B.IsMatPerCtr = '1' AND @WkToDate BETWEEN A.MatUseFrDate AND A.MatUseToDate) OR (B.IsMatPerCtr = '0' AND @WkToDate BETWEEN '19000101' AND '99991231')) --##05

 

 

END

ELSE

BEGIN

-- 경조사 코드가 없는 경우

-- 경조사에 아예내역이 없을 경우(NULL값일 경우) @OccurDays가 0으로 바뀜

SELECT @OccurDays = ISNULL(SUM(B.HoliDays), 0)

 

FROM \_TXHRWelConEmp AS A WITH(NOLOCK) JOIN \_TXHRWelCon AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.ConSeq = B.ConSeq

 

WHERE A.CompanySeq = @CompanySeq

AND B.WkItemSeq = @WkItemSeq

AND A.EmpSeq = @EmpSeq

 

END

 

--경조사프로세스를 사용안하는 경우 --##07

> IF NOT EXISTS (SELECT \* FROM \_TXHRWelConEmp WHERE CompanySeq = @CompanySeq AND ConSeq = @ConSeq AND @ConSeq \<\> 0 AND EmpSeq = @EmpSeq) --##05

BEGIN

>  

-- 사용일수 계산

SELECT @UseDays = COUNT(G.AbsDays)

FROM (SELECT A.AbsDate AS AbsDays

FROM \_TXPRWkAbsence AS A JOIN \_TPRWkItem AS B WITH(NOLOCK) ON B.CompanySeq = A.CompanySeq

AND B.WkItemSeq = A.WkItemSeq

AND B.SMAbsWkSort = 3069011 -- 경조사

JOIN \_TXHRWelCon AS C WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.WkItemSeq = B.WkItemSeq

AND C.ConSeq = @ConSeq

JOIN \_TXPRWkVactionApp AS E WITH(NOLOCK) ON A.CompanySeq = E.CompanySeq

AND A.AppSeq = E.VacAppSeq

AND C.ConSeq = E.ConSeq

 

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.WkItemSeq = @WkItemSeq

AND C.ConSeq = @ConSeq

AND (C.IsMatPerCtr = '0' AND A.AbsDate BETWEEN '19000101' AND '99991231')

AND (C.IsMatPerCtr = '0' AND @WkToDate BETWEEN '19000101' AND '99991231')

GROUP BY A.AbsDate

) AS G

-- 발생일수 계산

 

SELECT @OccurDays = ISNULL(SUM(A.HoliDays), 0)

FROM \_TXHRWelCon AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.WkItemSeq = @WkItemSeq

AND A.ConSeq = @ConSeq

AND (A.IsMatPerCtr = '0' AND @WkToDate BETWEEN '19000101' AND '99991231')

 

>  

 

END

-- 전체 사용가능일수, 발생일수 계산

INSERT INTO \#VacGetDays(EmpSeq, VacAppSeq, PossibleDays, PossibleDTCnt, PossibleMinCnt, OccurDays, YY)

SELECT @EmpSeq,

@VacAppSeq,

ISNULL(@OccurDays , 0) - ISNULL(@UseDays , 0) AS PossibleDays,

ISNULL(@OccurDTCnt , 0) - ISNULL(@UseHours, 0) AS PossibleDTCnt,

(ISNULL(@OccurDTCnt, 0) \* 60) - ISNULL(@UseMin, 0) AS PossibleMinCnt,

ISNULL(@OccurDays , 0) AS OccurDays,

@YY

 

> RETURN

IF EXISTS (SELECT 1

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionApp AS B WITH(NOLOCK) ON A.CfmSeq = B.VacAppSeq

JOIN \_TXPRWkVactionAppDtl AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.EmpSeq = C.EmpSeq

AND B.VacAppSeq = C.VacAppSeq

JOIN \#VacGetDays AS D ON C.EmpSeq = D.EmpSeq

AND C.VacAppSeq = D.VacAppSeq

JOIN \_TPRWkItem AS E ON B.CompanySeq = E.CompanySeq

AND B.WkItemSeq = E.WkItemSeq

WHERE B.CompanySeq = @CompanySeq

AND B.EmpSeq = @EmpSeq

AND A.Status = 0

AND A.CfmCode = '1' -- 확정여부

AND @ConfirmSeq = 22000007 -- 휴가신청내역

AND (@WkEnvVal2 \<\> '1' OR (@WkEnvVal2 = '1' AND E.SMAbsWkSort NOT IN (3069002,3069003))) -- 환경설정

AND D.PossibleDTCnt \< B.DTCnt

AND ISNULL(E.SMAbsWkSort, 0) NOT IN (3069004, 3069005, 3069011, 3069007, 3069008, 3069009, 3069010) --##01 보건, 기타, 공가, 출장, 교육, 근무, 경조사 제외

)

BEGIN

 

>  

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

3055 , -- @1을(를) 초과하였습니다. (SELECT \* FROM \_TCAMessageLanguage WHERE Message LIKE '%초과%')

@LanguageSeq ,

6147, N'사용가능일수' -- SELECT \* FROM \_TCADictionary WHERE Word like '%사용가능%

RETURN

END

-- \##01

ELSE IF EXISTS(SELECT 1

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionApp AS B WITH(NOLOCK) ON A.CfmSeq = B.VacAppSeq

JOIN \_TXPRWkVactionAppDtl AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.EmpSeq = C.EmpSeq

AND B.VacAppSeq = C.VacAppSeq

JOIN \#VacGetDays AS D ON C.EmpSeq = D.EmpSeq

AND C.VacAppSeq = D.VacAppSeq

JOIN \_TPRWkItem AS E ON B.CompanySeq = E.CompanySeq

AND B.WkItemSeq = E.WkItemSeq

WHERE B.CompanySeq = @CompanySeq

AND B.EmpSeq = @EmpSeq

AND A.Status = 0

AND A.CfmCode = '1' -- 확정여부

AND @ConfirmSeq = 22000007 -- 휴가신청내역

AND D.PossibleDays \< B.AppDays

AND E.SMAbsWkSort IN (3069011) -- 경조사만

)

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

3055 , -- @1을(를) 초과하였습니다. (SELECT \* FROM \_TCAMessageLanguage WHERE Message LIKE '%초과%')

@LanguageSeq ,

6147, N'사용가능일수' -- SELECT \* FROM \_TCADictionary WHERE Word like '%사용가능%

RETURN

END

 

--------------------------------------------------

-- 1일한도 이상일 경우 체크(분수로 체크한다)

--------------------------------------------------

---- 이미 발생되어있는 결근 담기 --##04

CREATE TABLE \#TXPRWkAbsence (CompanySeq INT, EmpSeq INT, AbsDate NCHAR(8), MinCnt DECIMAL(19, 5))

INSERT INTO \#TXPRWkAbsence (CompanySeq, EmpSeq , AbsDate , MinCnt)

SELECT A.CompanySeq, A.EmpSeq, A.AbsDate, SUM(ISNULL(A.MinCnt, 0)) AS MinCnt

FROM \_TXPRWkAbsence AS A

WHERE A.CompanySeq = @CompanySeq

AND A.EmpSeq = @EmpSeq

AND A.AbsDate BETWEEN @WkFrDate AND @WkToDate

GROUP BY A.CompanySeq, A.EmpSeq, A.AbsDate

 

---- 총 결근시간이 근무한도 넘을때 메세지 띄움

IF EXISTS (SELECT 1

FROM (SELECT C.EmpSeq, C.VacDate AS VacDate, SUM(ISNULL(C.MinCnt, 0)) + ISNULL(D.MinCnt, 0) AS MinCnt, C.LimitWkCnt \* 60 AS WkHour

FROM \_TXPRWkVactionAppDtl AS C JOIN \#TCOMConfirm AS A ON C.VacAppSeq = A.CfmSeq -- 자기 데이터를 가져와서 체크해야 함.

AND C.VacDate BETWEEN @WkFrDate AND @WkToDate

 

JOIN \_TXPRWkVactionApp AS B WITH(NOLOCK) ON C.CompanySeq = B.CompanySeq

AND C.VacAppSeq = B.VacAppSeq

 

LEFT OUTER JOIN \#TXPRWkAbsence AS D WITH(NOLOCK) ON C.CompanySeq = D.CompanySeq

AND C.VacDate = D.AbsDate

AND C.EmpSeq = D.EmpSeq

WHERE B.CompanySeq = @CompanySeq

AND A.CfmCode = '1' -- 확정처리할 때만 걸린다.

AND @ConfirmSeq = 22000007 -- 휴가신청내역

AND C.EmpSeq IN (SELECT EmpSeq FROM \#VacEmp) -- Dtl의 사원으로 체크

-- 휴가취소건이 있는 경우 한도체크 제외

AND NOT EXISTS (SELECT 1

FROM \_TXPRWkVactionAppCancel

WHERE CompanySeq = @CompanySeq

AND VacAppSeq = C.VacAppSeq

AND EmpSeq = C.EmpSeq

AND IsCancel = '1' -- 휴가취소

)

GROUP BY C.EmpSeq, C.VacDate, C.LimitWkCnt, C.MinCnt, D.MinCnt) AS A

WHERE (1=1)

AND A.MinCnt \> A.WKHour

)

BEGIN

 

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1192 , -- 해당 @1 @2이(가) 초과되었습니다. (SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 1192)

@LanguageSeq ,

0, '휴가일에' , -- SELECT \* FROM \_TCADictionary WHERE Word like '%결근자내역%'

0, '1일한도'

 

RETURN

END

 

 

--------------------------------------------------

-- 휴가취소/취소신청된 자료 확정

--------------------------------------------------

SELECT @IsCancel = CASE WHEN B.IsCancel = '1' THEN '2' ELSE '1' END

FROM \#TCOMConfirm AS A

JOIN \_TXPRWkVactionAppCancel AS B ON B.CompanySeq = @CompanySeq AND A.CfmSeq = B.VacAppSeq

WHERE A.Status = 0

AND ISNULL(A.CfmCode, '') \<\> '1'

AND (B.IsCancel = '1' OR B.IsCancelApp = '1')

IF @IsCancel = '2'

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1173 , -- @1된 자료는 @2@3할 수 없습니다.(SELECT \* FROM \_TCAMessageLanguage WHERE Message LIKE '%없습니다.%')

@LanguageSeq ,

76240, '휴가취소' , -- SELECT \* FROM \_TCADictionary WHERE Word like '%휴가취소%'

607 , '확정' , -- SELECT \* FROM \_TCADictionary WHERE Word like '%확정%'

18311, '취소' -- SELECT \* FROM \_TCADictionary WHERE Word like '취소%'

RETURN

END

 

IF @IsCancel = '1'

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1173 , -- @1된 자료는 @2@3할 수 없습니다.(SELECT \* FROM \_TCAMessageLanguage WHERE Message LIKE '%없습니다.%')

@LanguageSeq ,

72736, '취소신청' , -- SELECT \* FROM \_TCADictionary WHERE Word like '%취소신청%'

607 , '확정' , -- SELECT \* FROM \_TCADictionary WHERE Word like '%확정%'

18311, '취소' -- SELECT \* FROM \_TCADictionary WHERE Word like '취소%'

RETURN

END

--------------------------------------------------

-- 체크에 걸리지 않은 경우

--------------------------------------------------

-- 결근내역 생성&삭제 \##02

IF EXISTS (SELECT 1 FROM \#TCOMConfirm WHERE Status = 0 AND CfmCode = '1' AND @ConfirmSeq = 22000007) -- 휴가신청내역

BEGIN

UPDATE \_TXPRWkVactionApp

 

SET IsCfm = ISNULL(A.CfmCode, '0'),

LastUserSeq = @UserSeq,

LastDateTime = GETDATE()

 

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionApp AS B ON B.VacAppSeq = A.CfmSeq

AND B.CompanySeq = @CompanySeq

 

WHERE A.Status = 0

AND @ConfirmSeq = 22000007 -- 휴가신청내역

 

 

 

-- 신청코드로 등록된 데이터가 있으면 우선 삭제

DELETE A

FROM \_TXPRWkAbsence AS A JOIN \_TXPRWkVactionAppDtl AS B ON A.CompanySeq = B.CompanySeq -- (2020.09.15)

AND A.EmpSeq = B.EmpSeq

AND A.AppSeq = B.VacAppSeq

JOIN \#TCOMConfirm AS C ON B.VacAppSeq = C.CfmSeq -- \##02

WHERE A.CompanySeq = @CompanySeq

--AND A.EmpSeq = @EmpSeq -- (2020.09.15)

--AND A.AppSeq = @VacAppSeq

AND A.SMInputType = 5047002

 

 

 

-- 원래 해당 일자에 결근내역이 없었다면 새로 결근내역 생성

INSERT INTO \_TXPRWkAbsence(CompanySeq ,

AbsDate ,

EmpSeq ,

Seq ,

WkItemSeq ,

IsNextDate ,

AbsBegDate ,

BegTime ,

AbsEndDate ,

EndTime ,

WkTypeSeq ,

WkTeamSeq ,

WkFormSeq ,

DayTypeSeq ,

SMDTCType ,

DTCnt ,

MinCnt ,

Remark ,

SMInputType ,

AppSeq ,

SMYyOccurType,

LastUserSeq ,

LastDateTime

)

 

SELECT @CompanySeq,

A.VacDate ,

A.EmpSeq ,

ISNULL((SELECT MAX(Seq) FROM \_TXPRWkAbsence WHERE CompanySeq = @CompanySeq AND EmpSeq = A.EmpSeq AND AbsDate = A.VacDate), 0) +

ROW_NUMBER() OVER (PARTITION BY A.CompanySeq, A.EmpSeq, A.VacDate ORDER BY A.CompanySeq, A.EmpSeq, A.VacDate),

A.WkItemSeq ,

A.IsNextDate,

A.WkBegDate ,

A.BegTime ,

A.WkEndDate ,

A.EndTime ,

A.WkTypeSeq ,

A.WkTeamSeq ,

A.WkFormSeq ,

A.DayTypeSeq,

B.SMDTCType ,

CASE WHEN B.SMDTCType = 3068001 THEN A.DTCnt / CASE WHEN ISNULL(A.LimitWkCnt, 0) = 0 THEN 1 ELSE A.LimitWkCnt END -- 일

WHEN B.SMDTCType = 3068002 THEN A.DTCnt -- 시간

WHEN B.SMDTCType = 3068003 THEN 1 -- 회수

END,

A.MinCnt,

N'휴가신청확정',

5047002, -- 휴가신청

A.VacAppSeq,

A.SMYyOccurType,

@UserSeq,

GETDATE()

 

FROM \_TXPRWkVactionAppDtl AS A JOIN \_TPRWkItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.WkItemSeq = B.WkItemSeq

JOIN \#TCOMConfirm AS C ON A.VacAppSeq = C.CfmSeq -- \##02

WHERE A.CompanySeq = @CompanySeq

--AND A.EmpSeq = @EmpSeq -- (2020.09.15)

--AND A.VacAppSeq = @VacAppSeq

 

 

 

IF @WkEnvVal = '1'

BEGIN

-- 사원별 근태항목별 합계

CREATE TABLE \#EmpWkItemSum

(

EmpSeq INT,

WkItemSeq INT,

IsHalf NCHAR(1),

AbsDate NCHAR(8),

DTCnt DECIMAL(19, 5),

MinCnt DECIMAL(19, 5)

)

-- 해당 근태일자에 결근내역을 사원별로 합계

INSERT INTO \#EmpWkItemSum(EmpSeq, WkItemSeq, IsHalf, AbsDate, DTCnt, MinCnt)

SELECT A.EmpSeq, A.WkItemSeq, B.IsHalf, A.AbsDate, SUM(A.DTCnt), SUM(A.MinCnt)

FROM \_TXPRWkAbsence AS A JOIN \_TPRWkItem AS B ON A.CompanySeq = B.CompanySeq

AND A.WkItemSeq = B.WkItemSeq

 

JOIN \_TXPRWkVactionAppDtl AS C ON A.CompanySeq = C.CompanySeq -- (2020.09.15)

AND A.EmpSeq = C.EmpSeq

AND A.AppSeq = C.VacAppSeq

AND A.AbsDate = C.VacDate -- (2021.01.06)

JOIN \#TCOMConfirm AS D ON C.VacAppSeq = D.CfmSeq -- \##02

WHERE A.CompanySeq = @CompanySeq

--AND A.EmpSeq = @EmpSeq -- (2020.09.15)

--AND A.AppSeq = @VacAppSeq

AND A.SMInputType = 5047002

AND B.SMWkItemSort = 5012002 -- 결근내역

GROUP BY A.EmpSeq, A.WkItemSeq, B.IsHalf, A.AbsDate

---- 기존내역삭제(2020.07.06)

--DELETE \_TPRWkAbsEmp

-- FROM \_TPRWkAbsEmp AS A JOIN \_TXPRWkAbsence AS B ON A.EmpSeq = B.EmpSeq

-- AND A.WkItemSeq = B.WkItemSeq

-- AND A.AbsDate = B.AbsDate

-- WHERE A.CompanySeq = @CompanySeq

-- AND B.SMInputType = 5047002

-- AND A.EmpSeq = @EmpSeq

-- AND B.AppSeq = @VacAppSeq

-- 결근내역생성(2020.07.06)

INSERT INTO \_TPRWkAbsEmp(CompanySeq ,

AbsDate ,

EmpSeq ,

WkItemSeq ,

IsHalf ,

Remark ,

CCSeq ,

SMInputType ,

UMGrpSeq ,

UMWkGrpSeq ,

Seq ,

DTCnt ,

DTime ,

MinCnt ,

LastUserSeq ,

LastDateTime

)

SELECT @CompanySeq ,

AbsDate ,

EmpSeq ,

WkItemSeq ,

IsHalf ,

'휴가신청에서 생성',

0 ,

0 ,

0 ,

0 ,

0 ,

DTCnt ,

'' ,

MinCnt ,

@UserSeq ,

GETDATE()

FROM \#EmpWkItemSum AS A

 

WHERE NOT EXISTS (SELECT 1

FROM \_TPRWkAbsEmp

WHERE CompanySeq = @CompanySeq

AND AbsDate = A.AbsDate

AND EmpSeq = A.EmpSeq

AND WkItemSeq = A.WkItemSeq

)

END

 

 

-- 초과신청확정시에 -적치 등록(2020.09.21)

IF @WkEnvVal2 = '1'

BEGIN

 

IF (SELECT SMAbsWkSort FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq) = 3069002 -- 연차일 경우만 적치일수로

BEGIN

 

-- 확정시에는 초과일수 반영

UPDATE \_TXPRWkYyOccurDays

SET OverDays = ISNULL(S.OverDays , 0) + T.OverDays , -- 초과일수

OverDTCnt = ISNULL(S.OverDTCnt, 0) + T.OverDTCnt, -- 초과값

LastUserSeq = @UserSeq ,

LastDateTime = GETDATE()

FROM \_TXPRWkYyOccurDays AS S JOIN (SELECT B.YY, B.EmpSeq,

CASE WHEN ISNULL(SUM(ISNULL(B.OverDaysApp , 0)), 0) \> 0 THEN 0 ELSE ISNULL(SUM(ISNULL(B.OverDaysApp , 0)), 0) END AS OverDays,

CASE WHEN ISNULL(SUM(ISNULL(B.OverDTCntApp, 0)), 0) \> 0 THEN 0 ELSE ISNULL(SUM(ISNULL(B.OverDTCntApp, 0)), 0) END AS OverDTCnt

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionAppOver AS B ON A.CfmSeq = B.VacAppSeq

WHERE B.CompanySeq = @CompanySeq

AND @ConfirmSeq = 22000007 -- 휴가신청내역

GROUP BY B.YY, B.EmpSeq

) AS T ON S.Yy = T.YY

AND S.EmpSeq = T.EmpSeq

 

WHERE S.CompanySeq = @CompanySeq

--AND S.Yy = @YY -- \##02

--AND S.EmpSeq = @EmpSeq

AND S.SMYyType = 5043001 -- 기본연차

 

END

 

END

 

 

END

ELSE IF EXISTS (SELECT 1 FROM \#TCOMConfirm WHERE Status = 0 AND CfmCode = '0' AND @ConfirmSeq = 22000007) -- 결근 삭제

BEGIN

 

UPDATE \_TXPRWkVactionApp

 

SET IsCfm = ISNULL(A.CfmCode, '0'),

LastUserSeq = @UserSeq,

LastDateTime = GETDATE()

 

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionApp AS B ON B.VacAppSeq = A.CfmSeq

AND B.CompanySeq = @CompanySeq

 

WHERE A.Status = 0

AND @ConfirmSeq = 22000007 -- 휴가신청내역

 

 

 

--

IF @WkEnvVal = '1'

BEGIN

 

-- 기존내역삭제

DELETE \_TPRWkAbsEmp

FROM \_TPRWkAbsEmp AS A JOIN \_TXPRWkAbsence AS B ON A.EmpSeq = B.EmpSeq

AND A.WkItemSeq = B.WkItemSeq

AND A.AbsDate = B.AbsDate

 

JOIN \_TXPRWkVactionAppDtl AS C ON B.CompanySeq = C.CompanySeq -- (2020.09.15)

AND B.EmpSeq = C.EmpSeq

AND B.AppSeq = C.VacAppSeq

JOIN \#TCOMConfirm AS D ON C.VacAppSeq = D.CfmSeq -- \##02

WHERE A.CompanySeq = @CompanySeq

AND B.SMInputType = 5047002

--AND A.EmpSeq = @EmpSeq -- (2020.09.15)

--AND B.AppSeq = @VacAppSeq

 

END

 

 

 

-- 신청코드로 등록된 데이터가 있으면 우선 삭제

DELETE A

FROM \_TXPRWkAbsence AS A JOIN \_TXPRWkVactionAppDtl AS B ON A.CompanySeq = B.CompanySeq -- (2020.09.15)

AND A.EmpSeq = B.EmpSeq

AND A.AppSeq = B.VacAppSeq

JOIN \#TCOMConfirm AS C ON B.VacAppSeq = C.CfmSeq -- \##02

WHERE A.CompanySeq = @CompanySeq

--AND A.EmpSeq = @EmpSeq -- (2020.09.15)

--AND A.AppSeq = @VacAppSeq

AND A.SMInputType = 5047002

 

 

 

-- 초과신청확정시에 -적치 등록(2020.09.21)

IF @WkEnvVal2 = '1'

BEGIN

IF (SELECT SMAbsWkSort FROM \_TPRWkItem WHERE CompanySeq = @CompanySeq AND WkItemSeq = @WkItemSeq) = 3069002 -- 연차일 경우만 적치일수로

BEGIN

 

-- 확정 해제시에는 차감

UPDATE \_TXPRWkYyOccurDays

SET OverDays = S.OverDays - ISNULL(T.OverDays, 0),

OverDTCnt = S.OverDTCnt - ISNULL(T.OverDTCnt, 0),

LastUserSeq = @UserSeq,

LastDateTime = GETDATE()

FROM \_TXPRWkYyOccurDays AS S JOIN (SELECT B.YY, B.EmpSeq,

CASE WHEN ISNULL(SUM(ISNULL(B.OverDaysApp , 0)), 0) \> 0 THEN 0 ELSE ISNULL(SUM(ISNULL(B.OverDaysApp , 0)), 0) END AS OverDays,

CASE WHEN ISNULL(SUM(ISNULL(B.OverDTCntApp, 0)), 0) \> 0 THEN 0 ELSE ISNULL(SUM(ISNULL(B.OverDTCntApp, 0)), 0) END AS OverDTCnt

FROM \#TCOMConfirm AS A JOIN \_TXPRWkVactionAppOver AS B ON A.CfmSeq = B.VacAppSeq

WHERE B.CompanySeq = @CompanySeq

AND @ConfirmSeq = 22000007 -- 휴가신청내역

GROUP BY B.YY, B.EmpSeq

) AS T ON S.Yy = T.YY

AND S.EmpSeq = T.EmpSeq

WHERE S.CompanySeq = @CompanySeq

--AND S.Yy = @YY -- \##02

--AND S.EmpSeq = @EmpSeq

AND S.SMYyType = 5043001 -- 기본연차

--확정해제시 \_TXPRWkVactionAppOver 삭제 --##06

IF 0 = (SELECT CfmCode FROM \#TCOMConfirm)

BEGIN

--지금 확정 해제하는 휴가의 초과 일수 찾아서 삭제

DELETE B

FROM \#TCOMConfirm AS A

JOIN \_TXPRWkVactionAppOver AS B ON B.CompanySeq = @CompanySeq --초과일수를 담는 테이블

AND B.VacAppSeq = A.CfmSeq --바로 같은건이면 삭제

WHERE A.CfmCode = 0 --확정 취소

END --확정해제시 \_TXPRWkVactionAppOver 삭제 --##06

 

END -- 연차

 

END -- 초과신청

 

END -- 결근삭제

 

RETURN

GO

exec \_SWCOMGroupWareStatus @pWorkingTag=N'E',

@CompanySeq=N'1',

@LanguageSeq=N'1',

@UserSeq=N'1',

@WorkKind=N'EmpVac',

@TblKey=N'635',

@TopUserName=N'',

@TopUserPosition=N'',

@RevUserName=N'',

@RevUserPosition=N'',

@DocID=N'128567'

ROLLBACK TRAN

--SELECT \* FROM \_TCOMGROUPWARE WHERE WORKKIND = 'EmpVac' AND TBLKEY = 635

--SELECT \* FROM \_TXPRWkVactionApp WHERE VACREASON LIKE '2025-09-0%'