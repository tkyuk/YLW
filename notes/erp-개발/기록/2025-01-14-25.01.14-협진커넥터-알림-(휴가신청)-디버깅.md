---
title: 25.01.14 협진커넥터 알림 (휴가신청) 디버깅
date: 2025-01-14
tags: [erp, 개발, 기록]
---

# 25.01.14 협진커넥터 알림 (휴가신청) 디버깅

BEGIN TRAN

GO

 

 

ALTER PROCEDURE hct_SCASaveNoticeMessage

@WorkingTag NVARCHAR(10),                                        -- 워킹태그

@CompanySeq INT,                                                        -- 법인 내부코드

@LanguageSeq INT,                                                        -- 언어 내부코드

@UserSeq INT,                                                        -- 작업자 내부코드

@DeptSeq INT,                                                        -- 부서 내부코드

@MessageTemplateSeq INT,                                                        -- 알림메시지 템플릿 코드

@TblKey NVARCHAR(200)='',                                -- 프로그램의 Key값 (수주 내부 key), 프로그램 정보는 Jump 필요 시 입력

@PgmSeq INT=0,                                                        -- 화면내부코드

@ToPgmSeq INT=0,                                                        -- 점프Pgmseq

@JumpMode INT=0,                                                        -- 점프 유형 : 150001 신규, 150002 조회, 150003 저장, 150006 추가

@JumpData XML='',                                                        -- 점프 데이터

@Msg1 NVARCHAR(1000) = '',                        -- 메시지 내용에 들어가는 param1

@Msg2 NVARCHAR(1000) = '',                        -- 메시지 내용에 들어가는 param2

@Msg3 NVARCHAR(1000) = '',                        -- 메시지 내용에 들어가는 param3

@Msg4 NVARCHAR(1000) = '',                        -- 메시지 내용에 들어가는 param4

@Msg5 NVARCHAR(1000) = '',                        -- 메시지 내용에 들어가는 param5

@MaxTblKey NVARCHAR(MAX) = '',

@MsgEmpSeq INT=0,

@WorkKind NVARCHAR(100) = '',

@IsCfm NCHAR(1) = ''

AS

 

DECLARE @MessageType INT,

@Status INT,

@Results NVARCHAR(250),

@message NVARCHAR(255),

@ROWCOUNT INT,

@ERROR INT,

@IsMail INT,                                                -- 메일

@IsMemo INT,                                                -- 메모

@IsSMS INT,                                                -- SMS

> @IsFast                                        INT,                                                -- 중요도
>
> @IsCfmEmp                 INT,                                                -- 상위결제자

@EmpSeq INT,                                                

@RecvTelNo NVARCHAR(50),                                

@RecvEmail NVARCHAR(MAX),                                

@LoginUserName NVARCHAR(30),                                

@LoginUserID NVARCHAR(30),                                

@LoginDeptName NVARCHAR(30),                                

@LoginCompanyName NVARCHAR(30),                                

@LoginEmpName NVARCHAR(30),                                

@LoginEmpID NVARCHAR(30),                                

@MobilePgmSeq INT,                                

@MessageTitle NVARCHAR(1000),                                

@MessageBody NVARCHAR(MAX),                                

@MessageTitleHTML NVARCHAR(MAX),                                

@MessageBodyHTML NVARCHAR(MAX),                                

@SendMessageID INT,                                                

@ProductType INT,                                                

> @CfmEmpLevel                        INT                                                        

 

CREATE TABLE \#Tmp_TblKey

(

IDX_NO INT,

TblKey INT

)

 

CREATE TABLE \#Tmp_Info(

EmpSeq INT

,TblKey INT

,Msg1 NVARCHAR(100)

,Msg2 NVARCHAR(100)

,Msg3 NVARCHAR(100)

,Msg4 NVARCHAR(100)

,Msg5 NVARCHAR(100)

)

 

-- 휴가신청일괄 확정인 경우 연차내역 가져오기 위한 임시테이블 생성 24.10.21 SJPARK

CREATE TABLE \#Tmp_YyUseDaysEmp

(

Yy NCHAR(4), -- 사용연도

EmpSeq INT, -- 사원코드

SMYyType INT, -- 연차구분

WkTypeDate NCHAR(8), -- 근무유형기준일자

OccurDays DECIMAL(19,5), -- 총발생일수(이월적치일수+발생일수+추가발생일수)

OccurDTCnt DECIMAL(19,5), -- 총발생시간(이월적치시간+발생시간+추가발생시간)

UseFrDate NCHAR(8),

UseToDate NCHAR(8),

YyRuleSeq INT, -- 연차기준코드

YyTypeCnt INT, -- 연차구분갯수 : 1이면 자체 사용시간계산, 2이면 우선순위적용

UseDispSeq INT, -- 연차사용기준에 따른 사용일수 적용 순서 (5044001 :개정연차먼저사용, 5044002: 기본연차먼저사용)

SMYyUseBase INT, -- 연차사용기준

WkTypeSeq INT, -- 근무유형코드

UnitTm DECIMAL(19,5), -- 단위시간

UseDTCnt DECIMAL(19,5), -- 사용시간

PayDTCnt DECIMAL(19,5), -- 잔여시간

IsOccurOver NCHAR(1) -- 발생시간을 넘어선경우

)

 

 

IF (ISNULL(@MaxTblKey, '') \<\> '')

BEGIN

IF SUBSTRING(@MaxTblKey, LEN(@MaxTblKey)-1, LEN(@MaxTblKey)) \<\> '\_/'

BEGIN

SELECT @MaxTblKey = @MaxTblKey + '\_/'

END

 

INSERT INTO \#Tmp_TblKey

SELECT A.IDX_NO, A.Code

FROM \_FCOMSplit(@MaxTblKey,'\_/') AS A

-- 휴가신청일괄확정인 경우 (hct_SXPRWkVacationAppCfm)

IF @WorkKind = 'VactionApp'

BEGIN

INSERT INTO \#Tmp_YyUseDaysEmp(Yy, EmpSeq, SMYyType, WkTypeDate, OccurDays, OccurDTCnt, UseFrDate, UseToDate,

YyRuleSeq, YyTypeCnt, UseDispSeq, SMYyUseBase, WkTypeSeq, UnitTm, UseDTCnt, PayDTCnt, IsOccurOver)

SELECT A.Yy, A.EmpSeq, SMYyType, LEFT(C.VacFrDate,4)+'1231', -- @WkToDate -\> OccurToDate로(2020.05.12) -\> @WkToDate 사용시점으로 무조건 유지하기로(2020.12.14)

ISNULL(PileDays, 0) + ISNULL(OccurDays, 0) + ISNULL(AddOccurDays, 0) - ISNULL(BasePileDays, 0) - ISNULL(AddPileDays, 0),

ISNULL(PileDTCnt, 0) + ISNULL(OccurDTCnt, 0) + ISNULL(AddOccurDTCnt, 0) - ISNULL(BasePileDTCnt, 0) - ISNULL(AddPileDTCnt, 0),

UseFrDate, UseToDate, 0,0,0,0,0,0, 0,0,'0'

FROM \#Tmp_TblKey AS Z

JOIN \_TXPRWkVactionApp AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq

AND C.VacAppSeq = Z.TblKey

JOIN \_TDAEmp AS B WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.EmpSeq = C.EmpSeq

LEFT OUTER JOIN \_TXPRWkYyOccurDays AS A WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

AND A.EmpSeq = C.EmpSeq

AND A.Yy = LEFT(C.VacFrDate,4)

WHERE A.CompanySeq = @CompanySeq

--AND UseFrDate \<= '20240101'

--AND UseToDate \>= '20241231'

-- 연차 사용 내역 조회

EXEC \_SXPRWkYyUseDaysGet @CompanySeq

 

 

INSERT INTO \#Tmp_Info(EmpSeq, TblKey, Msg1 ,Msg2 ,Msg3 ,Msg4 ,Msg5)

SELECT B.EmpSeq AS EmpSeq

,Z.TblKey AS TblKey -- 일괄확정 아닌 경우 0

,B.EmpName AS Msg1

,CONVERT(DECIMAL(19,2),ISNULL(C.OccurDays,0)) AS Msg2

,CONVERT(DECIMAL(19,2),ISNULL(UseDTCnt, 0) / CASE WHEN ISNULL(UnitTm, 0) = 0 THEN 1.0 ELSE ISNULL(UnitTm, 0) END) AS Msg3

,CASE WHEN @IsCfm = '1' THEN '확정' ELSE '취소' END AS Msg4

,'' AS Msg5

FROM \#Tmp_TblKey AS Z

JOIN \_TXPRWkVactionApp AS A WITH(NOLOCK) ON A.CompanySeq = @CompanySeq

AND A.VacAppSeq = Z.TblKey

JOIN \_TDAEmp AS B WITH(NOLOCK) ON B.CompanySeq = A.CompanySeq

AND B.EmpSeq = A.EmpSeq

LEFT OUTER JOIN \_TXPRWkYyOccurDays AS C WITH(NOLOCK) ON C.CompanySeq = A.CompanySeq

AND C.EmpSeq = A.EmpSeq

AND C.Yy = LEFT(A.VacFrDate,4)

LEFT OUTER JOIN \#Tmp_YyUseDaysEmp AS D WITH(NOLOCK) ON D.EmpSeq = A.EmpSeq

AND D.Yy = LEFT(A.VacFrDate,4)

END

ELSE

BEGIN

INSERT INTO \#Tmp_Info(EmpSeq, TblKey)

SELECT @MsgEmpSeq, 0

END

END

DECLARE @EmpCellphone TABLE (UserSeq INT , EmpSeq INT, Cellphone NVARCHAR(200)) -- 핸드폰 번호 암호화로 자릿수 증가

 

--1. 제품군 가져오기

IF EXISTS(SELECT \* FROM SysColumns WHERE id = OBJECT_ID(N'\_TCACompanyConfig') AND name = N'ProductType')

BEGIN

SELECT @ProductType = ISNULL(ProductType, 282001) FROM \_TCACompanyConfig WITH(NOLOCK) WHERE CompanySeq = @CompanySeq

END

ELSE

BEGIN

SELECT @ProductType = 282001

END

 

-- 2. 권한 확인

IF NOT EXISTS (SELECT 1 FROM OTCANoticeMessageSetting WITH(NOLOCK) WHERE MessageTemplateSeq = @MessageTemplateSeq AND CompanySeq = @CompanySeq)

BEGIN

SET @message = ISNULL((SELECT Message FROM \_TCAMessage WITH(NOLOCK) WHERE MessageSeq = 1178 AND LanguageSeq = @LanguageSeq), '') + '\[Check0\]' -- +사용권한이 없습니다

RAISERROR (@message, 16, 1, N'number', 5);

RETURN

END

 

-- 3. 프로그램, 메시지 그룹별 설정값 조회

SELECT @IsMail = IsMail,

@IsMemo = IsMemo,

@IsSMS = IsSMS,

@IsFast = (CASE @IsFast WHEN 1 THEN @IsFast ELSE IsFast END),

@MobilePgmSeq = ISNULL(MobilePgmSeq, 0), -- SMS 문자 발송시 사용한다.

@MessageTitle = ISNULL(MessageTitle, N''),

@MessageBody = ISNULL(MessageBody, N''),

@MessageTitleHTML = ISNULL(MessageTitleHTML, N''),

@MessageBodyHTML = ISNULL(MessageBodyHTML, N''),

> @IsCfmEmp                         = ISNULL(IsCfmEmp         , N'0'), -- 상위결재자 메시지 전송여부
>
> @CfmEmpLevel                 = ISNULL(CfmEmpLevel , N'1') -- 상위레벨

FROM OTCANoticeMessageSetting WITH(NOLOCK)

WHERE MessageTemplateSeq = @MessageTemplateSeq

AND CompanySeq = @CompanySeq

 

IF @@ROWCOUNT = 0 -- 설정값이 없는경우 기본값 설정

BEGIN

SET @IsMemo = 1 -- 설정값이 없는경우 메모만 전송한다.

END

-- 4. 기본값 조회

-- 제품군별 사원정보 조회 (인사모듈이 달라서 제품군별 분리)

IF @ProductType = 282001 --제뉴인

BEGIN

IF EXISTS (SELECT 1 FROM \_TCACompanyMultiUser WHERE CompanySeq = @CompanySeq AND UserSeq = @UserSeq)

BEGIN

SELECT @LoginUserName = ISNULL(A.UserName , N''),

@LoginUserID = ISNULL(A.UserId , N''),

@LoginEmpName = ISNULL(B.EmpName , N''),

@LoginEmpID = ISNULL(B.Empid , N''),

@LoginCompanyName = ISNULL(C.CompanyName , N''),

--@SendEmail = CASE WHEN ISNULL(@SendEmail, N'') = N'' THEN ISNULL(A.PwdMailAdder , N'') ELSE @SendEmail END,

@EmpSeq = ISNULL(B.EmpSeq , 0),

@DeptSeq = CASE WHEN ISNULL(@DeptSeq, 0) = 0 THEN ISNULL(B.DeptSeq, 0) ELSE @DeptSeq END

FROM \_TCACompanyMultiUser AS M WITH(NOLOCK)

LEFT OUTER JOIN \_TCAUser AS A WITH(NOLOCK) ON A.CompanySeq = M.CompanySeq AND A.UserSeq = M.UserSeq

LEFT OUTER JOIN \_fnAdmEmpInOutord(@CompanySeq, N'') AS B ON M.EmpSeq = B.EmpSeq

LEFT OUTER JOIN \_TCACompany AS C WITH(NOLOCK) ON (M.CompanySeq = C.CompanySeq)

WHERE M.UserSeq = @UserSeq

AND M.CompanySeq = @CompanySeq

END

ELSE

BEGIN

SELECT @LoginUserName = ISNULL(A.UserName , N''),

@LoginUserID = ISNULL(A.UserId , N''),

@LoginEmpName = ISNULL(B.EmpName , N''),

@LoginEmpID = ISNULL(B.Empid , N''),

@LoginCompanyName = ISNULL(C.CompanyName , N''),

--@SendEmail = CASE WHEN @SendEmail = N'' THEN ISNULL(A.PwdMailAdder , N'') ELSE @SendEmail END,

@EmpSeq = ISNULL(B.EmpSeq , 0),

@DeptSeq = CASE WHEN ISNULL(@DeptSeq, 0) = 0 THEN ISNULL(B.DeptSeq, 0) ELSE @DeptSeq END

FROM \_TCAUser A WITH(NOLOCK)

LEFT OUTER JOIN \_fnAdmEmpInOutord(@CompanySeq, N'') AS B ON A.EmpSeq = B.EmpSeq

LEFT OUTER JOIN \_TCACompany C WITH(NOLOCK) ON (A.CompanySeq = C.CompanySeq)

WHERE A.UserSeq = @UserSeq

AND A.CompanySeq = @CompanySeq

END

END

ELSE -- Ever, Geni

BEGIN

IF EXISTS (SELECT 1 FROM \_TCACompanyMultiUser WHERE CompanySeq = @CompanySeq AND UserSeq = @UserSeq)

BEGIN

SELECT @LoginUserName = ISNULL(A.UserName , N''),

@LoginUserID = ISNULL(A.UserId , N''),

@LoginEmpName = ISNULL(B.EmpName , N''),

@LoginEmpID = ISNULL(B.Empid , N''),

@LoginCompanyName = ISNULL(C.CompanyName , N''),

--@SendEmail = CASE WHEN @SendEmail = N'' THEN ISNULL(A.PwdMailAdder , N'') ELSE @1 END,

@EmpSeq = ISNULL(B.EmpSeq , 0),

@DeptSeq = CASE WHEN ISNULL(@DeptSeq, 0) = 0 THEN ISNULL(B.DeptSeq, 0) ELSE @DeptSeq END

FROM \_TCACompanyMultiUser AS M WITH(NOLOCK)

LEFT OUTER JOIN \_TCAUser AS A WITH(NOLOCK) ON A.CompanySeq = M.CompanySeq AND A.UserSeq = M.UserSeq

LEFT OUTER JOIN \_VWHREmpInfoLast AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq AND M.EmpSeq = B.EmpSeq

LEFT OUTER JOIN \_TCACompany AS C WITH(NOLOCK) ON M.CompanySeq = C.CompanySeq

WHERE M.UserSeq = @UserSeq

AND M.CompanySeq = @CompanySeq

END

ELSE

BEGIN

SELECT @LoginUserName = ISNULL(A.UserName , N''),

@LoginUserID = ISNULL(A.UserId , N''),

@LoginEmpName = ISNULL(B.EmpName , N''),

@LoginEmpID = ISNULL(B.Empid , N''),

@LoginCompanyName = ISNULL(C.CompanyName , N''),

--@SendEmail = CASE WHEN @SendEmail = N'' THEN ISNULL(A.PwdMailAdder , N'') ELSE @SendEmail END,

@EmpSeq = ISNULL(B.EmpSeq , 0),

@DeptSeq = CASE WHEN ISNULL(@DeptSeq, 0) = 0 THEN ISNULL(B.DeptSeq, 0) ELSE @DeptSeq END

FROM \_TCAUser AS A WITH(NOLOCK)

LEFT OUTER JOIN \_VWHREmpInfoLast AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq AND A.EmpSeq = B.EmpSeq

LEFT OUTER JOIN \_TCACompany AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

WHERE A.UserSeq = @UserSeq

AND A.CompanySeq = @CompanySeq

END

END

 

> -- 부서정보

SET @LoginDeptName = ISNULL((SELECT ISNULL(A.DeptName, N'')

> FROM \_TDADept A WITH(NOLOCK)
>
> WHERE DeptSeq = @DeptSeq AND A.CompanySeq = @CompanySeq ) , N'')

 

-- 핸드폰 입력 : 내부사원

INSERT INTO @EmpCellphone (UserSeq, EmpSeq, Cellphone)

SELECT A.UserSeq, A.EmpSeq, B.Cellphone

FROM \_TCAUser A WITH(NOLOCK)

INNER JOIN \_TDAEmpIn B WITH(NOLOCK) ON (A.EmpSeq = B.EmpSeq AND B.Cellphone \> '')

 

-- 핸드폰 입력 : 외부사원

INSERT INTO @EmpCellphone (UserSeq, EmpSeq, Cellphone)

SELECT A.UserSeq, A.EmpSeq, B.Cellphone

FROM \_TCAUser A WITH(NOLOCK)

INNER JOIN \_TDAEmpOutContract B WITH(NOLOCK) ON (A.EmpSeq = B.EmpSeq AND B.Cellphone \> '')

WHERE NOT EXISTS(SELECT 1 FROM @EmpCellphone C WHERE A.EmpSeq = C.EmpSeq)

 

 

-- 5. 메시지 변환 : 제목

IF (@MessageTitle \<\> N'')

BEGIN

SET @MessageTitle = REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageTitle,

'#LOGIN_USERNAME#', @LoginUserName), '#LOGIN_USERID#', @LoginUserID),'#LOGIN_EMPNAME#', @LoginEmpName),

'#LOGIN_EMPID#', @LoginEmpID),'#LOGIN_COMPANYNAME#', @LoginCompanyName),'#LOGIN_DEPTNAME#', @LoginDeptName),

> '#MSG1#', @MSG1), '#MSG2#', @MSG2), '#MSG3#', @MSG3), '#MSG4#', @MSG4), '#MSG5#', @MSG5)

END

IF (@MessageTitleHTML \<\> N'')

BEGIN

SET @MessageTitleHTML = REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageTitleHTML,

'#LOGIN_USERNAME#', @LoginUserName), '#LOGIN_USERID#', @LoginUserID),'#LOGIN_EMPNAME#', @LoginEmpName),

'#LOGIN_EMPID#', @LoginEmpID),'#LOGIN_COMPANYNAME#', @LoginCompanyName),'#LOGIN_DEPTNAME#', @LoginDeptName),

> '#MSG1#', @MSG1), '#MSG2#', @MSG2), '#MSG3#', @MSG3), '#MSG4#', @MSG4), '#MSG5#', @MSG5)

END

 

-- 5. 메시지 변환 : 내용

IF (@MessageBody \<\> N'')

BEGIN

SET @MessageBody = REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,

'#LOGIN_USERNAME#', @LoginUserName), '#LOGIN_USERID#', @LoginUserID),'#LOGIN_EMPNAME#', @LoginEmpName),

'#LOGIN_EMPID#', @LoginEmpID),'#LOGIN_COMPANYNAME#', @LoginCompanyName),'#LOGIN_DEPTNAME#', @LoginDeptName),

> '#MSG1#', @MSG1), '#MSG2#', @MSG2), '#MSG3#', @MSG3), '#MSG4#', @MSG4), '#MSG5#', @MSG5), '#MESSAGE_TITLE#', @MessageTitle)

END

IF (@MessageBodyHTML \<\> N'')

BEGIN

SET @MessageBodyHTML = REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBodyHTML,

'#LOGIN_USERNAME#', @LoginUserName), '#LOGIN_USERID#', @LoginUserID),'#LOGIN_EMPNAME#', @LoginEmpName),

'#LOGIN_EMPID#', @LoginEmpID),'#LOGIN_COMPANYNAME#', @LoginCompanyName), '#LOGIN_DEPTNAME#', @LoginDeptName),

> '#MSG1#', @MSG1), '#MSG2#', @MSG2), '#MSG3#', @MSG3), '#MSG4#', @MSG4), '#MSG5#', @MSG5), '#MESSAGE_TITLE#', @MessageTitleHTML)

END

--BEGIN TRAN

>  
>
> -- 알림메시지 전송대상자 테이블

DECLARE @User TABLE (UserSeq INT, AppYN NCHAR(1))

 

> INSERT INTO @User(UserSeq, AppYN)
>
> SELECT DISTINCT U.UserSeq,'0'        -- 메시지 그룹별 수신자 등록
>
> FROM OTCANoticeMessageSecuGroup A WITH(NOLOCK)
>
> JOIN OTCANoticeGroupMember AS G WITH(NOLOCK) ON A.GroupSeq = G.GroupSeq

JOIN \_TCAUser AS U WITH(NOLOCK) ON G.UserSeq = U.UserSeq

WHERE A.MessageTemplateSeq = @MessageTemplateSeq

AND A.CompanySeq = @CompanySeq

> UNION
>
> SELECT DISTINCT A.UserSeq,'0'        -- 메시지 사용자별 수신자 등록
>
> FROM OTCANoticeMessageSecu A WITH(NOLOCK)

WHERE MessageTemplateSeq = @MessageTemplateSeq

AND CompanySeq = @CompanySeq

> UNION
>
> SELECT DISTINCT B.UserSeq,'0' -- 상위결재자
>
> FROM dbo.\_fnCABossList(@CompanySeq, @EmpSeq, @CfmEmpLevel) AS A
>
> INNER JOIN \_TCAUser AS B WITH(NOLOCK) ON A.CfmEmpSeq = B.EmpSeq
>
> WHERE @IsCfmEmp = 1 -- 상위결재자 전송여부
>
> AND B.CompanySeq = @CompanySeq

 

 

 

/\*

> -- 6. 알림 테이블에 데이터 저장

IF @IsMemo = 1

BEGIN

> -- 알림메시지 테이블 업데이트

-- 1. 템플릿 등록된 건 =\> 사원 \* 신청자 갯수만큼 INSERT

-- 2. 신청자 건 =\> 신청자 갯수만큼 INSERT

INSERT INTO OTCANoticeMessage(CompanySeq, MessageID,

SendUserSeq, MessageType, MessageTitle, MessageBody, RecvUserSeq, IsMemo, IsFast, CrDateTime,

TblKey, PgmSeq, ToPgmSeq, JumpMode, JumpData, MessageTemplateSeq, IsChecked)

-- SELECT @CompanySeq, ROW_NUMBER() OVER(ORDER BY A.UserSeq) + ISNULL((SELECT MAX(MessageID) FROM OTCANoticeMessage), 0) AS MessageID,

-- @UserSeq, 1 MessageType, @MessageTitle, @MessageBody, A.UserSeq AS RecvUserSeq, @IsMemo, @IsFast, GETDATE() CrDateTime,

> --@TblKey, @PgmSeq, @ToPgmSeq, @JumpMode, @JumpData, @MessageTemplateSeq, 0

-- FROM (SELECT DISTINCT UserSeq FROM @User) AS A

-- 템플릿 등록된 건 =\> 사원 \* 신청자 갯수만큼 INSERT

SELECT @CompanySeq, ROW_NUMBER() OVER(ORDER BY A.UserSeq) + ISNULL((SELECT MAX(MessageID) FROM OTCANoticeMessage), 0) AS MessageID,

@UserSeq, 1 MessageType

,CASE WHEN ISNULL(B.TblKey,0) \<\> 0

THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageTitle END

 

,CASE WHEN ISNULL(B.TblKey,0) \<\> 0

THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageBody END

 

 

, A.UserSeq AS RecvUserSeq, @IsMemo, @IsFast, GETDATE() CrDateTime,

> CASE WHEN ISNULL(B.TblKey,0) \<\> 0 THEN B.TblKey ELSE @TblKey END AS TblKey

, @PgmSeq, @ToPgmSeq, @JumpMode, @JumpData, @MessageTemplateSeq, 0

FROM (SELECT DISTINCT UserSeq FROM @User WHERE AppYN = '0' ) AS A

LEFT OUTER JOIN \#Tmp_Info AS B ON 1=1

 

INSERT INTO OTCANoticeMessage(CompanySeq, MessageID,

SendUserSeq, MessageType, MessageTitle, MessageBody, RecvUserSeq, IsMemo, IsFast, CrDateTime,

TblKey, PgmSeq, ToPgmSeq, JumpMode, JumpData, MessageTemplateSeq, IsChecked)

-- 신청자 건 =\> 신청자 갯수만큼 INSERT

SELECT @CompanySeq, ROW_NUMBER() OVER(ORDER BY A.UserSeq) + ISNULL((SELECT MAX(MessageID) FROM OTCANoticeMessage), 0) AS MessageID,

@UserSeq, 1 MessageType

,CASE WHEN ISNULL(B.TblKey,0) \<\> 0

THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageTitle END

 

,CASE WHEN ISNULL(B.TblKey,0) \<\> 0

THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageBody END

 

 

, A.UserSeq AS RecvUserSeq, @IsMemo, @IsFast, GETDATE() CrDateTime,

> CASE WHEN ISNULL(B.TblKey,0) \<\> 0 THEN B.TblKey ELSE @TblKey END AS TblKey

, @PgmSeq, @ToPgmSeq, @JumpMode, @JumpData, @MessageTemplateSeq, 0

FROM \#Tmp_Info AS B

JOIN \_TCAUser AS A WITH(NOLOCK) ON A.CompanySeq = @CompanySeq

AND A.EmpSeq = B.EmpSeq

 

 

> -- OTCANoticeMessageMaster : 알림메시지 마스터성 테이블
>
> -- 기존 알림 데이터가 존재하는 사용자는 알림영역에 뱃지 표시여부 업데이트
>
> UPDATE OTCANoticeMessageMaster
>
> SET IsBadge = 1
>
> WHERE UserSeq IN (SELECT DISTINCT UserSeq FROM @User)

 

> -- 알림 데이터가 없는 사용자는 알림영역에 뱃지 표시여부 추가
>
> INSERT INTO OTCANoticeMessageMaster(CompanySeq, UserSeq, IsBadge)
>
> SELECT DISTINCT @CompanySeq, A.UserSeq, 1
>
> FROM @User AS A
>
> LEFT OUTER JOIN OTCANoticeMessageMaster AS B ON B.CompanySeq = @CompanySeq AND B.UserSeq = A.UserSeq
>
> WHERE B.UserSeq IS NULL

 

SET @ROWCOUNT = @@ROWCOUNT

IF @@ERROR \<\> 0

BEGIN

ROLLBACK TRAN

RETURN

END

END

\*/

--===================================================================================

-- 메일 기능 추가 24.10.14 SJPARK

--===================================================================================

CREATE TABLE \#Tmp_Email(

IDX_NO INT ,

Subject1 NVARCHAR(MAX),

SendMessage NVARCHAR(MAX),

Email NVARCHAR(100)

)

INSERT INTO \#Tmp_Email(IDX_NO,Subject1,SendMessage,Email)

SELECT DENSE_RANK() OVER(ORDER BY B.TblKey) AS IDX_NO

,CASE WHEN @WorkingTag = 'D'

THEN REPLACE(CASE WHEN ISNULL(B.TblKey,0) \<\> 0 THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageTitle,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageTitle END,'신청','삭제')

ELSE CASE WHEN ISNULL(B.TblKey,0) \<\> 0 THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageTitle,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageTitle END

END AS Subject1

,CASE WHEN ISNULL(B.TblKey,0) \<\> 0

THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageBody END AS SendMessage

,ISNULL(C.PwdMailAdder, D.Email)

--,A.UserSeq AS RecvUserSeq

--,B.TblKey

FROM (SELECT DISTINCT UserSeq FROM @User WHERE AppYN = '0' ) AS A

LEFT OUTER JOIN \#Tmp_Info AS B ON 1=1

LEFT OUTER JOIN \_TCAUser AS C ON C.CompanySeq = @CompanySeq

AND C.UserSeq = A.UserSeq

LEFT OUTER JOIN \_TDAEmpIn AS D ON D.CompanySeq = C.CompanySeq

AND D.EmpSeq = C.EmpSeq

LEFT OUTER JOIN \_TDAEmp AS E ON E.CompanySeq = @CompanySeq

AND E.EmpSeq = B.EmpSeq

 

WHERE ISNULL(D.Email,C.PwdMailAdder) \<\>''

UNION

-- 신청자 건 =\> 신청자 갯수만큼 INSERT

SELECT DENSE_RANK() OVER(ORDER BY B.TblKey) AS IDX_NO

-- 삭제인 경우 신청 -\> 삭제로 변경

,CASE WHEN @WorkingTag = 'D'

THEN REPLACE(CASE WHEN ISNULL(B.TblKey,0) \<\> 0 THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageTitle,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageTitle END,'신청','삭제')

ELSE CASE WHEN ISNULL(B.TblKey,0) \<\> 0 THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageTitle,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageTitle END

END AS Subject1

,CASE WHEN ISNULL(B.TblKey,0) \<\> 0

THEN REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(@MessageBody,'#MSG1#',B.Msg1),'#MSG2#',B.Msg2),'#MSG3#',B.Msg3),'#MSG4#',B.Msg4),'#MSG5#',B.Msg5) ELSE @MessageBody END AS SendMessage

,ISNULL(C.PwdMailAdder, D.Email)

--,A.UserSeq AS RecvUserSeq

--,B.TblKey

FROM \#Tmp_Info AS B

LEFT OUTER JOIN \_TCAUser AS C ON C.CompanySeq = @CompanySeq

AND C.EmpSeq = B.EmpSeq

LEFT OUTER JOIN \_TDAEmpIn AS D ON D.CompanySeq = C.CompanySeq

AND D.EmpSeq = C.EmpSeq

LEFT OUTER JOIN \_TDAEmp AS E ON E.CompanySeq = @CompanySeq

AND E.EmpSeq = B.EmpSeq

WHERE ISNULL(D.Email,C.PwdMailAdder) \<\>''

 

 

SELECT IDX_NO, Subject1, SendMessage, STRING_AGG(Email, ';') AS Email

INTO \#Result_Email

FROM \#Tmp_Email

GROUP BY IDX_NO,Subject1,SendMessage

 

DECLARE @Subject1 NVARCHAR(MAX),

@SendMessage NVARCHAR(MAX),

@from_address NVARCHAR(200)

 

--SELECT @from_address = A.PwdMailAdder

-- FROM \_TCAUser AS A

-- LEFT OUTER JOIN \_TDADept AS B ON B.CompanySeq = A.CompanySeq

-- WHERE A.CompanySeq = @CompanySeq

-- AND A.UserSeq = @UserSeq

 

SELECT '1', \* FROM \#Result_Email -- @@@@

 

 

RETURN

GO

DECLARE @CONST\_#BIZ_IN_DataBlock1 INT

, @CONST\_#BIZ_OUT_DataBlock1 INT

SELECT @CONST\_#BIZ_IN_DataBlock1 = 0

, @CONST\_#BIZ_OUT_DataBlock1 = 0

 

 

 

IF @CONST\_#BIZ_IN_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, VacAppSeq INT, IsCfm NCHAR(1)

)

SET @CONST\_#BIZ_IN_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, YY NCHAR(4), WeekName NVARCHAR(100), WeekSerialNo INT, Confirm NVARCHAR(1), AllDTCnt DECIMAL(19, 5),

AllMinCnt INT, EmpSeq INT, EmpName NVARCHAR(100), EmpID NVARCHAR(20), DeptName NVARCHAR(100), UMJpName

NVARCHAR(100), VacAppSeq INT, SMVacAppTermType INT, SMVacAppTermTypeName NVARCHAR(100), WkItemName

NVARCHAR(100), WkItemSeq INT, AppDate NCHAR(8), FileSeq INT, IsCfm NCHAR(1), VacDate NCHAR(8), WkDate

NCHAR(8), VacFrDate NCHAR(8), VacToDate NCHAR(8), WkName NVARCHAR(10), BegTime NVARCHAR(10), Hours DECIMAL(19,

5), EndTime NVARCHAR(10), PossibleDays DECIMAL(19, 5), PossibleDTCnt DECIMAL(19, 5), PossibleMinCnt INT,

WkTypeSeq INT, WkFormSeq INT, DayTypeSeq INT, WkTypeName NVARCHAR(100), WkFormName NVARCHAR(100), DayTypeName

NVARCHAR(100), WkHour DECIMAL(19, 5), AppDays DECIMAL(19, 5), DTCnt DECIMAL(19, 5), MinCnt INT, VacReason NVARCHAR(1000), WkEmpName NVARCHAR(100), TelNo NVARCHAR(20), Seq INT, WkTeamName NVARCHAR(100), WkTeamSeq INT, AppDay DECIMAL(19, 5), AppFrDate NCHAR(8), AppToDate NCHAR(8), DeptSeq INT, IsCfmIn NCHAR(1), OldVacDate NCHAR(8), OldWkItemSeq INT, DtlWkItemName NVARCHAR(100), DtlWkItemSeq INT, IsNextDate NCHAR(1), WkBegDate NCHAR(8), WkEndDate NCHAR(8), LimitWkCnt DECIMAL(19, 5), IsFile NCHAR(1), Tag NVARCHAR(1), RemainWeekDTCnt DECIMAL(19, 5), RemainMonthDTCnt DECIMAL(19, 5), RemainYearDTCnt DECIMAL(19, 5), AllWeekDTCnt DECIMAL(19, 5), AllMonthDTCnt DECIMAL(19, 5), AllYearDTCnt DECIMAL(19, 5), WkFrDate NCHAR(8), WkToDate NCHAR(8), WeekBaseSeq INT, WeekSerl INT, BasicBegTm NVARCHAR(4), BasicEndTm NVARCHAR(4), IsCC NCHAR(1), SMAbsWkSortName NVARCHAR(100), SMAbsWkSort INT, IsEmp NVARCHAR(1), IsCancel NCHAR(1), CancelReason NVARCHAR(200), Remark NVARCHAR(200), IsCancelApp NCHAR(1), AppEmpSeq INT, AppEmpName NVARCHAR(100), SMDTCType INT, IsHalf NVARCHAR(1), CustName NVARCHAR(100), CustSeq INT, IsCancelSel NVARCHAR(1), IsCancelNew NCHAR(1), SMProgressType INT, Title NVARCHAR(100), AppText NVARCHAR(200), FrDate NVARCHAR(8), ToDate NVARCHAR(8), Reason NVARCHAR(200), ConfirmEmp NVARCHAR(100), ConfirmDate NCHAR(8), IsCancelCfm NCHAR(1), IsLowDept NCHAR(1), SMProgressTypeName NVARCHAR(100), IsNotUsed NVARCHAR(1), ISDisableCheck NVARCHAR(1), ESSMessage NVARCHAR(100), ESSStatus INT, IsQryCancel NCHAR(1), WkFrTime NCHAR(4), WkToTime NCHAR(4), CancleDay DECIMAL(19, 5), CancleDTCnt DECIMAL(19, 5), CancleMinCnt DECIMAL(19, 5), VacDay DECIMAL(19, 5), VacDTCnt DECIMAL(19, 5), VacMinCnt DECIMAL(19, 5), Dummy1 NVARCHAR(100), Dummy2 NVARCHAR(100), Dummy3 NVARCHAR(100), Dummy4 NVARCHAR(100), Dummy5 NVARCHAR(100), Dummy6 NVARCHAR(100), Dummy7 DECIMAL(19, 5), Dummy8 DECIMAL(19, 5), Dummy9 DECIMAL(19, 5), Dummy10 DECIMAL(19, 5), ConName NVARCHAR(100), ConSeq INT

)

SET @CONST\_#BIZ_OUT_DataBlock1 = 1

 

END

 

DECLARE @INPUT_ERROR_MESSAGE NVARCHAR(4000)

, @INPUT_ERROR_SEVERITY INT

, @INPUT_ERROR_STATE INT

, @INPUT_ERROR_PROCEDURE NVARCHAR(128)

INSERT INTO \#BIZ_IN_DataBlock1 (WorkingTag, IDX_NO, DataSeq, Selected, Status, Result, ROW_IDX, IsChangedMst, TABLE_NAME, VacAppSeq, IsCfm)

SELECT N'', 1216, 1, 0, 0, NULL, NULL, NULL, N'DataBlock1', N'2411', N'1' UNION ALL

SELECT N'', 1217, 2, 0, 0, NULL, NULL, NULL, N'DataBlock1', N'2412', N'1' UNION ALL

SELECT N'', 1218, 3, 0, 0, NULL, NULL, NULL, N'DataBlock1', N'2413', N'1' UNION ALL

SELECT N'', 1219, 4, 0, 0, NULL, NULL, NULL, N'DataBlock1', N'2416', N'1' UNION ALL

SELECT N'', 1220, 5, 0, 0, NULL, NULL, NULL, N'DataBlock1', N'2417', N'1' UNION ALL

SELECT N'', 1221, 6, 0, 0, NULL, NULL, NULL, N'DataBlock1', N'2418', N'1'

 

IF @@ERROR \<\> 0 RETURN

 

 

DECLARE @HasError NCHAR(1)

, @UseTransaction NCHAR(1)

-- 내부 SP용 파라메터

, @ServiceSeq INT

, @MethodSeq INT

, @WorkingTag NVARCHAR(10)

, @CompanySeq INT

, @LanguageSeq INT

, @UserSeq INT

, @PgmSeq INT

, @IsTransaction BIT

 

SET @HasError = N'0'

SET @UseTransaction = N'0'

 

BEGIN TRY

 

SET @ServiceSeq = 2720287

--SET @MethodSeq = 23

SET @WorkingTag = N''

SET @CompanySeq = 1

SET @LanguageSeq = 1

SET @UserSeq = 1

SET @PgmSeq = 2720507

SET @IsTransaction = 1

-- InputData를 OutputData에 복사

INSERT INTO \#BIZ_OUT_DataBlock1(WorkingTag, IDX_NO, DataSeq, Selected, MessageType, Status, Result, ROW_IDX, IsChangedMst, VacAppSeq, IsCfm)

SELECT WorkingTag, IDX_NO, DataSeq, Selected, MessageType, Status, Result, ROW_IDX, IsChangedMst, VacAppSeq, IsCfm

FROM \#BIZ_IN_DataBlock1

 

-- ExecuteOrder : 1 : Start

EXEC \_SXPRWKVacationAppSelCfmCheck

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

 

IF @@ERROR \<\> 0 OR EXISTS(SELECT \* FROM \#BIZ_OUT_DataBlock1 WHERE Status != 0)

BEGIN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 1 : End

 

-- ExecuteOrder : 2 : Start

 

SET @UseTransaction = N'1'

BEGIN TRAN

EXEC \_SXPRWKVacationAppSelCfm

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

 

IF @@ERROR \<\> 0 OR EXISTS(SELECT \* FROM \#BIZ_OUT_DataBlock1 WHERE Status != 0)

BEGIN

--ROLLBACK TRAN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 2 : End

 

ROLLBACK TRAN

SET @UseTransaction = N'0'

 

GOTO_END:

SELECT WorkingTag, IDX_NO, DataSeq, Selected, MessageType

, CASE

WHEN Status = 0 OR Status IS NULL THEN

-- 정상인건 중에

CASE

WHEN @HasError = N'1' THEN

-- 오류가 발생된 건이면

CASE

WHEN @UseTransaction = N'1' THEN

999999 -- 트랜잭션인 경우

ELSE

999998 -- 트랜잭션이 아닌 경우

END

ELSE

-- 오류가 발생되지 않은 건이면

0

END

ELSE

Status

END AS Status

, Result, ROW_IDX, IsChangedMst, VacAppSeq

FROM \#BIZ_OUT_DataBlock1

ORDER BY IDX_NO, ROW_IDX

 

END TRY

BEGIN CATCH

-- SQL 오류인 경우는 여기서 처리가 된다

IF @UseTransaction = N'1'

ROLLBACK TRAN

DECLARE @ERROR_MESSAGE NVARCHAR(4000)

, @ERROR_SEVERITY INT

, @ERROR_STATE INT

, @ERROR_PROCEDURE NVARCHAR(128)

 

SELECT @ERROR_MESSAGE = ERROR_MESSAGE()

, @ERROR_SEVERITY = ERROR_SEVERITY()

, @ERROR_STATE = ERROR_STATE()

, @ERROR_PROCEDURE = ERROR_PROCEDURE()

RAISERROR (@ERROR_MESSAGE, @ERROR_SEVERITY, @ERROR_STATE, @ERROR_PROCEDURE)

 

RETURN

END CATCH

 

-- SQL 오류를 제외한 체크로직으로 발생된 오류는 여기서 처리

IF @HasError = N'1' AND @UseTransaction = N'1'

ROLLBACK TRAN

 

 

DROP TABLE \#BIZ_IN_DataBlock1

DROP TABLE \#BIZ_OUT_DataBlock1

 

ROLLBACK TRAN