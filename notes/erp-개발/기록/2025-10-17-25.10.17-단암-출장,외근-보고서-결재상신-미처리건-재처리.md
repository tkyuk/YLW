---
title: 25.10.17 단암 출장,외근 보고서 결재상신 미처리건 재처리
date: 2025-10-17
tags: [erp, 개발, 기록]
---

# 25.10.17 단암 출장,외근 보고서 결재상신 미처리건 재처리

DECLARE

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 1,

@PgmSeq INT = 9999,

@ConfirmSeq INT = 1,

@MessageType INT ,

@Status INT ,

@Results NVARCHAR(250)

 

SET NOCOUNT ON

 

DECLARE @BizTripSeq INT,

@CfmCode NVARCHAR(1),

@BizTripType INT

 

-- 사용할 변수를 선언한다.

DECLARE @Count INT , -- 임시테이블의 행 수 변수

@Seq INT , -- 채번 변수

@Date NCHAR(8) , -- 채번시 기준일 날짜 변수

@MaxNo NVARCHAR(20), -- 최종번호 변수

@WkEnvValue NCHAR(1),

@IsAbcense NVARCHAR(1),

@WkItemSeq INT

,@SlipRemark NVARCHAR(2000)

 

 

 

select DISTINCT TblKey AS BizTripSeq

INTO \#Tmp_Seq

from \_TCOMGroupWare as a

where WorkKind in('BizEduReport_das','BizTripReport_das','BizOutReport_das') and IsProg = '1' and IsEnd = '1'

 

 

SELECT @CfmCode = '1',

@BizTripType = 1,

@WkItemSeq = C.ValueSeq

FROM das_TARBizTripReport AS B

LEFT OUTER JOIN \_TDAUMinorValue AS C ON C.CompanySeq = B.CompanySeq

AND C.MinorSeq = B.UMBizType

AND C.Serl = 15000003

WHERE B.BizTripType = '1'

 

-- 출장, 외근

CREATE TABLE \#BIZ_OUT_DataBlock3

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, EmpSeq INT, AbsDate NCHAR(8), WkItemSeq INT, DTCnt DECIMAL(19, 5), MinCnt DECIMAL(19, 5), SMDTCType INT, IsHalf NCHAR(1), OldWkItemSeq INT, SMHalfType INT , BizTripType INT

> ,BizTripSeq INT

)

-- 출장,외근 보고서인 경우

IF @BizTripType IN(1,2,3) --AND ISNULL(@CfmCode,'0') = '1'

BEGIN

INSERT INTO \#BIZ_OUT_DataBlock3

(

WorkingTag, IDX_NO ,DataSeq , MessageType , Status , Result ,

EmpSeq , AbsDate ,WkItemSeq , OldWkItemSeq, DTCnt , MinCnt ,

SMDTCType , IsHalf ,SMHalfType , BizTripType, BizTripSeq

)

SELECT IIF(@CfmCode = '1','A','D') AS WorkingTag

,ROW_NUMBER() OVER (ORDER BY (SELECT 1)) AS IDX_NO

,ROW_NUMBER() OVER (ORDER BY (SELECT 1)) AS DataSeq

,NULL AS MessageType

,0 AS Status

,NULL AS Result

,A.TripEmpSeq AS EmpSeq

,B.TripDate AS AbsDate

,D.ValueSeq AS WkItemSeq

,D.ValueSeq AS OldWkItemSeq

,B.DTCnt AS DTCnt

,B.MinCnt AS MinCnt

,3068001 AS SMDTCType

,0 AS IsHalf

,0 AS SMHalfType

> ,C.BizTripType
>
> ,A.BizTripSeq

FROM \_TARBizTripCost AS A WITH(NOLOCK)

OUTER APPLY (SELECT B.TripDate, SUM(B.WkTime) AS DTCnt, SUM(B.WkTime)\*60 AS MinCnt

FROM das_TARBizTripReportPJTAmt AS B WITH(NOLOCK)

WHERE B.CompanySeq = A.CompanySeq

AND B.BizTripSeq = A.BizTripSeq

GROUP BY B.TripDate

) AS B

> LEFT OUTER JOIN das_TARBizTripReport AS C ON C.CompanySeq = A.CompanySeq
>
> AND C.BizTripSeq = A.BizTripSeq

LEFT OUTER JOIN \_TDAUMinorValue AS D ON D.CompanySeq = C.CompanySeq

AND D.MinorSeq = C.UMBizType

AND D.Serl = 15000003

WHERE A.CompanySeq = @CompanySeq

AND A.BizTripSeq IN(SELECT BizTripSeq FROM \#Tmp_Seq)

AND ISNULL(A.TripEmpSeq,0) \<\> 0

AND LTRIM(RTRIM(ISNULL(B.TripDate,''))) \<\>''

> AND C.BizTripType IN(1,2,3)

 

 

-- 이미 생성된 건 삭제

DELETE \#BIZ_OUT_DataBlock3

FROM \_TPRWkAbsEmp AS A

JOIN \#BIZ_OUT_DataBlock3 AS B ON A.CompanySeq = @CompanySeq

AND A.WkItemSeq = B.WkItemSeq

AND A.EmpSeq = B.EmpSeq

AND A.AbsDate = B.AbsDate

WHERE B.Status = 0

AND B.WorkingTag IN ('A')

 

-- 근태환경설정체크(2020.10.23)

SELECT @WkEnvValue = ISNULL(EnvValue, '0') FROM \_TXPRWkEnv WHERE CompanySeq = @CompanySeq AND EnvSeq = 3 AND IsUse = '1'

 

-- 환경설정읽기(2021.11.11)

EXEC \_SWCOMEnv @CompanySeq, 3568, @UserSeq, @@ProcID, @IsAbcense OUTPUT

 

CREATE TABLE \#Tmp_EmpWkType

(

WkDate NCHAR(8) , -- 근태일자

EmpSeq INT , -- 사원코드

SMWkKindSeq INT , -- 근무구간

WkTypeSeq INT , -- 근무유형

SMWkTypeSortSeq INT , -- 근무유형종류코드

WkTeamSeq INT , -- 교대조

WkFormSeq INT , -- 근무형태

WeekDaySeq INT , -- 시스템요일코드

SMWeekDaySeq INT , -- 요일코드

DayTypeSeq INT , -- 일구분코드

FlexSeq INT , -- 유연근무내역코드

HoliBaseSeq INT , -- 휴일기준코드

IsNextDate NCHAR(1) , -- 익일여부

WkBegDate NCHAR(8) , -- 근태시작일자

BegTime NCHAR(4) , -- 시작시간

WkEndDate NCHAR(8) , -- 근태종료일자

EndTime NCHAR(4) , -- 종료시간

FullTime DECIMAL(19,5), -- 기준만근시간

WeekBaseSeq INT -- 주차코드

)

 

CREATE TABLE \#Temp_AbsTime

(

EmpSeq INT ,

AbsDate NVARCHAR(8) ,

WkItemSeq INT ,

DTCnt DECIMAL(19,5),

FullTime DECIMAL(19,5)

)

 

--===========

-- 중복체크

--===========

-- 1. 해당 날짜에 근태내역이 존재하는 경우

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

102 , -- @1 데이터가 존재합니다. (SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 102)

@LanguageSeq ,

70856, N'급여용근태내역생성' -- SELECT \* FROM \_TCADictionary WHERE Word like '%급여용근태내역생성%'

UPDATE \#BIZ_OUT_DataBlock3

SET Result = @Results ,

MessageType = @MessageType,

Status = @Status

FROM \#BIZ_OUT_DataBlock3 AS A

INNER JOIN ( SELECT A1.AbsDate, A1.EmpSeq, A1.WkItemSeq

FROM \#BIZ_OUT_DataBlock3 AS A1

INNER JOIN \_TPRWkEmpDd AS A2 WITH(NOLOCK)

ON A1.AbsDate = A2.WkDate

AND A1.EmpSeq = A2.EmpSeq

AND A2.CompanySeq = @CompanySeq

WHERE A1.WorkingTag IN ('A', 'U')

AND A1.Status = 0

--AND ISNULL(A1.IsHalf, '0') \<\> '1' -- 반차사용제외

) AS B

ON A.AbsDate = B.AbsDate

AND A.EmpSeq = B.EmpSeq

AND A.WkItemSeq = B.WkItemSeq

WHERE @WkEnvValue = '0' -- 근태환경설정(2021.06.22) : 환경설정값이 '0'일 때만 해당 체크를 한다.

-- 2. 같은날짜, 같은사원에 대해 같은근태항목이 입력되었을 경우

/\* \_TXPRWkAbsence(결근근태내역)에 저장해야 하는데, 이 테이블은 근태항목(WkItemSeq)이 키가 아니어서 Save에서 중복키 오류 발생함

그렇다고 여기서 근태항목을 제거하면 저장이안됨 설계가 엉망진창임 \*/

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

6 , -- 중복된 @1 @2가(이) 입력되었습니다. (SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 6)

@LanguageSeq ,

2755, N'결근자내역' -- SELECT \* FROM \_TCADictionary WHERE Word like '%결근자내역%'

UPDATE \#BIZ_OUT_DataBlock3

SET Result = REPLACE(@Results,'@2', ''),

MessageType = @MessageType,

Status = @Status

FROM \#BIZ_OUT_DataBlock3 AS A

INNER JOIN (SELECT S.AbsDate, S.EmpSeq , S.WkItemSeq

FROM (

SELECT A1.AbsDate, A1.EmpSeq , A1.WkItemSeq

FROM \#BIZ_OUT_DataBlock3 AS A1

WHERE A1.WorkingTag IN ('A','U')

AND A1.Status = 0

UNION ALL

SELECT A1.AbsDate, A1.EmpSeq , A1.WkItemSeq

FROM \_TPRWkAbsEmp AS A1

WHERE A1.CompanySeq = @CompanySeq

AND NOT EXISTS ( SELECT 1

FROM \#BIZ_OUT_DataBlock3

WHERE WorkingTag ='D' -- \##01 삭제 시에만 체크 제외해야 하는데 'U'일때도 제외하고 있어 SAVE SP에서 중복키 오류나서 수정

AND Status = 0

AND AbsDate = A1.AbsDate

AND EmpSeq = A1.EmpSeq

AND OldWkItemSeq = A1.WkItemSeq

)

) AS S

GROUP BY S.AbsDate, S.EmpSeq, S.WkItemSeq

HAVING COUNT(1) \> 1

) AS B

ON A.AbsDate = B.AbsDate

AND A.EmpSeq = B.EmpSeq

AND A.WkItemSeq = B.WkItemSeq

PRINT '중복체크까지 완료'

 

-- 연차 휴가일 경우 1일 이상의 결근 내역 등록하지 못하도록 수정

-- Old Key(OldWkItemSeq, OldAbsDate에 대해선 체크하지 않는다.)

-- Old Key가 변경되는 경우에는 시간이 차감되기 때문에 체크 Logic에서 제외,

-- (+)무단결근, 월차를 포함하고 다른 근태항목이더라도 결근을 집계 했을때 기준만근시간보다 초과하면 체크 하도록 수정 \##02

IF EXISTS (SELECT 1 FROM \#BIZ_OUT_DataBlock3 AS A JOIN \_TPRWkItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.WkItemSeq = B.WkItemSeq

WHERE A.Status = 0 AND A.WorkingTag IN ('A','U') AND B.SMAbsWkSort IN (3069001, 3069002, 3069003, 3069011) ) --무단, 연차, 월차, 경조사 \##02

BEGIN

EXEC dbo.\_SWCOMMessage @MessageType OUTPUT,

@Status OUTPUT,

@Results OUTPUT,

1192 , -- 해당 @1 @2이(가) 초과되었습니다. (SELECT \* FROM \_TCAMessageLanguage WHERE MessageSeq = 1192)

@LanguageSeq ,

63844, N'결근' , -- SELECT \* FROM \_TCADictionary WHERE Word like '%결근%'

2377, N'일자' -- SELECT \* FROM \_TCADictionary WHERE Word like '%일자%'

INSERT INTO \#Tmp_EmpWkType(WkDate, EmpSeq)

SELECT DISTINCT A.AbsDate, A.EmpSeq

FROM \#BIZ_OUT_DataBlock3 AS A

JOIN \_TPRWkItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.WkItemSeq = B.WkItemSeq

WHERE A.Status = 0 AND A.WorkingTag IN ('A','U') AND B.SMAbsWkSort IN (3069001, 3069002, 3069003, 3069011) --##02

 

EXEC \_SXPRWkTypeDayTypeGet @CompanySeq

INSERT INTO \#Temp_AbsTime(EmpSeq, AbsDAte, WkItemSeq, DTCnt, FullTime)

SELECT A.EmpSeq, A.AbsDate, A.WkItemSeq, CASE WHEN B.SMDTCType = 3068001 THEN A.DtCnt \* ISNULL(C.FullTime, 0) ELSE A.DtCnt END, C.FullTime

FROM \_TPRWkAbsEmp AS A WITH(NOLOCK)

JOIN \_TPRWkItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.WkItemSeq = B.WkItemSeq

LEFT OUTER JOIN \#Tmp_EmpWkType AS C ON A.EmpSeq = C.EmpSeq

AND A.AbsDate = C.WKDate

WHERE A.CompanySeq = @CompanySeq AND B.SMAbsWkSort IN (3069001, 3069002, 3069003, 3069011) --##02

AND B.SMDTCType IN (3068001, 3068002)

AND EXISTS (SELECT 1 FROM \#BIZ_OUT_DataBlock3 WHERE EmpSeq = A.EmpSeq AND AbsDate = A.AbsDate)

--

UPDATE A

SET A.DtCnt = CASE WHEN D.SMDTCType = 3068001 THEN B.DtCnt \* ISNULL(C.FullTime, 0) ELSE A.DtCnt END

FROM \#Temp_AbsTime AS A

JOIN \#BIZ_OUT_DataBlock3 AS B ON A.EmpSEq = B.EmpSeq

AND A.AbsDAte = B.AbsDate

AND A.WkItemSeq = B.WkItemSeq

LEFT OUTER JOIN \#Tmp_EmpWkType AS C ON A.EmpSeq = C.EmpSeq

AND A.AbsDate = C.WKDate

JOIN \_TPRWkItem AS D WITH(NOLOCK) ON D.CompanySeq = @CompanySeq

AND A.WkItemSeq = D.WkItemSeq

WHERE B.Status = 0 AND B.WorkingTag IN ('A', 'U') AND D.SMAbsWkSort IN (3069001, 3069002, 3069003, 3069011) --##02

AND D.SMDTCType IN (3068001, 3068002)

 

INSERT INTO \#Temp_AbsTime(EmpSeq, AbsDAte, WkItemSeq, DTCnt, FullTime)

SELECT A.EmpSeq, A.AbsDate, A.WkItemSeq, CASE WHEN B.SMDTCType = 3068001 THEN A.DtCnt \* ISNULL(C.FullTime, 0) ELSE A.DtCnt END, C.FullTime

FROM \#BIZ_OUT_DataBlock3 AS A WITH(NOLOCK)

JOIN \_TPRWkItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND A.WkItemSeq = B.WkItemSeq

LEFT OUTER JOIN \#Tmp_EmpWkType AS C ON A.EmpSeq = C.EmpSeq

AND A.AbsDate = C.WKDate

WHERE A.Status = 0 AND A.WorkingTag IN ('A', 'U') AND B.SMAbsWkSort IN (3069001, 3069002, 3069003, 3069011) --##02

AND B.SMDTCType IN (3068001, 3068002)

AND NOT EXISTS (SELECT 1 FROM \#Temp_AbsTime WHERE EmpSeq = A.EmpSeq AND AbsDate = A.AbsDate AND WkItemSeq = A.WkItemSeq)

 

 

--select '#Temp_AbsTime', \* from \#Temp_AbsTime

 

UPDATE A

SET A.Result = @Results,

A.MessageType = @MessageType,

A.Status = @Status

FROM \#BIZ_OUT_DataBlock3 AS A

JOIN (

SELECT EmpSeq, AbsDate, SUM(DtCnt) AS DtCnt, MAX(FullTime) AS FullTime

FROM \#Temp_AbsTime

GROUP BY EmpSeq, AbsDate --, WkItemSeq --다른 근태항목이더라도 결근을 집계 했을때 기준만근시간보다 초과하면 체크 함 --##02

HAVING SUM(DtCnt) \> MAX(FullTime)

) AS B ON A.EmpSeq = B.EmpSeq AND A.AbsDAte = B.AbsDate -- AND A.WkItemSeq = B.WkItemSeq

WHERE A.Status = 0 AND A.WorkingTag IN ('A', 'U')

END

> DELETE FROM \#BIZ_OUT_DataBlock3 WHERE Status \<\> 0
>
>  

IF EXISTS(SELECT TOP 1 1 FROM \#BIZ_OUT_DataBlock3 WHERE Status \<\> 0)

BEGIN

SELECT @Status = A.Status, @Results = A.Result

FROM \#BIZ_OUT_DataBlock3 AS A

WHERE Status \<\> 0

RETURN

END

ELSE

BEGIN

SELECT @Status = 0, @Results = '', @MessageType = 0

END

 

-- 변수 선언

>  

-- 로그테이블 남기기(마지막 파라메터는 반드시 한줄로 보내기)

EXEC \_SCOMXmlLog @CompanySeq ,

@UserSeq ,

'\_TPRWkAbsEmp', -- 원테이블명 \_TPRBasPulog

'#BIZ_OUT_DataBlock3', -- 템프테이블명

'AbsDate, EmpSeq, WkItemSeq' , -- 키가 여러개일 경우는 , 로 연결한다.

@PgmSeq,

'' -- 템프테이블에 키명칭이 다를 경우

 

 

-- \_TXPRWkAbsence에 반영하기 위한 근태항목 정보를 담는다

-- 근무근태종류가 연차, 기타휴가이거나 경조사항목에 연결된 근태항목

CREATE TABLE \#Temp_AbsWkItem(WkItemSeq INT)

TRUNCATE TABLE \#Tmp_EmpWkType

 

 

INSERT INTO \#Temp_AbsWkItem(WkITemSeq)

SELECT WkITemSeq FROM \_TPRWkItem WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND SMAbsWkSort IN (3069002, 3069003, 3069006)

UNION

SELECT WkItemSeq FROM \_TXHRWelCon WITH(NOLOCK) WHERE CompanySeq = @CompanySeq AND ISNULL(WkItemSeq, 0) \<\> 0

 

EXEC \_SWCOMEnv @CompanySeq, 3568, @UserSeq, @@ProcID, @IsAbcense OUTPUT

 

-----------------------------------------------------------------------------------------

-- INSERT

-----------------------------------------------------------------------------------------

> IF EXISTS (SELECT 1 FROM \#BIZ_OUT_DataBlock3 WHERE WorkingTag = 'A' AND Status = 0)

BEGIN

INSERT INTO \_TPRWkAbsEmp

(

CompanySeq , AbsDate , EmpSeq , WkItemSeq , IsHalf ,

Remark , CCSeq , SMInputType , UMGrpSeq , UMWkGrpSeq ,

LastUserSeq , LastDateTime, DTCnt , MinCnt

)

SELECT @CompanySeq , AbsDate , EmpSeq , WkItemSeq ,

CASE WHEN A.SMHalfType = 3142001 THEN '1'

WHEN A.SMHalfType = 3142002 THEN '2'

ELSE 0

END , -- 반차구분(2020.02.26)

CASE WHEN @BizTripType = 1 THEN '출장'

WHEN @BizTripType = 2 THEN '교육'

WHEN @BizTripType = 3 THEN '외근' END

> \+ ' 결근내역 자동생성' AS Remark , 0 AS CCSeq , 3116003 , 0 AS UMGrpSeq , 0 AS UMWkGrpSeq , --화면에서 입력된것은 입력구분을 사용자등록으로 고정 \##02

@UserSeq , GETDATE() , ISNULL(A.DTCnt , 0), ISNULL(A.MinCnt, 0)

FROM \#BIZ_OUT_DataBlock3 AS A

WHERE A.WorkingTag = 'A'

AND A.Status = 0

IF @@ERROR \<\> 0

BEGIN

SELECT @Status = 9999,

@Results = '결근 내역 생성 중 오류가 발생했습니다.'

RETURN -- 에러가 발생하면 리턴

END

 

 

SELECT @CompanySeq , AbsDate , EmpSeq , WkItemSeq ,

CASE WHEN A.SMHalfType = 3142001 THEN '1'

WHEN A.SMHalfType = 3142002 THEN '2'

ELSE 0

END , -- 반차구분(2020.02.26)

CASE WHEN @BizTripType = 1 THEN '출장'

WHEN @BizTripType = 2 THEN '교육'

WHEN @BizTripType = 3 THEN '외근' END

> \+ ' 결근내역 자동생성' AS Remark , 0 AS CCSeq , 3116003 , 0 AS UMGrpSeq , 0 AS UMWkGrpSeq , --화면에서 입력된것은 입력구분을 사용자등록으로 고정 \##02

@UserSeq , GETDATE() , ISNULL(A.DTCnt , 0), ISNULL(A.MinCnt, 0)

FROM \#BIZ_OUT_DataBlock3 AS A

WHERE A.WorkingTag = 'A'

AND A.Status = 0

IF @@ERROR \<\> 0

BEGIN

SELECT @Status = 9999,

@Results = '결근 내역 생성 중 오류가 발생했습니다.'

RETURN -- 에러가 발생하면 리턴

END

 

 

END

 

IF @IsAbcense = '1' AND EXISTS (SELECT 1 FROM \#BIZ_OUT_DataBlock3 WHERE Status = 0 AND WorkingTag IN ('U','A'))

BEGIN

>  

SELECT A.AbsDate, A.EmpSeq, ROW_NUMBER() OVER(PARTITION BY A.AbsDate, A.EmpSeq ORDER BY DataSeq) AS Seq, A.WkItemSeq, A.DtCnt, A.MinCnt, A.BizTripType

INTO \#Temp_TXPRWkAbsence

FROM \#BIZ_OUT_DataBlock3 AS A

WHERE A.Status = 0

AND A.WorkingTag IN ('U','A')

AND A.WkItemSeq IN (SELECT WkItemSeq FROM \#Temp_AbsWkItem)

 

 

SELECT A.AbsDate, A.EmpSeq, MAX(A.Seq) AS MAXSeq

INTO \#Temp_MAXSeq FROM \_TXPRWkAbsence AS A WITH(NOLOCK)

WHERE EXISTS (SELECT 1 FROM \#Temp_TXPRWkAbsence WHERE EmpSeq = A.EmpSeq AND AbsDate = A.AbsDate)

AND A.CompanySeq = @CompanySeq

GROUP BY A.AbsDate, A.EmpSeq

 

-- 사원, 근태일을 넣는다.

INSERT INTO \#Tmp_EmpWkType(WkDate, EmpSeq)

SELECT AbsDate, EmpSeq

FROM \#Temp_TXPRWkAbsence

GROUP BY AbsDate, EmpSeq --중복 방지 : 1일 1근태 202411270095

 

-- 사원별 정보

EXEC \_SXPRWkTypeDayTypeGet @CompanySeq

 

--select '#Temp_TXPRWkAbsence', \* from \#Temp_TXPRWkAbsence

--select '#Tmp_EmpWkType', \* from \#Tmp_EmpWkType

INSERT INTO \_TXPRWkAbsence

(

CompanySeq , AbsDate , EmpSeq , Seq , WkItemSeq ,

IsNextDate , AbsBegDate , BegTime , AbsEndDate , EndTime ,

WkTypeSeq , WkTeamSeq , WkFormSeq , DayTypeSeq , SMDTCType ,

DTCnt , MinCnt , Remark , SMInputType , AppSeq ,

SMYyOccurType, LastUserSeq , LastDateTime

)

SELECT @CompanySeq , A.AbsDate , A.EmpSeq , ISNULL(Z.MaxSeq, 0) + A.Seq, A.WkItemSeq ,

'0' , A.AbsDate , C.BegTime , A.AbsDate , C.EndTime , --사원근무유형의 정상근무 시작시간/종료시간이 기본값으로 입력되도록 한다 --##01

ISNULL(C.WkTypeSeq, 0), ISNULL(C.WkTeamSeq, 0), ISNULL(C.WkFormSeq, 0), ISNULL(C.DayTypeSeq, 0), B.SMDTCType ,

A.DTCnt , A.MinCnt ,

> CASE WHEN @BizTripType = 1 THEN '출장'

WHEN @BizTripType = 2 THEN '교육'

WHEN @BizTripType = 3 THEN '외근' END+ ' 결근내역 자동생성' , 5047001 , 0 ,

0 , @UserSeq , GETDATE()

FROM \#Temp_TXPRWkAbsence AS A

LEFT OUTER JOIN \#Temp_MAXSeq AS Z ON A.EmpSeq = Z.EmpSeq

AND A.AbsDate = Z.AbsDate

JOIN \_TPRWkItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND A.WkItemSeq = B.WkItemSeq

LEFT OUTER JOIN \#Tmp_EmpWkType AS C ON A.EmpSeq = C.EmpSeq

AND A.AbsDate = C.WkDate

 

IF @@ERROR \<\> 0

BEGIN

SELECT @Status = 9999,

@Results = '결근 내역 생성 중 오류가 발생했습니다.'

RETURN -- 에러가 발생하면 리턴

END

END

 

UPDATE das_TARBizTripReportAmtWKDtl

SET IsConfirm = 1

FROM das_TARBizTripReportAmtWKDtl AS A

WHERE A.CompanySeq = @CompanySeq

AND A.BizTripSeq IN(SELECT BizTripSeq FROM \#BIZ_OUT_DataBlock3)

 

SELECT

X_OUTER.BizTripSeq, -- 1. 그룹화 키

STUFF((

-- 2. 해당 BizTripSeq에 속하는 모든 Cost 상세 정보를 문자열로 합치는 서브쿼리

SELECT

', ' +

-- 목적 (R18)

CASE WHEN ISNULL(R18.ValueText, '0') = '0' THEN '' ELSE CASE WHEN B.BizTripType = '2' THEN LEFT(ISNULL(B.EduName, ''), 15) ELSE LEFT(ISNULL(X.Purpose, ''), 10) END END

-- 분류명 (R19)

\+ CASE WHEN ISNULL(R19.ValueText, '0') = '0' THEN '' ELSE CASE WHEN ISNULL(R18.ValueText, '0') = '0' THEN '' ELSE ' ' END + ISNULL(K.ValueText, '') END

-- 출장기간 (R20)

\+ CASE WHEN ISNULL(R20.ValueText, '0') = '0' THEN '' ELSE CASE WHEN ISNULL(R18.ValueText, '0') = '0' AND ISNULL(R19.ValueText, '0') = '0' THEN '' ELSE ' ' END

\+ CASE WHEN X.TripFrDate = X.TripToDate THEN SUBSTRING(X.TripFrDate, 5, 2) + '/' + RIGHT(X.TripFrDate, 2)

ELSE SUBSTRING(X.TripFrDate, 5, 2) + '/' + RIGHT(X.TripFrDate, 2) + '~' + SUBSTRING(X.TripToDate, 5, 2) + '/' + RIGHT(X.TripToDate, 2) END END

-- 사용일 (R21)

\+ CASE WHEN ISNULL(R21.ValueText, '0') = '0' THEN '' ELSE CASE WHEN ISNULL(C.UseDate, '') = '' THEN '' ELSE ' ' +

CASE WHEN B.BizTripType IN ('4', '5') THEN C.UseDate ELSE SUBSTRING(C.UseDate, 5, 2) + '/' + SUBSTRING(C.UseDate, 7, 2) END END END

-- 출장지 (R22)

\+ CASE WHEN ISNULL(R22.ValueText, '0') = '0' THEN '' ELSE CASE WHEN B.BizTripType IN ('1', '3')

THEN CASE WHEN ISNULL(X.TripPlace, '') = '' THEN '' ELSE ' ' + X.TripPlace END

ELSE CASE WHEN ISNULL(B.Eduplace, '') = '' THEN '' ELSE ' ' + B.Eduplace END END END

-- 출장업체 (R23)

\+ CASE WHEN ISNULL(R23.ValueText, '0') = '0' THEN '' ELSE CASE WHEN B.BizTripType IN ('1', '3')

THEN CASE WHEN ISNULL(X.TripCust , '') = '' THEN '' ELSE ' ' + X.TripCust END

ELSE CASE WHEN ISNULL(B.Institute, '') = '' THEN '' ELSE ' ' + B.Institute END END END

-- 거래처 (R24)

\+ CASE WHEN ISNULL(R24.ValueText, '0') = '0' THEN '' ELSE CASE WHEN ISNULL(C.CustName, '') = ''

THEN '' ELSE ' ' + C.CustName END END

-- 식대명단 (R25)

\+ CASE WHEN ISNULL(R25.ValueText, '0') = '0' THEN '' ELSE CASE WHEN ISNULL(C.PaymealName, '') = ''

THEN '' ELSE ' ' + C.PaymealName END END

-- 근무장소 (R29)

\+ CASE WHEN ISNULL(R29.ValueText, '0') = '0' THEN '' ELSE CASE WHEN ISNULL(C.WorkPlace, '') = '' THEN '' ELSE ' ' + C.WorkPlace END END

FROM

\_TARBizTripCost AS X WITH(NOLOCK) -- 내부 쿼리 테이블 X

LEFT OUTER JOIN das_TARBizTripReport AS B WITH(NOLOCK) ON X.CompanySeq = B.CompanySeq AND X.BizTripSeq = B.BizTripSeq

LEFT OUTER JOIN das_TARBizTripReportAmt AS C WITH(NOLOCK) ON C.CompanySeq = X.CompanySeq AND C.BizTripSeq = X.BizTripSeq

LEFT OUTER JOIN \_TDAUMinor AS J WITH(NOLOCK) ON C.CompanySeq = J.CompanySeq AND C.UMCostSeq = J.MinorSeq AND J.MajorSeq = 1110352

LEFT OUTER JOIN \_TDAUMinorValue AS K WITH(NOLOCK) ON J.CompanySeq = K.CompanySeq AND J.MinorSeq = K.MinorSeq AND K.Serl = 15000012

LEFT OUTER JOIN \_TDAUMinorValue AS R18 WITH(NOLOCK) ON J.CompanySeq = R18.CompanySeq AND J.MinorSeq = R18.MinorSeq AND R18.Serl = 15000018

LEFT OUTER JOIN \_TDAUMinorValue AS R19 WITH(NOLOCK) ON J.CompanySeq = R19.CompanySeq AND J.MinorSeq = R19.MinorSeq AND R19.Serl = 15000019

LEFT OUTER JOIN \_TDAUMinorValue AS R20 WITH(NOLOCK) ON J.CompanySeq = R20.CompanySeq AND J.MinorSeq = R20.MinorSeq AND R20.Serl = 15000020

LEFT OUTER JOIN \_TDAUMinorValue AS R21 WITH(NOLOCK) ON J.CompanySeq = R21.CompanySeq AND J.MinorSeq = R21.MinorSeq AND R21.Serl = 15000021

LEFT OUTER JOIN \_TDAUMinorValue AS R22 WITH(NOLOCK) ON J.CompanySeq = R22.CompanySeq AND J.MinorSeq = R22.MinorSeq AND R22.Serl = 15000022

LEFT OUTER JOIN \_TDAUMinorValue AS R23 WITH(NOLOCK) ON J.CompanySeq = R23.CompanySeq AND J.MinorSeq = R23.MinorSeq AND R23.Serl = 15000023

LEFT OUTER JOIN \_TDAUMinorValue AS R24 WITH(NOLOCK) ON J.CompanySeq = R24.CompanySeq AND J.MinorSeq = R24.MinorSeq AND R24.Serl = 15000024

LEFT OUTER JOIN \_TDAUMinorValue AS R25 WITH(NOLOCK) ON J.CompanySeq = R25.CompanySeq AND J.MinorSeq = R25.MinorSeq AND R25.Serl = 15000025

LEFT OUTER JOIN \_TDAUMinorValue AS R29 WITH(NOLOCK) ON J.CompanySeq = R29.CompanySeq AND J.MinorSeq = R29.MinorSeq AND R29.Serl = 15000029

WHERE

-- 상관관계 조건: 외부 쿼리의 BizTripSeq와 일치하는 행만 처리

X.BizTripSeq = X_OUTER.BizTripSeq

AND X.CompanySeq = X_OUTER.CompanySeq

-- 기존 필터 조건 유지

AND X.CompanySeq = 1

AND X.BizTripSeq IN (SELECT BizTripSeq FROM \#BIZ_OUT_DataBlock3)

ORDER BY X.BizTripSeq, X.TripFrDate -- 합치기 순서 보장

FOR XML PATH('')

), 1, 2, '') AS SlipRemark -- 최종 합쳐진 문자열

INTO \#Tmp_SlipRemark

FROM

\_TARBizTripCost AS X_OUTER WITH(NOLOCK) -- 외부 쿼리 테이블 (BizTripSeq를 가져옴)

WHERE

X_OUTER.CompanySeq = 1

AND X_OUTER.BizTripSeq IN (SELECT BizTripSeq FROM \#BIZ_OUT_DataBlock3)

GROUP BY

X_OUTER.BizTripSeq, X_OUTER.CompanySeq -- BizTripSeq 별로 그룹화

ORDER BY

X_OUTER.BizTripSeq

 

UPDATE das_TARBizTripReport

SET SlipRemark = B.SlipRemark

FROM das_TARBizTripReport AS A

> JOIN \#Tmp_SlipRemark AS B ON B.BizTripSeq = A.BizTripSeq

WHERE A.CompanySeq = @CompanySeq

 

END

 

RETURN

 

--select \*

-- from \_TCOMGroupWare as a

-- where WorkKind in('BizTripReport_das','BizOutReport_das') and IsProg = '1' and IsEnd = '1'