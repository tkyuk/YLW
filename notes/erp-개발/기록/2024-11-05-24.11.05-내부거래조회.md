---
title: 24.11.05 내부거래조회
date: 2024-11-05
tags: [erp, 개발, 기록]
---

# 24.11.05 내부거래조회

DROP PROC emk_SWACInnerDealListSubQuery_tTEST

GO

CREATE PROC emk_SWACInnerDealListSubQuery_tTEST

@ServiceSeq INT ,

@WorkingTag NVARCHAR(10),

@CompanySeq INT ,

@LanguageSeq INT ,

@UserSeq INT ,

@PgmSeq INT ,

@IsTransaction BIT

AS

 

DECLARE @AccDateFr NCHAR(6),

@AccDateTo NCHAR(6),

@AccDateFr1 NCHAR(8),

@AccDateTo1 NCHAR(8),

@CFSCoName NVARCHAR(100),

@CustCFSCoName NVARCHAR(100)

 

SELECT @AccDateFr = ISNULL(A.AccDateFr, ''),

@AccDateTo = ISNULL(A.AccDateTo, ''),

@CFSCoName = ISNULL(A.CFSCoName, ''),

@CustCFSCoName = ISNULL(A.CustCFSCoName, '')

FROM \#BIZ_IN_DataBlock1 AS A

 

IF ISNULL(@AccDateFr, '') = '' SELECT @AccDateFr = '190001'

IF ISNULL(@AccDateTo, '') = '' SELECT @AccDateTo = '999912'

 

-- 관리항목X계정별원장 화면의 로직을 바탕으로 작성하였습니다.(\_SWACLedgerQueryRemBalance)

DECLARE @WordSubTotal NVARCHAR(100),

@FormatSeq INT,

@FrFSItemSort INT,

@ToFSItemSort INT,

@BitCnt INT

 

EXEC @WordSubTotal = \_FCOMGetWord @LanguageSeq , 15107 , N'소계'

 

--=================================================

-- 대상계정과목

--=================================================

CREATE TABLE \#AccSeqList

(

AccSeq INT

)

 

SELECT @FormatSeq = A.FormatSeq

FROM \_TCOMFSForm AS A WITH(NOLOCK)

JOIN \_TCOMFSKind AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq AND A.FSKindSeq = B.FSKindSeq

JOIN \_TCOMFSDomain AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq AND A.FSDomainSeq = C.FSDomainSeq

WHERE A.CompanySeq = @CompanySeq

AND C.FSDomainNo = 'GAAPFS'

AND B.FSKindNo = 'TB'

AND A.IsDefault = '1'

AND LEFT(@AccDateTo, 6) BETWEEN A.FrYM AND A.ToYM

-- 전체조회시

SELECT @FrFSItemSort = MIN(FSItemSort),

@ToFSItemSort = MAX(FSItemSort)

FROM \_TCOMFSFormTree WITH(NOLOCK)

WHERE CompanySeq = @CompanySeq

AND FormatSeq = @FormatSeq

 

INSERT INTO \#AccSeqList (AccSeq)

SELECT a.FSItemSeq

FROM \_TCOMFSFormTree AS A WITH(NOLOCK) JOIN \_TDAAccount AS ACC WITH(NOLOCK)

ON A.CompanySeq = ACC.CompanySeq

AND A.FSItemSeq = ACC.AccSeq

WHERE A.CompanySeq = @CompanySeq

AND A.FormatSeq = @FormatSeq

AND A.FSItemSort \>= @FrFSItemSort

AND A.FSItemSort \<= @ToFSItemSort

AND ACC.IsSlip = '1'

 

CREATE TABLE \#SlipSum

(

AccSeq INT,

AccName NVARCHAR(100),

UMCostType INT,

UMCostTypeName NVARCHAR(100),

RemSeq1 INT,

Rem1Name NVARCHAR(100),

RemSeq2 INT,

Rem2Name NVARCHAR(100),

RemValSeq1 INT,

RemVal1Name NVARCHAR(100),

RemRefValue1 NVARCHAR(100),

RemValSeq2 INT,

RemVal2Name NVARCHAR(100),

RemRefValue2 NVARCHAR(100),

CurrSeq INT,

CurrName NVARCHAR(100),

IniForAmt DECIMAL(22,5),

DrForAmt DECIMAL(22,5),

CrForAmt DECIMAL(22,5),

RemForAmt DECIMAL(22,5),

IniAmt DECIMAL(22,5),

DrAmt DECIMAL(22,5),

CrAmt DECIMAL(22,5),

RemAmt DECIMAL(22,5)

)

 

SELECT @AccDateFr1 = @AccDateFr + '01',

@AccDateTo1 = @AccDateTo + '31'

EXEC emk_SWCOMSlipSumDealListQuery1 -- 관리항목인 경우에도 화폐가져오도록 서브 SP 추가 24.06.04 SJPARK

--EXEC \_SWCOMSlipSumQuery1

@WorkingTag = @WorkingTag , -- 현재는 사용안함

@CompanySeq = @CompanySeq , -- 법인코드

@LanguageSeq = @LanguageSeq , -- 언어

@UserSeq = @UserSeq , -- 사용자

@SMAccStd = 1 , -- 회계기준구분 -- 화면에서 넘어온 회계기준구분으로 조회

@QueryKind = 'REM' , -- ACC:계정, REM:관리항목별, 2REM:2레벨관리항목

@IsCurr = '0' , -- 1 : 외화

@AccUnit = 1 , -- 회계단위

@SlipUnit = 0 , -- 전표관리단위

@AccDateFr = @AccDateFr1 , -- 회계일(From)

@AccDateTo = @AccDateTo1 , -- 회계일(To)

@UMCostType = 0 , -- 비용구분

@CurrSeq = 0 , -- 통화

@RemSeq1 = 1017 , -- 관리항목1(거래처)

@RemSeq2 = 0 , -- 관리항목2

@RemValSeq1 = 0 , -- 관리항목값1

@RemValSeq2 = 0 -- 관리항목값2

 

-- 모두 0인 것은 삭제

-- 기간내 비용/수익계정은 같은변에 (+)(-)하는 경우가 있지만, 조회가 되지 않아 Jump도 할 수 없다는 의뢰가 있었음

-- BS계정 중에서만 이월,차/대,잔액이 모두 0인 것만 조회하지 않기로 함

DELETE \#SlipSum

FROM \#SlipSum AS A

JOIN \_TDAAccount AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.AccSeq = B.AccSeq

WHERE A.IniAmt = 0 AND A.DrAmt = 0 AND A.CrAmt = 0 AND A.RemAmt = 0

AND B.SMAccKind IN (4018001, 4018002, 4018003)

 

--============================================================================

-- \[계정코드출력타입\]에 따라 계정코드를 출력하기 위하여 로직추가.. dhkim3 시작

--============================================================================

-- 대상 계정과목코드 담기 : 담지 않을 경우 모든 계정과목을 대상으로 함

CREATE TABLE \#AccSeqIn

(

AccSeq INT

)

 

 

-- 대상 계정과목의 계정코드 출력

CREATE TABLE \#AccSeqOut

(

AccNo NVARCHAR(100),

AccSeq INT

)

 

EXEC \_SWACAccNo @CompanySeq, @PgmSeq

--==========================================================================

-- \[계정코드출력타입\]에 따라 계정코드를 출력하기 위하여 로직추가.. dhkim3 끝

--==========================================================================

 

-- CODE_YARDI 담을 임시테이블 생성 24.05.20 SJPARK

CREATE TABLE \#TMP_CodeYardi(

AccSeq INT

,FSItemYardiCode NVARCHAR(100)

,FSItemName NVARCHAR(100) -- 2024.05.28 ghwi

)

 

INSERT INTO \#TMP_CodeYardi(AccSeq, FSItemYardiCode, FSItemName)

SELECT C.AccSeq, MAX(B.FSItemYardiCode), MAX(B.FSItemName)

FROM emk_TACMRForm1Calc AS A WITH(NOLOCK)

JOIN emk_TACMRForm1 AS B WITH(NOLOCK) ON B.CompanySeq = A.CompanySeq

AND B.Form1Seq = A.Form1Seq

AND B.UMFormulaCalcKind = 1000677002 -- 설정방법 : 계정과목

JOIN \_TDAAccount AS C WITH(NOLOCK) ON C.CompanySeq = A.CompanySeq

AND C.AccSeq = A.FSItemSeq

AND ISNULL(C.IsSlip, '') = '1' -- 기표하는계정만

AND ISNULL(C.IsNotSlip, '') \<\> '1' -- 분개전표에서 설정불가 인 것 제외

GROUP BY C.AccSeq

 

 

 

SELECT ISNULL(F.CFSCoName, '') AS CFSCoName, -- 법인명

ISNULL(F.CFSCoNo, '') AS CFSCoNo, -- 법인번호

ISNULL(F.CFSCoUnit, 0) AS CFSCoUnit, -- 법인내부코드(추출여부에 체크된 관계사, 즉 현재법인)

ISNULL(D.CFSCoName, '') AS CustCFSCoName, -- 관계사법인명

ISNULL(D.CFSCoNo, '') AS CustCFSCoNo, -- 관계사법인번호

ISNULL(C.CFSCoUnit, 0) AS CustCFSCoUnit, -- 관계사법인내부코드(계열사의 거래처내부코드)

ISNULL(H.MinorName, '') AS SMAccKindName, -- 계정대분류

ISNULL(G.SMAccKind, 0) AS SMAccKind, -- 계정대분류내부코드

ISNULL(G.AccName, '') AS AccName, -- 계정과목

ISNULL(G.AccNo, '') AS AccNo, -- 계정과목번호

ISNULL(A.AccSeq, 0) AS AccSeq, -- 계정과목내부코드

ISNULL(I.CashItemName, '') AS CashItemName, -- 현금흐름과목

ISNULL(A.CashItemSeq, 0) AS CashItemSeq, -- 현금흐름과목내부코드

SUM((ISNULL(A.DrAmt, 0) - ISNULL(A.CrAmt, 0)) \* ISNULL(G.SMDrOrCr, 0))

AS Amt, -- 금액

ISNULL(SUM(A.DrAmt), 0) AS DrAmt, -- 차변금액

ISNULL(SUM(A.CrAmt), 0) AS CrAmt -- 대변금액

,ISNULL(J.FSItemYardiCode,'') AS FSItemYardiCode -- 24.05.20 SJPARK

,ISNULL(J.FSItemName,'') AS FSItemName -- Yardi_계정명 -- 2024.05.28 ghwi

,ISNULL(K.CurrName ,'') AS CurrName -- 화폐 추가 24.06.04 SJPARK

,ISNULL(A.CurrSeq , 0) AS CurrSeq -- 화폐 추가 24.06.04 SJPARK

FROM \_TACSlipRow AS A WITH(NOLOCK)

JOIN \_TACCFSCompanyCust AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq -- 연결자료추출환경설정 화면의 우측 거래처 시트

AND A.CoCustSeq = C.CustSeq

JOIN \_TACCFSCompany AS D WITH(NOLOCK) ON C.CompanySeq = D.CompanySeq -- 연결자료추출환경설정 화면

AND C.CFSCoUnit = D.CFSCoUnit

JOIN \_TDACust AS E WITH(NOLOCK) ON C.CompanySeq = E.CompanySeq

AND C.CustSeq = E.CustSeq

JOIN \_TACCFSCompany AS F WITH(NOLOCK) ON A.CompanySeq = F.CompanySeq -- 연결자료추출환경설정 화면

AND F.IsSelf = '1' -- 추출회사여부

JOIN \_TDAAccount AS G WITH(NOLOCK) ON A.CompanySeq = G.CompanySeq

AND A.AccSeq = G.AccSeq

LEFT OUTER JOIN \_TDASMinor AS H WITH(NOLOCK) ON G.CompanySeq = H.CompanySeq

AND G.SMAccKind = H.MinorSeq

LEFT OUTER JOIN \_TACDCashItem AS I WITH(NOLOCK) ON A.CompanySeq = I.CompanySeq

AND A.CashItemSeq = I.CashItemSeq

LEFT OUTER JOIN \#TMP_CodeYardi AS J WITH(NOLOCK) ON J.AccSeq = A.AccSeq

LEFT OUTER JOIN \_TDACurr AS K WITH(NOLOCK) ON K.CompanySeq = A.CompanySeq

AND K.CurrSeq = A.CurrSeq

 

WHERE A.CompanySeq = @CompanySeq

AND ISNULL(A.IsSet, '0') = '1' -- 승인된 건만 조회

AND(

--ISNULL(G.SMAccKind, 0) IN (4018004, 4018005) -- PL계정(계정대분류 비용, 수익)

--OR

(ISNULL(G.SMAccKind, 0) IN (4018001, 4018002, 4018003) AND -- BS계정(계정대분류 자산, 자본, 부채)은 계정과목에 현금흐름과목종류가 설정된 건만 조회합니다.

ISNULL(G.SMCashItemKind, 0) \<\> 0) AND

ISNULL(A.CashItemSeq, 0) \<\> 0)

AND LEFT(ISNULL(A.AccDate, ''), 6) BETWEEN @AccDateFr AND @AccDateTo

AND (@CFSCoName = '' OR ISNULL(F.CFSCoName, '') LIKE @CFSCoName + '%')

AND (@CustCFSCoName = '' OR ISNULL(D.CFSCoName, '') LIKE @CustCFSCoName + '%')

AND F.CFSCoNo \<\> D.CFSCoNo -- 자회사 건 조회안되도록 추가 24.05.23 SJPARK

GROUP BY ISNULL(F.CFSCoName, '') ,

ISNULL(F.CFSCoNo, '') ,

ISNULL(F.CFSCoUnit, 0) ,

ISNULL(D.CFSCoName, '') ,

ISNULL(D.CFSCoNo, '') ,

ISNULL(C.CFSCoUnit, 0) ,

ISNULL(H.MinorName, '') ,

ISNULL(G.SMAccKind, 0) ,

ISNULL(G.AccName, '') ,

ISNULL(G.AccNo, '') ,

ISNULL(A.AccSeq, 0) ,

ISNULL(I.CashItemName, ''),

ISNULL(A.CashItemSeq, 0)

,ISNULL(J.FSItemYardiCode,'')

,ISNULL(J.FSItemName,'')

,ISNULL(A.CurrSeq , 0)

,ISNULL(K.CurrName ,'')

HAVING (SUM((ISNULL(A.DrAmt, 0) - ISNULL(A.CrAmt, 0)) \* ISNULL(G.SMDrOrCr, 0)) \<\> 0 -- 금액이 있는 건만 조회합니다.

OR ISNULL(SUM(A.DrAmt), 0) \<\> 0

OR ISNULL(SUM(A.CrAmt), 0) \<\> 0)

 

-- BS계정(계정대분류 자산, 자본, 부채)에 대한 잔액 조회

SELECT ISNULL(F.CFSCoName, '') AS CFSCoName, -- 법인명

ISNULL(F.CFSCoNo, '') AS CFSCoNo, -- 법인번호

ISNULL(F.CFSCoUnit, 0) AS CFSCoUnit, -- 법인내부코드(추출여부에 체크된 관계사, 즉 현재법인)

ISNULL(D.CFSCoName, '') AS CustCFSCoName, -- 관계사법인명

ISNULL(D.CFSCoNo, '') AS CustCFSCoNo, -- 관계사법인번호

ISNULL(C.CFSCoUnit, 0) AS CustCFSCoUnit, -- 관계사법인내부코드(계열사의 거래처내부코드)

ISNULL(H.MinorName, '') AS SMAccKindName, -- 계정대분류

ISNULL(G.SMAccKind, 0) AS SMAccKind, -- 계정대분류내부코드

ISNULL(G.AccName, '') AS AccName, -- 계정과목

ISNULL(G.AccNo, '') AS AccNo, -- 계정과목번호

ISNULL(A.AccSeq, 0) AS AccSeq, -- 계정과목내부코드

'' AS CashItemName, -- 현금흐름과목

0 AS CashItemSeq, -- 현금흐름과목내부코드

--ISNULL(A.RemAmt, 0) AS Amt, -- 금액(BS계정 잔액)

ISNULL(A.IniAmt, 0) AS Amt, -- 금액(BS계정 잔액) 24.10.08

ISNULL(A.DrAmt, 0) AS DrAmt, -- 차변금액

ISNULL(A.CrAmt, 0) AS CrAmt -- 대변금액

,ISNULL(J.FSItemYardiCode,'') AS FSItemYardiCode -- 24.05.20 SJPARK

,ISNULL(J.FSItemName, '') AS FSItemName -- Yardi_계정명 -- 2024.05.28 ghwi

,ISNULL(K.CurrName ,'') AS CurrName -- 화폐 추가 24.06.04 SJPARK

,ISNULL(A.CurrSeq , 0) AS CurrSeq -- 화폐 추가 24.06.04 SJPARK

FROM \#SlipSum AS A

JOIN \_TACCFSCompanyCust AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq -- 연결자료추출환경설정 화면의 우측 거래처 시트

AND A.RemValSeq1 = C.CustSeq

JOIN \_TACCFSCompany AS D WITH(NOLOCK) ON C.CompanySeq = D.CompanySeq -- 연결자료추출환경설정 화면

AND C.CFSCoUnit = D.CFSCoUnit

JOIN \_TDACust AS E WITH(NOLOCK) ON C.CompanySeq = E.CompanySeq

AND C.CustSeq = E.CustSeq

JOIN \_TACCFSCompany AS F WITH(NOLOCK) ON F.CompanySeq = @CompanySeq -- 연결자료추출환경설정 화면

AND F.IsSelf = '1' -- 추출회사여부

LEFT OUTER JOIN \_TDAAccount AS G WITH(NOLOCK) ON G.CompanySeq = @CompanySeq

AND A.AccSeq = G.AccSeq

LEFT OUTER JOIN \_TDASMinor AS H WITH(NOLOCK) ON G.CompanySeq = H.CompanySeq

AND G.SMAccKind = H.MinorSeq

LEFT OUTER JOIN \#TMP_CodeYardi AS J WITH(NOLOCK) ON J.AccSeq = A.AccSeq

LEFT OUTER JOIN \_TDACurr AS K WITH(NOLOCK) ON K.CompanySeq = @CompanySeq

AND K.CurrSeq = A.CurrSeq

WHERE ISNULL(G.SMAccKind, 0) IN (4018001, 4018002, 4018003,4018004, 4018005) -- BS계정(계정대분류 자산, 자본, 부채)

AND ISNULL(G.IsCash, '0') \<\> '1' -- 현금성여부에 체크된 계정은 조회대상에서 제외합니다.

AND A.RemSeq1 = 1017

AND (@CFSCoName = '' OR ISNULL(F.CFSCoName, '') LIKE @CFSCoName + '%')

AND (@CustCFSCoName = '' OR ISNULL(D.CFSCoName, '') LIKE @CustCFSCoName + '%')

AND F.CFSCoNo \<\> D.CFSCoNo -- 자회사 건 조회안되도록 추가 24.05.23 SJPARK

ORDER BY CashItemName, ISNULL(F.CFSCoName, ''), ISNULL(G.AccNo, ''), ISNULL(D.CFSCoNo, '')

 

RETURN

--=======================

-- 최종조회

--=======================

-- PL계정(계정대분류 비용, 수익) 및 BS계정(계정대분류 자산, 자본, 부채, 현금흐름과목 설정된 것만)에 대한 발생금액 조회

INSERT INTO \#Result

SELECT ISNULL(F.CFSCoName, '') AS CFSCoName, -- 법인명

ISNULL(F.CFSCoNo, '') AS CFSCoNo, -- 법인번호

ISNULL(F.CFSCoUnit, 0) AS CFSCoUnit, -- 법인내부코드(추출여부에 체크된 관계사, 즉 현재법인)

ISNULL(D.CFSCoName, '') AS CustCFSCoName, -- 관계사법인명

ISNULL(D.CFSCoNo, '') AS CustCFSCoNo, -- 관계사법인번호

ISNULL(C.CFSCoUnit, 0) AS CustCFSCoUnit, -- 관계사법인내부코드(계열사의 거래처내부코드)

ISNULL(H.MinorName, '') AS SMAccKindName, -- 계정대분류

ISNULL(G.SMAccKind, 0) AS SMAccKind, -- 계정대분류내부코드

ISNULL(G.AccName, '') AS AccName, -- 계정과목

ISNULL(G.AccNo, '') AS AccNo, -- 계정과목번호

ISNULL(A.AccSeq, 0) AS AccSeq, -- 계정과목내부코드

ISNULL(I.CashItemName, '') AS CashItemName, -- 현금흐름과목

ISNULL(A.CashItemSeq, 0) AS CashItemSeq, -- 현금흐름과목내부코드

SUM((ISNULL(A.DrAmt, 0) - ISNULL(A.CrAmt, 0)) \* ISNULL(G.SMDrOrCr, 0))

AS Amt, -- 금액

ISNULL(SUM(A.DrAmt), 0) AS DrAmt, -- 차변금액

ISNULL(SUM(A.CrAmt), 0) AS CrAmt -- 대변금액

,ISNULL(J.FSItemYardiCode,'') AS FSItemYardiCode -- 24.05.20 SJPARK

,ISNULL(J.FSItemName,'') AS FSItemName -- Yardi_계정명 -- 2024.05.28 ghwi

,ISNULL(K.CurrName ,'') AS CurrName -- 화폐 추가 24.06.04 SJPARK

,ISNULL(A.CurrSeq , 0) AS CurrSeq -- 화폐 추가 24.06.04 SJPARK

FROM \_TACSlipRow AS A WITH(NOLOCK)

JOIN \_TACCFSCompanyCust AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq -- 연결자료추출환경설정 화면의 우측 거래처 시트

AND A.CoCustSeq = C.CustSeq

JOIN \_TACCFSCompany AS D WITH(NOLOCK) ON C.CompanySeq = D.CompanySeq -- 연결자료추출환경설정 화면

AND C.CFSCoUnit = D.CFSCoUnit

JOIN \_TDACust AS E WITH(NOLOCK) ON C.CompanySeq = E.CompanySeq

AND C.CustSeq = E.CustSeq

JOIN \_TACCFSCompany AS F WITH(NOLOCK) ON A.CompanySeq = F.CompanySeq -- 연결자료추출환경설정 화면

AND F.IsSelf = '1' -- 추출회사여부

JOIN \_TDAAccount AS G WITH(NOLOCK) ON A.CompanySeq = G.CompanySeq

AND A.AccSeq = G.AccSeq

LEFT OUTER JOIN \_TDASMinor AS H WITH(NOLOCK) ON G.CompanySeq = H.CompanySeq

AND G.SMAccKind = H.MinorSeq

LEFT OUTER JOIN \_TACDCashItem AS I WITH(NOLOCK) ON A.CompanySeq = I.CompanySeq

AND A.CashItemSeq = I.CashItemSeq

LEFT OUTER JOIN \#TMP_CodeYardi AS J WITH(NOLOCK) ON J.AccSeq = A.AccSeq

LEFT OUTER JOIN \_TDACurr AS K WITH(NOLOCK) ON K.CompanySeq = A.CompanySeq

AND K.CurrSeq = A.CurrSeq

 

WHERE A.CompanySeq = @CompanySeq

AND ISNULL(A.IsSet, '0') = '1' -- 승인된 건만 조회

AND(

--ISNULL(G.SMAccKind, 0) IN (4018004, 4018005) -- PL계정(계정대분류 비용, 수익)

--OR

(ISNULL(G.SMAccKind, 0) IN (4018001, 4018002, 4018003) AND -- BS계정(계정대분류 자산, 자본, 부채)은 계정과목에 현금흐름과목종류가 설정된 건만 조회합니다.

ISNULL(G.SMCashItemKind, 0) \<\> 0) AND

ISNULL(A.CashItemSeq, 0) \<\> 0)

AND LEFT(ISNULL(A.AccDate, ''), 6) BETWEEN @AccDateFr AND @AccDateTo

AND (@CFSCoName = '' OR ISNULL(F.CFSCoName, '') LIKE @CFSCoName + '%')

AND (@CustCFSCoName = '' OR ISNULL(D.CFSCoName, '') LIKE @CustCFSCoName + '%')

AND F.CFSCoNo \<\> D.CFSCoNo -- 자회사 건 조회안되도록 추가 24.05.23 SJPARK

GROUP BY ISNULL(F.CFSCoName, '') ,

ISNULL(F.CFSCoNo, '') ,

ISNULL(F.CFSCoUnit, 0) ,

ISNULL(D.CFSCoName, '') ,

ISNULL(D.CFSCoNo, '') ,

ISNULL(C.CFSCoUnit, 0) ,

ISNULL(H.MinorName, '') ,

ISNULL(G.SMAccKind, 0) ,

ISNULL(G.AccName, '') ,

ISNULL(G.AccNo, '') ,

ISNULL(A.AccSeq, 0) ,

ISNULL(I.CashItemName, ''),

ISNULL(A.CashItemSeq, 0)

,ISNULL(J.FSItemYardiCode,'')

,ISNULL(J.FSItemName,'')

,ISNULL(A.CurrSeq , 0)

,ISNULL(K.CurrName ,'')

HAVING (SUM((ISNULL(A.DrAmt, 0) - ISNULL(A.CrAmt, 0)) \* ISNULL(G.SMDrOrCr, 0)) \<\> 0 -- 금액이 있는 건만 조회합니다.

OR ISNULL(SUM(A.DrAmt), 0) \<\> 0

OR ISNULL(SUM(A.CrAmt), 0) \<\> 0)

UNION ALL

-- BS계정(계정대분류 자산, 자본, 부채)에 대한 잔액 조회

SELECT ISNULL(F.CFSCoName, '') AS CFSCoName, -- 법인명

ISNULL(F.CFSCoNo, '') AS CFSCoNo, -- 법인번호

ISNULL(F.CFSCoUnit, 0) AS CFSCoUnit, -- 법인내부코드(추출여부에 체크된 관계사, 즉 현재법인)

ISNULL(D.CFSCoName, '') AS CustCFSCoName, -- 관계사법인명

ISNULL(D.CFSCoNo, '') AS CustCFSCoNo, -- 관계사법인번호

ISNULL(C.CFSCoUnit, 0) AS CustCFSCoUnit, -- 관계사법인내부코드(계열사의 거래처내부코드)

ISNULL(H.MinorName, '') AS SMAccKindName, -- 계정대분류

ISNULL(G.SMAccKind, 0) AS SMAccKind, -- 계정대분류내부코드

ISNULL(G.AccName, '') AS AccName, -- 계정과목

ISNULL(G.AccNo, '') AS AccNo, -- 계정과목번호

ISNULL(A.AccSeq, 0) AS AccSeq, -- 계정과목내부코드

'' AS CashItemName, -- 현금흐름과목

0 AS CashItemSeq, -- 현금흐름과목내부코드

--ISNULL(A.RemAmt, 0) AS Amt, -- 금액(BS계정 잔액)

ISNULL(A.IniAmt, 0) AS Amt, -- 금액(BS계정 잔액) 24.10.08

ISNULL(A.DrAmt, 0) AS DrAmt, -- 차변금액

ISNULL(A.CrAmt, 0) AS CrAmt -- 대변금액

,ISNULL(J.FSItemYardiCode,'') AS FSItemYardiCode -- 24.05.20 SJPARK

,ISNULL(J.FSItemName, '') AS FSItemName -- Yardi_계정명 -- 2024.05.28 ghwi

,ISNULL(K.CurrName ,'') AS CurrName -- 화폐 추가 24.06.04 SJPARK

,ISNULL(A.CurrSeq , 0) AS CurrSeq -- 화폐 추가 24.06.04 SJPARK

FROM \#SlipSum AS A

JOIN \_TACCFSCompanyCust AS C WITH(NOLOCK) ON C.CompanySeq = @CompanySeq -- 연결자료추출환경설정 화면의 우측 거래처 시트

AND A.RemValSeq1 = C.CustSeq

JOIN \_TACCFSCompany AS D WITH(NOLOCK) ON C.CompanySeq = D.CompanySeq -- 연결자료추출환경설정 화면

AND C.CFSCoUnit = D.CFSCoUnit

JOIN \_TDACust AS E WITH(NOLOCK) ON C.CompanySeq = E.CompanySeq

AND C.CustSeq = E.CustSeq

JOIN \_TACCFSCompany AS F WITH(NOLOCK) ON F.CompanySeq = @CompanySeq -- 연결자료추출환경설정 화면

AND F.IsSelf = '1' -- 추출회사여부

LEFT OUTER JOIN \_TDAAccount AS G WITH(NOLOCK) ON G.CompanySeq = @CompanySeq

AND A.AccSeq = G.AccSeq

LEFT OUTER JOIN \_TDASMinor AS H WITH(NOLOCK) ON G.CompanySeq = H.CompanySeq

AND G.SMAccKind = H.MinorSeq

LEFT OUTER JOIN \#TMP_CodeYardi AS J WITH(NOLOCK) ON J.AccSeq = A.AccSeq

LEFT OUTER JOIN \_TDACurr AS K WITH(NOLOCK) ON K.CompanySeq = @CompanySeq

AND K.CurrSeq = A.CurrSeq

WHERE ISNULL(G.SMAccKind, 0) IN (4018001, 4018002, 4018003,4018004, 4018005) -- BS계정(계정대분류 자산, 자본, 부채)

AND ISNULL(G.IsCash, '0') \<\> '1' -- 현금성여부에 체크된 계정은 조회대상에서 제외합니다.

AND A.RemSeq1 = 1017

AND (@CFSCoName = '' OR ISNULL(F.CFSCoName, '') LIKE @CFSCoName + '%')

AND (@CustCFSCoName = '' OR ISNULL(D.CFSCoName, '') LIKE @CustCFSCoName + '%')

AND F.CFSCoNo \<\> D.CFSCoNo -- 자회사 건 조회안되도록 추가 24.05.23 SJPARK

ORDER BY CashItemName, ISNULL(F.CFSCoName, ''), ISNULL(G.AccNo, ''), ISNULL(D.CFSCoNo, '')

 

 

 

RETURN

GO

DECLARE @CONST\_#BIZ_IN_DataBlock1 INT

, @CONST\_#BIZ_OUT_DataBlock1 INT

SELECT @CONST\_#BIZ_IN_DataBlock1 = 0

, @CONST\_#BIZ_OUT_DataBlock1 = 0

 

 

 

IF @CONST\_#BIZ_IN_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, AccDateFr NCHAR(6), AccDateTo NCHAR(6), CFSCoName NVARCHAR(100), CustCFSCoName NVARCHAR(100)

)

SET @CONST\_#BIZ_IN_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, AccDateFr NCHAR(6), AccDateTo NCHAR(6), CFSCoName NVARCHAR(100), CFSCoNo NVARCHAR(100), CFSCoUnit INT, CustCFSCoName NVARCHAR(100), CustCFSCoNo NVARCHAR(100), CustCFSCoUnit INT, SMAccKindName NVARCHAR(100), SMAccKind INT, AccName NVARCHAR(100), AccSeq INT, CashItemName NVARCHAR(100), CashItemSeq INT, Amt DECIMAL(19, 5), DrAmt DECIMAL(19, 5), CrAmt DECIMAL(19, 5), AccNo NVARCHAR(100), FSItemYardiCode NVARCHAR(100), SumAmt DECIMAL(19, 5), FSItemName NVARCHAR(100), CurrName NVARCHAR(100), CurrSeq INT

)

SET @CONST\_#BIZ_OUT_DataBlock1 = 1

 

END

 

DECLARE @INPUT_ERROR_MESSAGE NVARCHAR(4000)

, @INPUT_ERROR_SEVERITY INT

, @INPUT_ERROR_STATE INT

, @INPUT_ERROR_PROCEDURE NVARCHAR(128)

INSERT INTO \#BIZ_IN_DataBlock1 (WorkingTag, IDX_NO, DataSeq, Selected, Status, Result, ROW_IDX, IsChangedMst, TABLE_NAME, AccDateFr, AccDateTo, CFSCoName, CustCFSCoName)

SELECT N'A', 1, 1, 1, 0, NULL, NULL, N'1', N'DataBlock1', N'202410', N'202410', N'에코%', N'%탑'

IF @@ERROR \<\> 0 RETURN

 

 

DECLARE @HasError NCHAR(1)

, @UseTransaction NCHAR(1)

-- 내부 SP용 파라메터

, @ServiceSeq INT

, @MethodSeq INT

, @WorkingTag NVARCHAR(10)

, @CompanySeq INT

, @LanguageSeq INT

, @UserSeq INT

, @PgmSeq INT

, @IsTransaction BIT

 

SET @HasError = N'0'

SET @UseTransaction = N'0'

 

BEGIN TRY

 

SET @ServiceSeq = 101820373

--SET @MethodSeq = 1

SET @WorkingTag = N''

SET @CompanySeq = 1

SET @LanguageSeq = 1

SET @UserSeq = 1

SET @PgmSeq = 101820389

SET @IsTransaction = 0

 

-- ExecuteOrder : 1 : Start

EXEC emk_SWACInnerDealListQuery_TTEST

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

 

IF @@ERROR \<\> 0

BEGIN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 1 : End

 

GOTO_END:

 

END TRY

BEGIN CATCH

-- SQL 오류인 경우는 여기서 처리가 된다

IF @UseTransaction = N'1'

ROLLBACK TRAN

DECLARE @ERROR_MESSAGE NVARCHAR(4000)

, @ERROR_SEVERITY INT

, @ERROR_STATE INT

, @ERROR_PROCEDURE NVARCHAR(128)

 

SELECT @ERROR_MESSAGE = ERROR_MESSAGE()

, @ERROR_SEVERITY = ERROR_SEVERITY()

, @ERROR_STATE = ERROR_STATE()

, @ERROR_PROCEDURE = ERROR_PROCEDURE()

RAISERROR (@ERROR_MESSAGE, @ERROR_SEVERITY, @ERROR_STATE, @ERROR_PROCEDURE)

 

RETURN

END CATCH

 

-- SQL 오류를 제외한 체크로직으로 발생된 오류는 여기서 처리

IF @HasError = N'1' AND @UseTransaction = N'1'

ROLLBACK TRAN

 

 

DROP TABLE \#BIZ_IN_DataBlock1

DROP TABLE \#BIZ_OUT_DataBlock1

 

 

GO

 

IF OBJECT_ID ('tempdb..#BIZ_IN_DataBlock1') IS NOT NULL DROP TABLE \#BIZ_IN_DataBlock1

IF OBJECT_ID ('tempdb..#BIZ_OUT_DataBlock1') IS NOT NULL DROP TABLE \#BIZ_OUT_DataBlock1

 

 

 

DECLARE @CONST\_#BIZ_IN_DataBlock1 INT

, @CONST\_#BIZ_OUT_DataBlock1 INT

SELECT @CONST\_#BIZ_IN_DataBlock1 = 0

, @CONST\_#BIZ_OUT_DataBlock1 = 0

 

 

 

IF @CONST\_#BIZ_IN_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, AccDateFr NCHAR(6), AccDateTo NCHAR(6), CFSCoName NVARCHAR(100), CustCFSCoName NVARCHAR(100)

)

SET @CONST\_#BIZ_IN_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, AccDateFr NCHAR(6), AccDateTo NCHAR(6), CFSCoName NVARCHAR(100), CFSCoNo NVARCHAR(100), CFSCoUnit INT, CustCFSCoName NVARCHAR(100), CustCFSCoNo NVARCHAR(100), CustCFSCoUnit INT, SMAccKindName NVARCHAR(100), SMAccKind INT, AccName NVARCHAR(100), AccSeq INT, CashItemName NVARCHAR(100), CashItemSeq INT, Amt DECIMAL(19, 5), DrAmt DECIMAL(19, 5), CrAmt DECIMAL(19, 5), AccNo NVARCHAR(100), FSItemYardiCode NVARCHAR(100), SumAmt DECIMAL(19, 5), FSItemName NVARCHAR(100), CurrName NVARCHAR(100), CurrSeq INT

)

SET @CONST\_#BIZ_OUT_DataBlock1 = 1

 

END

 

DECLARE @INPUT_ERROR_MESSAGE NVARCHAR(4000)

, @INPUT_ERROR_SEVERITY INT

, @INPUT_ERROR_STATE INT

, @INPUT_ERROR_PROCEDURE NVARCHAR(128)

INSERT INTO \#BIZ_IN_DataBlock1 (WorkingTag, IDX_NO, DataSeq, Selected, Status, Result, ROW_IDX, IsChangedMst, TABLE_NAME, AccDateFr, AccDateTo, CFSCoName, CustCFSCoName)

SELECT N'A', 1, 1, 1, 0, NULL, NULL, N'1', N'DataBlock1', N'202410', N'202410', N'에코%', N'%탑'

IF @@ERROR \<\> 0 RETURN

 

 

DECLARE @HasError NCHAR(1)

, @UseTransaction NCHAR(1)

-- 내부 SP용 파라메터

, @ServiceSeq INT

, @MethodSeq INT

, @WorkingTag NVARCHAR(10)

, @CompanySeq INT

, @LanguageSeq INT

, @UserSeq INT

, @PgmSeq INT

, @IsTransaction BIT

 

SET @HasError = N'0'

SET @UseTransaction = N'0'

 

BEGIN TRY

 

SET @ServiceSeq = 101820373

--SET @MethodSeq = 1

SET @WorkingTag = N''

SET @CompanySeq = 1

SET @LanguageSeq = 1

SET @UserSeq = 1

SET @PgmSeq = 101820389

SET @IsTransaction = 0

 

-- ExecuteOrder : 1 : Start

EXEC emk_SWACInnerDealListQuery_TTEST

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

 

IF @@ERROR \<\> 0

BEGIN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 1 : End

 

GOTO_END:

 

END TRY

BEGIN CATCH

-- SQL 오류인 경우는 여기서 처리가 된다

IF @UseTransaction = N'1'

ROLLBACK TRAN

DECLARE @ERROR_MESSAGE NVARCHAR(4000)

, @ERROR_SEVERITY INT

, @ERROR_STATE INT

, @ERROR_PROCEDURE NVARCHAR(128)

 

SELECT @ERROR_MESSAGE = ERROR_MESSAGE()

, @ERROR_SEVERITY = ERROR_SEVERITY()

, @ERROR_STATE = ERROR_STATE()

, @ERROR_PROCEDURE = ERROR_PROCEDURE()

RAISERROR (@ERROR_MESSAGE, @ERROR_SEVERITY, @ERROR_STATE, @ERROR_PROCEDURE)

 

RETURN

END CATCH

 

-- SQL 오류를 제외한 체크로직으로 발생된 오류는 여기서 처리

IF @HasError = N'1' AND @UseTransaction = N'1'

ROLLBACK TRAN

 

 

DROP TABLE \#BIZ_IN_DataBlock1

DROP TABLE \#BIZ_OUT_DataBlock1