---
title: 25.01.07 엘에스머트리얼즈 BOM승인_sys 변경대상품목 차수 -1 조회 건
date: 2025-01-07
tags: [erp, 개발, 기록]
---

# 25.01.07 엘에스머트리얼즈 BOM승인_sys 변경대상품목 차수 -1 조회 건

BEGIN TRAN

 

GO

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*

FORM NAME - FrmWPDBOMECOList (ECO등록조회)

DEFINITION - ECO등록조회_자재별모품목상세조회_WBS

DESCRIPTION - ECO등록된 자재별 모품목 상세조회

CREATE DATE - 2013.10.28 CREATE BY 김고은

UPDATE DATE - 2013.10.28 UPDATE BY

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

CREATE PROCEDURE dbo.sys_SWPDBOMCfmChangeBOMQuery_TTEST

@ServiceSeq INT = 0,

@WorkingTag NVARCHAR(10)= '',

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 0,

@PgmSeq INT = 0,

@IsTransaction BIT = 0

AS

 

--==================================================

-- 변수선언

--==================================================

DECLARE @ItemSeq INT,

@BOMReqSeq INT,

@ItemBomRev NCHAR(2)

--==================================================

-- 변수에 담기

--==================================================

SELECT TOP 1

@ItemSeq = ISNULL(ItemSeqTree, 0)

, @BOMReqSeq = ISNULL(BOMReqSeq , 0)

FROM \#BIZ_IN_DATABLOCK1

 

SELECT @ItemBomRev = A.ItemBOMRevRemark

FROM sys_TPDBOMReqManagement AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.ItemSeq = @ItemSeq

AND A.BOMReqSeq = @BOMReqSeq

SELECT @ItemBOMRev

 

SELECT @ItemBOMRev = RIGHT('00' + CONVERT(NVARCHAR, CONVERT(INT, @ItemBOMRev) -1),2)

 

CREATE TABLE \#ReversedBOM

(

CompanySeq INT NOT NULL,

Seq INT NOT NULL,

ParentSeq INT NOT NULL,

ItemSeq INT NOT NULL,

Sort INT NOT NULL,

TreeLevel INT NOT NULL,

ItemRev CHAR(4) NOT NULL,

NeedQtyNumerator DECIMAL(20,10) null,

NeedQtyDenominator DECIMAL(20,10) null,

TextLevel NVARCHAR(60) null

)

INSERT INTO \#ReversedBOM

SELECT @CompanySeq, 1, 0, @ItemSeq, 1, 1, @ItemBOMRev, 1, 1, '1'

 

 

EXEC \_SPDReversBOM @CompanySeq, @ItemSeq, @ItemBOMRev, 1, 1, 0, '1'

 

 

 

 

 

DELETE \#ReversedBOM FROM \#ReversedBOM WHERE NeedQtyDenominator = 0 -- 소요량이 필수이기 때문에 분모가 0인 건은 삭제

 

SELECT A.Seq AS Seq, A.ParentSeq AS ParentSeq, A.Sort, A.TreeLevel,

ISNULL(A.ItemSeq, 0) AS ItemSeq,

CASE B.SMStatus WHEN 2001002 THEN '◈폐기◈' ELSE '' END +

ISNULL(B.ItemName, '') + '(' + RTRIM(ISNULL(B.ItemNo, '')) + ') \[' + LTRIM(RTRIM(ISNULL(A.ItemRev, ''))) + '\]' + ' 수량 : ' +

CAST(CASE ISNULL(A.ItemSeq, 0) WHEN @ItemSeq THEN CAST((ISNULL(NeedQtyNumerator, 0) / ISNULL(NeedQtyDenominator, 0)) AS DECIMAL(20, 10))

ELSE CAST((ISNULL(NeedQtyDenominator, 0) / ISNULL(NeedQtyNumerator, 0)) AS DECIMAL(20, 10))

END AS NVARCHAR(100) ) AS NodeName,

ISNULL(NeedQtyNumerator, 0) AS NeedQtyNumerator ,

ISNULL(NeedQtyDenominator, 0) AS NeedQtyDenominator,

CASE D.SMAssetGrp WHEN 6008002 THEN 'ProdItem'

WHEN 6008004 THEN 'SemiItem'

WHEN 6008005 THEN 'AssyItem'

ELSE 'MatItem'

END AS NodeImg, -- 2013.03.28 김권우

CASE ISNULL(A.ItemSeq, 0) WHEN @ItemSeq THEN ISNULL(NeedQtyNumerator, 0) / ISNULL(NeedQtyDenominator, 0)

ELSE ISNULL(NeedQtyDenominator, 0) / ISNULL(NeedQtyNumerator, 0) END AS Qty,

-- 시트 조회 항목 추가 -- 11.12.09 BY 김세호

LTRIM(RTRIM(ISNULL(A.ItemRev, ''))) AS ItemBOMRev,

CASE B.SMStatus WHEN 2001002 THEN '◈폐기◈' ELSE '' END + +B.ItemName AS ItemName,

B.ItemNo AS ItemNo,

B.Spec AS Spec,

C.UnitSeq AS UnitSeq,

C.UnitName AS UnitName,

D.SMAssetGrp AS AssetSeq,

E.MinorName AS AssetName

FROM \#ReversedBOM AS A WITH(NOLOCK)

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq

AND A.ItemSeq = B.ItemSeq

LEFT OUTER JOIN \_TDAUnit AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq

AND B.UnitSeq = C.UnitSeq

LEFT OUTER JOIN \_TDAItemAsset AS D WITH(NOLOCK) ON B.CompanySeq = D.CompanySeq

AND B.AssetSeq = D.AssetSeq

LEFT OUTER JOIN \_TDASMinor AS E WITH(NOLOCK) ON D.CompanySeq = E.CompanySeq

AND D.SMAssetGrp = E.MinorSeq

ORDER BY A.TextLevel

 

RETURN

GO

 

IF OBJECT_ID ('tempdb..#BIZ_IN_DataBlock1') IS NOT NULL DROP TABLE \#BIZ_IN_DataBlock1

IF OBJECT_ID ('tempdb..#BIZ_OUT_DataBlock1') IS NOT NULL DROP TABLE \#BIZ_OUT_DataBlock1

IF OBJECT_ID ('tempdb..#BIZ_IN_DataBlock2') IS NOT NULL DROP TABLE \#BIZ_IN_DataBlock2

IF OBJECT_ID ('tempdb..#BIZ_OUT_DataBlock2') IS NOT NULL DROP TABLE \#BIZ_OUT_DataBlock2

IF OBJECT_ID ('tempdb..#BIZ_IN_DataBlock3') IS NOT NULL DROP TABLE \#BIZ_IN_DataBlock3

IF OBJECT_ID ('tempdb..#BIZ_OUT_DataBlock3') IS NOT NULL DROP TABLE \#BIZ_OUT_DataBlock3

IF OBJECT_ID ('tempdb..#BIZ_IN_DataBlock4') IS NOT NULL DROP TABLE \#BIZ_IN_DataBlock4

IF OBJECT_ID ('tempdb..#BIZ_OUT_DataBlock4') IS NOT NULL DROP TABLE \#BIZ_OUT_DataBlock4

 

DECLARE @CONST\_#BIZ_IN_DataBlock1 INT

, @CONST\_#BIZ_IN_DataBlock4 INT

, @CONST\_#BIZ_OUT_DataBlock1 INT

, @CONST\_#BIZ_OUT_DataBlock4 INT

SELECT @CONST\_#BIZ_IN_DataBlock1 = 0

, @CONST\_#BIZ_IN_DataBlock4 = 0

, @CONST\_#BIZ_OUT_DataBlock1 = 0

, @CONST\_#BIZ_OUT_DataBlock4 = 0

 

 

IF @CONST\_#BIZ_IN_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, ItemSeqTree INT, BOMReqSeq INT

)

SET @CONST\_#BIZ_IN_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, ItemName NVARCHAR(500), ItemNo NVARCHAR(500), ItemSpec NVARCHAR(500), SMIsSetName NVARCHAR(100), ReqDateFr NVARCHAR(8), ReqDateTo NVARCHAR(8), EmpName NVARCHAR(100), ReqNo NVARCHAR(100), AssetName NVARCHAR(100), ItemSClassName NVARCHAR(100), ItemSeq INT, SMIsSet INT, EmpSeq INT, AssetSeq INT, ItemSClassSeq INT, IsAppl NVARCHAR(1), BOMReqNo NVARCHAR(100), RegDate NVARCHAR(8), LastEmpName NVARCHAR(100), LastUptDate NVARCHAR(8), RegEmpName NVARCHAR(100), Spec NVARCHAR(500), ItemSeqTree INT, BOMReqSeq INT, ItemBomRevTree NVARCHAR(2), TableName NVARCHAR(100), Agree1 NVARCHAR(1), Agree2 NVARCHAR(1), Agree3 NVARCHAR(1), CheckDelvTeamManager NVARCHAR(1), Agree1Remark NVARCHAR(1000), Agree2Remark NVARCHAR(1000), Agree3Remark NVARCHAR(1000), DelvTeamManagerRemark NVARCHAR(1000), RegCompleteYn NVARCHAR(1)

)

SET @CONST\_#BIZ_OUT_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_IN_DataBlock4 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock4

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, AssetName NVARCHAR(100), ItemName NVARCHAR(100), ItemNo NVARCHAR(100), Spec NVARCHAR(100), UnitName NVARCHAR(100), ItemBOMRev NVARCHAR(100), IsLastRev NVARCHAR(100), Qty DECIMAL(19, 5), AssetSeq INT, UnitSeq INT, ItemSeq INT, ItemClassLName INT, ItemClassMName INT, ItemClassSName INT, ItemClassLSeq INT, ItemClassMSeq INT, ItemClassSSeq INT

)

SET @CONST\_#BIZ_IN_DataBlock4 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock4 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock4

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, AssetName NVARCHAR(100), ItemName NVARCHAR(100), ItemNo NVARCHAR(100), Spec NVARCHAR(100), UnitName NVARCHAR(100), ItemBOMRev NVARCHAR(100), IsLastRev NVARCHAR(100), Qty DECIMAL(19, 5), AssetSeq INT, UnitSeq INT, ItemSeq INT, ItemClassLName INT, ItemClassMName INT, ItemClassSName INT, ItemClassLSeq INT, ItemClassMSeq INT, ItemClassSSeq INT

)

SET @CONST\_#BIZ_OUT_DataBlock4 = 1

 

END

 

DECLARE @INPUT_ERROR_MESSAGE NVARCHAR(4000)

, @INPUT_ERROR_SEVERITY INT

, @INPUT_ERROR_STATE INT

, @INPUT_ERROR_PROCEDURE NVARCHAR(128)

INSERT INTO \#BIZ_IN_DataBlock1 (WorkingTag, IDX_NO, DataSeq, Selected, Status, Result, ROW_IDX, IsChangedMst, TABLE_NAME, ItemSeqTree, BOMReqSeq)

SELECT N'', 1, 1, 1, 0, NULL, NULL, NULL, N'DataBlock1', N'175734', N'56'

IF @@ERROR \<\> 0 RETURN

 

 

DECLARE @HasError NCHAR(1)

, @UseTransaction NCHAR(1)

-- 내부 SP용 파라메터

, @ServiceSeq INT

, @MethodSeq INT

, @WorkingTag NVARCHAR(10)

, @CompanySeq INT

, @LanguageSeq INT

, @UserSeq INT

, @PgmSeq INT

, @IsTransaction BIT

 

SET @HasError = N'0'

SET @UseTransaction = N'0'

 

BEGIN TRY

 

SET @ServiceSeq = 54620077

--SET @MethodSeq = 11

SET @WorkingTag = N''

SET @CompanySeq = 1

SET @LanguageSeq = 1

SET @UserSeq = 1

SET @PgmSeq = 54620072

SET @IsTransaction = 0

 

-- ExecuteOrder : 1 : Start

EXEC sys_SWPDBOMCfmChangeBOMQuery_TTEST

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

IF @@ERROR \<\> 0

BEGIN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 1 : End

 

GOTO_END:

END TRY

BEGIN CATCH

-- SQL 오류인 경우는 여기서 처리가 된다

IF @UseTransaction = N'1'

ROLLBACK TRAN

DECLARE @ERROR_MESSAGE NVARCHAR(4000)

, @ERROR_SEVERITY INT

, @ERROR_STATE INT

, @ERROR_PROCEDURE NVARCHAR(128)

 

SELECT @ERROR_MESSAGE = ERROR_MESSAGE()

, @ERROR_SEVERITY = ERROR_SEVERITY()

, @ERROR_STATE = ERROR_STATE()

, @ERROR_PROCEDURE = ERROR_PROCEDURE()

RAISERROR (@ERROR_MESSAGE, @ERROR_SEVERITY, @ERROR_STATE, @ERROR_PROCEDURE)

 

RETURN

END CATCH

 

-- SQL 오류를 제외한 체크로직으로 발생된 오류는 여기서 처리

IF @HasError = N'1' AND @UseTransaction = N'1'

ROLLBACK TRAN

 

 

DROP TABLE \#BIZ_IN_DataBlock1

DROP TABLE \#BIZ_OUT_DataBlock1

DROP TABLE \#BIZ_IN_DataBlock4

DROP TABLE \#BIZ_OUT_DataBlock4

 

ROLLBACK TRAN