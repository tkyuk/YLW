---
title: 자재예상재고조회_dhi / 소요량 합계 미 조회
date: 2024-04-15
tags: [erp, 개발, 기록]
---

# 자재예상재고조회_dhi / 소요량 합계 미 조회

DROP PROCEDURE dhi_SPUForeMatListQuery_ttest

GO

CREATE PROCEDURE dhi_SPUForeMatListQuery_ttest

@ServiceSeq INT = 0,

@WorkingTag NVARCHAR(10)= '',

@CompanySeq INT = 1,

@LanguageSeq INT = 1,

@UserSeq INT = 0,

@PgmSeq INT = 0,

@IsTransaction CHAR(1)

AS

-- 조회조건 변수 선언

DECLARE

@FactUnit INT,

@DateFr NVARCHAR(8),

@DateTo NVARCHAR(8),

@AssetType INT,

@ItemName NVARCHAR(100),

@ItemNo NVARCHAR(100),

@Spec NVARCHAR(100),

@nItemPoint INT,

@AddDelvDate DECIMAL(19,5),

@DelvDateFr NVARCHAR(8),

@DelvDateTo NVARCHAR(8),

@PODelvDateFr NVARCHAR(8),

@PODelvDateTo NVARCHAR(8),

@DateYM NCHAR(6), -- 20190806.기준월

@IsLink NCHAR(1), -- 20190806.링크드여부

@CurrYM NCHAR(6), -- 현재월

@ForeDateFr NCHAR(8),

@ForeDateTo NCHAR(8)

 

-- 조회부

SELECT

@FactUnit = ISNULL(FactUnit, 0),

@DateFr = ISNULL(DateFr, ''),

@DateTo = ISNULL(DateTo, ''),

@AssetType = ISNULL(AssetType, 0),

@ItemName = ISNULL(ItemName, ''),

@ItemNo = ISNULL(ItemNo, ''),

@Spec = ISNULL(Spec, ''),

@DateYM = ISNULL(DateYM, ''), -- 20190806.기준월

@IsLink = ISNULL(IsLink, '') -- 20190806.링크드여부

FROM \#BIZ_IN_DataBlock1 AS X

IF @IsLink = '1'

BEGIN

SELECT

@DateFr = @DateYM + '01',

@DateTo = CONVERT(NCHAR(8),DATEADD(D,-1,DATEADD(M,1,@DateYM + '01')),112)

END

-- EnvSeq = 4 BOM 자리수

EXEC dbo.\_SCOMEnv @CompanySeq,4,@UserSeq,@@PROCID,@nItemPoint OUTPUT

-- 자재납기일

EXEC dbo.\_SCOMEnv @CompanySeq,2000136,@UserSeq,@@PROCID,@AddDelvDate OUTPUT

SELECT

@DelvDateFr = CONVERT(NVARCHAR(8), DATEADD(DAY, -8, @DateFr ),112),

@DelvDateTo = CONVERT(NVARCHAR(8), DATEADD(DAY, -1, @DateFr ),112),

@PODelvDateFr = CONVERT(NVARCHAR(8), DATEADD(DAY, -1, DATEADD(Month,-3,@DateFr)),112),

@PODelvDateTo = CONVERT(NVARCHAR(8), DATEADD(DAY, -1, @DateFr ),112)

--================================================================================================================================

-- 타이틀구하기

--================================================================================================================================

-- 타이틀

CREATE TABLE \#Title (

ColIDX INT IDENTITY(0,1),

TitleName NVARCHAR(100),

TitleSeq INT,

TitleName2 NVARCHAR(100),

TitleSeq2 INT

)

-- From ~ To 기간 날짜 리스트

INSERT \#Title (TitleName, TitleSeq, TitleName2, TitleSeq2)

SELECT

SUBSTRING(A.Solar,1,4) + '-' + SUBSTRING(A.Solar,5,2) + '-' + SUBSTRING(A.Solar,7,2),

A.Solar,

B.TitleName2,

B.TitleSeq2

FROM \_TCOMCalendar AS A

LEFT OUTER JOIN (SELECT TitleName2 = '소요예정', TitleSeq2 = 1

UNION ALL

SELECT TitleName2 = '입고예정', TitleSeq2 = 2

UNION ALL

SELECT TitleName2 = '예상재고', TitleSeq2 = 3

) AS B ON 1=1

WHERE (1=1)

AND A.Solar BETWEEN @DateFr AND @DateTo

ORDER BY A.Solar, B.TitleSeq2

--================================================================================================================================

-- BEGIN 고정값 구하기

--================================================================================================================================

CREATE TABLE \#TmpCol (

RowIDX INT IDENTITY(0,1),

ItemName NVARCHAR(100),

ItemNo NVARCHAR(100),

Spec NVARCHAR(100),

ItemSeq INT,

PrevStockQty DECIMAL(19,5),

FinForeCastStockQty DECIMAL(19,5),

NeedDelvDate NVARCHAR(8)

)

CREATE TABLE \#TmpCol2 (

RowIDX INT IDENTITY(0,1),

ItemName NVARCHAR(100),

ItemNo NVARCHAR(100),

Spec NVARCHAR(100),

ItemSeq INT,

PrevStockQty DECIMAL(19,5),

FinForeCastStockQty DECIMAL(19,5),

NeedDelvDate NVARCHAR(8)

)

CREATE TABLE \#FixCol (

RowIDX INT IDENTITY(0,1),

ItemName NVARCHAR(100),

ItemNo NVARCHAR(100),

Spec NVARCHAR(100),

ItemSeq INT,

PrevStockQty DECIMAL(19,5), -- 이월재고

FinForeCastStockQty DECIMAL(19,5), -- 최종예상재고

NeedDelvDate NVARCHAR(8), -- 자재입고필요일

NotInQty DECIMAL(19,5), -- 입고검사대기수량

TotNeedQty DECIMAL(19,5), -- 소요량합계

TotInQty DECIMAL(19,5), -- 입고예정합계

FirstDelvDate NVARCHAR(8), -- 입고예정일

LastDelvDate NVARCHAR(8), -- 입고예정일2

StepQty DECIMAL(19,5), -- 구매간격수량

SafetyQty DECIMAL(19,5), -- 안전재고수량

PurQty DECIMAL(19,5), -- 구매수량

NotDelvQty DECIMAL(19,5), -- 발주납품수량

SumInQty DECIMAL(19,5), -- 총입고예정수량

PurEstQty DECIMAL(19,5), -- 구매산정량

ExptStockQty DECIMAL(19,5), -- 예상재고수량

DelvDate NCHAR(8),

EtcInQty DECIMAL(19,5), -- 자재기타입고수량

EtcOutQty DECIMAL(19,5) -- 자재기타출고수량

)

CREATE TABLE \#GetInOutItem (

ItemSeq INT

)

CREATE TABLE \#GetInOutStock (

WHSeq INT,

FunctionWHSeq INT,

ItemSeq INT,

UnitSeq INT,

PrevQty DECIMAL(19,5),

InQty DECIMAL(19,5),

OutQty DECIMAL(19,5),

StockQty DECIMAL(19,5),

STDPrevQty DECIMAL(19,5),

STDInQty DECIMAL(19,5),

STDOutQty DECIMAL(19,5),

STDStockQty DECIMAL(19,5)

)

INSERT INTO \#GetInOutItem

SELECT ItemSeq

FROM \_TDAItem AS A WITH(NOLOCK)

JOIN \_TDAItemAsset AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq AND A.AssetSeq = B.AssetSeq

JOIN \_TDASMinor AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq AND B.SMAssetGrp = C.MinorSeq AND C.MinorValue = 1 -- 자재

WHERE (1=1)

AND A.CompanySeq = @CompanySeq

AND (@AssetType = 0 OR A.AssetSeq = @AssetType)

AND (@ItemName = '' OR A.ItemName LIKE @ItemName + '%')

AND (@ItemNo = '' OR A.ItemNo LIKE @ItemNo + '%')

AND (@Spec = '' OR A.Spec LIKE @Spec + '%')

-- 현재월구함

SELECT @CurrYM = CONVERT(NCHAR(6),GETDATE(),112)

-- 현재월이 조회월보다 이전인 경우 조회월기준 최종예상재고를 이월재고로 조회

IF @IsLink = '1' AND @CurrYM \< @DateYM

BEGIN

SELECT

@ForeDateFr = @CurrYM + '01',

@ForeDateTo = CONVERT(NCHAR(8),DATEADD(d,-1,@DateYM + '01'),112)

EXEC dhi_SPUForeMatListGetForeStkQty

@CompanySeq = @CompanySeq,

@FactUnit = @FactUnit,

@DateFr = @ForeDateFr,

@DateTo = @ForeDateTo,

@AssetType = @AssetType,

@ItemName = @ItemName,

@ItemNo = @ItemNo,

@Spec = @Spec,

@UserSeq = @UserSeq

INSERT INTO \#TmpCol (ItemName ,ItemNo ,Spec ,ItemSeq ,PrevStockQty)

SELECT ItemName ,ItemNo ,Spec ,ItemSeq ,PrevStockQty FROM \#TmpCol2

END

ELSE

BEGIN

-- 창고재고 가져오기 (이월)

EXEC \_SLGGetInOutStock

@CompanySeq = @CompanySeq, -- 법인코드

@BizUnit = 0, -- 사업부문

@FactUnit = @FactUnit, -- 생산사업장

@DateFr = @DateFr, -- 조회기간Fr

@DateTo = @DateFr, -- 조회기간To

@WHSeq = 0, -- 창고지정

@SMWHKind = 0, -- 창고구분별 조회

@CustSeq = 0, -- 수탁거래처

@IsTrustCust = '', -- 수탁여부

@IsSubDisplay = '', -- 기능창고 조회

@IsUnitQry = '', -- 단위별 조회

@QryType = 'S' -- 'S': 1007 실재고, 'B':1008 자산포함, 'A':1011 가용재고

INSERT INTO \#TmpCol (ItemName, ItemNo, Spec, ItemSeq, PrevStockQty)

SELECT

B.ItemName, B.ItemNo, B.Spec, X.ItemSeq, SUM(A.PrevQty)

FROM \#GetInOutItem AS X

LEFT OUTER JOIN \#GetInOutStock AS A ON X.ItemSeq = A.ItemSeq

LEFT OUTER JOIN \_TDAItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND X.ItemSeq = B.ItemSeq

GROUP BY B.ItemName, B.ItemNo, B.Spec, X.ItemSeq

END

--================================================================================================================================

-- END 고정값 구하기

--================================================================================================================================

--================================================================================================================================

-- BEGIN 가변값 구하기

--================================================================================================================================

--------------

-- 생산계획 --

--------------

SELECT

B.EndDate AS ProdDate,

ItemSeq,

SUM(Qty) AS Qty -- A.ProdReqDate -\> B.EndDate 변경 20191002

INTO \#TPDDailyProdPlan

FROM dhi_TPDProdReqM AS A WITH(NOLOCK)

JOIN dhi_TPDProdReqItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq AND A.ProdReqSeq = B.ProdReqSeq

WHERE (1=1)

AND A.CompanySeq = @CompanySeq

AND B.EndDate BETWEEN @DateFr AND @DateTo -- 소요량 계산시 생산의뢰 의뢰일 기준이 아닌 생산완료일 기준으로 수정..

GROUP BY B.EndDate, B.ItemSeq

 

--SELECT \* FROM \#TPDDailyProdPlan WHERE ItemSeq = 29230

 

--SELECT A.ProdReqDate,B.EndDate ,\*

-- FROM dhi_TPDProdReqM AS A WITH(NOLOCK)

-- JOIN dhi_TPDProdReqItem AS B WITH(NOLOCK) ON A.CompanySeq = B.CompanySeq AND A.ProdReqSeq = B.ProdReqSeq

--WHERE ItemSeq = 29230

 

 

--------------

-- 생산계획 --

--------------

--------------

-- 소요예정 --

--------------

SELECT

a.ItemSeq,

MAX(a.ItemBomRev) AS ItemBomRev,

case when FrDate = '' then '19000101' else FrDate end as FrTermValidity,

case when ToDate = '' then '99991231' else ToDate end as ToTermValidity

INTO \#tmpBOMTerm

FROM \_TPDBOM a

JOIN \_TPDROUItemProcRevFactUnit b on a.CompanySeq = b.CompanySeq and a.ItemSeq = b.ItemSeq and a.ItemBomRev = b.BomRev

JOIN \_TDAItem c on a.ItemSeq = c.ItemSeq

WHERE a.CompanySeq = 1

--AND (@AssetType = 0 OR c.AssetSeq = @AssetType)

--AND (@ItemName = '' OR c.ItemName LIKE @ItemName + '%')

--AND (@ItemNo = '' OR c.ItemNo LIKE @ItemNo + '%')

--AND (@Spec = '' OR c.Spec LIKE @Spec + '%')

GROUP BY a.ItemSeq, FrDate, ToDate

 

select distinct

a.ItemSeq,

max(a.ItemBomRev) as ItemBomRev

into \#tmpBOMRev

from \#tmpBOMTerm a

join \#TPDDailyProdPlan b on A.ItemSeq = B.ItemSeq and b.ProdDate between a.FrTermValidity and a.ToTermValidity

group by a.ItemSeq

 

SELECT \* FROM \#tmpBOMTerm AS A WHERE A.ItemSeq = 29230

SELECT \* FROM \#TPDDailyProdPlan AS A WHERE A.ItemSeq = 29230

 

SELECT \* FROM \#tmpBOMRev AS A WHERE A.ItemSeq = 29230 RETURN

 

 

 

select distinct

a.ItemSeq,

a.ItemBomRev

into \#tmpBOMRev2

from \#tmpBOMTerm a

join \#TPDDailyProdPlan b on A.ItemSeq = B.ItemSeq and b.ProdDate between a.FrTermValidity and a.ToTermValidity

 

 

declare

@vcItemSeq int,

@vcItemName nvarchar(200),

@vcItemNo nvarchar(200),

@vcStatus nvarchar(10),

@vcResult nvarchar(100)

 

select @vcStatus = 'OK', @vcResult = ''

 

if exists (select ItemSeq, count(\*) from \#tmpBOMRev2 group by ItemSeq having count(\*) \> 1)

begin

select

@vcItemSeq = ItemSeq

from \#tmpBOMRev2

group by ItemSeq

having count(\*) \> 1

order by count(\*) desc

 

select

@vcItemName = ItemName,

@vcItemNo = ItemNo

from \_TDAItem

where ItemSeq = @vcItemSeq

select @vcStatus = 'ERR', @vcResult = @vcItemName +'('+ @vcItemNo +')의 차수가 중복됩니다. 확인부탁드립니다.'

end

 

-- @@@@

SELECT \* FROM \#TPDDailyProdPlan AS A WHERE A.ItemSeq = 29230

SELECT \* FROM \#tmpBOMRev AS A WHERE A.ItemSeq = 29230

SELECT \* FROM \_TPDBOM AS A WHERE ItemSeq = 29230 AND A.SubItemSeq = 29174

SELECT \* FROM \_TPDBOM AS A WHERE A.SubItemSeq = 29174

 

SELECT \* FROM \_TDAITEM WHERE ITEMNO = 'P323K0002SM'

RETURN

 

SELECT \*

FROM \#TPDDailyProdPlan AS A

JOIN \_TPDBOM AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.ItemSeq = B.ItemSeq

JOIN \#tmpBOMRev AS C ON B.ItemSeq = C.ItemSeq AND B.ItemBomRev = C.ItemBomRev

WHERE C.ItemSeq = 29174

RETURN

 

 

-- 일자별생산계획기준 BOM 풀어서 1레벨 자재 소요량 계산

;WITH TNeedQty(ProdDate, GoodItemSeq, Qty, ItemSeq, ItemBOMRev, SubItemSeq, InLossRate, NeedQtyNumerator, NeedQtyDenominator, NeedQty) AS (

SELECT

A.ProdDate, A.ItemSeq, A.Qty, A.ItemSeq, B.SubItemBomRev, B.SubItemSeq, B.InLossRate, B.NeedQtyNumerator, B.NeedQtyDenominator,

CASE WHEN NeedQtyDenominator \<\> 0 THEN (B.NeedQtyNumerator / B.NeedQtyDenominator) ELSE 0 END AS NeedQty

FROM \#TPDDailyProdPlan AS A

JOIN \_TPDBOM AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq AND A.ItemSeq = B.ItemSeq

JOIN \#tmpBOMRev AS C ON B.ItemSeq = C.ItemSeq AND B.ItemBomRev = C.ItemBomRev

--JOIN (SELECT ItemSeq, MAX(ItemBomRev) AS ItemBomRev FROM \_TPDBOM WHERE CompanySeq = @CompanySeq GROUP BY ItemSeq) AS C ON B.ItemSeq = C.ItemSeq AND B.ItemBomRev = C.ItemBomRev

JOIN \_TDAItem AS D WITH(NOLOCK) ON B.CompanySeq = D.CompanySeq AND B.SubItemSeq = D.ItemSeq

JOIN \_TDAItemAsset AS E WITH(NOLOCK) ON D.CompanySeq = E.CompanySeq AND D.AssetSeq = E.AssetSeq

JOIN \_TDASMinor AS F WITH(NOLOCK) ON E.CompanySeq = F.CompanySeq AND E.SMAssetGrp = F.MinorSeq AND F.MinorValue = 1 -- 자재

WHERE B.SMDelvType \<\> 6032003 -- BOM전개 상세 제외

)

SELECT A.ProdDate

,A.SubItemSeq AS ItemSeq,

SUM(CASE WHEN U.SMDecPointSeq = 1003001 THEN ROUND((A.NeedQty) \* A.Qty, @nItemPoint\*2, 0) -- 반올림 \*2를 한 이유는 소요량은 BOM 자리수로 분자/분모이기 때문에

WHEN U.SMDecPointSeq = 1003002 THEN ROUND((A.NeedQty) \* A.Qty, @nItemPoint\*2, -1) -- 절사

WHEN U.SMDecPointSeq = 1003003 THEN ROUND((A.NeedQty) \* A.Qty + CAST(4 AS DECIMAL(19, 5)) / POWER(CAST(10 AS DECIMAL(19, 5)),(@nItemPoint\*2 + 1)), @nItemPoint\*2) -- 올림

ELSE ROUND((A.NeedQty) \* A.Qty + CAST(4 AS DECIMAL(19, 5)) / POWER(CAST(10 AS DECIMAL(19, 5)),(@nItemPoint\*2 + 1)), @nItemPoint\*2) END)

AS NeedQty

INTO \#TNeedQty

FROM TNeedQty AS A

JOIN \#TmpCol AS B ON A.SubItemSeq = B.ItemSeq -- 자재

JOIN \_TDAItem AS C WITH(NOLOCK) ON B.ItemSeq = C.ItemSeq

JOIN \_TDAItemAsset AS D WITH(NOLOCK) ON C.AssetSeq = D.AssetSeq

LEFT OUTER JOIN \_TDAUnit AS U WITH(NOLOCK) ON C.CompanySeq = U.CompanySeq AND C.UnitSeq = U.UnitSeq

GROUP BY A.ProdDate, A.SubItemSeq

 

 

SELECT \* FROM \#TmpCol

SELECT \* FROM \#TNeedQty

RETURN

 

--------------

-- 소요예정 --

--------------

-- 진행체크할 테이블값 테이블

CREATE TABLE \#TMP_PROGRESSTABLE (

IDOrder INT IDENTITY,

TABLENAME NVARCHAR(100)

)

CREATE TABLE \#TMP_SOURCETABLE (

IDOrder INT IDENTITY,

TABLENAME NVARCHAR(100)

)

-- 진행체크할 데이터 테이블

CREATE TABLE \#Temp_PUDelv (

IDX_NO INT IDENTITY,

DelvSeq INT,

DelvSerl INT,

ItemSeq INT,

DelvDate NVARCHAR(8),

Qty DECIMAL(19,5)

)

-- 진행체크할 데이터 테이블

CREATE TABLE \#Temp_PUOrd (

IDX_NO INT IDENTITY,

POSeq INT,

POSerl INT,

ItemSeq INT,

DelvDate NVARCHAR(8),

Qty DECIMAL(19,5),

IsStop NCHAR(1)

)

-- 진행체크할 데이터 테이블

CREATE TABLE \#Temp_PO (

IDX_NO INT IDENTITY,

POSeq INT,

POSerl INT,

ItemSeq INT,

DelvDate NVARCHAR(8),

Qty DECIMAL(19,5)

)

CREATE TABLE \#Temp_PO2 (

POSeq INT,

POSerl INT,

ItemSeq INT, DelvDate NVARCHAR(8),

Qty DECIMAL(19,5)

)

CREATE TABLE \#Temp_Delv (

IDX_NO INT IDENTITY,

DelvSeq INT,

DelvSerl INT,

ItemSeq INT,

DelvInDate NVARCHAR(8),

Qty DECIMAL(19,5)

)

CREATE TABLE \#Temp_DelvIn (

IDX_NO INT IDENTITY,

DelvInSeq INT,

DelvInSerl INT,

ItemSeq INT,

DelvInDate NVARCHAR(8),

Qty DECIMAL(19,5)

)

-- 진행체크할 데이터 테이블

CREATE TABLE \#Temp_PUOrdNot (

IDX_NO INT IDENTITY,

POSeq INT,

POSerl INT,

ItemSeq INT,

Qty DECIMAL(19,5)

)

-- 진행 내역 테이블

CREATE TABLE \#TCOMProgressTracking (

IDX_NO INT,

IDOrder INT,

Seq INT,

Serl INT,

SubSerl INT,

Qty DECIMAL(19,5),

StdQty DECIMAL(19,5),

Amt DECIMAL(19,5),

VAT DECIMAL(19,5)

)

-- 진행 내역 테이블

CREATE TABLE \#TCOMSourceTracking (

IDX_NO INT,

IDOrder INT,

Seq INT,

Serl INT,

SubSerl INT,

Qty DECIMAL(19,5),

StdQty DECIMAL(19,5),

Amt DECIMAL(19,5),

VAT DECIMAL(19,5)

)

--------------

-- 입고예정 --

--------------

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*

입고예정 수량을 계산할 때, 기본적으로는 발주에 등록되어 있는

입고예정수량을 기준으로 계산하나, 기 입고가 된 내역이 있는 품목에 대해서는

기 입고일자를 입고예정일로 간주하여 처리한다.

즉 기 입고된 일자가 조회기간의 시작을 보다 작을 경우

해당 입고일에 입고 예정수량으로 표현해 주기 위해

기 입고내역을 뒤져 해당하는 입고건의 원천 발주내역을 조회한다.

\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 기간내 입고된 내역에 대해 예정일 등록

INSERT INTO \#Temp_DelvIn

SELECT A.DelvInSeq, B.DelvInSerl, B.ItemSeq, A.DelvInDate, B.Qty

FROM \_TPUDelvIn a with(nolock)

join \_TPUDelvInItem b with(nolock) on a.DelvInSeq = b.DelvInSeq

WHERE A.CompanySeq = @CompanySeq

AND A.DelvInDate BETWEEN @DateFr AND @DateTo

 

TRUNCATE TABLE \#TMP_SOURCETABLE

TRUNCATE TABLE \#TCOMSourceTracking

INSERT INTO \#TMP_SOURCETABLE(TABLENAME)

SELECT '\_TPUORDPOItem'

EXEC \_SCOMSourceTracking @CompanySeq, '\_TPUDelvInItem', '#Temp_DelvIn', 'DelvInSeq', 'DelvInSerl', ''

insert \#Temp_PO2

select

B.Seq,

B.Serl,

A.ItemSeq,

A.DelvInDate,

A.Qty

from \#Temp_DelvIn A

join \#TCOMSourceTracking B on A.IDX_No = B.IDX_No

join \_TPUORDPOItem C on B.Seq = C.POSeq and B.Serl = C.POSerl

where (1=1)

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 기간내 납품된 내역에 대해 예정일 등록

INSERT INTO \#Temp_Delv

SELECT A.DelvSeq, B.DelvSerl, B.ItemSeq, A.DelvDate, B.Qty

FROM \_TPUDelv a with(nolock)

join \_TPUDelvItem b with(nolock) on a.DelvSeq = b.DelvSeq

WHERE A.CompanySeq = @CompanySeq

AND A.DelvDate BETWEEN @DateFr AND @DateTo

TRUNCATE TABLE \#TMP_SOURCETABLE

TRUNCATE TABLE \#TCOMSourceTracking

INSERT INTO \#TMP_SOURCETABLE(TABLENAME)

SELECT '\_TPUORDPOItem'

EXEC \_SCOMSourceTracking @CompanySeq, '\_TPUDelvItem', '#Temp_Delv', 'DelvSeq', 'DelvSerl', ''

insert \#Temp_PO2

select

B.Seq,

B.Serl,

A.ItemSeq,

A.DelvInDate,

case when C.IsStop \<\> '1' then C.Qty else A.Qty end

from \#Temp_Delv A

join \#TCOMSourceTracking B on A.IDX_No = B.IDX_No

join \_TPUORDPOItem C on B.Seq = C.POSeq and B.Serl = C.POSerl

left outer join \#Temp_PO2 D on B.Seq = D.POSeq and B.Serl = D.POSerl

where (1=1)

and D.POSeq is null

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 구매발주 납기일기준 발주수량 조회

INSERT INTO \#Temp_PO

SELECT A.POSeq ,A.POSerl ,A.ItemSeq ,A.DelvDate ,A.Qty

FROM \_TPUORDPOItem AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.DelvDate BETWEEN @DateFr AND @DateTo

AND A.IsStop \<\> '1' -- 입고예정 수량 계산 시 중단건을 제외하도록 수정 2019.10.14 임종석 요청

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

INSERT INTO \#TMP_PROGRESSTABLE(TABLENAME) SELECT '\_TPUDelvItem'

INSERT INTO \#TMP_PROGRESSTABLE(TABLENAME) SELECT '\_TPUDelvInItem'

EXEC \_SCOMProgressTracking @CompanySeq, '\_TPUORDPOItem', '#Temp_PO', 'POSeq', 'POSerl', ''

select a.\*

into \#tmpProcess

from \#TCOMProgressTracking a

join (select IDX_No, max(IDOrder) as IDOrder from \#TCOMProgressTracking group by IDX_No) b on a.IDX_No = b.IDX_No and a.IDOrder = b.IDOrder

select

a.POSeq, a.POSerl, a.ItemSeq, sum(b.Qty) as Qty

into \#tmpDelvQty

from \#Temp_PO a

join \#tmpProcess b on a.IDX_No = b.IDX_No

group by a.POSeq, a.POSerl, a.ItemSeq

--select \* from \#tmpDelvQty

 

update \#Temp_PO set

Qty = a.Qty - b.Qty

from \#Temp_PO a

join \#tmpDelvQty b on a.POSeq = b.POSeq and a.POSerl = b.POSerl

 

update \#Temp_PO set Qty = 0 from \#Temp_PO where Qty \< 0

 

insert \#Temp_PO select \* from \#Temp_PO2

delete from \#Temp_PO where Qty = 0

select ItemSeq, DelvDate, sum(Qty) as Qty

INTO \#TPUORDItem

from \#Temp_PO

group by ItemSeq, DelvDate

--------------

-- 입고예정 --

--------------

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 예상재고

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

----------------------

-- 발주 미납품수량 --

----------------------

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

INSERT INTO \#Temp_PUOrd

SELECT A.POSeq ,A.POSerl ,A.ItemSeq ,@DateFr AS DelvDate ,A.Qty, A.IsStop

FROM \_TPUORDPOItem AS A WITH(NOLOCK)

WHERE A.CompanySeq = @CompanySeq

AND A.DelvDate BETWEEN @PODelvDateFr AND @PODelvDateTo

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

INSERT INTO \#TMP_PROGRESSTABLE(TABLENAME)

SELECT '\_TPUDelvItem'

EXEC \_SCOMProgressTracking @CompanySeq, '\_TPUORDPOItem', '#Temp_PUOrd', 'POSeq', 'POSerl', ''

SELECT A.ItemSeq, A.DelvDate,

SUM(CASE ISNULL(A.IsStop, '0') WHEN '0' THEN CASE WHEN A.Qty - ISNULL(B.Qty, 0) \< 0 THEN 0 ELSE A.Qty - ISNULL(B.Qty, 0) END ELSE 0 /\*CASE WHEN (A.Qty - B.Qty) \< 0 THEN 0 ELSE (A.Qty - B.Qty) - (A.Qty - B.Qty)\*/ END) AS Qty

--과입고된 경우 입고예정수량 마이너스로 나오는 것 보정 필요

--SUM(CASE ISNULL(A.IsStop, '0') WHEN '0' THEN A.Qty - ISNULL(B.Qty, 0) ELSE 0 /\*CASE WHEN (A.Qty - B.Qty) \< 0 THEN 0 ELSE (A.Qty - B.Qty) - (A.Qty - B.Qty)\*/ END) AS Qty

INTO \#TPUNotDelvQty

FROM \#Temp_PUOrd AS A

LEFT OUTER JOIN (SELECT A.IDX_NO ,SUM(A.Qty) AS Qty

FROM \#TCOMProgressTracking AS A

JOIN \_TPUDelvItem AS B WITH(NOLOCK) ON B.CompanySeq = @CompanySeq

AND A.Seq = B.DelvSeq

AND A.Serl = B.DelvSerl

JOIN \_TPUDelv AS C WITH(NOLOCK) ON B.CompanySeq = C.CompanySeq AND B.DelvSeq = C.DelvSeq

WHERE A.IDOrder = 1

AND C.DelvDate \< @DateFr

GROUP BY A.IDX_NO) AS B ON A.IDX_NO = B.IDX_NO

GROUP BY A.ItemSeq, A.DelvDate

--UPDATE \#TPUNotDelvQty SET Qty = CASE WHEN Qty \< 0 THEN 0 ELSE Qty END --과입고를 건건이 찾기보다 sum된 것이 마이너스인걸 보정하는 것이 더 나을 것 같은데 잘 모르겠음 20190927

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

----------------------

-- 발주 미납품수량 --

----------------------

----------------------

-- 수입검사대기수량 --

----------------------

select a.ItemSeq, convert(char(8), dateadd(d, 15, b.DelvDate), 112) as DelvDate, SUM(a.Qty) as Qty

into \#TPUDelv

from \_TPUDelvItem a

join \_TPUDelv b WITH(NOLOCK) ON a.CompanySeq = b.CompanySeq AND a.DelvSeq = b.DelvSeq

where a.SMQcType = 6035002

and b.DelvDate \<= @DelvDateTo

group by a.ItemSeq, b.DelvDate

----------------------

-- 수입검사대기수량 --

----------------------

SELECT A.ItemSeq, A.StdDate, SUM(A.Qty) AS Qty

INTO \#tmp_forecast

FROM (SELECT ItemSeq, DelvDate AS StdDate, Qty FROM \#TPUORDItem

UNION ALL

SELECT ItemSeq, ProdDate AS StdDate, NeedQty \* -1 AS Qty FROM \#TNeedQty

UNION ALL

SELECT ItemSeq, Delvdate AS StdDate, Qty FROM \#TPUDelv

UNION ALL

SELECT ItemSeq, Delvdate AS StdDate, Qty FROM \#TPUNotDelvQty

) AS A

GROUP BY A.ItemSeq, A.StdDate

SELECT A.ItemSeq, A.StdDate, SUM(B.Qty) AS Qty

INTO \#TForeCast

FROM \#tmp_forecast AS A

JOIN \#tmp_forecast AS B ON A.ItemSeq = B.ItemSeq

AND A.StdDate \>= B.StdDate

GROUP BY A.ItemSeq, A.StdDate

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

 

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 예상재고

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 현재월이 조회월보다 이전인 경우 수입검사대기, 구매미납 삭제

IF @IsLink = '1' AND @CurrYM \< @DateYM

BEGIN

DELETE FROM \#TForeCast

DELETE FROM \#TPUNotDelvQty

END

--요청사항 이월재고 및 일자별 소요예정, 입고예정, 예상재고가 없는 항목은 조회되지 않도록 요청합니다.

INSERT \#FixCol (ItemName ,ItemNo ,Spec ,ItemSeq ,PrevStockQty)

SELECT

ItemName

,ItemNo

,Spec

,ItemSeq

,PrevStockQty

FROM \#TmpCol A

WHERE ISNULL(A.PrevStockQty,0) \<\> 0

OR EXISTS (SELECT TOP 1 1 FROM \#TNeedQty B WHERE A.ItemSeq = B.ItemSeq AND B.NeedQty \<\> 0)

OR EXISTS (SELECT TOP 1 1 FROM \#TPUORDItem B WHERE A.ItemSeq = B.ItemSeq AND B.Qty \<\> 0)

OR EXISTS (SELECT TOP 1 1 FROM \#TForeCast B WHERE A.ItemSeq = B.ItemSeq AND (ISNULL(B.Qty,0) + ISNULL(A.PrevStockQty,0))\<\> 0)

ORDER BY ItemName, ItemNo, Spec

 

-- 소요예정 = 1 입고예정 = 2 예상재고 = 3

CREATE TABLE \#Result

(

ColIDX INT,

RowIDX INT,

Qty DECIMAL(19,5)

)

CREATE TABLE \#TPUDelvIn

(

ItemSeq int,

DelvDate int,

Qty DECIMAL(19,5)

)

insert \#TPUDelvIn

select ItemSeq, DelvDate, Qty from \#TPUORDItem

union all

select ItemSeq, DelvDate, Qty from \#TPUDelv

select ItemSeq, DelvDate, sum(Qty) as Qty into \#TPUDelvInR from \#TPUDelvIn group by ItemSeq, DelvDate

 

 

 

-- @@@@

SELECT \* FROM \#FixCol

SELECT \* FROM \#TNeedQty

SELECT \* FROM \#Title

 

RETURN

SELECT C.ColIDX, A.RowIDX, B.NeedQty

FROM \#FixCol AS A

JOIN \#TNeedQty AS B ON A.ItemSeq = B.ItemSeq

JOIN \#Title AS C ON B.ProdDate = C.TitleSeq AND C.TitleSeq2 = 1 -- 소요예정

 

 

 

RETURN

 

 

 

 

 

 

 

INSERT INTO \#Result

SELECT C.ColIDX, A.RowIDX, B.NeedQty

FROM \#FixCol AS A

JOIN \#TNeedQty AS B ON A.ItemSeq = B.ItemSeq

JOIN \#Title AS C ON B.ProdDate = C.TitleSeq AND C.TitleSeq2 = 1 -- 소요예정

UNION

SELECT C.ColIDX, A.RowIDX, B.Qty

FROM \#FixCol AS A

--JOIN \#TPUORDItem AS B ON A.ItemSeq = B.ItemSeq

JOIN \#TPUDelvInR AS B ON A.ItemSeq = B.ItemSeq

JOIN \#Title AS C ON B.DelvDate = C.TitleSeq AND C.TitleSeq2 = 2 -- 입고예정

UNION

SELECT C.ColIDX, A.RowIDX, (ISNULL(B.Qty,0) + ISNULL(A.PrevStockQty,0)) AS Qty

FROM \#FixCol AS A

LEFT OUTER JOIN \#TForeCast AS B ON A.ItemSeq = B.ItemSeq

JOIN \#Title AS C ON B.StdDate = C.TitleSeq AND C.TitleSeq2 = 3 -- 예상재고

--================================================================================================================================

-- END 가변값 구하기

--================================================================================================================================

SELECT

ROWIDX, SUM(Qty) AS Qty

FROM \#Result AS B

JOIN \#Title AS C ON B.ColIDX = C.ColIDX AND C.TitleSeq2 = 1

GROUP BY RowIDX

SELECT \* FROM \#Result AS B

SELECT \* FROM \#Title AS C WHERE C.TitleSeq2 = 1

 

 

RETURN

 

 

 

 

 

 

 

 

 

-- 예상재고(-) 중 가장 빠른 일자에서 자재납기일 (-) 처리

SELECT

RowIDX, MIN(TitleSeq) AS TitleSeq

INTO \#tmpTitle

FROM \#Result AS B

JOIN \#Title AS C ON B.ColIDX = C.ColIDX AND C.TitleSeq2 = 3

WHERE B.Qty \< 0

GROUP BY B.RowIDX

SELECT

ROWIDX, SUM(Qty) AS Qty

INTO \#tmpOutPlan

FROM \#Result AS B

JOIN \#Title AS C ON B.ColIDX = C.ColIDX AND C.TitleSeq2 = 1

GROUP BY RowIDX

 

 

 

 

 

 

SELECT

ROWIDX, SUM(Qty) AS Qty, MIN(C.TitleSeq) AS FirstDelvDate, MAX(C.TitleSeq) AS LastDelvDate

INTO \#tmpInPlan

FROM \#Result AS B

JOIN \#Title AS C ON B.ColIDX = C.ColIDX AND C.TitleSeq2 = 2

GROUP BY RowIDX

SELECT

ItemSeq, SUM(SafetyQty) AS SafetyQty

INTO \#tmpSafe

FROM \_TDAWHItem

WHERE CompanySeq = @CompanySeq

GROUP BY ItemSeq

SELECT

ItemSeq, SUM(Qty) AS Qty

INTO \#tmpDelv

FROM \#TPUDelv

--WHERE DelvDate = @DelvDateFr

GROUP BY ItemSeq

SELECT

ItemSeq, SUM(Qty) AS Qty

INTO \#tmpNotDelv

FROM \#TPUNotDelvQty

GROUP BY ItemSeq

SELECT

B.RowIDX, SUM(B.Qty) AS Qty

INTO \#tmpStk

FROM \#Result AS B

JOIN \#Title AS C ON B.ColIDX = C.ColIDX AND C.TitleSeq2 = 3 -- 예상재고

GROUP BY B.RowIDX

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 자재기타 입고/출고 수량

SELECT

A.ItemSeq,

SUM(CASE WHEN A.InOutType = 41 THEN A.Qty ELSE 0 END) AS EtcInQty,

SUM(CASE WHEN A.InOutType = 31 THEN A.Qty ELSE 0 END) AS EtcOutQty

INTO \#tmp_EtcInOut

FROM \_TLGInOutDailyItem AS A WITH(NOLOCK)

JOIN \_TLGInOutDaily AS B WITH(NOLOCK) ON A.InOutType = B.InOutType

AND A.InOutSeq = B.InOutSeq

AND A.CompanySeq = B.CompanySeq

LEFT OUTER JOIN \_TDAItem AS C WITH(NOLOCK) ON A.CompanySeq = C.CompanySeq

AND A.ItemSeq = C.ItemSeq

WHERE A.CompanySeq = @CompanySeq

AND A.InOutType IN (41, 31) -- 자재기타입고, 자재기타출고

AND (B.InOutDate BETWEEN @DateFr AND @DateTo)

AND (@AssetType = 0 OR C.AssetSeq = @AssetType)

AND (@ItemName = '' OR C.ItemName LIKE @ItemName + '%')

AND (@ItemNo = '' OR C.ItemNo LIKE @ItemNo + '%')

AND (@Spec = '' OR C.Spec LIKE @Spec + '%')

GROUP BY A.ItemSeq

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 기준일 이전 미납수량

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

INSERT INTO \#Temp_PUOrdNot

SELECT A.POSeq, A.POSerl, A.ItemSeq, A.Qty

FROM \_TPUORDPOItem AS A WITH(NOLOCK)

WHERE (1=1)

AND A.CompanySeq = @CompanySeq

AND A.DelvDate \< @DateFr

AND A.IsStop \<\> '1'

TRUNCATE TABLE \#TMP_PROGRESSTABLE

TRUNCATE TABLE \#TCOMProgressTracking

INSERT INTO \#TMP_PROGRESSTABLE(TABLENAME)

SELECT '\_TPUDelvItem'

EXEC \_SCOMProgressTracking @CompanySeq, '\_TPUORDPOItem', '#Temp_PUOrdNot', 'POSeq', 'POSerl', ''

--select

-- a.ItemSeq, sum(isnull(a.Qty,0) - isnull(b.Qty,0)) as Qty

--into \#tmpNotDelvPO

--from \#Temp_PUOrdNot a

-- left outer join \#TCOMProgressTracking b on a.IDX_NO = b.IDX_NO

--group by a.ItemSeq

select

a.POSeq, a.POSerl, a.ItemSeq, isnull(a.Qty,0) as Qty

into \#tmpPONot

from \#Temp_PUOrdNot a

-- where a.ItemSeq = 14820

select

a.POSeq, a.POSerl, a.ItemSeq, sum(isnull(b.Qty,0)) as Qty

into \#tmpDelvNot

from \#Temp_PUOrdNot a

left outer join \#TCOMProgressTracking b on a.IDX_NO = b.IDX_NO

-- where a.ItemSeq = 14820

group by a.POSeq, a.POSerl, a.ItemSeq

select

p.ItemSeq, isnull(p.Qty,0) - isnull(d.Qty,0) as Qty

into \#tmpNotDelvPO2

from \#tmpPONot p

left outer join \#tmpDelvNot d on p.POSeq = d.POSeq and p.POSerl = d.POSerl

--group by p.ItemSeq

select

ItemSeq, sum(Qty) as Qty

into \#tmpNotDelvPO

from \#tmpNotDelvPO2

where Qty \> 0

group by ItemSeq

 

/\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*/

-- 자재납기필요일

UPDATE a

SET NeedDelvDate = CASE WHEN ka.RowIDX IS NOT NULL THEN CONVERT(NVARCHAR(8), DATEADD(DAY, @AddDelvDate, CONVERT(NVARCHAR(8),TitleSeq,112)),112) ELSE '' END,

DelvDate = CASE WHEN ka.RowIDX IS NOT NULL THEN CONVERT(NVARCHAR(8), DATEADD(DAY, @AddDelvDate, CONVERT(NVARCHAR(8),TitleSeq,112)),112) ELSE '' END,

NotInQty = ISNULL(ke.Qty,0), -- 입고검사대기수량

NotDelvQty = ISNULL(kf.Qty,0),

TotNeedQty = ISNULL(kb.Qty,0),

TotInQty = ISNULL(kc.Qty,0),

FirstDelvDate = ISNULL(kc.FirstDelvDate,0),

LastDelvDate = CASE WHEN kc.FirstDelvDate = kc.LastDelvDate THEN '' ELSE kc.LastDelvDate END,

StepQty = CASE WHEN ISNULL(bp.StepQty,0) = 0 THEN 1 ELSE bp.StepQty END, -- 최종예상재고 구할시 Divide 0 오류 피하기 위해 0일시 1로 세팅

SafetyQty = ISNULL(kd.SafetyQty,0),

SumInQty = ISNULL(kc.Qty,0) + isnull(nt.Qty,0), /\*+ ISNULL(ke.Qty,0) + ISNULL(kf.Qty,0)\*/-- 입고예정 수량에 기 입고된 내역이 포함되었으므로, 입고예정수량의 총합이 총입고예정수량이 됨

--ExptStockQty = ISNULL(a.PrevStockQty,0) + ISNULL(ke.Qty,0) + ISNULL(kc.Qty,0) /\*+ ISNULL(kf.Qty,0)\*/ - ISNULL(kb.Qty,0) -- 이월재고 + 입고검사대기수량 + 총입고예정량 - 소요량합계

ExptStockQty = (ISNULL(a.PrevStockQty,0) + ISNULL(kc.Qty,0) + ISNULL(kg.EtcInQty,0)) - (ISNULL(kb.Qty,0) + ISNULL(kg.EtcOutQty,0)) + isnull(nt.Qty,0), -- 이월재고 + 입고검사대기수량 + 총입고예정량 - 소요량합계

EtcInQty = ISNULL(kg.EtcInQty,0),

EtcOutQty = ISNULL(kg.EtcOutQty,0)

FROM \#FixCol AS a

LEFT OUTER JOIN \#tmpTitle AS ka ON a.RowIDX = ka.RowIDX -- 예상재고

LEFT OUTER JOIN \#tmpOutPlan AS kb ON a.RowIDX = kb.RowIDX -- 소요예정

LEFT OUTER JOIN \#tmpInPlan AS kc ON a.RowIDX = kc.RowIDX -- 입고예정

LEFT OUTER JOIN \#tmpSafe AS kd ON a.ItemSeq = kd.ItemSeq

LEFT OUTER JOIN \#tmpDelv AS ke ON a.ItemSeq = ke.ItemSeq

LEFT OUTER JOIN \#tmpNotDelv AS kf ON a.ItemSeq = kf.ItemSeq

LEFT OUTER JOIN \_TPUBASEBuyPriceItem AS bp ON bp.CompanySeq = @CompanySeq AND a.ItemSeq = bp.ItemSeq AND bp.IsPrice = '1' -- 대표단가여부

LEFT OUTER JOIN \#tmp_EtcInOut AS kg ON a.ItemSeq = kg.ItemSeq

left outer join \#tmpNotDelvPO AS nt on a.ItemSeq = nt.ItemSeq

 

-- 최종예상재고 = 이월재고 + 총입고예정수량 - 소요예정합계

UPDATE A

SET FinForeCastStockQty = A.PrevStockQty + A.SumInQty - A.TotNeedQty + A.NotInQty + A.EtcInQty - A.EtcOutQty

FROM \#FixCol AS A

JOIN \#tmpStk AS B ON A.RowIDX = B.RowIDX

-- 구매산정량

UPDATE A

SET PurEstQty = CASE WHEN A.FinForeCastStockQty \>= A.SafetyQty THEN 0

WHEN A.FinForeCastStockQty \< 0 THEN CEILING((A.FinForeCastStockQty \* -1 + A.SafetyQty) / A.StepQty) \* A.StepQty

WHEN A.FinForeCastStockQty \>= 0 THEN CEILING((A.SafetyQty - A.FinForeCastStockQty) / A.StepQty) \* A.StepQty

ELSE 0

END,

PurQty = CASE WHEN A.FinForeCastStockQty \>= A.SafetyQty THEN 0

WHEN A.FinForeCastStockQty \< 0 THEN CEILING((A.FinForeCastStockQty \* -1 + A.SafetyQty) / A.StepQty) \* A.StepQty

WHEN A.FinForeCastStockQty \>= 0 THEN CEILING((A.SafetyQty - A.FinForeCastStockQty) / A.StepQty) \* A.StepQty

ELSE 0

END

FROM \#FixCol AS A

IF @IsLink \<\> '0' -- 체크값이 0일때만 Title부분 구함.

BEGIN

delete from \#Title

END

-- 자재재고현황_dhi에서 고정부만 필요하여 WorkingTag로 구분지어서 SELECT 함.

 

 

--if exists (select 0 from \#FixCol)

--begin

-- select @vcItemSeq = @vcItemSeq

--end

--else

--begin

-- insert \#FixCol

-- select

-- '' as ItemName, '' as ItemNo, '' as Spec, 0 as ItemSeq,

-- 0 as PrevStockQty, 0 as FinForeCastStockQty, '' as NeedDelvDate, 0 as NotInQty, 0 as TotNeedQty,

-- 0 as TotInQty, '' as FirstDelvDate, '' as LastDelvDate, 0 as StepQty, 0 as SafetyQty,

-- 0 as PurQty, 0 as NotDelvQty, 0 as SumInQty, 0 as PurEstQty, 0 as ExptStockQty,

-- '' as DelvDate, 0 as EtcInQty, 0 as EtcOutQty

--end

 

IF @WorkingTag = 'Z'

BEGIN

-- 고정부

SELECT ItemSeq,

ExptStockQty -- 예상재고수량

FROM \#FixCol ORDER BY RowIDX

END

ELSE

BEGIN

SELECT \* FROM \#Title ORDER BY ColIDX -- 시트헤더

SELECT \*, @vcStatus as ErrStatus, @vcResult as ErrResult FROM \#FixCol ORDER BY RowIDX -- 고정부

SELECT \* FROM \#Result ORDER BY ColIDX, RowIDX -- 가변부

END

 

RETURN

go

BEGIN TRAN

DECLARE @CONST\_#BIZ_IN_DataBlock1 INT

, @CONST\_#BIZ_IN_DataBlock2 INT

, @CONST\_#BIZ_IN_DataBlock3 INT

, @CONST\_#BIZ_IN_DataBlock4 INT

, @CONST\_#BIZ_OUT_DataBlock1 INT

, @CONST\_#BIZ_OUT_DataBlock2 INT

, @CONST\_#BIZ_OUT_DataBlock3 INT

, @CONST\_#BIZ_OUT_DataBlock4 INT

SELECT @CONST\_#BIZ_IN_DataBlock1 = 0

, @CONST\_#BIZ_IN_DataBlock2 = 0

, @CONST\_#BIZ_IN_DataBlock3 = 0

, @CONST\_#BIZ_IN_DataBlock4 = 0

, @CONST\_#BIZ_OUT_DataBlock1 = 0

, @CONST\_#BIZ_OUT_DataBlock2 = 0

, @CONST\_#BIZ_OUT_DataBlock3 = 0

, @CONST\_#BIZ_OUT_DataBlock4 = 0

 

 

IF @CONST\_#BIZ_IN_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, DateFr NVARCHAR(8), DateTo NVARCHAR(8), ItemName NVARCHAR(100), ItemNo NVARCHAR(100), Spec NVARCHAR(100), FactUnit INT, AssetType INT, DateYM CHAR(6), IsLink CHAR(1)

)

SET @CONST\_#BIZ_IN_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock1 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock1

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, FactUnitName NVARCHAR(100), DateFr NVARCHAR(8), DateTo NVARCHAR(8), AssetTypeName NVARCHAR(100), ItemName NVARCHAR(100), ItemNo NVARCHAR(100), Spec NVARCHAR(100), FactUnit INT, AssetType INT, DateYM CHAR(6), IsLink CHAR(1)

)

SET @CONST\_#BIZ_OUT_DataBlock1 = 1

 

END

 

IF @CONST\_#BIZ_IN_DataBlock2 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock2

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, TitleName NVARCHAR(100), TitleSeq INT, TitleName2 NVARCHAR(100), TitleSeq2 INT

)

SET @CONST\_#BIZ_IN_DataBlock2 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock2 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock2

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, TitleName NVARCHAR(100), TitleSeq INT, TitleName2 NVARCHAR(100), TitleSeq2 INT

)

SET @CONST\_#BIZ_OUT_DataBlock2 = 1

 

END

 

IF @CONST\_#BIZ_IN_DataBlock3 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock3

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, ItemName NVARCHAR(100), ItemNo NVARCHAR(100), Spec NVARCHAR(100), ItemSeq INT, PrevStockQty DECIMAL(19, 5), FinForeCastStockQty DECIMAL(19, 5), NeedDelvDate NVARCHAR(8), NotInQty DECIMAL(19, 5), TotNeedQty DECIMAL(19, 5), TotInQty DECIMAL(19, 5), FirstDelvDate CHAR(8), LastDelvDate CHAR(8), StepQty DECIMAL(19, 5), SafetyQty DECIMAL(19, 5), PurQty DECIMAL(19, 5), DelvDate NVARCHAR(8), Qty DECIMAL(19, 5), STDUnitSeq INT, IsInclusedVAT NVARCHAR(1), VATRate DECIMAL(19, 5), NotDelvQty DECIMAL(19, 5), SumInQty DECIMAL(19, 5), PurEstQty DECIMAL(19, 5), ExptStockQty DECIMAL(19, 5), EtcInQty DECIMAL(19, 5), EtcOutQty DECIMAL(19, 5), ErrStatus NVARCHAR(100), ErrResult NVARCHAR(100)

)

SET @CONST\_#BIZ_IN_DataBlock3 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock3 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock3

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, ItemName NVARCHAR(100), ItemNo NVARCHAR(100), Spec NVARCHAR(100), ItemSeq INT, PrevStockQty DECIMAL(19, 5), FinForeCastStockQty DECIMAL(19, 5), NeedDelvDate NVARCHAR(8), NotInQty DECIMAL(19, 5), TotNeedQty DECIMAL(19, 5), TotInQty DECIMAL(19, 5), FirstDelvDate CHAR(8), LastDelvDate CHAR(8), StepQty DECIMAL(19, 5), SafetyQty DECIMAL(19, 5), PurQty DECIMAL(19, 5), DelvDate NVARCHAR(8), Qty DECIMAL(19, 5), STDUnitSeq INT, IsInclusedVAT NVARCHAR(1), VATRate DECIMAL(19, 5), NotDelvQty DECIMAL(19, 5), SumInQty DECIMAL(19, 5), PurEstQty DECIMAL(19, 5), ExptStockQty DECIMAL(19, 5), EtcInQty DECIMAL(19, 5), EtcOutQty DECIMAL(19, 5), ErrStatus NVARCHAR(100), ErrResult NVARCHAR(100)

)

SET @CONST\_#BIZ_OUT_DataBlock3 = 1

 

END

 

IF @CONST\_#BIZ_IN_DataBlock4 = 0

BEGIN

CREATE TABLE \#BIZ_IN_DataBlock4

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, ColIDX INT, RowIDX INT, Qty DECIMAL(19, 5)

)

SET @CONST\_#BIZ_IN_DataBlock4 = 1

 

END

 

IF @CONST\_#BIZ_OUT_DataBlock4 = 0

BEGIN

CREATE TABLE \#BIZ_OUT_DataBlock4

(

WorkingTag NCHAR(1)

, IDX_NO INT

, DataSeq INT

, Selected INT

, MessageType INT

, Status INT

, Result NVARCHAR(255)

, ROW_IDX INT

, IsChangedMst NCHAR(1)

, TABLE_NAME NVARCHAR(255)

 

, ColIDX INT, RowIDX INT, Qty DECIMAL(19, 5)

)

SET @CONST\_#BIZ_OUT_DataBlock4 = 1

 

END

 

DECLARE @INPUT_ERROR_MESSAGE NVARCHAR(4000)

, @INPUT_ERROR_SEVERITY INT

, @INPUT_ERROR_STATE INT

, @INPUT_ERROR_PROCEDURE NVARCHAR(128)

INSERT INTO \#BIZ_IN_DataBlock1 (WorkingTag, IDX_NO, DataSeq, Selected, Status, Result, ROW_IDX, IsChangedMst, TABLE_NAME, DateFr, DateTo, ItemName, ItemNo, Spec, FactUnit, AssetType, DateYM, IsLink)

SELECT N'A', 1, 1, 1, 0, NULL, NULL, N'1', N'', N'20240401', N'20240415', N'', N'0323J0001', N'', N'1', N'', N'202404', N'0'

IF @@ERROR \<\> 0 RETURN

 

 

DECLARE @HasError NCHAR(1)

, @UseTransaction NCHAR(1)

-- 내부 SP용 파라메터

, @ServiceSeq INT

, @MethodSeq INT

, @WorkingTag NVARCHAR(10)

, @CompanySeq INT

, @LanguageSeq INT

, @UserSeq INT

, @PgmSeq INT

, @IsTransaction BIT

 

SET @HasError = N'0'

SET @UseTransaction = N'0'

 

BEGIN TRY

 

SET @ServiceSeq = 29020036

--SET @MethodSeq = 1

SET @WorkingTag = N''

SET @CompanySeq = 1

SET @LanguageSeq = 1

SET @UserSeq = 1

SET @PgmSeq = 29020028

SET @IsTransaction = 0

 

-- ExecuteOrder : 1 : Start

EXEC dhi_SPUForeMatListQuery_ttest

@ServiceSeq

, @WorkingTag

, @CompanySeq

, @LanguageSeq

, @UserSeq

, @PgmSeq

, @IsTransaction

IF @@ERROR \<\> 0

BEGIN

SET @HasError = N'1'

GOTO GOTO_END

END

 

-- ExecuteOrder : 1 : End

 

GOTO_END:

END TRY

BEGIN CATCH

-- SQL 오류인 경우는 여기서 처리가 된다

IF @UseTransaction = N'1'

ROLLBACK TRAN

DECLARE @ERROR_MESSAGE NVARCHAR(4000)

, @ERROR_SEVERITY INT

, @ERROR_STATE INT

, @ERROR_PROCEDURE NVARCHAR(128)

 

SELECT @ERROR_MESSAGE = ERROR_MESSAGE()

, @ERROR_SEVERITY = ERROR_SEVERITY()

, @ERROR_STATE = ERROR_STATE()

, @ERROR_PROCEDURE = ERROR_PROCEDURE()

RAISERROR (@ERROR_MESSAGE, @ERROR_SEVERITY, @ERROR_STATE, @ERROR_PROCEDURE)

 

RETURN

END CATCH

 

-- SQL 오류를 제외한 체크로직으로 발생된 오류는 여기서 처리

IF @HasError = N'1' AND @UseTransaction = N'1'

ROLLBACK TRAN

 

 

DROP TABLE \#BIZ_IN_DataBlock1

DROP TABLE \#BIZ_OUT_DataBlock1

DROP TABLE \#BIZ_IN_DataBlock2

DROP TABLE \#BIZ_OUT_DataBlock2

DROP TABLE \#BIZ_IN_DataBlock3

DROP TABLE \#BIZ_OUT_DataBlock3

DROP TABLE \#BIZ_IN_DataBlock4

DROP TABLE \#BIZ_OUT_DataBlock4

 

ROLLBACK TRAN

1. 자재예상재고조회_dhi 화면에서 첨부 이미지 처럼 품번 '0323J0001' 을 입력하고 데이터 조회 시,

   소요량 합계가 조회되지 않습니다.

2\. BOM등록 및 제품별공정소요자재등록 화면 상에는 정상적으로 데이터가 입력이 되어 있는 상태입니다

 

   해당 컬럼의 데이터가 조회되지 않는 원인 확인 및 수정 요청 합니다.

 

 

\[자재예상재고조회_dhi\] 화면

소요량 계산 시 생산완료일 기준으로 계산하고 있습니다.

해당 자재의 상위품목 'P323K0002SM' 의 생산완료일은 '**2024-04-02**' 이며

각 차수 적용일은

02 : **2024-03-11** ~ 

03 : **2024-04-08** ~ 으로

02차수로 소요량 계산되기 때문에 '소요량 합계' 미 조회된 것으로 확인하였습니다.